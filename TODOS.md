# Implementation plan — `UsageRegistry` leak fix + distributed cost-report aggregation (Option B)

**Branch:** `fix/For-API-update` · **Status:** Phase 4 COMPLETE (CHECKPOINT 4 reached — live registry removed; the success-path leak is fixed structurally; cost reporting is event-sourced end to end; full `make agent-test` + `make agent-check` green). Next: Phase 5 (P0.1 `--mock-inference`).

**Design source (locked):** [`wip/registry/registry-lifecycle-synthesis.md`](wip/registry/registry-lifecycle-synthesis.md) → "For the planning phase". Companion: [`wip/distributed-execution/tracing-cost-reporting.md`](wip/distributed-execution/tracing-cost-reporting.md) (T2/P1), [`wip/distributed-execution/README.md`](wip/distributed-execution/README.md) (P0.1/P1).

This doc turns the locked decisions into a sequenced, checkpointed implementation. **Line numbers below are indicative (branch at time of writing); the symbol names are the durable anchors.**

---

## 0. What we are building, in one screen

The `UsageRegistry` is a per-run, in-process cost buffer with lifecycle **open → populate → report → close**. Two problems are one lifecycle:

- **The leak** = the *close* never happens on the success path (registry opened in `pipeline_run_setup`, closed only on failure).
- **Distributed cost reporting (T2/P1)** = the cross-worker *replay-populate* is never wired (events land in the backend, never become a report).

**Option B (locked) resolves both by removal:** usage rides on `PipeOutput` like `graph_spec` does — assembled from the trace-event stream at the end of the run, in both DIRECT and TEMPORAL modes — and the **submitter** renders the cost report from that field. With nothing populating a submitter-side registry, the registry is removed entirely; the leak becomes structurally impossible and `inject_tokens_usages` + the live-add trio retire.

**The load-bearing decoupling:** today usage events are only emitted when `--graph` + tracing are on (the event log is created inside `if is_generate_graph`). Once the live registry is gone, `--no-graph` would lose cost. So we add a dedicated, default-on **`--costs` / `is_generate_costs`** switch (mirrors `--graph` / `is_generate_graph`) over the **shared event-log transport**: `--graph` gates graph (node/edge) events + `GraphSpecAssembler`; `--costs` gates `UsageReportEvent`s + `UsageAggregator`. Each rides its artifact back on `PipeOutput`. `--cost-report` is folded into `--costs` (removed; no back-compat).

---

## 1. Decisions this plan makes (forks the synthesis left to planning)

These are decided here so implementation is unambiguous. Each is open to veto at plan review.

- **D1 — P0.1 sequencing: core cost work FIRST (pytest-validated, no spend), then P0.1, then skill upgrade.** The leak fix + cross-worker cost aggregation is the primary deliverable and is fully validatable deterministically without `--mock-inference`: the existing `tests/integration/pipelex/temporal/tracing/test_split_worker_usage.py` already substitutes the inference activity with a server-side wrapper that synthesizes a real `LLMJob` cross-process — we extend that exact pattern to assert assembled cost. P0.1 (`--mock-inference`) is only needed for **CLI-level** (3-process `pipelex run bundle`) cheap cross-worker validation, which is exactly what the **skill upgrade** consumes — so P0.1 is sequenced as the bridge into the skill upgrade (Phase 5), not the critical path's start. *If you prefer the cheap CLI harness available from day one, P0.1 reorders cleanly to Phase 1 — it has no dependency on the cost work.*
- **D2 — Usage artifact on `PipeOutput`: `tokens_usages: list[AnyTokensUsage] | None = None` + `usage_assembly_error: str | None = None`.** Literal mirror of `graph_spec` / `graph_assembly_error`. `AnyTokensUsage` is already a discriminated union of pydantic `BaseModel`s, already serialized in `UsageReportEvent` through the event-log backend → safe across the Temporal boundary and through the `StoragePayloadCodec`. The submitter renders with the existing `CostRegistry.generate_report(tokens_usages=...)` which already takes a usage list. (Rejected: a pre-computed cost summary — less flexible, duplicates the renderer.)
- **D3 — One generalized assembly hook, single event read → both artifacts.** Generalize the existing graph hook to read the event stream once and feed it into **both** `GraphSpecAssembler` (if `--graph`) and `UsageAggregator` (if `--costs`). DIRECT: `assemble_graph_on_output` → `assemble_tracing_on_output` (or keep name, add usage). TEMPORAL: `act_assemble_graph` → returns a small `TracingAssemblyResult{graph_spec, tokens_usages}`; `WfPipeRun.run` Step 2 sets both on `pipe_output`. Rationale: the events are read once and aggregating usage is trivial (`[e.tokens_usage for e in events if isinstance(e, UsageReportEvent)]`); a second activity dispatch / event read is wasted latency + history. *Lower-risk fallback if the proven graph path must stay byte-for-byte untouched: an additive sibling `act_assemble_usage` / `assemble_usage_on_output` that does its own read. Documented; not chosen.*
- **D4 — Thread the two gates to the worker on the tracing context (`GraphContext`).** Add `emit_graph_events: bool` and `emit_usage_events: bool` to `GraphContext` (already serialized through `JobMetadata`, already the per-run tracing context). The worker (`WfPipeRouter.run`) reads them to decide whether to wire the tracer's event log (graph) and whether to `set_event_log` on the report delegate (usage); the assembly step reads them to decide what to assemble. Today the *presence* of `graph_context` threads the single graph bit — now the context is present whenever **graph OR costs** is on, and the two booleans say which. (Rejected: new `JobMetadata` fields — the context is the natural home.)
- **D5 — In costs-only mode (`--no-graph --costs`), still open the tracing context but pass `event_log=None` to the graph tracer.** The tracer accumulates the in-memory graph (cheap, never assembled/output when graph off) and keeps minting node ids; graph **events** are suppressed (emission is guarded by `self._event_log is not None` in `graph_tracer.py`); the usage event-log context is registered via `set_event_log` so usage events emit. This keeps `GraphContext` as the single keying mechanism for both event types (the report delegate's `_event_log_contexts` is keyed by `graph_context.lookup_key`) and avoids a deep refactor of the delegate's keying. *Deferred optimization: skip the in-memory tracer entirely in costs-only mode.*
- **D6 — Channel model (DECIDED).** `--costs` (default ON) = collect + assemble + render-eligibility. `is_generate_cost_report_file_enabled` gates the CSV channel; `is_log_costs_to_console` gates the console channel — applied **only when `--costs` is on** (synthesis's "which channel" framing). Remove the per-inference-job live console logging in `_report_*_job` (worker-side noise; the synthesis says the report is rendered submitter-side, never on the worker's console) and let `is_log_costs_to_console` mean the **end-of-run** console channel. **Decided (user-confirmed): flip `is_log_costs_to_console` default `false → true`** in `pipelex/pipelex.toml` **and** `.pipelex/pipelex.toml`, so the CLI shows the cost table by default when `--costs` is on — parity with `--graph` producing visible output by default. Library embedders who don't want console spam set it false. (Applied in Phase 3.)
- **D7 — P0.1 `--mock-inference` is an orthogonal signal, not a new `PipeRunMode` enum value.** Adding a `PipeRunMode` member forces an exhaustive-`match`/`case` burn-down across the codebase (python standards forbid `case _`). Instead carry a boolean (e.g. `is_mock_inference` on the run params that already thread to the worker alongside `run_mode`): the operator treats it as LIVE for **dispatch** (the activity IS scheduled), and the **activity body** swaps in `ContentGeneratorDry` when the flag is set. Lower blast radius, no enum-exhaustiveness churn.

---

## 2. Current architecture — anchors to edit (compact reference)

**Registry + reporting (`pipelex/reporting/`):**

- `reporting_manager.py` — `ReportingManager` (process singleton via `hub.get_report_delegate()`):
  - `_usage_registries: dict[str, UsageRegistry]` (`~:61`), `_event_log_contexts: dict[str,_EventLogContext]` (`~:64`) — keyed by `graph_context.lookup_key`.
  - `setup` (`~:102`, seeds `UNTITLED`), `open_registry` (`~:216`, raises on dup), `close_registry` (`~:393`, idempotent `pop`).
  - Live trio: `_get_registry_strict` (`~:115`), `_get_or_create_registry` (`~:126`), `_try_add_to_registry` (`~:137`).
  - `inject_tokens_usages` (`~:223`) — **zero production callers**.
  - `_report_llm_job`/`_report_img_gen_job`/`_report_extract_job`/`_report_search_job` (`~:152-210`) — each: `_try_add_to_registry` + `_emit_usage_event` + per-job console log gated by `is_log_costs_to_console`.
  - `_emit_usage_event` (`~:237`): `graph_context is None → return`; fast path `_emit_via_registered_context` (`~:264`) when `_event_log_contexts[lookup_key]` exists; else `_emit_usage_event_runner_fallback` (`~:285`, gated only by `tracing_config.is_enabled`).
  - `set_event_log` (`~:71`) / `clear_event_log` (`~:92`) — **keep** (the usage transport).
  - `generate_report` (`~:365`) — iterates registries; renders via `CostRegistry.generate_report(...)`.
- `reporting_protocol.py` — `ReportingProtocol` + `ReportingNoOp` (update in lockstep with any surface change).
- `reporting_types.py` — `AnyTokensUsage = Annotated[LLMTokensUsage|ImgGenTokensUsage|ExtractTokensUsage|SearchTokensUsage, Field(discriminator="model_type")]` (all pydantic `BaseModel`).
- `pipelex/cogt/usage/cost_registry.py` — `CostRegistry.generate_report(pipeline_run_id, tokens_usages, unit_scale, cost_report_file_path=None, print_to_console=True)` (`~:38`) renders Rich table + optional CSV directly from a usage list. `complete_cost_report` (`~:257`).
- `pipelex/tracing/usage_aggregator.py` — `UsageAggregator.aggregate(events) -> list[AnyTokensUsage]` (the `GraphSpecAssembler.assemble` analogue).
- `pipelex/tracing/trace_events.py` — `UsageReportEvent(TraceEvent){ node_id, tokens_usage }` (`~:162`).

**Lifecycle / gating:**

- `pipelex/pipeline/pipeline_run_setup.py` — event-log + tracer created inside `if execution_config.is_generate_graph:` (`~:184-200`); `make_event_log` (`~:189`); `open_registry` + `registry_opened=True` (`~:209-210`); `set_event_log` if `event_log is not None` (`~:213-219`); `finally` gates all cleanup behind `if not success:` incl. `close_registry` (`~:264-281`) — **this asymmetry is the leak.**
- `pipelex/pipeline/runner.py` — `PipelexRunner.execute_pipeline` (`~:77`); finally closes tracer `if is_generate_graph` (`~:205`), `clear_event_log` (`~:212`), library teardown — **never closes the registry** (the forgotten resource). Returns `PipelexPipelineExecuteResponse.from_pipe_output(pipe_output, ...)` (`~:237`). `start_pipeline` raises `NotImplementedError` here (`~:255`) — the API has its own.
- `pipelex/pipe_run/pipe_run.py` — DIRECT `PipeRun.run` finally → `assemble_graph_on_output(pipe_output, pipeline_run_id, domain_code, main_pipe_code)` (`~:70-76`). Runs in the submitter process.
- `pipelex/pipe_run/graph_assembly.py` — `assemble_graph_on_output` (`~:22`): gate `if not tracing_config.is_enabled: return`; `make_event_log` → `read_events` → `GraphSpecAssembler.assemble` → sets `pipe_output.graph_spec`.
- `pipelex/temporal/tprl_pipe/act_assemble_graph.py` — `act_assemble_graph(AssembleGraphArg{pipeline_run_id,domain_code,main_pipe_code}) -> GraphSpec|None` (`~:29`).
- `pipelex/temporal/tprl_pipe/wf_pipe_run.py` — Step 2 dispatches `act_assemble_graph`, sets `pipe_output.graph_spec` / `pipe_output.graph_assembly_error` (`~:76-94`).
- `pipelex/temporal/tprl_pipe/temporal_pipe_run.py` — `TemporalPipeRun.run` (`~:47-88`) returns `rehydrate_pipe_output_with_crate(pipe_output, crate)`.
- `pipelex/temporal/tprl_pipe/wf_pipe_router.py` — per-workflow tracing gated `if tracing_config.is_enabled and graph_context is not None:` (`~:75-127`): `BufferingEventLog` → `open_tracer(event_log=...)` + `set_event_log(...)`; finally drains via `act_flush_trace_events` + `clear_event_log` (`~:151-171`).
- `pipelex/graph/graph_context.py` — `GraphContext` (`~:12`); `lookup_key = tracer_key or graph_id` (`~:34`). **D4 adds the two flags here.**
- `pipelex/graph/graph_tracer.py` — every graph event emit guarded by `if self._event_log is not None:` (`~:620,671,731,774,815,857,469`). (D5 relies on this.)
- `pipelex/system/configuration/configs.py` — `PipelineExecutionConfig` (`~:153`, holds `is_generate_graph`, `with_graph_config_overrides` `~:163`); `ReportingConfig` (`~:94`, holds `is_log_costs_to_console`, `is_generate_cost_report_file_enabled`); `TracingConfig` (`~:123`, `is_enabled` master flag).

**CLI surfaces:**

- Main CLI `run`: `pipelex/cli/commands/run/{pipe_cmd,method_cmd,bundle_cmd}.py` declare `--graph/--no-graph` and `--cost-report/--no-cost-report`; `_run_core.py` `with_graph_config_overrides(generate_graph=graph,...)` (`~:145`) and the post-run `generate_report(pipeline_run_id=..., print_to_console=...)` (`~:289-301`, gated by `cost_report`/`is_log_costs_to_console`/`is_generate_cost_report_file_enabled`).
- Agent CLI `run`: `pipelex/cli/agent_cli/commands/run/...` — has `--graph/--no-graph` only; **no `--cost-report`, never calls `generate_report`** (`with_graph_config_overrides` `~:51`, graph output `~:103-146`).

**Cross-repo consumers:**

- `cocode/cocode/swe/swe_cmd.py` — `get_report_delegate().generate_report()` with **no** `pipeline_run_id` (multiple sites). Re-reports all (leaked) registries → must migrate to render from its returned `pipe_output.tokens_usages`.
- `pipelex-api` — `/pipeline/execute` → `ApiRunner.execute_pipeline`; `/pipeline/start` → `ApiRunner.start_pipeline` calls `pipeline_run_setup` **directly** (the "orphaned open"). No registry calls of its own; `PipelexPipelineExecuteResponse` wraps `pipe_output` (so D2's field is auto-exposed). Leak vanishes once `pipeline_run_setup` stops opening.

---

## 3. Phased plan (keep the tree green at every checkpoint)

Build the new event-driven path alongside the old live registry, cut the renderers over, then delete the old. At no point are both summed (no double-count): the old path renders from the registry, the new from `PipeOutput`; only one renderer is active at a time.

### Phase 1 — Decouple emission: shared transport, two independent gates

- [x] Add `is_generate_costs: bool` to `PipelineExecutionConfig` (`configs.py`), default in `pipelex/pipelex.toml` `[pipelex.pipeline_execution_config]` = `true`; mirror in `.pipelex/pipelex.toml` (real value, never commented). **Also mirrored in `pipelex/kit/configs/pipelex.toml`** (the client `init config` template — it carries `is_generate_graph` too, so it needs the sibling key).
- [x] Generalize `with_graph_config_overrides` → `with_execution_overrides(generate_graph=None, generate_costs=None, force_include_full_data=None, mock_inputs=None)` (updated callers: main `_run_core.py`, agent `_run_core.py`, `graph_rendering.py` ×2, `dry_run_pipeline.py`, `bundle_validator.py`, and all test helpers via rename). `ApiRunner` lives in the separate `pipelex-api` repo and calls `pipeline_run_setup` directly — no change here.
- [x] Add `--costs/--no-costs` typer option to all three main `run` subcommands + agent CLI subcommands; thread `costs` into `with_execution_overrides(generate_costs=costs)`. (Left `--cost-report` in place — removed in Phase 3.) Main CLI: `bool | None` (None=use config); agent CLI: `bool = True` (matches its `--graph` default-explicit style). Agent local path only (`run_pipeline_core`); the agent API path (`run_pipeline_core_api`) doesn't build execution_config.
- [x] Add `emit_graph_events: bool` + `emit_usage_events: bool` to `GraphContext` (D4). Default both `True` (legacy "context present → emit both"); propagated through `copy_for_child`.
- [x] `pipeline_run_setup.py`: broadened the gate `if is_generate_graph:` → `if is_generate_graph or is_generate_costs:`; create `event_log` when `tracing_config.is_enabled`; `open_tracer(event_log=event_log if is_generate_graph else None, ...)` (D5); set `emit_graph_events`/`emit_usage_events` on the context via `model_copy`; call `set_event_log(...)` only when `is_generate_costs and event_log is not None`.
- [x] `runner.py` finally: broadened tracer-close gate to `is_generate_graph or is_generate_costs`.
- [x] `reporting_manager.py`: gate usage emission on `emit_usage_events`. **Post-review hardening (F2):** the gate is hoisted into the dispatcher `_emit_usage_event` (before the context lookup) so it guards BOTH the fast path (`_emit_via_registered_context`) and the runner fallback — rather than relying on the cross-file invariant "the fast path is only reachable when costs on" (which the process-global `_event_log_contexts` + best-effort `clear_event_log` could violate via a leaked context + reused `pipeline_run_id`/`workflow_id`). The fallback's own early-return was removed (now dead). Pinned by `test_emit_usage_event_gating.py::test_registered_context_{suppresses_usage_when_costs_off,emits_when_costs_on}`.
- [x] `wf_pipe_router.py`: kept the `graph_context is not None` gate; wire the tracer event log only when `emit_graph_events`, `set_event_log` only when `emit_usage_events`; **carry the emit flags onto `wf_graph_context`** (open_tracer returns a fresh context with default flags, so they must be copied alongside `parent_node_id`).
- [x] **Extra gate discovered (not in original inventory):** `pipe_run.py` (DIRECT finally) now gates `assemble_graph_on_output` on `graph_context.emit_graph_events`. Needed because costs-default-on keeps the shared event stream alive under `--no-graph`; without the gate, a costs-only run would read its usage events and set a node-less `GraphSpec` (`UsageReportEvent` → `pass` in the assembler, so nodes=[] but the object is non-None → CLI treats it as truthy → graph output under `--no-graph`). Phase 2's D3 generalization of this hook will subsume the gate.
- [x] **Tests:** `tests/unit/pipelex/reporting/test_emit_usage_event_gating.py` (fallback honors `emit_usage_events`), `tests/unit/pipelex/graph/test_graph_context_emit_flags.py` (defaults + `copy_for_child` propagation), `tests/integration/pipelex/pipeline/test_pipeline_run_setup_emit_gates.py` (costs-only registers usage log + sets flags; graph-only does neither). Live registry still accumulates — no renderer change yet.
- [x] `make agent-check` (pyright 0 / mypy clean) + targeted tests green (`reporting` unit, `graph` unit/e2e, `pipeline` integration, `temporal` unit + `temporal/tracing`, `cli` unit/integration/e2e, `pipes` integration, `agent_cli` e2e — 1340+ passed, 1 pre-existing xfail) + `make tb` green.

> **🔶 CHECKPOINT 1 — transport decoupled.** Update §6 cold-start notes: confirm event emission now keys off `is_generate_costs` independent of `--graph`, in both modes; list any match/case or gate sites discovered beyond the inventory above. The registry is still live and still the only thing rendered.

### Phase 2 — Assemble usage onto `PipeOutput` (additive; nothing removed)

> **Review-driven additions (from [`wip/registry/phase1-emit-decoupling-review.md`](wip/registry/phase1-emit-decoupling-review.md)).** Phase 2 reopens both the DIRECT and TEMPORAL assembly arms and the `open_tracer` surface, so the review's F1/E1/A2 cluster lands here rather than as separate patches. The three checklist items tagged **(A2)**, **(F1)**, **(E1)** below are NOT optional polish — without them Phase 2 re-touches each arm and leaves the same gaps. Full `GraphContext.from_execution_config` factory (A1) and the full costs-only tracer skip (E1-deep) + T5 stay **deferred** (see §7).

- [x] **(A2 — do FIRST, single-owner prerequisite)** Threaded `emit_graph_events` / `emit_usage_events` into `GraphTracerManager.open_tracer`, `GraphTracer.setup`, `GraphTracerProtocol.setup`, `GraphTracerNoOp.setup` so the returned `GraphContext` is born with the correct flags. **Removed both post-hoc `model_copy` footguns:** `pipeline_run_setup.py`'s emit-flag `model_copy` is gone entirely; `wf_pipe_router.py`'s remaining `model_copy` now carries ONLY `parent_node_id` (the emit flags ride from `open_tracer`). Defaults stay `True` for direct constructions / `copy_for_child`.
- [x] `pipe_output.py`: added `tokens_usages: list[AnyTokensUsage] | None = None` + `usage_assembly_error: str | None = None` (D2). `prepare_for_temporal` uses `model_copy(update=...)` (preserves both); `rehydrate_pipe_output_with_crate` only touches `working_memory*` (doesn't drop them). No import cycle (cogt doesn't import `pipe_output`).
- [x] DIRECT: renamed `graph_assembly.py` → `tracing_assembly.py`; single `read_events` feeds both `GraphSpecAssembler` (if `assemble_graph`) and `UsageAggregator.aggregate` (if `assemble_usage`); top gate `is_enabled and (graph or costs)` (D3, D5). New `TracingAssembly` model (shared with the Temporal activity) + `assemble_tracing(...)` core + `assemble_tracing_on_output(...)` wrapper. Best-effort try/except mirrors the graph path; read failures set the requested `*_assembly_error`(s), a graph `ValidationError` sets `graph_assembly_error` but usage still aggregates. `pipe_run.py` DIRECT finally now gates on `(emit_graph_events or emit_usage_events)` and passes both flags (subsumes the Phase-1 standalone gate).
- [x] **(E1 — cheap half)** Gated the per-pipe graph serialization on `parent_graph_context.emit_graph_events` (AND the existing `data_inclusion.*`) at ALL sites:
  - `pipe_abstract.py` — **input** IOSpec data dumps (`smart_dump`/`rendered_pretty_text`/`rendered_pretty_html`).
  - `pipe_abstract.py` — **output** IOSpec data dumps (same three on `main_stuff`).
  - `pipe_abstract.py` — **pipe/concept registry** (`self.model_dump(mode="json")` + per-concept `model_json_schema()`).
  - `graph_tracer.teardown` — skips the spec-building (edge-correlation passes + node-spec construction) and returns `None` when `emit_graph_events` is False (flag stored on the tracer in `setup`).
  - **Extra site beyond the plan inventory:** `pipe_parallel.py` `_register_branch_outputs_with_graph_tracer` — early-returns when `emit_graph_events` is False (it builds IOSpec payloads + `register_controller_output` purely for the graph). This also makes `rendered_pretty_*` graph-only callers everywhere → clean E1 spy target.

  `on_pipe_start` still mints node ids (usage-event `node_id` correlation unaffected). Deep optimization (skip the in-memory tracer entirely) stays deferred (§7).
- [x] TEMPORAL: renamed `act_assemble_graph` → `act_assemble_tracing` (file `act_assemble_tracing.py`, fn, `@activity.defn(name="act_assemble_tracing")`, `AssembleGraphArg` → `AssembleTracingArg` with `assemble_graph`/`assemble_usage` flags); returns `TracingAssembly` (named `TracingAssembly`, not `…Result`). `WfPipeRun.run` Step 2 reads `pipe_job.job_metadata.graph_context`, gates dispatch on `(emit_graph_events or emit_usage_events)`, sets `graph_spec` + `tokens_usages`, and on `ActivityError` sets `*_assembly_error` only for the requested concern. Registration in `tasks.py` + `excluded_activities` in `tracing/helpers.py` + skill refs (`mode-2-setup.md`, `queue-options-battery.md`) updated. **(F1 — both sites gated on `emit_graph_events`):** (1) `wf_pipe_run.py` activity dispatch (the activity returns `graph_spec=None` for costs-only since `assemble_graph=False`), AND (2) `wf_pipe_router.py` finally in-memory assignment (now `… and graph_context.emit_graph_events`; `close_tracer` also returns `None` in costs-only via the E1 teardown gate).
- [x] **Tests (no spend):** `test_tracing_assembly.py` (rewrite of `test_graph_assembly.py`) covers the `assemble_tracing` core (flag gating, usage aggregation, costs-only `graph_spec` None, per-concern error fields, graph `ValidationError` still aggregates usage, KeyError propagates, wrapper applies/leaves fields). `test_graph_tracer_emit_gating.py` pins the setup-stamps-flags + teardown-skips-spec gate. `test_direct_tracing_assembly.py` (NEW, DIRECT via `PipelexRunner.execute_pipeline`, dry): costs-on populates `tokens_usages` + `graph_spec`; **costs-only → `graph_spec is None`** (F1 DIRECT); graph-only → `tokens_usages is None`; **E1** — `rendered_pretty_html` spied: called in graph+full-data mode, NOT called in costs-only+full-data. `test_split_worker_usage.py` extended: read-back cross-worker events feed `UsageAggregator` → one record per `UsageReportEvent`, none dropped.
- [x] `make agent-check` (ruff/plxt clean, pyright 0/0, mypy clean) + targeted tests green: pipe_run/graph/tracing unit, pipeline integration (incl new DIRECT test), pipes integration, graph e2e, temporal unit + temporal integration (incl tracing, 42 passed, + non-tracing 96 passed serial), core + reporting (1151 passed) + `make tb`.

> **🔶 CHECKPOINT 2 — usage rides on `PipeOutput` in both modes.** Cold-start notes: record the assembly hook's final shape (generalized vs sibling), the activity rename, and the cross-worker pytest that proves aggregation without spend. **Confirm the review cluster landed:** A2 (flags threaded into `open_tracer`, both `model_copy`s gone), F1 (costs-only `graph_spec is None` in both modes — both TEMPORAL sites gated), E1-cheap (no per-pipe serialization under `--no-graph --costs`). Old registry path still renders; PipeOutput field populated in parallel.

### Phase 3 — Cut renderers over to `PipeOutput`; re-home channel configs; remove `--cost-report`

- [x] Main `_run_core.py`: replaced the post-run `get_report_delegate().generate_report(pipeline_run_id=...)` with the new shared **`render_run_cost_report(pipeline_run_id=, tokens_usages=pipe_output.tokens_usages, is_generate_costs=execution_config.is_generate_costs)`** (new module `pipelex/reporting/cost_report_renderer.py`). The renderer owns the full D6 channel decision (console via `is_log_costs_to_console`, CSV via `is_generate_cost_report_file_enabled`), short-circuits on `not is_generate_costs or not tokens_usages` (falsy guard suppresses the spurious "No report" warning on no-inference runs), and keeps the `(OSError, PipelexError)` "run-succeeded" catch. Dropped the now-unused `get_report_delegate` import from `_run_core.py`.
- [x] Removed `--cost-report/--no-cost-report` from all three main subcommands + the `cost_report` param from `execute_run`/`_execute_run` + the call threading; updated `--costs` help text (no longer mentions `--cost-report`). Changelog deferred to Phase 7.
- [x] Agent CLI (**user-directed: cost in stdout JSON only, no stderr table**): `run_pipeline_core` builds a structured `cost_report` (`{total_cost, by_model:[{model, model_type, nb_tokens_input, nb_tokens_output, cost}]}`, real USD) via the new `CostRegistry.build_cost_summary(...)` and attaches it to the **`--with-memory` JSON envelope** (`side_effects["cost_report"]`). It rides on disk always + in the stdout result under `--with-memory`; the markdown renderer ignores the key → JSON-only. Compact mode stays the bare concept JSON (piping unaffected). No Rich console table on the agent surface. `build_cost_summary` returns `None` for zero-cost runs, so dry runs carry no `cost_report`. Agent API path (`run_pipeline_core_api`) untouched. **Deferred idea (user):** consider renaming `--with-memory` later.
- [x] **D6 applied:** flipped `is_log_costs_to_console` `false → true` in `pipelex/pipelex.toml`, `.pipelex/pipelex.toml`, **and `pipelex/kit/configs/pipelex.toml`** (the client template — kept in sync so a fresh `init config` gets the same default; mirrors how Phase 1 handled `is_generate_costs`). `make tb` green. Removed the per-job live console logging (`is_log_costs_to_console` → `CostRegistry.complete_cost_report` + `log.verbose`) from all four `_report_*_job` methods in `reporting_manager.py`; `is_log_costs_to_console` now means **only** the end-of-run console channel. Also updated `docs/configuration/config-practical/reporting-config.md` (default `true`, end-of-run semantics — it directly documents the key I flipped).
- [ ] **cocode (cross-repo) — DEFERRED to release coordination (NOT a Phase-3 blocker).** cocode pins released `pipelex==0.31.0`, whose `PipeOutput` has **no** `tokens_usages` field; migrating `swe_cmd.py` now would break cocode immediately with no published version to bump to. Phase 3 does **not** break cocode: `get_report_delegate().generate_report()` still exists on this branch and the registry is still populated in parallel — cocode only *must* migrate before **Phase 4** removes the registry/`generate_report`. Do the `swe_cmd.py` migration (`generate_report()` → `CostRegistry.generate_report(tokens_usages=<pipe_output.tokens_usages>, ...)`) + pin bump together when a pipelex version carrying these changes is published.
- [x] **Dry-run suppression (user-directed; gate refined post-review):** never emit a report for a dry run, but DO report real runs even when they cost nothing. Gate is **"did the run do real work" — any tokens OR any cost** (`AggregatedCosts.has_reportable_usage`), NOT total-cost-only. Total-cost-only wrongly hid real runs on free/zero-price models (e.g. Ollama ships `costs = {input = 0, output = 0}`); they now report their token usage with `total_cost` 0, while dry runs (zero tokens, zero cost) stay suppressed. The single source of truth is `CostRegistry.aggregate_costs(...) -> AggregatedCosts` (records, grouped_by_model, model_types, **total_cost**, **total_nb_tokens**), consumed by `render_report` (table/CSV footer), `build_cost_summary` (agent JSON, returns `None` only when no reportable usage), and the renderer's gate — one definition of the run total, no disagreement. `compute_total_cost_of_usages` (the redundant 2nd total computation) was removed; `generate_report` is now `render_report(aggregate_costs(...))`.
- [x] **Tests:** `tests/unit/pipelex/reporting/test_cost_report_renderer.py` (gating: costs-off / tokens-None / tokens-empty / **no-reportable-usage (dry)** / both-channels-off → no-op; **free-model with tokens → rendered**; console-only and CSV-only channel dispatch, spying `CostRegistry.render_report`). `tests/unit/pipelex/cogt/usage/test_cost_registry.py` (`aggregate_costs` totals + `has_reportable_usage` for priced/free/dry; `build_cost_summary` priced / **free→summary with total 0** / dry→None). `tests/integration/pipelex/pipeline/test_cost_report_rendering.py` (hand-built non-zero usage → table shows model + totals + `0.2000`; **free model → table rendered with token counts + `0.0000`**; CSV channel writes a populated file; **DIRECT dry run → usage populated + registry fidelity but table SUPPRESSED (no reportable work)**; `--no-costs` → `tokens_usages is None` → nothing). `tests/integration/pipelex/cli/agent_cli/test_run_cost_report.py` (mocked runner: non-zero usage → `cost_report` in the `--with-memory` JSON envelope; **free model → `cost_report` with total 0**; dry run → no `cost_report`).
- [x] `make agent-check` (ruff/plxt clean, pyright 0/0, mypy 2012 files clean) + `make tb` + targeted tests green: reporting unit (incl new renderer), pipeline integration (incl new rendering + direct-tracing), CLI unit/integration/e2e, agent CLI unit/integration/e2e (incl offline subprocess dry run), temporal unit, system unit (~1170+ passed across batches). cocode suite N/A until its migration (deferred above).

> **🔶 CHECKPOINT 3 — rendering is event-sourced; `--cost-report` gone; D6 applied (`is_log_costs_to_console` now defaults `true`).** Cold-start notes: confirm the only cost renderer is `CostRegistry.generate_report(tokens_usages=...)` fed from `PipeOutput`, the live registry is now unused by any renderer, both TOMLs flipped + `make tb` green, and cocode's migration + pin bump status. This is a natural handoff boundary (a coherent unit landed; the next phase opens a new area — deletion).

### Phase 4 — Remove the live registry + dead code (the leak vanishes structurally)

- [x] `reporting_manager.py`: deleted `_usage_registries`, `open_registry`, `close_registry`, `_get_registry_strict`, `_get_or_create_registry`, `_try_add_to_registry`, `inject_tokens_usages`, `generate_report`, **and the `UsageRegistry`/`UsageRegistryRoot` models** (full removal, no passthrough). `_report_*_job` now call only `_emit_usage_event` (the per-run `pipeline_run_id` local + `_try_add_to_registry` line dropped from each). `setup` clears `_event_log_contexts` (no `UNTITLED` seed). Pruned now-dead imports (`Sequence`, `Path`, `RootModel`/`Field`, `ReportingManagerError`, `CostRegistry`, `LLMTokensUsage`, `SpecialPipelineId`, `TokensUsage`, `ensure_path`/`get_incremental_file_path`, `empty_list_factory_of`); kept `get_config` (runner fallback). `ReportingManagerError` left in `cogt/exceptions.py` as a public error class (out of scope to delete — it owns a `type_uri` + generated doc page).
- [x] `reporting_protocol.py`: dropped `open_registry`/`generate_report`/`close_registry` from `ReportingProtocol` AND `ReportingNoOp`; kept `report_inference_job`/`setup`/`teardown`/`set_event_log`/`clear_event_log` with `@override`.
- [x] `pipeline_run_setup.py`: removed `open_registry` + `registry_opened` + the `close_registry` in `finally` (**the leak fix**). Event-log `set_event_log`/`clear_event_log` + tracer cleanup retained.
- [x] `bundle_validator.py`: removed the per-sweep `open_registry`/`close_registry` + the now-pointless `try/finally` (the loop runs directly). The unique `dry_run_`-prefixed `pipeline_run_id` is still generated and threaded into `prepare_pipe_job`. Dropped the unused `get_report_delegate` import.
- [x] `tests/conftest.py`: removed `open_registry`/`close_registry` from the `job_metadata` fixture (now a plain `return` fixture — ruff converted the teardown-less generator to `return`; annotation updated `Generator[JobMetadata,...]` → `JobMetadata`). Same conversion applied to `workflow_run_id` (temporal/workflows conftest) + `tprl_job_metadata` (temporal/content_generation conftest); dropped their now-unused `Generator`/`get_report_delegate` imports.
- [x] Characterization test: deleted `test_failure_after_open_registry_closes_registry` + `test_failure_before_open_registry_does_not_close_registry` (+ the module-docstring bullet, + the unused `pipeline_run_setup_module` import); **added** `test_success_path_leaves_no_per_run_reporting_state` — DRY `execute_pipeline` with NDJSON tracing + costs on; asserts `_usage_registries` attribute is **gone** and **this run's** `pipeline_run_id` is cleared from `_event_log_contexts` (assert against the run's own key, NOT global emptiness — `_event_log_contexts` is a process-global singleton sibling xdist tests pollute; global-emptiness flaked under `-n auto`, key-specific is robust).
- [ ] **Tests (cross-repo, DEFERRED to release coordination — NOT a CP4 blocker):** `pipelex-api` `/pipeline/execute` + `/pipeline/start` leave no per-run state / reusing a `pipeline_run_id` no longer 500s; cocode multi-run no longer re-reports prior runs. Both repos pin a *published* pipelex (registry still present there) — verify when the pin bumps to a build carrying this branch. Same coordination point as cocode's Phase-3 `swe_cmd.py` migration.
- [x] **Full `make agent-test` green** (6886 passed, 2 skipped, 4 xfailed, 4 xpassed) + **`make agent-check` green** (ruff/plxt clean, pyright 0/0, mypy 2012 files clean).

> **🔶 CHECKPOINT 4 — leak fixed by removal; cost reporting event-sourced end to end (DIRECT + cross-worker via pytest).** This is the primary deliverable landed. Cold-start notes: list every deleted symbol + every updated implementer of `ReportingProtocol`, the new characterization assertion, and confirm full suite green. Commit boundary.

### Phase 5 — P0.1: `--mock-inference` (cheap cross-worker validation affordance)

> **Relationship to [`wip/dry-run-refactor/followup-leaf-run-mode-mock.md`](wip/dry-run-refactor/followup-leaf-run-mode-mock.md) (decided 2026-06-06).** Same mechanism (mock at the inference leaf, *inside* the dispatched activity), two triggers. That deferred follow-up makes `run_mode=DRY` honor the backend (DRY-on-Temporal dispatches and mocks inside) and **subsumes most of this phase** — but NOT all of it (see the reportability note below). We are NOT pulling it onto this branch (it collapses every operator dry path, re-expresses the boot fallback, has open pre-flight questions, and belongs to a different plan). Instead, build the mock **leaf-first so this phase IS that follow-up's Phase B1 core** — a reusable leaf-level dry helper invoked inside the activity bodies — triggered by the interim `is_mock_inference` flag. followup-leaf's B2 then re-keys the same helper from `is_mock_inference` → `run_mode=DRY`; whether it then *fully retires* `--mock-inference` is an open sub-decision parked in §7 (the cheap rendered-report validation is the one thing DRY can't cover). No throwaway either way.
>
> **Why a flag and not just `--dry-run`:** today DRY swaps in `ContentGeneratorDry()` *pre-dispatch* inside each operator's `_dry_run_operator_pipe` (`pipe_llm.py:406`, `pipe_compose.py:294`, `pipe_img_gen.py:304`, `pipe_extract.py:206`, `pipe_structure.py:220`, boot fallback `pipelex.py:368`), so `--dry-run --temporal` never reaches the activity. `--mock-inference` keeps `run_mode=LIVE` (operators dispatch as normal) and fakes only the inference leaf. It is a genuinely *distinct* mode from DRY: LIVE runs real PipeFunc / templating / control flow (`pipe_func.py:262` proves DRY skips user Python) and mocks only the AI call.

- [ ] Add `is_mock_inference: bool` to **`JobMetadata`** — the payload that threads into every `LLMAssignment`/activity, so the leaf can read it. (`run_mode` lives only on `PipeRunParams`, submitter-side, and does NOT currently reach the leaf; `JobMetadata` is the same carrier followup-leaf B1 will use for `run_mode`.) Single-writer from `prepare_pipe_job` off `PipeRunParams`. Add `--mock-inference` to the `run` subcommands (distinct from `--dry-run`; sets `run_mode=LIVE` + `is_mock_inference=True`).
- [ ] **Leaf-first mock (= followup-leaf B1 core):** extract `ContentGeneratorDry`'s bodies into a shared `cogt/content_generation/dry_mock.py` and branch at the leaf (`cogt/content_generation/llm_generate.py` — `llm_gen_text:12` / `llm_gen_object` / `llm_gen_object_list`, plus the extract / img-gen / compose equivalents the activities call): when `job_metadata.is_mock_inference`, route to the dry helper. **No operator dry-site changes** — `run_mode=LIVE` means operators take `_live_run_operator_pipe` and dispatch normally; the activity body just sees the flag.
- [ ] **⚠️ Synthetic usage must be REPORTABLE (interaction with Phase 3's dry-suppression).** `_report_dry_llm_job` (`content_generator_dry.py:38`) emits *zero-token, zero-cost* `LLMTokensUsage` → `has_reportable_usage` is False → the Phase-3 gate **suppresses** the cost report (correct for `--dry-run`, wrong for `--mock-inference`, whose point — and Phase 6 Tier 8b's assertion — is that a report *renders* from cross-worker usage). The mock-inference path must emit **synthetic non-zero** counts (parameterize the shared helper: zero for DRY, non-zero for mock-inference). This is also the durable reason mock-inference ≠ DRY at the reporting layer: DRY (zero/suppressed) can never validate report *rendering* cheaply; only a non-zero mock can.
- [ ] **Tests:** a router+runner run with `--temporal --mock-inference` produces `wf_*__w_act_{pid}_{uuid}.ndjson` (runner-side `writer_id=act_*`) deterministically, a non-empty assembled `tokens_usages`, AND a **non-suppressed rendered cost report** — no LLM spend.
- [ ] `make agent-check` + temporal tests.

> **🔶 CHECKPOINT 5 — cheap cross-process inference mocking works.** Cold-start notes: the flag's name + threading path, the four mock sites touched, and the deterministic NDJSON evidence command. Independent of Phases 1–4's correctness; gates the skill's cheap arm.

### Phase 6 — Upgrade the `temporal-e2e-validate` skill to validate distributed cost reporting

- [ ] `references/mode-2-tiers.md`: add **Tier 8b — Cross-worker cost report assembly**. Using the split router+runner workers + `--mock-inference` (Phase 5) so it's free and deterministic: run a multi-worker bundle with `--temporal --mock-inference --no-logo --costs`, then assert the submitter renders a **single** end-of-run cost report whose usage came from all workers (router + runner; for parent/child, A+B+C). Capture the NDJSON (`writer_id` set incl. `act_*`) and the rendered total. Add a **live arm** (real spend, opt-in) mirroring Tier 8's live block for the real-payload case.
- [ ] Add a `--no-costs` negative check: emits no `usage_report` events and renders no table.
- [ ] `SKILL.md` + `references/mode-2-tiers.md` Step 7 master table: add the Tier 8b row(s); update the description/trigger text to mention cost-report validation. Fix any `act_assemble_graph` references touched by the Phase-2 rename.
- [ ] Reconcile the existing Tier 8 note ("dry-run does NOT exercise this path") to point at `--mock-inference` as the cheap deterministic way (it previously said integration-test or live only).

> **🔶 CHECKPOINT 6 — skill validates distributed cost reporting cheaply + live.** Cold-start notes: the new tier's exact commands, the assertion of a single aggregated report, and which references changed.

### Phase 7 — Docs, changelog, wrap

- [ ] CHANGELOG `[Unreleased]`: `--costs`/`--no-costs` (default on), removal of `--cost-report`, distributed cost reporting now produces a report, registry leak fixed. (Use `[Unreleased]` on this branch.)
- [ ] Update the wip docs to "as-built": flip `wip/distributed-execution/tracing-cost-reporting.md` T2 to FIXED + `README.md` P1 to DONE; mark `wip/registry/` synthesis status as implemented; record any deferred follow-ups (e.g. D5's costs-only tracer skip, cost-per-node correlation) in `wip/registry/` per the deferred-items convention.
- [ ] Final `make agent-check` + `make agent-test`.

---

## 4. Test surface (synthesis §"Test surface to design" → where it lands)

- Success-path no-leak characterization → **Phase 4** (mirrors the deleted failure-path test).
- DIRECT `--costs` renders correct non-zero cost → **Phase 3**.
- DIRECT `--no-graph --costs` still renders (proves decoupling) → **Phase 1** (emission) + **Phase 3** (render).
- `--no-costs` emits nothing, renders nothing → **Phase 1** + **Phase 3**.
- Cross-worker (A/B/C → one report) → **Phase 2** (pytest, wrapper, no spend) + **Phase 6** (CLI, `--mock-inference` + live).
- `pipelex-api` `/execute` + `/start` leave no registry across requests → **Phase 4**.

---

## 5. Risks / watch-items

- **Double-count window:** none by construction — old renders from registry, new from `PipeOutput`; cutover (Phase 3) flips the single active renderer; deletion (Phase 4) removes the registry. The DIRECT fidelity cross-check (Phase 2/3) guards numeric drift between the two sources.
- **The `_emit_usage_event` early-returns** are the subtle correctness core: `graph_context is None` (neither concern) stays; the **runner fallback must additionally gate on `emit_usage_events`** (Phase 1) or graph-only mode wrongly emits usage.
- **`act_assemble_graph` rename** ripples into the skill + temporal tests + worker activity registration — grep before renaming.
- **cocode + pipelex-api are separate repos:** cocode breaks at call time until migrated (Phase 3); pipelex-api needs only verification (leak gone, usage auto-exposed via `pipe_output`). Coordinate the `pipelex` pin bump.
- **Payload size:** `tokens_usages` rides on every Temporal `PipeOutput` when `--costs` on (default). Records are small and the `StoragePayloadCodec` offloads >1MB — fine, but note it.
- **Config↔model↔TOML drift:** `make tb` after every `configs.py` / `*.toml` touch (Phases 1, 3).

---

## 6. Cold-start notes (fill in at each checkpoint)

> Keep this section current so a fresh session can resume without re-reading the whole tree. After each checkpoint, append: what landed, decisions taken/changed, new file:line anchors discovered, and the exact next unchecked box.

- **CP1 (DONE — transport decoupled):** Emission now keys off `is_generate_costs` independent of `--graph`, in BOTH modes. DIRECT (`pipeline_run_setup`): tracer opened when graph OR costs; `event_log` created when tracing enabled; passed to the graph tracer only when graph on (costs-only → `event_log=None`, tracer still mints node ids); `set_event_log` (usage transport) only when costs on; the run's `GraphContext` carries `emit_graph_events`/`emit_usage_events` (propagated via `copy_for_child`). TEMPORAL (`wf_pipe_router`): same gating, plus the emit flags are copied onto `wf_graph_context`. usage emission is gated on `emit_usage_events` in the dispatcher `reporting_manager._emit_usage_event` (hoisted post-review per F2 — guards both the fast path and the runner fallback; see the Phase-1 checklist note).
  - **Gate sites touched (final inventory):** `pipeline_run_setup.py` (tracer-open gate + `event_log if is_generate_graph else None` + flags + `set_event_log` gate), `runner.py` finally (tracer-close gate), `reporting_manager.py` dispatcher `_emit_usage_event` (usage gate, hoisted), `wf_pipe_router.py` (tracer event-log + `set_event_log` gates + flag carry), and **one new site beyond the original inventory:** `pipe_run.py` DIRECT finally gates `assemble_graph_on_output` on `emit_graph_events` (preserves `--no-graph` now that the event stream is shared — see the Phase-1 checklist note). No `match`/`case` exhaustiveness sites were involved (the gates are plain booleans).
  - **Config:** `is_generate_costs` default `true` in `pipelex/pipelex.toml`, `.pipelex/pipelex.toml`, AND `pipelex/kit/configs/pipelex.toml`. `with_graph_config_overrides` fully renamed to `with_execution_overrides` (prod + tests; zero remaining refs).
  - **State:** registry still live + still the only thing rendered (no renderer cutover yet). Success-path leak (registry + event-log context + tracer not closed) is unchanged/intended until Phase 4. Next unchecked box: **Phase 2** — add `tokens_usages` + `usage_assembly_error` to `PipeOutput` (D2), then generalize the DIRECT/TEMPORAL assembly hook to also feed `UsageAggregator`.
- **CP2 (DONE — usage rides on `PipeOutput` in both modes):** Assembly hook is the **generalized** shape (D3): one `read_events`, fed into both `GraphSpecAssembler` and `UsageAggregator`. DIRECT lives in `pipelex/pipe_run/tracing_assembly.py` (renamed from `graph_assembly.py`) — `assemble_tracing(pipeline_run_id, *, assemble_graph, assemble_usage, domain_code, main_pipe_code) -> TracingAssembly` core + `assemble_tracing_on_output(...)` wrapper. TEMPORAL: activity renamed `act_assemble_graph` → **`act_assemble_tracing`** (`act_assemble_tracing.py`, `name="act_assemble_tracing"`, arg `AssembleTracingArg` with `assemble_graph`/`assemble_usage`), returns the SAME `TracingAssembly` model (shared across the boundary; named `TracingAssembly`, not `TracingAssemblyResult`). Cross-worker proof without spend: `test_split_worker_usage.py::test_runner_usage_aggregates_to_tokens_usages` reads the router+runner NDJSON back and asserts `UsageAggregator.aggregate` yields one record per emitted `UsageReportEvent`.
  - **Review cluster confirmed landed:** **A2** — emit flags threaded into `open_tracer`/`setup`; `pipeline_run_setup.py`'s emit-flag `model_copy` deleted, `wf_pipe_router.py`'s `model_copy` now carries only `parent_node_id`. **F1** — costs-only `graph_spec is None` in BOTH modes: DIRECT proven by `test_direct_tracing_assembly.py::test_costs_only_leaves_graph_spec_none`; TEMPORAL both sites gated (`wf_pipe_run.py` dispatch passes `assemble_graph=False` → activity returns `graph_spec=None`; `wf_pipe_router.py` finally gated on `graph_context.emit_graph_events` + `close_tracer` returns `None` in costs-only). **E1-cheap** — per-pipe serialization (`pipe_abstract.py` input/output IOSpec + pipe/concept registry), `graph_tracer.teardown` spec-build, AND `pipe_parallel.py` controller-output registration all gated on `emit_graph_events`; pinned by the `rendered_pretty_html` spy in `test_direct_tracing_assembly.py`.
  - **New file:line anchors:** `pipe_run/tracing_assembly.py` (`TracingAssembly`, `assemble_tracing`, `assemble_tracing_on_output`); `temporal/tprl_pipe/act_assemble_tracing.py` (`AssembleTracingArg`, `act_assemble_tracing`); `graph_tracer.py` `__init__` `self._emit_graph_events` + gated `teardown`; `pipe_output.py` `tokens_usages` / `usage_assembly_error`.
  - **State:** old registry path STILL renders the cost report (no renderer cutover yet); the PipeOutput `tokens_usages` field is populated in parallel. Success-path leak unchanged/intended until Phase 4. Next unchecked box: **Phase 3** — cut the main `_run_core.py` renderer over to `response.pipe_output.tokens_usages` via `CostRegistry.generate_report(tokens_usages=...)`, remove `--cost-report`, apply D6 (`is_log_costs_to_console` default `false → true` in both TOMLs + `make tb`), migrate cocode.
- **CP3 (DONE — rendering is event-sourced; `--cost-report` gone; D6 applied):** The **only** cost renderer is now `CostRegistry.generate_report(tokens_usages=...)`, fed from `PipeOutput.tokens_usages` via the new shared **`pipelex/reporting/cost_report_renderer.py::render_run_cost_report`** (keyword-only: `pipeline_run_id`, `tokens_usages`, `is_generate_costs`). Both CLIs call it: main `_run_core.py` (before the completion recap, using `response.pipeline_run_id` / `pipe_output.tokens_usages`) and agent `_run_core.py` (right after `pipe_output`, using `pipe_output.pipeline_run_id`). The renderer owns the D6 channel decision and the run-succeeded error catch; it no-ops on `not is_generate_costs or not tokens_usages`.
  - **The live registry is now unused by any in-repo renderer** — `ReportingManager.generate_report` / `_usage_registries` are still populated in parallel (deleted in Phase 4) but nothing renders from them on this branch except (out-of-repo) cocode. Success-path leak still present/intended until Phase 4.
  - **`--cost-report` fully removed:** option gone from `pipe_cmd`/`method_cmd`/`bundle_cmd`; `cost_report` param gone from `execute_run`/`_execute_run` (+ the 4 `cost_report=None` test args in `test_traceback_flag_run_core.py`). `--costs` is now the sole cost switch.
  - **D6 applied + verified:** `is_log_costs_to_console` flipped `false → true` in all THREE TOMLs (`pipelex/pipelex.toml`, `.pipelex/pipelex.toml`, `pipelex/kit/configs/pipelex.toml`); `make tb` green. Per-job console logging removed from all four `_report_*_job` (now end-of-run-only). Doc `reporting-config.md` updated.
  - **Dry-run suppression (user-directed, applied; refined post-review):** no cost report (table, CSV, or agent JSON) is emitted for a run that did **no reportable work — zero tokens AND zero cost** (a dry run). A real run is reported even when it cost nothing, so free/zero-price models (e.g. Ollama `costs = {input = 0, output = 0}`) still show their token usage (with `total_cost` 0). The earlier "total cost is 0" gate was a code-review finding (#1: it wrongly hid free-model runs). Single source of truth: `CostRegistry.aggregate_costs(...) -> AggregatedCosts.has_reportable_usage`, shared by the renderer gate, `render_report`, and `build_cost_summary` (#4/#7: one total, no double aggregation). The whole renderer body (aggregation + CSV path setup + render) is now inside the run-succeeded `try` (#2).
  - **Agent CLI cost = stdout JSON only (user-directed, applied):** no Rich table on the agent surface. `run_pipeline_core` attaches a `cost_report` object (`{total_cost, by_model}`, real USD) to the `--with-memory` JSON envelope via `CostRegistry.build_cost_summary`; markdown ignores the key; compact mode stays bare (piping intact). Deferred user idea: rename `--with-memory`.
  - **New file:line anchors:** `pipelex/reporting/cost_report_renderer.py` (`render_run_cost_report` — aggregates once, gates on `has_reportable_usage`, then renders, all inside the run-succeeded `try`) — used by the MAIN CLI only; `pipelex/cogt/usage/cost_registry.py` new `AggregatedCosts` NamedTuple (records, grouped_by_model, model_types, total_cost, total_nb_tokens + `has_reportable_usage`), `aggregate_costs` (single aggregation pass, replaces `_aggregate_by_model`), `render_report` (split out of `generate_report`, renders a pre-aggregated `AggregatedCosts`), `build_cost_summary` (consumes `aggregate_costs`); `generate_report` = `render_report(aggregate_costs(...))`; `compute_total_cost_of_usages` removed; main `_run_core.py` cost block = single `render_run_cost_report(...)` call; agent `_run_core.py` adds `cost_report` to `side_effects` (no console render).
  - **Deferred (code-review #3/#6):** two design decisions captured in [`wip/registry/cost-report-deferred-decisions.md`](wip/registry/cost-report-deferred-decisions.md) — (#3) cost reporting is coupled to `tracing_config.is_enabled` (costs-on + tracing-off ⇒ no report), and (#6) the agent CLI gates costs on the raw `costs` param while the main CLI gates on the resolved `is_generate_costs`. Both need a deliberate call, not a reflexive fix.
  - **cocode status:** migration DEFERRED to release coordination (cocode pins released `pipelex==0.31.0` with no `tokens_usages` field; Phase 3 doesn't break it; it must migrate before Phase 4's registry removal lands in a published pipelex — do `swe_cmd.py` migration + pin bump together then).
  - Next unchecked box: **Phase 4** — delete `_usage_registries`/`open_registry`/`close_registry`/`_get_registry_strict`/`_get_or_create_registry`/`_try_add_to_registry`/`inject_tokens_usages`/`generate_report` from `reporting_manager.py`, drop them from `ReportingProtocol`/`ReportingNoOp`, remove `open_registry`+`registry_opened`+`close_registry` from `pipeline_run_setup.py` (THE leak fix), strip the sweep registry calls in `bundle_validator.py` + the `job_metadata` conftest fixture wiring, and rewrite the characterization test for the success-path no-leak case. Full `make agent-test` required (touches shared run/teardown + hub/reporting).
- **CP4 (DONE — leak fixed by removal; cost reporting event-sourced end to end):** The live `UsageRegistry` and its entire surface are gone. The success-path leak is now structurally impossible (nothing opens a per-run registry, so nothing can fail to close one).
  - **Deleted symbols (`reporting_manager.py`):** `UsageRegistry`, `UsageRegistryRoot`, `_usage_registries`, `open_registry`, `close_registry`, `_get_registry_strict`, `_get_or_create_registry`, `_try_add_to_registry`, `inject_tokens_usages`, `generate_report`. The four `_report_*_job` methods now call only `_emit_usage_event`. `setup` clears `_event_log_contexts` (no `UNTITLED` seed). The emit core (`_emit_usage_event` dispatcher + `_emit_via_registered_context` fast path + `_emit_usage_event_runner_fallback`) is **unchanged** — that's the surviving usage transport.
  - **Protocol (`reporting_protocol.py`):** `ReportingProtocol` + `ReportingNoOp` now expose only `report_inference_job` / `setup` / `teardown` / `set_event_log` / `clear_event_log`. Only two implementers exist (`ReportingManager`, `ReportingNoOp`) — both updated.
  - **Leak fix (`pipeline_run_setup.py`):** `open_registry` + `registry_opened` + the `finally` `close_registry` removed. `set_event_log`/`clear_event_log` + tracer cleanup retained.
  - **`bundle_validator.py`:** per-sweep `open_registry`/`close_registry` + the wrapping `try/finally` removed (loop runs directly); the unique `dry_run_`-prefixed id is still threaded into `prepare_pipe_job`.
  - **`ReportingManagerError`** (cogt/exceptions.py) kept as a now-unraised public error class (owns a `type_uri` + generated doc page — deleting it is a separate error-docs concern, out of Phase-4 scope; only a test *comment* still names it).
  - **New characterization assertion:** `test_success_path_leaves_no_per_run_reporting_state` (DRY `execute_pipeline`, NDJSON tracing + costs on) asserts `not hasattr(delegate, "_usage_registries")` and `response.pipeline_run_id not in delegate._event_log_contexts`. **Key-specific, not global-emptiness** — `_event_log_contexts` is a process-global singleton and global emptiness flaked under `-n auto` (sibling-test pollution).
  - **Collateral test migrations (beyond the plan inventory, all green):** deleted `tests/unit/pipelex/reporting/test_no_orphan_registries.py` (pinned the now-gone three-method split); cleaned `test_reporting_event_emission.py` (dropped registry seeding + the two `inject_tokens_usages` tests; rewrote `test_no_event_log_works_as_before` → `test_setup_and_teardown_without_event_log`); migrated unit + integration `test_bundle_validator.py` (`_patch_env` now returns a 4-tuple — no `report_delegate`; the registry-lifecycle unit test → `test_sweep_threads_unique_dry_run_id_into_prepare`; integration `…use_distinct_registry_ids` → `…use_distinct_run_ids` spying `prepare_pipe_job`; `…do_not_collide_on_registry` → `…run_with_genuine_overlap`); migrated the two `preliminary_text*_e2e.py` `_count_llm_calls_for` → `_count_llm_report_calls(report_spy)` spying `get_report_delegate().report_inference_job` (the canonical per-call hook — workers + the dry content generator both call it with `inference_job=`); removed the `_usage_registries` fidelity block from `test_cost_report_rendering.py`; stripped `get_report_delegate().generate_report()` from the cogt `test_search/img_gen/extract` tests + deleted the standalone `test_llm_report_without_running_anything`; removed the dead commented `# get_report_delegate().generate_report()` lines from `test_llm_inference.py`; dropped registry wiring from `pipe_job_helpers.py` + the emit-gates `_cleanup`.
  - **State:** primary deliverable LANDED. Cost reporting is event-sourced end to end (DIRECT proven by `test_direct_tracing_assembly.py`/`test_cost_report_rendering.py`; cross-worker by `test_split_worker_usage.py`, no spend). cocode + pipelex-api verification stays deferred to the release pin-bump (see the unchecked Phase-4 cross-repo box). **Committed** — Phases 1–4 span `feab157a`…`f533a667` (code tree clean; only Phase-5-prep planning-doc edits pending as of 2026-06-06). Next unchecked box: **Phase 5** — add the `is_mock_inference` signal + `--mock-inference` flag (D7), swap in `ContentGeneratorDry` inside the activity bodies, prove deterministic cross-process `tokens_usages` with no spend.
- **CP5:** _pending_
- **CP6:** _pending_

---

## 7. Out of scope / explicitly deferred

- DynamoDB as a validation target (NDJSON-first; the read-back is backend-agnostic so DDB rides along for free).
- P0.2 follow-ons (`strict_mode`, `BatchWriteItem`, NDJSON shared-FS check, per-thread writer_id, tz-aware `JobMetadata.started_at`).
- D5's costs-only in-memory-tracer skip (the **deep** half of review item **E1** — Phase 2 does the cheap `emit_graph_events` serialization gate; fully skipping the tracer in costs-only mode is the follow-on); cost-per-node correlation via `UsageReportEvent.node_id`.
- The deeper T3 "request-scoped tracing state instead of process-singleton" refactor.
- **Review item A1 (full owner):** a `GraphContext.from_execution_config(...)` factory that owns the entire `(graph, costs) → (event_log routing, set_event_log, flags)` decision for both arms. Phase 2's A2 (flags into `open_tracer`) captures most of the value; the full factory is deferred polish.
- **Review item T5:** `wf_pipe_router.py:122` setup-failure `except` doesn't restore `job_metadata.graph_context`, so the pipe can run with a `tracer_key` pointing at a closed tracer. Pre-existing latent (not introduced by Phase 1, Temporal unshipped) — flag-and-fix candidate.
- **Review transitional T1–T4** are resolved/made-moot by Phases 3–4 (folding `--cost-report` into `--costs`, cutting the renderer to `PipeOutput`, removing the registry); T6 (test-helper dedup / conftest factory) is cleanup to fold into Phase 2's test work. See `wip/registry/phase1-emit-decoupling-review.md` for the full triage.
- **`is_mock_inference` fate after [`wip/dry-run-refactor/followup-leaf-run-mode-mock.md`](wip/dry-run-refactor/followup-leaf-run-mode-mock.md) (decided 2026-06-06: build interim now, settle retirement there).** That follow-up makes `run_mode=DRY` honor the backend and re-keys the shared leaf dry helper (built here in Phase 5) from `is_mock_inference` → `run_mode` in its Phase B2 — so Phase 5 is B1's core, not throwaway. **Open sub-decision for B2:** *fully retire* `--mock-inference` vs *keep a thin reportable-mock*. Full retirement is clean for cross-worker dispatch + `tokens_usages` assembly validation (DRY covers those once it dispatches), but DRY emits zero-token usage and Phase 3 suppresses the report for it — so the **cheap rendered-cost-report validation** (Phase 6 Tier 8b) is unique to a non-zero-usage mock. If that validation must stay cheap, a thin reportable-mock survives; otherwise Tier 8b re-keys to assert `tokens_usages` (not the rendered table) and the flag retires. Decide at B2 time, not now.
