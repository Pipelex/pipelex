# Vacuous presence lint — implementation tracker

**Design:** [`wip/full-optional/design.md`](wip/full-optional/design.md) — read it first; this tracker assumes its decisions (entry-pipe scope, descriptor substrate, one composition point, the `input_presence_vacuous` wire name, the hint-lint cap riding along) and does not restate the rationale. Its §9 holds the three decisions, ratified as designed.

## Live state — rewrite this block, never append to it

Record only what the tools cannot re-derive: the SHA a step landed in, a decision and its reason, the exact error a gate printed. Never branch, PR or working-tree status.

- **Where the work stands:** Phases 1 through 3 are closed and 4.4 is done. 4.3 is in its review-triage loop, each round triaged with every thread replied to and resolved and the bots re-pinged on the fix commit; the loop closes when all three have reviewed the head SHA with nothing open. 4.5 follows.
- **What the `validate all` test found (step 2.1's open question):** `_blueprints` IS populated on the `acquire_library` path, so the accessor works and no fallback was needed. `LibraryManager.get_accumulated_blueprints` returns them, `collect_current_library_entry_pipe_refs()` qualifies each bundle's `main_pipe`, and `test_agent_cli_validate_all_warns_on_the_main_pipe` proves it end to end. The transported-crate path (a direct `load_from_crate`, used by the Temporal workflow) accumulates none, which yields the same silence as a batch with no `main_pipe` — stated in the accessor's docstring.
- **Decisions taken since the design:** §9's three ratified as designed. Beyond them: (a) `build_input_form` gained an optional `qualified_crate` param and `qualify_current_library_crate()` was extracted beside it, so the crate is qualified ONCE per validate instead of twice — the design called this opportunistic and it fell out cleanly, closing that engine-hints deferral; (b) `build_current_library_hint_warnings()` was DELETED rather than kept, since the composition point supersedes it and nothing else called it; (c) the builder's `validate_all` is the one channel left out, recorded with its structural cause in `wip/full-optional/deferred.md`.
- **Gates last run green:** `make agent-check` and the full `make agent-test` both green on `f536e7c9e`, and on `51e0f9d9f` and `24520d69f` before it. No local storage override was in place, so the boot tests ran normally.
- **The §8 inbox items were filed without committing them**, deliberately. All three live in the workspace-root `wip/inbox/` (`2026-08-25-workspace-input-presence-vacuous-spec-and-conformance.md`, `2026-08-25-pipelex-js-input-presence-vacuous-enum-member.md`, `2026-08-25-pipelex-starter-js-point-shape-to-avoid-at-the-lint.md`), each with the frontmatter and the `repo/path` evidence the queue contract asks for. That repo carries a large set of staged deletions from an earlier triage that is not this session's to sweep up, so committing there would drag them in.

## Standing rules

- **Branch:** `feature/Full-optional` off `dev` — it already carried the design commit when implementation started, so the plan's `feature/Full-optional-lint` was never cut. The PR targets `dev`. The prefix vocabulary is closed and spelled out (`feature/`, never `feat/`).
- **Tests before code** (red → green) for every rule in the design's decision table; mutation-test a green test by breaking the predicate it guards and watching it go red.
- **Gates before any push:** `make agent-check` and the full `make agent-test` (never `make test`). If the local `.pipelex/pipelex_override.toml` storage override is still in place, the boot tests it reds are pre-existing — move it aside for the full run.
- **Docs in the same change**, changelog under `## [Unreleased]` only, no counts, no mention of `wip/` docs in the changelog.
- **Cross-repo work goes to `../wip/inbox/`** (design §8) at close — never widen this diff into another repo.
- **Update this tracker as work happens:** tick a box the moment its step is done, append a dated line to the progress log after every commit, gate run, decision and surprise, and rewrite the live-state block the moment it stops being true.

## Phase 1 — the lint itself

Pure, unit-tested, not yet wired anywhere.

- [x] 1.1 Add `PipeValidationErrorType.INPUT_PRESENCE_VACUOUS = "input_presence_vacuous"` with its advisory-only comment beside `OPTIONAL_FORCE_REDUNDANT`; extend the enum's exhaustive `match` properties (`is_controller_input_drift`, `is_inadequate_output`) so pyright stays green; confirm `tests/unit/pipelex/errors/test_validation_error_types.py` picks the member up unchanged.
- [x] 1.2 Write the unit decision table first, red: `tests/unit/pipelex/pipeline/test_vacuous_presence_warnings.py` over hand-built `PipeInputFormDescriptor`s — one case per row of design §3 (all-optional object warns; one required field is silent; field-less object warns with the variant wording; `?` is silent; variable list silent; fixed-count list silent; every scalar/file kind silent; `unknown` silent), plus: a non-entry pipe with the warning shape is silent, `!` warns and names the marker, output order is by entry pipe ref then authored slot order, and the item shape (category `pipe_validation`, bare `pipe_code`, `domain_code`, `variable_names`, no `concept_code`).
- [x] 1.3 Implement `pipelex/pipeline/vacuous_presence_warnings.py`: `build_vacuous_presence_warnings(*, input_form, entry_pipe_refs) -> list[ValidationErrorItem]`, the two message variants of design §D7, module docstring stating the rule and the one-level depth.
- [x] 1.4 Green; mutation-test at least the `gating` read (flip to `required`: the variable-list case must go red) and the `required` scan (drop the empty-fields branch: the field-less case must go red).

## Phase 2 — composition point, entry pipes, every channel

- [x] 2.1 Add the accumulated-blueprints accessor to `LibraryManagerAbstract` / `LibraryManager` (design §D2). Write the `validate all` test first: a library dir holding a bundle with a `main_pipe` whose gating input is all-optional must produce the warning on the `validate all` envelope — this is the test that proves `_blueprints` is populated on the `acquire_library` path; if it is not, record what you found in the live-state block and decide between populating it and deriving entry refs another way before going on.
- [x] 2.2 Add `pipelex/pipeline/advisory_warnings.py` (design §D6): `build_advisory_warnings(*, taint_analyses, input_form, entry_pipe_refs, qualified_crate)` pure, and `collect_advisory_warnings(*, pipes, entry_pipe_refs)` gathering the ingredients inside the window (taint walk, descriptor, the current library's crate qualified once). A helper that qualifies every bundle's `main_pipe` with `PipeFactory.make_pipe_ref_with_domain` — the spelling `select_primary_blueprint` uses — lives beside it or in `blueprint_selection.py`.
- [x] 2.3 Hint-lint prerequisite (design §D6): in `hint_warnings.py`, elide interpolated authored tokens beyond a fixed length and cap findings per site with an "and N more" tail; unit-test both in `test_hint_warnings.py`. Extend the existing cap-free tests only where the cap changes their expectation.
- [x] 2.4 Switch every site to the composition point: `validate_in_process.py` (pure builder, with the `input_form` and taint analyses it already computes and the entry refs from `result.blueprints`); every site in `cli/agent_cli/commands/validate/_validate_core.py` and in `builder/operations/validate_ops.py`; the bare CLI's `_echo_optionality_warnings` becomes `_echo_advisory_warnings`. Remove the direct `build_optionality_warnings` imports at those sites; fix the docstrings in `validation_report.py` and `base_exceptions.py` that name it as the warnings source.
- [x] 2.5 Integration tests, protocol path (`tests/integration/pipelex/pipeline/test_protocol_validate.py` or a sibling module): entry pipe with a plain all-optional input warns and the report stays valid and runnable; the same input shape on a non-main pipe of the same bundle is silent; a class-backed concept whose pydantic fields all carry defaults warns; a batch with no `main_pipe` is silent; `?` on the same slot is silent; the hint lint still rides beside it.
- [x] 2.6 Agent-CLI envelope test for `validate bundle` (the warning rides `warnings`, `is_valid` stays true, exit code unchanged) and the markdown rendering shows it under "Warnings"; builder `validate_bundle_content` twin.
- [x] 2.7 Verify the three presentation surfaces the engine-hints deferral asked to check now that hint lints reach them: agent-CLI JSON and markdown, bare-CLI yellow echo, builder-ops JSON — one fixture with all three families firing at once, asserting family order and item shape.

**CHECKPOINT 1 — after Phase 2.** ✅ Reached. The live-state block above records what the `validate all` test found, the three deviations from the design, and where the gate results live.

## Phase 3 — vocabulary, docs, changelog

- [x] 3.1 `generate_corpus_vocabulary_cmd.py`: add the advisory exclusion for `input_presence_vacuous` (same reason as `optional_force_redundant`); run `make generate-corpus-vocabulary`; confirm the corpus exhaustivity and linter-exclusion gates stay green.
- [x] 3.2 `docs/building-methods/pipes/understanding-optionality.md`: the `warnings` bullet becomes a short list of the advisory lints (useless `!`, vacuous presence, the intent-hint trio), each with its wire name and one sentence.
- [x] 3.3 `docs/building-methods/concepts/inline-structures.md`: a note that `required` defaults to `false` in the long form, that the shorthand string field is required, and that an all-optional structure named by a gating method input triggers `input_presence_vacuous` — with the two remedies.
- [x] 3.4 `docs/under-the-hood/input-form-descriptor.md` § "Required, presence and gating": state that the lint is defined on `gating` and why.
- [x] 3.5 `docs/tools/cli/agent-cli.md` advisory note: list the three families; delete the sentence saying the hint lints are not wired into the CLI array; keep the single-pipe exclusion.
- [x] 3.6 `docs/under-the-hood/error-model.md` and `docs/contribute/mthds-test-corpus.md`: the advisory-only registry members are now several; reword the "one has no shape" passages accordingly.
- [x] 3.7 `CHANGELOG.md` `[Unreleased]`: one **Added** entry for the lint (bold label, a few sentences: what fires, where, the two remedies, entry-pipe scope) and one **Changed** entry for the composition point (every whole-bundle validate channel now carries the same advisory families, the hint lints included, with the message cap). No counts.
- [x] 3.8 If `docs/specs/` links or a drift contract fire on any touched file, resolve through `make drift-plan` / `make drift-ack` with an honest rationale.

## Phase 4 — gates, PR, follow-ups

- [x] 4.1 `make agent-check` green.
- [x] 4.2 Full `make agent-test` green (override moved aside if needed); record the run in the progress log.
- [ ] 4.3 Commit and open the PR against `dev`; wait for Greptile and Codex by SHA (`gh pr view --json reviews,comments`), triage with `/review-pr-agents`, reply and resolve threads in the same pass.
- [x] 4.4 File the inbox items of design §8: `workspace` (protocol spec section + conformance rows and tests), `pipelex-js` (`categorize.ts` member), `pipelex-starter-js` (docs pointer). Each with `repo/path:line` evidence.
- [ ] 4.5 Add a dated status line to `wip/full-optional/design.md` naming the merge SHA, and rewrite the live-state block here for the last time. Do not delete or archive this tracker — that is a separate decision after review.

**CHECKPOINT 2 — after Phase 4.** The work is done when the PR is merged and the inbox items are filed; the release that carries the lint is what unblocks the conformance and corpus-consumer halves, and that version goes in the live-state block when it is cut.

## Progress log

- 2026-08-25 — Design written to `wip/full-optional/design.md` from the inbox request; the three amendments (entry-pipe scope, descriptor substrate, one composition point) recorded as decisions to ratify. No code yet.
- 2026-08-25 — Phase 1 done. The enum member landed with its advisory comment and all five exhaustive `match` properties extended (the tracker named two; there are five). `build_vacuous_presence_warnings` is pure over `PipeInputFormDescriptor`s and never touches the crate.
- 2026-08-25 — **Surprise, and a correction to step 1.4's stated expectation.** Flipping the `gating` read to `required` did NOT redden the variable-list case: a `Concept[]` slot is a `list` node, so the `kind != OBJECT` check excludes it before `gating` is ever consulted — and on an object node the deriver computes `gating = required and not (is_list and no item_count)`, which for a non-list kind is exactly `required`. The two reads are therefore indistinguishable on every descriptor the deriver builds today. Added `test_a_non_gating_object_slot_is_silent_even_when_required`, a hand-built object slot with `required=True, gating=False`, which is the only shape that can tell the reads apart; the mutation now reddens it. The `required`-scan mutation (refusing the empty-`fields` case) reddens the field-less test as the tracker predicted.
- 2026-08-25 — Phase 2 done, CHECKPOINT 1 reached. `advisory_warnings.py` composes the three families in one fixed order (optionality, presence vacuity, hints); `collect_entry_pipe_refs` lives in `blueprint_selection.py` beside the one-main-pipe rule it deliberately differs from. Every warning-assembling site switched — `validate_in_process`, the agent CLI's, the builder's, and the bare CLI's echo (now `_echo_advisory_warnings`). The hint lint gained its payload bounds (`MAX_HINT_FINDINGS_PER_SITE`, `MAX_AUTHORED_TOKEN_LENGTH`), which was D6's stated prerequisite for letting it reach a channel an author invokes casually.
- 2026-08-25 — **Scope call.** The builder's `validate_all` was left without a `warnings` key. It never had one, and it cannot grow one at the call site: it delegates to `acquire_and_validate`, which tears its library down before returning, so there is no open window left to collect in. Fixing it means reshaping a method several callers share — outside a lint's blast radius. Written up with its remedy in `wip/full-optional/deferred.md`; the same conclusion was already reached in `wip/pr-1026-review-notes.md`.
- 2026-08-25 — Phase 3 done. Corpus vocabulary regenerated with the advisory exclusion (one added `[error.input_presence_vacuous]` block, nothing reshuffled). Docs updated across the six targets, plus two fixes to things the change made visible: the bare CLI's `docs/tools/cli/validate.md` had never documented its yellow advisory echo at all, and `docs/under-the-hood/error-model.md` plus the `validation_error_types` module docstring still said the registry was a union of THREE enums — stale since `HintLintErrorType` landed.
- 2026-08-25 — Two gate surprises, both resolved. (1) `make agent-check` ran `fix-keyword-only`, which keyword-only'd `elide_authored_token`'s subject as CLAUDE.md warns it will; taken as-is and the three call sites made keyword, rather than recording a grant. (2) The full `make agent-test` reddened `test_runner_validate_plumbing.py` — it mocks the window-dependent artifact builders by name at the `validate_in_process` namespace so the body runs with no real library, and the new `qualify_current_library_crate` / `build_advisory_warnings` were not in that list. Worth knowing: the OLD hint-lint wrapper was lenient about a missing current library and the new helper is strict, which is what turned an unmocked name into a raise.
- 2026-08-25 — Both gates green on the full tree: `make agent-check` clean, `make agent-test` all passed.
- 2026-08-25 — PR #1152 opened against `dev`; all CI checks green. Round one of review triage: Codex raised two P2s, verified empirically by a read-only subagent before either was acted on.
- 2026-08-25 — **A pre-existing descriptor bug the lint made visible, fixed in `24520d69f`.** Codex was right that the field-less message variant was unreachable, and the cause was worse than reported: `_blueprint_node_for_chain` decided a concept was structured by testing the merged structure table for *truthiness*, while the engine's own `ConceptFactory` branches on `structure is not None`. An authored-but-empty `[concept.X.structure]` — legal MTHDS, backed at run time by a field-less structured model with an empty object schema — therefore fell through to the prose branch, and the descriptor claimed `kind: "prose"` with a `refines` link to `native.Text` nobody authored, contradicting the schema beside it. `_merged_structure` now answers `None` versus `{}`. Both new tests were mutation-checked by restoring the truthiness test and watching them redden.
- 2026-08-25 — **Codex's second finding declined.** Adding an abstract `get_accumulated_blueprints` does break any external `LibraryManagerAbstract` implementation, but none exists in or outside this repo, `docs/specs/pipelex-transport-boundary.md` forbids consumers reaching library-manager internals, and the suggested `return []` default would silently report zero entry pipes rather than fail loudly. Recorded as breaking in the changelog instead, matching how `build_validation_report`'s required-argument change was handled.
- 2026-08-25 — Round two: cubic raised ten threads. Seven were acted on in `51e0f9d9f`, two declined with reasons on the thread, one taken half-way. The one that mattered: **a domain is a dotted path** (`is_domain_code_valid` accepts `legal.contracts`), so `pipe_ref.partition(".")` was truncating the warning locator's `domain_code`. Both advisory lints now split through `QualifiedRef.parse` — the optionality lint carried the same bug before this branch and was fixed alongside, per the repo's flag-and-fix-existing-bugs rule.
- 2026-08-25 — **The 'every channel agrees' claim was overstated and is now narrowed rather than widened.** Two surfaces carry no advisories: the builder's `validate_all` (structural, already deferred) and the bare CLI's single-pipe `validate <PIPE_CODE>`, which makes no bundle-wide claim and which neither lint has anything to say about. The changelog, the CLI reference, the optionality guide and the channel test's docstring now all say so. Declined: dropping the hint-lint assertions from the agent-CLI test (that contract change is the point of this PR) and genericizing the design's §8 cross-repo paths (a follow-up item has to name repo/path to be actionable).
- 2026-08-25 — A real test leak cubic caught: two channels the advisory test calls directly (`validate_bundle` and `do_validate_all_libraries_and_dry_run`) leave a library current for a caller that does not run under pytest. An autouse fixture now tears down whatever was left and restores the outer binding.
- 2026-08-25 — All twelve review threads replied to and resolved in the same pass; the three bots re-pinged with their exact trigger phrases.
- 2026-08-25 — Round three. Greptile came back 5/5 with no blocking failures, and CI went fully green. Codex raised one further P2 and cubic seven more (one P2, six P3).
- 2026-08-25 — **The empty-structure fix had a twin one layer down, and Codex found it.** `reflect_structure_class` answers `None` for two different situations — a registered class declaring no field, and a field whose annotation it could not map — and `_class_backed_node` treated both as opaque, so a class-backed concept demanding nothing came back `kind: "unknown"` and the object-only guard silenced the lint. The deriver now separates them: zero fields is a reading, not a failure to read. The shared helper keeps its `None` because its other consumer, the native consistency probe, reads that as "structureless by design" — changing it there reddened `Dynamic` and `Composite`, which is how that distinction was found. Declined the other half of the finding: a model whose fields could not be read cannot honestly be told it "declares no required field".
- 2026-08-25 — cubic's enum-convention point taken: the lint reads `FieldKind.is_object` (a new property mirroring the `is_list` beside it) and the `PresenceMarker.is_force` that already existed, rather than comparing members. Its P2 about `load_empty_library` accumulating one library per call was verified and **declined for this PR** — the fixture is class-scoped and shared by most of the suite, several modules calling it a dozen-plus times, so this is a conftest-level decision rather than a lint's; written up in `deferred.md`.
- 2026-08-25 — Remaining cubic P3s all applied: the error-model page no longer says the registry is the enums the runtime *raises* (advisory members are never raised) nor that the corpus `error.*` namespace is still to come, and the design doc's status and channel-contract sentences match what actually landed.
- 2026-08-25 — Round three landed in `f536e7c9e` (the deriver fix and the enum-convention reads) and `a10765d35` (the error-model, design and deferred-doc wording), with `make agent-check` and the full `make agent-test` green on that tree. All eight threads replied to and resolved in the same pass, and the three bots re-pinged.
- 2026-08-25 — Round four. CI fully green on `a10765d35` and Greptile at six reviews with nothing blocking; cubic raised five more. Three taken: the hardcoded site count in this log dropped rather than corrected (the workspace rule is no counts at all), the design's §9 heading no longer presents settled decisions as pending, and — the one that was a real inaccuracy — `deferred.md` claimed the vacuous lint never comments on a library you merely depend on, which holds on the bundle channels but NOT on `validate all`, where `collect_current_library_entry_pipe_refs()` reads the same accumulated blueprints the hint lint does.
- 2026-08-25 — **Two round-four P2s declined, both about the integration module's library lifecycle.** One repeats the `load_empty_library` accumulation already deferred. The other asks `_warnings` to restore the outer current-library binding instead of clearing it; verified false — `load_empty_library` captures `prev_library_id` before the class's FIRST `_load` (the `if library_id is None` guard) and restores it at class teardown, so the outer binding survives every intervening clear. What the `finally` clears is the library `validate_bundle` leaves open on success, which is exactly the binding that should not escape the test.
- 2026-08-25 — Round five, all three taken and all documentation. The design's D2 said "the bare CLI" holds `ValidateBundleResult.blueprints`, which is true of its `validate bundle` and false of its `validate all` — the same distinction round four corrected in `deferred.md`, missed one doc over. The other two were this tracker contradicting its own storage rule: the live-state block recorded PR and branch status, and the header still called §9 pending. The branch deviation moved into the standing rule that named the wrong branch, which is where it belongs.
- 2026-08-25 — Round six. Codex raised one P2 and it was **declined**: a `@model_validator` can reject the `{}` the descriptor calls satisfiable, which is true, but both offered remedies cost more than the symptom (dropping class-backed coverage, or running author validator code from inside a lint) and the blind spot belongs to `InputFormDeriver.is_required()`, which the form is rendered from — so the lint is reporting what the form will actually do. Recorded in `deferred.md` with the shape a real fix would take.
