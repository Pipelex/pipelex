# Smart Inputs track — current work

Both native-concept prerequisites have landed on this branch (`feature/Smart-inputs`): **`YesNo`** (PR #1028, open → `dev`) and **`Date`** (PR #1029, merged in). What remains is the **Smart Inputs system itself** (signature-driven input shaping, decisions D1–D11).

**Next step (intended for a fresh session):** write `wip/inputs/smart-inputs-implementation-plan.md`, then execute it.

- **Start here:** `wip/inputs/README.md` — track roadmap, current state, and the cold-start facts for writing the plan.
- **Design (approved, do not relitigate):** `wip/inputs/smart-inputs-design.md` (D1–D11 + §9 surfaces checklist).
- **Plan templates to mirror:** `wip/inputs/datetime-implementation-plan.md` (best), `wip/inputs/yesno-implementation-plan.md`.
- **Deferred notes to triage into the plan:** listed at the bottom of `wip/inputs/README.md`.

The YesNo PR reviewer's guide that used to live here is archived at `wip/inputs/yesno-pr-reviewers-guide.md`.
