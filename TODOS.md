# S7b — the inference-backend surface: implementation plan

> **Charter:** [`wip/migrator-3/sequencing.md § S7b`](../wip/migrator-3/sequencing.md) (workspace root). **Substance:** [`wip/migrator-3/backend-config-surface.md`](../wip/migrator-3/backend-config-surface.md). **Ruling:** R10 (2026-08-17) — build the real fourth surface, not a doctor row and not an `update` bolt-on. Both documents were read in full when this plan was written (2026-08-17); this file orders the work, they hold the reasoning. When they disagree with what the code turns out to do, the code was measured later — record the disagreement in the Status block and in the substance document's *Decisions owed*, do not silently pick one.
>
> **Venue:** this worktree, `_backend/`, on `feature/Backend-surface`, cut from `dev` = `61c4786b6` (the reshape merge). Treat this worktree as the repo root; paths below are relative to it. `pipelex/` (the sibling checkout) stays on `dev` and is not touched. Nothing runs alongside S7b.
>
> **Why it blocks the release, in one sentence:** the reshape release is the first to carry `#1104`, which deleted `prompting_target` from the model-spec blueprint and from every backend TOML we ship — but `pipelex init` never overwrites, so the key survives in `inference/backends/*.toml` of every existing installation, where it is fatal twice (`extra_forbidden` from `[defaults]`; `NOT_HEADER_SHAPED`, fatal even leniently since `#1109`, from per-model entries), and no surface claims those files, no walk reaches them, and the coverage gate never asks because `#1104` predates the schema-1 cut. The remedy must ship *in* the release that breaks the file, since a user runs `pipelex migrate` after upgrading.

**What we are doing in one sentence:** make `<config dir>/inference/backends/*.toml` a real migration surface (`inference-backend`, registry + ledger + goldens + boot tolerance), ship its first entry — a `pre_history` `delete_key` of `prompting_target` over every root table — and, in the same PR, account for the `pipelex.toml` half of `#1104` with a `pre_history` entry inserted as `pipelex-config@2`, renumbering the reshape to `@3`.

## The shape of the change, in one picture

```
today                                            after S7b
─────                                            ─────────
registry: 3 surfaces, claim by file NAME         registry: 4 surfaces, claim by (subdirectory, name)
walk: root dir only, iterdir, not recursive      walk: each surface's own dir = root / surface.subdirectory
  ~/.pipelex/pipelex.toml        pipelex-config    ~/.pipelex/inference/backends/openai.toml   inference-backend
  ~/.pipelex/telemetry.toml      telemetry-config  ~/.pipelex/inference/backends/pipelex_gateway.toml
  ~/.pipelex/pipelex_service.toml service-config       ↑ matches pipelex_*.toml, must NOT go to pipelex-config
                                                        (the landmine; test first)
ledgers                                          ledgers
  pipelex-config@2  = reshape                      pipelex-config@2  = pre_history: prompting_config → templating_config
  telemetry-config@2 = pre_history                 pipelex-config@3  = reshape (renumbered)
                                                   telemetry-config@2 = pre_history (unchanged)
                                                   inference-backend@2 = pre_history: delete_key ["*"] prompting_target
boot: 3 loaders warn on a stale file             boot: 4 loaders — InferenceBackendLibrary.load joins them
```

## Rules of engagement

- **Checkpoints are mandatory stops.** At each ⛔ CHECKPOINT below the agent MUST stop working, complete the checkpoint checklist (status update, gates, commit), and end the session — even if context feels fresh. The next session cold-starts from this file's Status block, the charter and the substance document. Two of the checkpoints are also **(Louis)** goes: the design ratification at CP1 and the merge at CP4.
- **Update this file as you go.** Tick checkboxes when items complete; record decisions in the Status block the moment they are taken, not at the end. The four *Decisions owed* (D1–D4 below) each get a one-line ruling in Status **and** in `wip/migrator-3/backend-config-surface.md § Decisions owed` (workspace repo — edit it, say so in the hand-off, do not commit it; that is Louis' call).
- **TDD.** Every behaviour below is written as a failing test first (red), then made green. Mutation-test the tests that guard a rule (break the rule, watch the test go red) — the collision test and the "no walk into `deck/`" test in particular.
- **Verify, don't assume.** The workspace has watched premises die on re-measurement. Phase 0 re-measures the breakage on this laptop before a line is written.
- **The tool migrates this laptop, not the agent.** `~/.pipelex/inference/backends/` is a genuine stale specimen and is the first-machine evidence for S7's release step. **Do not hand-edit it, do not `pipelex init --reset`, do not run `pipelex migrate` for real (non-`--dry-run`) on it during this session.** Read-only probes only. If a value from it is needed in a fixture, hand-author the fixture from the file's table headers — never copy a personal file into the tree.
- **A ledger says what *we* removed.** The backend entry deletes `prompting_target` and nothing else. It must not touch `pipelex_gateway.toml`'s *served* specs (they are not in the file) and must not try to repair a user's own unknown per-model key (that is `#1109`'s rejection, and its message already names file, key and fix).
- **Rules from the memory index that bite here:** `git checkout` restores from the INDEX and deletes unstaged work — never "restore" with git mid-loop; `make umig` on an unchanged head is a no-op, and a `umigf` is only for an unreleased format change; drift-check reads the index (stage first); a new error class needs `make gei` + `make gep` and only the **full** `agent-test` catches it; a type projection cannot decide schema membership; a generated artifact (the goldens) is never a drift review target; record a subject grant BEFORE the first `agent-check` on a new module.

### Gates — the full checklist, run at every checkpoint

`make agent-check` and `make agent-test` do not cover everything this plan disturbs.

1. `make agent-check` clean.
2. `make cl` (`check-ledger`) and `make cmig` (`check-migration-schemas`) green — `cmig` is **not** in `agent-check`; it is in `make check`. Run both by name.
3. `make tb` (boot smoke) green — the registry imports the real config models, and a fourth surface that cannot build its defaults layer fails here first.
4. Full `make agent-test` green — this is what catches a new error class needing `make gei` / `make gep`, and a renamed test leaving `.test_durations` stale.
5. **`git add` the work, THEN `make drift-check`.** `drift-check` reads the git *index*; unstaged work reads as a false green. Touching `pipelex/system/configuration/config_surface.py` or `config_loader.py` opens `config-docs` (review: `docs/configuration/**`, `pipelex/migration/ledgers/`, `configs.py`, `config_cogt.py`); touching `pipelex/cli/commands/init/*.py` or `doctor_cmd.py` opens `cli-docs`. Resolve via the drift-review flow (`make drift-plan`, review for real, `make drift-ack CONTRACT=… RATIONALE="…"`); no bypass. Nothing in `drift.toml` triggers on `pipelex/migration/**` or `pipelex/cogt/model_backends/**` — so those edits open no contract, and their docs (`docs/migration-ledger.md`, `docs/configuration/config-technical/inference-backend-config.md`) must be updated by discipline, not by a gate.
6. `make docs-check` (strict mkdocs build) after any `docs/` edit.
7. Reconcile `.test_durations` after any test file/class/function rename (`.venv/bin/pytest --collect-only -q -m ""` to list ids).
8. `make check` once before the PR (it is what CI runs; it includes `cmig`, `openapi-check` and the drift gate).

## Status

**Current phase:** Phase 0 **done**; **Phase A done and committed** (`feae4db1a`, 2026-08-17). ⏸ **PAUSED here, mid-plan, at Louis' request** — not a checkpoint, just a clean seam: `git status --short` is empty, every gate Phase A owed is green, and **Phase B is not started**.

**Cold-start for the next session, in order:**

1. Read this Status block whole — every design decision Phase B is meant to build is already ruled here (D1–D4), measured against the code (F4–F12), and **refined into a concrete shape by the Phase B reading pass** (B1–B9 below, which is the expensive part of this session and must not be re-derived). Do not re-read `fingerprint.py`, `ledger_check.py`, `transform_check.py`, `diagnosis.py`, `snapshot.py`, `goldens.py`, `model_spec_factory.py`, `model_spec_keys.py` or `backend_library.py` to rediscover what B1–B9 already say; open them to write in them.
2. Skim the charter (`wip/migrator-3/sequencing.md § S7b`) and the substance (`wip/migrator-3/backend-config-surface.md`) for *why*; this file holds the *what* and the *order*. Where they disagree with F4–F12 or B1–B9, the later measurement wins — that is what the header of this file instructs.
3. Resume at **Phase B, first item: the partial projection** (B7 below gives its exact shape). Then the rest of Phase B in order, then **stop at CP1** (a genuine mandatory stop and Louis' ratification of D1–D4).

**What is NOT yet done and must not be assumed:** nothing in Phase B is started. There is no fourth surface, no `DocumentShape`, no third `DefaultsLayerKind`, no projection model, no `inference-backend` ledger, no goldens. B1–B9 are designs measured against the code, not code.

**Preconditions as re-measured on 2026-08-17 (Phase 0, read-only):**
- `git rev-list --left-right --count origin/dev...HEAD` = `0 2` — `dev` has not moved from `61c4786b6`; the branch carries `583f50de7 tidy` and `343bb3fa7 plan` (this file). `git status --short` empty.
- `grep -rn prompting_target ~/.pipelex/inference/backends/` still finds it: the `[defaults]` block of thirteen backend files there, plus five per-model occurrences in `portkey.toml`. **The premise is alive.**
- The loader probe from the substance § *Reproducing the evidence*, run from `.venv/bin/python` against the two real files, reproduces both halves exactly: `openai.toml` → `extra_forbidden ('prompting_target',)` on **all** of its model tables (every table fails, because the key sits in `[defaults]` and is merged into each); `portkey.toml` → the same on all of its tables, **plus** five `NOT_HEADER_SHAPED` rejections carrying the full remedy sentence.
- `~/.pipelex/inference/backends/` also holds `pipelex_inference.toml` (not kit-shipped) and two `.md` files — both specimens are real on this machine.
- **The registry-driven claim holds** (the assumption R10's cost estimate rests on). `build_config_surface_registry()` has exactly four non-test callers: the three dev-CLI commands (`pipelex/cli/dev_cli/commands/{check_ledger,check_migration_schemas,update_migration_schemas}_cmd.py`) and `migration/run.py`, which is what both `migrate` commands, the `doctor` row and the boot diagnosis go through. Each reaches the registry plus one top-level function. A fourth `Surface` plus a fourth ledger enters all of them without an edit — with the test-side exceptions in F8 below.

**Rulings taken (recommendations, for ratification at CP1):**
- **D1 — the surface claims every `*.toml` directly in `inference/backends/`, and nothing else.** Not `inference/backends.toml`: it sits one level up, it is a different shape (a table per backend, not per model), and `#1104` never touched it. The `.md` files fall out by extension. Reason goes in the registry docstring.
- **D2 — `inference/deck/` and `inference/routing_profiles.toml` do not join.** The deck already has a manifest-driven sync with the `x_custom_*` user-owned carve-out, which the migrator must not duplicate; routing profiles have neither mechanism and no unreleased break. Reason goes in the registry docstring beside the surface, not only in the wip document.
- **D3 — the shape, as designed against the code (see F4–F9):**
  - a third `DefaultsLayerKind`, **`COPIED_DOCUMENT`**: *a document shipped in the package and **copied** into the user's directory rather than merged beneath it; the values come from the model's own field defaults, applied to each entry as it is read.* It names the contrast with `PACKAGED_DOCUMENT` (merged beneath) in one word.
  - `reference_document_path` = the kit's **`portkey.toml`**. It is the one shipped backend file that exercises both halves of the document shape — a `[defaults]` block *and* header-shaped per-model keys — and it is the file whose per-model tables carried `prompting_target`, so it is the richest starting document a later non-pre-history entry could be replayed over. (`openai.toml` is the more typical file and would also work; this is the sub-decision to overturn at CP1 if you prefer typical to rich.)
  - `reference_documents()` = the reference document plus every other `*.toml` in the kit's backend directory, as convergence/neutrality witnesses.
  - `read_defaults_document()` → `{}`: there is no defaults *document* for this surface and one must not be faked. The defaults-layer rule holds because the projection has no required path, not because a document supplies one.
  - the document shape: `Surface.document_shape` (StrEnum, `WHOLE_DOCUMENT` for today's three, `MODEL_SPEC_TABLES` for this one) drives three things — the open-root fingerprint seed, `validate_document`, and `admits_unnamed_key`. `SurfaceFingerprint.document_root_is_open: bool = False` records the fact in the golden so a frozen link answers for itself.
  - the projection: `InferenceModelSpecFileNode`, in `model_backends/` beside the blueprint — the blueprint with `sdk` optional, nothing else changed, used **only** by the fingerprint. Its guard is a test that no field of it is required, which is what goes red the day a new required field lands on the blueprint.
  - `validate_document` for `MODEL_SPEC_TABLES` is **faithful to the loader**, not to the projection: pop `[defaults]`, split header-shaped keys off each model table, merge, validate against the real `InferenceModelSpecBlueprint`. One helper in `model_backends/` so the loader and the surface cannot drift.
- **D4 — the `pipelex-config` pre-history entry ships in this session**, with the reshape renumbered to `@3`.

**Findings from Phase 0 that the plan did not anticipate:**
- **F4 — `_check_open_node_addressing` refuses `*` at the root today**, by construction: `parent_record = before.paths.get(parent_path) if index else None`, so index 0 has no parent record and a leading `*` is reported `WILDCARD_NOT_AT_OPEN_NODE`. Its message already renders `parent_label` as *"the document root"*, so the extension is the one the code was written for: at index 0 the parent is the document root, and its openness is `before.document_root_is_open`. Bonus, and it is the behaviour we want: with the root open, an op naming a concrete model (`openai.sdk`) becomes `CONCRETE_KEY_UNDER_OPEN_NODE` — a backend ledger may not address one user's model by name.
- **F5 — `delete_key` with `table_path = ["*"]` does reach every root table.** `_expand_table_paths` seeds `[[]]`, and `_resolve_table` with an empty path returns the document itself, so the wildcard expands over every root key whose value is a table — `[defaults]` and every model entry alike. Proven by test in C1, not taken on trust.
- **F6 — the defaults-layer rule is vacuous for an open root, for free.** `check_defaults_layer` skips any path with a `*` segment, and every path of this surface has one.
- **F7 — the diagnosis needs one seam, and this is the item the plan missed.** An unknown *root* key resolves for free: `_resolve_against_schema` already falls back to the `*` child at the empty prefix, so `gpt-4o` lands on `*` with no change. But a **header-shaped per-model key** (`x-portkey-provider`, in the kit's `portkey.toml` and in every user's) resolves to neither `*.x-portkey-provider` nor `*.*`, so it would be reported `UnexplainedPath` — on **every** `pipelex migrate` run, since `migrate_file` diagnoses every file — and it would fail `test_no_surfaces_own_documents_carry_anything_the_diagnosis_cannot_explain` over the witnesses. Remedy: `diagnose_unexplained_paths` takes the surface (the runner already holds it at the call site) and asks `surface.admits_unnamed_key(node_path=…, key=…)` before reporting. For `MODEL_SPEC_TABLES` that is *the node is a model-spec table (`("*",)`) and the key is header-shaped with a legal header name* — deliberately narrower than "the model allows extras", so a genuine typo (`promting_target`) is still named. **Rejected alternative:** a per-record `accepts_unnamed_keys` on `PathFingerprint`. It is too coarse — it would silence exactly the key class this session exists for — and it would add a line to all ~200 records of every head golden.
- **F8 — two real-surface tests must change; they are the named exceptions to "absorbed without edits".** `test_the_registry_loads` pins the exact surface-id list, and `test_every_surface_has_a_reachable_defaults_layer` asserts `read_defaults_document()` is *truthy*, which is false for a surface that honestly has no defaults document (D3). The second is amended to state the rule per kind rather than by a truthiness proxy. `test_replay_properties.py` builds `REAL_SURFACES` at module level and the fourth surface joins its property runs automatically — watched, not pre-emptively changed.
- **F9 — there is no packaged (non-kit) backend directory** (`pipelex/inference/` does not exist), so both the reference document and the witnesses come from the kit. The kit's `pipelex_gateway.toml` is **comments only** — zero paths — which makes it a free witness and a poor reference document.
- **F10 — the renumber needs one hand-edit per moved golden, which the plan's `git mv`/copy recipe does not mention.** A fingerprint golden carries `"schema_version"` in its body, and `read_fingerprint_golden` refuses a body that disagrees with its filename. `umig` reads the stored head *first* (the destructive-overwrite guard), so a moved `fingerprint@2.json → @3.json` still saying `2` makes `umig` raise before it writes. So: move, hand-edit the one version line, then `umig` must be a byte-pure no-op — and that no-op is the proof. Same for the `@1 → @2` copy, which is frozen and never regenerated.
- **F11 — the loader reads the files `backends.toml` names, not a directory listing** (`f"{backends_dir_path}/{backend_name}.toml"`). So the boot-tolerance retry repairs what the loader *loaded*, while the walk repairs everything it *reaches* — the sentence Phase D owes a docstring. (`backends_dir_path` is a `str` in that API; the retry needs a `Path` at the edge.)
- **F12 — whichever kit backend file becomes the reference document is now coupled to the goldens.** `_check_head_defaults_document` byte-compares `defaults@N.toml` against it, so adding a model to that file turns `cmig` red until `make umig` runs. That is the designed workflow and the message says so, but the `add-model` skill should say it too (Phase E).

**What Phase A actually built (`feae4db1a`), so the next session can read the diff rather than re-derive it:**

- `Surface` gained `subdirectory: Path = Path()` (`Path()`, `Path("")` and `Path(".")` are all `PosixPath('.')`, and `directory / Path()` is `directory` — verified, so one neutral spelling and no special-casing). `base_file` became `str | None`, with a `check_the_surface_claims_some_file` validator refusing a surface that has neither a base file nor a glob.
- `surface_for_file_name(file_name=…)` → **`surface_for_file(subdirectory=…, file_name=…)`**. Only surfaces owning the file's directory are candidates; exact names still claim before globs *within* that directory. `files_by_surface_in_directory(directory=…)` keeps its signature (the argument is still the configuration root) and now iterates `sorted({surface.subdirectory …})`, skipping a subdirectory that does not exist, one level, `is_file()` only, `sorted` within each.
- Duplicate rejection is now per directory: `_reject_duplicates_per_directory` on `(subdirectory, base_file)` and `(subdirectory, tier_glob)`; surface ids stay globally unique. So two surfaces may both glob `*.toml` when they own different directories.
- `SurfaceBlock` gained `subdirectory: str = ""` and `base_file: str | None`, with the same claims-something validator. `coverage._check_ledger_agrees_with_registry` cross-checks the directory alongside id/base_file/tier_glob, comparing `as_posix()` on both sides so `""` and `"."` cannot read as a disagreement.
- `config_surface._is_within` now takes the **plan**, not a path, and asks `_subdirectory_owned_by(surface_id=…)`, which reads `load_ledger_cached(...).surface.subdirectory` and falls back to `Path()` on `MigrationLedgerError`. The kernel-layer rule holds: `config_surface.py` still does not import `surfaces.py`. Reach is per surface, so a `telemetry.toml` under `inference/backends/` stays out of reach.
- Tests: new `tests/unit/pipelex/migration/test_surface_directories.py` (`TestADirectoryIsHalfTheClaim`) holds the collision rule; `test_surface_registry.py` gained the two glob-scoping cases and the claims-nothing refusal; `test_coverage_gate.py` gained `test_a_ledger_describing_another_directory_is_refused`; `test_boot_tolerance.py` gained the in-reach / out-of-reach pair against a synthetic ledger that declares a subdirectory, and `write_synthetic_ledger` gained a `subdirectory=` parameter. `conftest.py` exports `CONFIGURATION_ROOT = Path()` (a module constant, because pyright's `reportCallInDefaultInitializer` refuses `Path()` as a *parameter* default — it is fine as a pydantic field default).
- **Both guards were mutation-tested.** Making the claim rule name-only again (`candidates = list(self.surfaces)`) reddens three of the four tests in `test_surface_directories.py`; hardcoding `subdirectory = Path()` in `_is_within` reddens `test_a_file_in_the_directory_its_surface_owns_is_in_reach`. Neither mutation was applied with git — the harness held the original text in memory and wrote it back.
- Docs: `docs/migration-ledger.md` § Surfaces gained a **Directory** column and the (directory, name) claim rule replacing the exact-before-globs quote; § The commands' "the walk is not recursive" paragraph became "each surface's own directory, one level deep"; § Boot tolerance gained the reach-is-per-surface sentence; the registry-vs-ledger paragraph now says why the directory has a second reader. `run.py`'s module docstring rewritten the same way.
- The three shipped ledgers needed **zero edits** and `make cl` / `make cmig` stayed green — the empty-subdirectory default is what buys that.

**Gates at the Phase A commit:** `agent-check` clean, `cl` + `cmig` green, `tb` green, targeted suite green (`tests/unit/pipelex/migration/`, `tests/unit/pipelex/system/`, `tests/unit/pipelex/cli/`, `tests/e2e/pipelex/cli/`, `tests/integration/pipelex/system/` — 1674 passed), `docs-check` builds, `drift-check` green after acking `config-docs` (the ack text is in `.drift/acks/config-docs.toml`). The **full** `agent-test` has not been run yet; it is owed at CP1.

**One local-environment event, done on Louis' explicit go:** `make tb` and `tests/integration/pipelex/system/test_load_config.py` were failing **at HEAD, before any of this work**, because this worktree's own gitignored `_backend/.pipelex/pipelex_override.toml` was still in the pre-reshape `[pipelex.*]` shape (proved by swapping the five source files back to their `HEAD` text and re-running). `pipelex migrate --yes` was run once; it rewrote exactly that one file, left `pipelex_override.toml.bak.20260817T113323Z` beside it, and touched nothing in `~/.pipelex/` (already at the current schema) — so the protected `~/.pipelex/inference/backends/` specimen is **untouched and still stale**, as S7's release step needs. `.gitignore` gained `*.bak.[0-9]*Z` so the timestamped copy `pipelex migrate` leaves in a project's `.pipelex/` is never committed — by us or by a user.

**B1–B9 — Phase B designed against the code (the reading pass is done; these supersede the prose in the Phase B section below where they differ):**

- **B1 — the open-root seed.** `compute_fingerprint` seeds `_walk_model(prefix=())` from `config_model.model_fields`. For an open root, call `_record_field(path=(WILDCARD_SEGMENT,), annotation=config_model, required=True, defaults_value=None, ancestry=(config_model,), collected=collected)` instead: because `config_model` is a `BaseModel`, `_record_field` writes `collected["*"] = PathFingerprint(value_type=TABLE_TYPE, required=True)` and then walks its fields under the `("*",)` prefix, giving `*.sdk`, `*.enabled`, … with **no phantom parent**. That is exactly the shape D3 asks for, and it needs no new branch inside `_record_field`. Add `document_root_is_open: bool = False` as a parameter of `compute_fingerprint` and as a field of `SurfaceFingerprint`; there are only two non-test callers, `Surface.fingerprint_at` and `coverage.check_surface:456`, and the latter should be switched to `surface.fingerprint_at(schema_version=current_version)` so the flag has one route.
- **B2 — `_check_open_node_addressing` (F4) needs one edit.** Line ~395: `parent_record = before.paths.get(parent_path) if index else None`. Replace the two lines with `if index: parent_record = …; parent_is_open = parent_record is not None and parent_record.open_node` / `else: parent_is_open = before.document_root_is_open`. The message already renders `parent_label` as *"the document root"* at index 0, so nothing else changes. Red-first in `test_ledger_check.py`.
- **B3 — the golden format moves for every surface, and that is expected.** `render_fingerprint_golden` dumps with `exclude_none=True`, **not** `exclude_defaults`, so a new `bool` field lands in every head golden as `"document_root_is_open": false` the first time `umig` runs. That is an *addition*, and `_refuse_a_destructive_head_overwrite` only refuses on removals and narrowings — so no `--force` is needed. Confirm `_check_head_link` compares parsed fingerprints (a stored golden lacking the key parses with the default) rather than text, and expect the `umig` diff to touch the three existing head goldens by one line each. **Measure this before writing the ledger**, and if `cmig` goes red on the three untouched surfaces, that is the thing to fix, not to regenerate around.
- **B4 — `Surface.validate_document(*, document: dict[str, Any]) -> str | None`.** Returns the reason the current schema refuses the document, or `None`. **Returning a reason rather than raising is deliberate: it avoids a new error class, and therefore avoids `make gei` + `make gep` and the full-`agent-test`-only failure they cause.** `transform_check._check_the_migrated_document_is_accepted` (line ~226) calls it in place of `surface.config_model.model_validate(document)` and builds its `MIGRATED_DOCUMENT_REJECTED` issue from the returned string.
- **B5 — the same function must not deep-merge for a copied document.** `_check_the_migrated_document_is_accepted` loads `after_text` (the `defaults@N` reference) and `deep_update`s the migrated text over it, because a migrated file is read *beneath* its defaults layer. A copied-document surface has no layer beneath it, and merging two independent backend files would validate a hybrid nobody has. Give `DefaultsLayerKind` a property — `is_layered_beneath_the_users_file` — returning `True` for `PACKAGED_DOCUMENT` and `MODEL_DEFAULTS` and `False` for `COPIED_DOCUMENT` (exhaustive `match`, no `case _`), and skip the base load when it is `False`.
- **B6 — the loader-faithful validation, and where it lives.** `backend_library.load` reads one file, pops `[defaults]`, and for **each** remaining root table runs `split_model_spec_keys` (rejected keys are fatal for `LOCAL_FILE`) then `InferenceModelSpecBlueprint.model_validate({**defaults, **key_split.fields})`. Note the asymmetry that makes the `#1104` breakage double: **`[defaults]` is *not* passed through `split_model_spec_keys`** — it is copied wholesale into every model's blueprint dict, which is why one `prompting_target` there fails *every* table with `extra_forbidden`, while a per-model one is rejected by name as `NOT_HEADER_SHAPED`. Put both the projection and the validator in a new `pipelex/cogt/model_backends/model_spec_document.py`, so the loader and the surface cannot drift.
- **B7 — the projection is a one-line subclass.** `class InferenceModelSpecFileNode(InferenceModelSpecBlueprint): sdk: str | None = None`. Subclassing makes the field-set drift test true by construction (nothing to keep in sync), and the guard that matters is the one D3 names: **no field of it is required** — which goes red the day a new required field lands on the blueprint. `sdk` still renders as `str` in the fingerprint, since `_strip_annotated` drops the `| None`; only `required` moves.
- **B8 — the diagnosis seam (F7).** `diagnose_unexplained_paths` gains a `surface` parameter (its one non-test caller, `runner.py:124`, already holds one; `test_real_surfaces.py:48` and `test_diagnosis.py` call it directly and must be updated). In `_diagnose_table`, when `_resolve_against_schema` returns `None`, ask `surface.admits_unnamed_key(node_path=schema_prefix, key=key)` before reporting. For `MODEL_SPEC_TABLES` that is `node_path == (WILDCARD_SEGMENT,) and is_header_shaped(key=key) and is_legal_header_name(key=key)` — deliberately narrower than "the model allows extras", so `promting_target` is still named while `x-portkey-provider` is not. A hyphenated known field (`max-tokens`) is admitted here on purpose: the loader already rejects it by name with the right advice, and this channel has none to add. Confirmed by reading: a root **model name** (`gpt-4o`) already resolves for free, because `_resolve_against_schema` falls back to the `*` child at the empty prefix.
- **B9 — the kit witnesses.** `kit_template_path: Path | None` gains a sibling `kit_template_dir: Path | None` (mutually exclusive, enforced by a validator), and `reference_documents()` enumerates that directory lazily — `is_file()`, matching the surface's own `tier_glob`, **excluding `reference_document_path` itself** so the reference document is not witnessed twice — labelling each `kit template '<name>'`. Lazy rather than a `list[Path]` resolved at registry-build time, because building the registry must not touch the filesystem. `_check_convergence` (`ledger_check.py:595`) is the only reader.

**Commits on the branch:** `583f50de7 tidy`, `343bb3fa7 plan`, `34fefae4a S7b Phase 0`, `feae4db1a Migration walk: a surface owns a directory, and a file is claimed by (directory, name)`.

**Open questions found while building:**

- **B3's golden diff is the one unmeasured thing in the Phase B design.** Everything else in B1–B9 was read off the code; whether `cmig` stays green over the three existing surfaces once `SurfaceFingerprint` gains a field is a prediction. Measure it first, before the ledger and before the goldens, because if it is wrong the fix is in the golden format and not in the fourth surface.
- **Whether `pipelex_override.toml.bak.…` should be pruned.** `pipelex migrate` left one in this worktree's `.pipelex/`; it is now gitignored rather than deleted, and the rotation/pruning behaviour of `backup.py` was not looked at this session. Not S7b's business unless a reviewer raises it.

---

## Phase 0 — re-measure, then read the seams (no code)

Read-only. Nothing is written but this file's Status block.

- [x] Re-verify the venue: `git fetch origin dev && git rev-list --left-right --count origin/dev...HEAD` (expect `0 1` or `0 N` — never `M N` with `M > 0`; if `dev` has moved, `git merge origin/dev` first, never rebase, and re-run the gates before going on). `git status --short` empty.
- [x] Re-measure the breakage on this laptop, read-only: `grep -rn prompting_target ~/.pipelex/inference/backends/` finds it; run the loader probe from the substance § *Reproducing the evidence* against `~/.pipelex/inference/backends/openai.toml` and `portkey.toml` from `.venv/bin/python` — expect `extra_forbidden ('prompting_target',)` from the `[defaults]` merge and *"'prompting_target' is not a known model-spec field, and not header-shaped"* per per-model occurrence. **If a `pipelex init --reset` has been run since and the key is gone, find another stale specimen (a teammate's machine, or a `git show 60aa77f5^:pipelex/kit/configs/inference/backends/portkey.toml` copy planted in a temp HOME) before building against a dead premise — and say so in Status.**
- [x] Read, in this order, so the design items in Phase A/B are built against the code and not the plan: `pipelex/migration/surfaces.py` (the `Surface` model, `surface_for_file_name`, `files_by_surface_in_directory`, `build_config_surface_registry`), `pipelex/migration/run.py` (the "walk is not recursive" docstring — it becomes false), `pipelex/migration/fingerprint.py:160-311` (`compute_fingerprint` seeds `prefix=()`; open nodes are `dict[str, X]` fields recorded under a literal `*`; `extra="allow"` keys are invisible; a `RootModel` projects a phantom `root.` prefix), `pipelex/migration/ledger.py:43-66` (`SurfaceBlock`) and `:175-196` (id ↔ version ↔ `current_schema_version` are forced to agree), `pipelex/migration/coverage.py:143-146, 223-266, 415-446, 496-519, 522-584` (pre-history claim, defaults-layer rule, ledger-vs-registry cross-check on exactly `id`/`base_file`/`tier_glob`, stored links), `pipelex/migration/ledger_check.py:157-163, 224-315, 595` (pre-history entries skip the walk entirely; `_check_open_node_addressing`; `_check_convergence` is the only reader of `reference_documents()`), `pipelex/migration/transform_check.py:130-149, 226-255, 279-315` (how `before@N` and `defaults@N` are consumed; the head-only "migrated document is accepted" check — **this validates the migrated document against `config_model`, and an open-root document cannot be validated that way; see Phase B**), `pipelex/migration/snapshot.py:47-104` (`umig` writes the head pair only, never `before@N`), `pipelex/migration/runner.py:296-325` and `backup.py:41-95` (backups land beside the file via `with_name`, so a subdirectory needs nothing there), `pipelex/system/configuration/config_surface.py` (`replay_surface_files_in_memory` **deep-merges** its paths — right for a tier stack, wrong for many independent backend files; `_is_within` says "directly, not underneath"; `stale_configuration_warning`), `pipelex/system/configuration/config_loader.py:129-151, 193-201, 430-487` (`existing_config_dirs` — the one derivation; `backends_dir_path` is resolved **per directory, project wins the whole directory** — different from the tiered merge; the copyable four-step tolerance pattern), `pipelex/system/pipelex_service/pipelex_service_config.py:59-75` (the smallest tolerance caller — the closest template), `pipelex/cogt/model_backends/backend_library.py:58-247, 291-326` (`load`, `_load_local_model_specs`, where a fourth caller hooks in), `pipelex/cogt/model_backends/model_spec_factory.py:18-74` (`InferenceModelSpecBlueprint`: `sdk` is the only required field; `ConfigModel` is `extra="forbid", strict=True`), `pipelex/cogt/model_backends/model_spec_keys.py:1-31, 138-174` (pure, silent, runs before log dispatch on some paths), `pipelex/cogt/models/model_manager.py:64-84` and `pipelex/runtime_boot.py:200-215, 480-505` (the boot path and where the parked warning is emitted), `tests/unit/pipelex/migration/conftest.py` (the synthetic-surface harness: `build_surface`, `build_entry(pre_history=…)`, `build_registry`), `tests/unit/pipelex/migration/test_migration_run.py:96-104` (the test that pins the *current* non-recursive rule with `inference/backends/pipelex_gateway.toml` as SPECIMEN — it flips meaning in Phase A), `tests/unit/pipelex/system/configuration/test_boot_tolerance.py`, `tests/e2e/pipelex/cli/test_migrate_commands.py:354-400` (`TestAPreReshapeMachine`), `docs/migration-ledger.md` § Surfaces (`:15-40`), § The commands (`:513`), § Boot tolerance (`:548`), § Reporting… (`:560`), the chain diagram (`:446`).
- [x] Verify early the assumption R10's cost estimate rests on: **`cmig`, `umig`, `cl`, `pipelex migrate`, the `doctor` pending-migrations row, `/release` step 3b and the `add-migration` skill are all registry-driven** — grep confirms the dev CLI reaches only `build_config_surface_registry()` plus one top-level function each (`check_ledger_cmd.py`, `check_migration_schemas_cmd.py`, `update_migration_schemas_cmd.py`, `migration/run.py`). Record in Status whether that held (it is a **Record** obligation of the S7b row).
- [x] Write D1–D4 into Status as *recommended*, with one sentence each, before any code. Phase A/B build to the recommendations; CP1 is where they get ratified or overturned.

## Phase A — the walk reaches a subdirectory, and the claim rule becomes (directory, name)

Pure registry/walk work; independent of D3. **The landmine test comes first.**

- [x] **Red first — the collision test.** In `tests/unit/pipelex/migration/test_surface_registry.py` (or a new `test_surface_directories.py`): a registry with a `pipelex_*.toml`-globbed surface at the root and a `*.toml`-globbed surface at `inference/backends`; a temp directory holding `pipelex.toml`, `pipelex_override.toml`, `inference/backends/pipelex_gateway.toml`, `inference/backends/openai.toml`, `inference/backends/notes.md`, `inference/deck/x.toml`, `inference/backends.toml`. Assert: the two backend `.toml`s are claimed by the backend surface and **not** by the root surface (the `pipelex_gateway.toml` specimen, by name a `pipelex_*.toml` match); `notes.md`, `deck/x.toml` and `inference/backends.toml` are claimed by nobody; the root files are claimed by the root surface only. Mutation-test it: make the claim rule name-only again and watch it go red on `pipelex_gateway.toml`.
- [x] `Surface` gains `subdirectory: Path` (relative; `Path()` / empty for the three existing surfaces — decide the neutral spelling once and use `Path(".")`-free comparisons; keep `frozen=True`, `extra="forbid"`). `base_file` becomes `str | None` (a family with no distinguished member has none) with a validator: at least one of `base_file` / `tier_glob` is set, and a surface with `base_file=None` **must** carry a glob.
- [x] `SurfaceRegistry.check_no_surface_shadows_another`: duplicates are now rejected on `(subdirectory, base_file)` and `(subdirectory, tier_glob)` pairs, not bare strings — two surfaces in different directories may share a glob shape. Keep the surface-id uniqueness.
- [x] `surface_for_file_name(file_name)` → `surface_for_file(*, subdirectory: Path, file_name: str)`: only surfaces whose `subdirectory` equals the file's are candidates; exact base files claim before globs **within that directory**; two globs claiming one file in the same directory stays a `MigrationRegistryError` by name (the contract's rule, unchanged). Update its one caller.
- [x] `files_by_surface_in_directory(directory)`: for each surface, enumerate `directory / surface.subdirectory` with `iterdir()` (still one level, still `is_file()` only, still "a directory that does not exist is skipped, not refused" — a machine with no `inference/` is an ordinary machine); a symlinked file is still enumerated by `iterdir` and written through (keep the S7 asymmetry: resolve the parent, never the file). Return order stays deterministic (`sorted`).
- [x] The ledger's `[surface]` block (`SurfaceBlock`) gains `subdirectory: str = ""` (TOML has no Path; convert at the edge) and `base_file` becomes optional there too; `coverage._check_ledger_agrees_with_registry` cross-checks the new field alongside `id`/`base_file`/`tier_glob` (it compares exactly those three today — extend the tuple, and add a red-first test in `test_coverage_gate.py` for a `subdirectory` disagreement). The three existing ledgers need **no** edit if the default is the empty subdirectory — verify `make cl`/`cmig` are still green with zero ledger changes before adding the fourth.
- [x] `_is_within` in `config_surface.py`: "directly in one of these directories" must become "directly in the directory *this file's surface* owns under one of these roots". Recommendation: `MigrationPlan` already carries `surface_id`; read the surface's `subdirectory` from the cached ledger (`load_ledger_cached(...).surface.subdirectory` — `config_surface.py` already imports `pipelex.migration.ledger`, and may **not** import `surfaces.py`, which imports every config model into the kernel layer's closure) and compare `path.parent.resolve()` against `root.resolve() / subdirectory`. `walked_dirs` stays the roots (`config_manager.existing_config_dirs`) for every caller, so the three existing loaders do not change and the "one derivation" property S7 fought for survives: the roots come from the loader, the subdirectory from the ledger the registry cross-checks. Red-first test in `test_boot_tolerance.py`: a stale plan for `<root>/inference/backends/openai.toml` is *in reach* under `walked_dirs=[root]`, and one for `<root>/inference/deck/x.toml` is not.
- [x] Rewrite the three places that state the non-recursive rule as a guarantee: `pipelex/migration/run.py:19-23` docstring, `config_surface.py:186-206` docstring, `docs/migration-ledger.md:513` (§ The commands) — the new sentence: *the walk is each surface's own directory under each configuration root, one level, and a subdirectory no surface owns (`deck/`) is never entered*; keep `pipelex_gateway.toml` as the specimen, now for the (directory, name) rule. Flip `tests/unit/pipelex/migration/test_migration_run.py:96-104` from "the specimen is never claimed" to "the specimen is claimed by the backend surface and by nothing else" — once the fourth surface exists (Phase B); until then it must keep asserting the root surface does not claim it.
- [x] `docs/migration-ledger.md` § Surfaces: describe `subdirectory` and the (directory, name) claim rule; § Legality → *Reserved paths and names* if it names the claim rule; the CHANGELOG bullet at `CHANGELOG.md:10` that says "not recursively, because a subdirectory such as `inference/backends/`…" is rewritten in Phase E, not now.
- [x] Gates 1–5 (the drift contract `config-docs` will open on `config_surface.py` — review `docs/configuration/**` and `pipelex/migration/ledgers/` for real, then ack). Commit: `Migration walk: a surface owns a directory, and a file is claimed by (directory, name)`.

## Phase B — the fourth surface: model, fingerprint shape, defaults-layer kind, schema-1 cut

This is the central modelling phase. Item 4 of the substance is the question; the recommendation below is a starting point to be **measured** against `compute_fingerprint`, `_check_open_node_addressing`, `_check_the_migrated_document_is_accepted`, and `read_defaults_document`, and overturned if any of them says no.

**The document shape.** A backend file is `[defaults]` + one root table per model name (+ header-shaped keys inside a model table). Neither `[defaults]` nor a model table is an `InferenceModelSpecBlueprint` on its own — only their merge is (`sdk` is required and normally lives in `[defaults]`). So the fingerprint must record the *file-node* shape: every root table is an all-optional partial spec, and the root itself is an open node.

- [ ] **The partial projection.** Recommendation: a mechanically-derived all-optional projection of `InferenceModelSpecBlueprint` (every field `X | None = None`, `extra="forbid"` irrelevant to the fingerprint since extras are invisible to it), produced by one function in the migration package (or beside the blueprint) so there is **one** field list — with a test pinning `set(projection.model_fields) == set(InferenceModelSpecBlueprint.model_fields)` so the two can never drift. Alternative if dynamic model creation reads badly under the python standards: an explicit hand-written `InferenceModelSpecFileNode` model beside the blueprint with the same drift test. Either way it is a **projection for the fingerprint and the transform check only** — the loader keeps validating the merge against the real blueprint; nothing about how a backend file boots changes here.
- [ ] **The open root.** `compute_fingerprint` seeds `prefix=()` from `config_model.model_fields`, so a `dict[str, Partial]` has to be a *field* to be recorded as an open node — which puts a phantom parent in every path (`models.*.sdk`) that no TOML document has, and a `RootModel` does the same with `root.`. Recommendation: give the surface a way to say *the document root is itself an open node whose value schema is `config_model`* — the smallest honest extension is a `Surface.document_root_is_open: bool` (name to taste) that `fingerprint_at` turns into a seed at path `("*",)` (one root open-node record with `open_node=True` and no parent, then the projection's fields beneath `*`), so the fingerprint records `*` and `*.sdk`, `*.enabled`, `*.model_type`, … and `[defaults]` is simply one of the `*` keys. Then `delete_key` with `table_path=["*"]`, `key="prompting_target"` addresses `*.prompting_target`, which is exactly the material the entry declares. **Measure `_check_open_node_addressing` against a root `*` before committing to this** — if it requires a recorded parent for the `*` segment, teach it that the root may be the open node (red-first test in `test_ledger_check.py`) rather than inventing a parent. Record the shape as built in Status and in `docs/migration-ledger.md` § The fingerprint.
- [ ] **The third `DefaultsLayerKind`** (D3). Recommendation, name to be ruled at CP1: `OPTIONAL_DOCUMENT` (*"the file is copied, not layered; every key of the document is optional at the document level and the model supplies the value, so an added field is absorbed by construction"*). What it means for the four readers of the kind:
  - `read_defaults_document()` → `{}`: there is no defaults document, and the defaults-layer rule (`coverage.check_defaults_layer` — every *required* path has a default) holds vacuously because the projection has no required path. Say so in the docstring; do not fake a document.
  - `render_reference_document()` → the **representative kit file**, e.g. the kit's `openai.toml`, named on the surface as `reference_document_path` (a new field, not a reuse of `packaged_document_path`, whose name means "merged beneath the user's file", which this is not). This is what `umig` snapshots as `defaults@N.toml`, so a *later, non-pre-history* entry has a real starting document to be replayed over (`transform_check._read_document_the_entry_migrates_from`), and `coverage._check_head_defaults_document` keeps its byte-compare. It is the substance's item 5 answered before a second entry arrives. Alternative considered and rejected: no `defaults@N.toml` for this kind — it works for a pre-history first entry (the head has no `after` and `_check_link` returns `[]`) and breaks the second entry.
  - `reference_documents()` → the reference document **plus every `*.toml` in the kit's backend directory** as convergence/neutrality witnesses (`kit_template_path: Path | None` generalises to `kit_template_paths`, or a `kit_template_dir` + glob — pick the smaller change; the three existing surfaces keep one path). More witnesses than any other surface has, at no cost, and it is exactly the set `pipelex init` copies. `_check_convergence` is the only reader (`ledger_check.py:595`).
  - the `check_defaults_layer_is_reachable` validator: extend the "packaged path ⇔ PACKAGED_DOCUMENT" pairing to "reference document path ⇔ OPTIONAL_DOCUMENT" and keep both mutually exclusive.
  - **`transform_check._check_the_migrated_document_is_accepted`** (head only) validates the migrated document against `config_model` — for an open-root document that means validating *each root table* against the projection (and, since the loader merges `[defaults]` into each model table, the honest acceptance check for this surface is *the merge of `[defaults]` and each model table validates against the real blueprint* — decide which, write it as a `Surface.validate_document(...)` seam the check calls, and say in its docstring which of the two it proves). Red-first test.
- [ ] **Register the surface** in `build_config_surface_registry()`: `surface_id = INFERENCE_BACKEND_CONFIG_SURFACE_ID` (spelled once in `config_surface.py` beside the other three — the constants comment there says "names the same three"; make it four), `title`, `subdirectory = Path("inference") / "backends"` (spell it from `config_loader`'s `INFERENCE_DIR_NAME` / `BACKENDS_DIR_NAME`, not a new literal), `base_file=None`, `tier_glob="*.toml"` (D1 — the `.md` files fall out by extension; `inference/backends.toml` is one level up and never enumerated), `config_model=<the projection>`, `document_root_is_open=True`, `defaults_layer_kind=<D3>`, `reference_document_path=<kit openai.toml>`, kit witnesses = the kit backend directory. Write the D1/D2 reasoning in the docstring right there (why not `backends.toml`, why not `deck/`, why not `routing_profiles.toml`).
- [ ] **The ledger at schema 1**: `pipelex/migration/ledgers/inference-backend.toml` with the `[surface]` block (`subdirectory = "inference/backends"`, no `base_file`, `tier_glob = "*.toml"`, `current_schema_version = 1`, `min_supported_schema_version = 0`) and zero entries. `make umig` writes `goldens/inference-backend/fingerprint@1.json` + `defaults@1.toml`. **Read the `fingerprint@1.json` by eye**: it must show `*` as an open node and `*.<field>` for every blueprint field, no phantom prefix, no `prompting_target` (the schema-1 cut is post-`#1104`, which is what makes the entry pre-history).
- [ ] Extend `tests/unit/pipelex/migration/test_real_surfaces.py` (`TestTheRealRegistry`, `TestTheCheckedInGoldens`) — they iterate the registry, so they should pass unchanged; add the one assertion that is new: the backend surface's fingerprint has an open root. Update `test_migration_run.py:96-104` (the specimen is now claimed by `inference-backend`, and by nothing else). Add a real-directory test: a temp config root seeded with the **kit** backend directory (`get_kit_configs_dir()/inference/backends`) is walked and every `.toml` is claimed by the backend surface, the `.md`s by nobody, and `pipelex migrate --dry-run` over it reports every file clean (the kit is at the current schema — the convergence property, proved on the walk and not only in the gate).
- [ ] Gates 1–5, plus **the full `agent-test`** (a new error class here needs `gei`/`gep`). Commit: `The inference-backend surface: an open-root document, an optional-document defaults layer, schema 1 cut`.

### ⛔ CHECKPOINT CP1 — MANDATORY STOP **(Louis: ratify D1–D4)**

- [ ] Status block updated: the shape as built (fingerprint seed, kind name, reference document, witnesses), what `_check_open_node_addressing` and the acceptance check needed, and whether the registry-driven machinery absorbed the fourth surface without edits (name every file outside `surfaces.py` + the new ledger that had to change, and why).
- [ ] D1–D4 written as *recommended* with the measurement that supports each; the substance document's § Decisions owed updated (workspace repo, not committed by this session).
- [ ] Gates 1–5 green; committed; **not pushed** (nothing is outward-facing before CP3).
- [ ] STOP. Louis rules D1–D4 (a "no" on D3's name is a rename; a "no" on the shape reopens Phase B). Next session starts at Phase C.

## Phase C — the two pre-history entries

Both are legal under the rules as they stand; both are checked by the pre-history path (no walk: `coverage.py:143-146`, `ledger_check.py:157-163`), which is exactly what lets a `move_key` land on a destination the frozen previous fingerprint already has.

### C1 — `inference-backend@2`

- [ ] **Red first**: a test in `tests/unit/pipelex/migration/` (beside `test_telemetry_back_entry.py`, e.g. `test_backend_back_entry.py`) that replays the packaged `inference-backend` ledger over a hand-authored pre-`#1104` backend document — `[defaults]` with `prompting_target`, one model table with it, one without, one header-shaped key (`x-portkey-config`) that must survive — and asserts: every `prompting_target` gone, every other key byte-untouched, the header key untouched, the replay is idempotent (second pass changes no bytes), the file then validates the way the loader validates it (merge `[defaults]` into each model, `InferenceModelSpecBlueprint.model_validate`, `split_model_spec_keys` reports nothing rejected).
- [ ] Bump `current_schema_version = 2` and append the entry: `id = "inference-backend@2"`, `to_schema_version = 2`, `introduced_in = "0.46.0"` (the reshape release; `/release` step 3b confirms it against the version actually cut), `breaking = true`, `safety = "safe"`, `pre_history = true`, title/description/guidance in the ledger's voice (guidance says: the key is deleted, not moved — the templating style is authored on the pipe now — and that a user's own unknown per-model key is *not* something this entry repairs, the loader's message names it), `declared_removed_paths = ["*.prompting_target"]`, and one op: `kind = "delete_key"`, `table_path = ["*"]`, `key = "prompting_target"`.
- [ ] Hand-author `goldens/inference-backend/before@2.toml` from the pre-`#1104` kit shape (`git show 60aa77f5^:pipelex/kit/configs/inference/backends/portkey.toml` is the richest specimen — `[defaults]` *and* per-model occurrences); trim to a few tables; header comment in the style of `goldens/telemetry-config/before@2.toml`; **no real value from any personal file**.
- [ ] `make umig` (head is now 2: writes `fingerprint@2.json` + `defaults@2.toml`; `fingerprint@1` stays behind frozen; the two must differ by nothing — no removal, no narrowing — or `PRE_HISTORY_HAS_A_DIFF` fires, which is the point). `make cl` and `make cmig` green. If `_check_open_node_addressing` or `_check_pre_history_source_is_declared_material` refuse `*.prompting_target` at the root, that is a Phase B shape defect — go back, do not widen the declaration.
- [ ] Also verify by hand what the substance promises: `["*"]` at the root matches `[defaults]` **and** every model table (both are root tables), the wildcard is a legal `table_path` for `delete_key`, and the op skips silently on a file already at the new shape (every kit file: convergence).

### C2 — `pipelex-config@2` (pre-history) and the reshape renumbered to `@3` (D4)

- [ ] **Red first**: extend `tests/e2e/pipelex/cli/test_migrate_commands.py::TestAPreReshapeMachine` (or a sibling class) with a v0.45.0-shaped `pipelex.toml` carrying `[pipelex.prompting_config]` (`default_prompting_style = { tag_style = "xml" }`) and `[pipelex.prompting_config.prompting_styles]` (a couple of targets) — the exact old shape is `git show v0.45.0:pipelex/pipelex.toml` lines 272–279 — and assert both loops carry it onto today's shape: `[inference.templating] default_templating_style = { tag_style = "xml" }`, no `prompting_config` anywhere, no `prompting_styles`, one backup, boot clean; and that the *packaged* `defaults@1.toml` (which never carried `prompting_config`) is unaffected by the new entry (a no-op link).
- [ ] The ledger edit, atomic (`MigrationLedger.check_entries_are_contiguous_and_named_for_their_version` forces id ↔ version ↔ `current_schema_version`): `current_schema_version = 3`; **insert** before the reshape an entry `id = "pipelex-config@2"`, `to_schema_version = 2`, `introduced_in = "0.46.0"`, `breaking = true`, `safety = "safe"`, `pre_history = true`, title *"Prompting styles become a templating default"* (or better), description (what `#1104` did to the file), guidance (per-target styles are gone as a concept — the style is authored on the pipe (`templating_style` on a `PipeLLM`); a tuned per-target map is dropped, and the default travels), `declared_removed_paths` = `pipelex.prompting_config`, `pipelex.prompting_config.default_prompting_style`, `pipelex.prompting_config.prompting_styles` (+ its `*` value paths if the checks want them spelled), ops in this order: `move_key` `["pipelex","prompting_config"] default_prompting_style → ["pipelex","templating_config"] default_templating_style` (creates the destination block table when absent — the Phase-1 placement rule), `delete_table ["pipelex","prompting_config","prompting_styles"]`, `delete_table ["pipelex","prompting_config"]`. **The operations address the true historical spelling `pipelex.…`, before the reshape renames `[pipelex]` — that is why it goes before, not after.** Then the reshape entry: `id = "pipelex-config@3"`, `to_schema_version = 3`, nothing else in it changes (its `move_key pipelex.templating_config → inference.templating` is what carries the new entry's destination onward).
- [ ] The golden chain: `git mv goldens/pipelex-config/fingerprint@2.json fingerprint@3.json`, `git mv defaults@2.toml defaults@3.toml`; then **copy** `fingerprint@1.json → fingerprint@2.json` and `defaults@1.toml → defaults@2.toml` (the frozen no-diff link: nothing in the models changed between the two, so the "no removal" claim holds by construction and the reshape's derived accounting is unchanged — the `@2 → @3` diff *is* today's `@1 → @2` diff); hand-author `goldens/pipelex-config/before@2.toml` (a small pre-`#1104` document: `[pipelex.prompting_config]` + `prompting_styles`, and one unrelated table so the neutrality of the other ops is witnessed). Then `make umig`: **the head (`@3`) is rewritten from live and must be byte-identical to the files `git mv` produced — `git diff --stat` on the goldens must show only the two copies and the `before@2.toml`.** That diff is the proof the renumber was pure; paste its shape into Status.
- [ ] Update every place that quotes the old id or files: `CHANGELOG.md:31` (`pipelex-config@2` → `@3` in the reshape's `**Migration:**` bullet), `tests/e2e/pipelex/cli/test_migrate_commands.py:17, 358`, `tests/unit/pipelex/pipeline/fixes/test_fix_applier_config_surface_shapes.py:55`, and grep the tree (`git grep -n 'pipelex-config@2'` across `pipelex/ tests/ docs/ CHANGELOG.md .claude/ wip/`) — every hit is either the *new* pre-history entry or a stale quote of the reshape; there is no third kind. The tests that read `goldens/pipelex-config/defaults@2.toml` as "the packaged file at schema 1" (`test_migrate_commands.py:9, 96, 117, 379`, `test_boot_tolerance.py:17, 62` and the others listed in the Phase 0 reading) — check each: after the renumber `defaults@2` is *still* the schema-1 shape (a copy), so most keep working by accident; make them read `defaults@1.toml` where they mean "the pre-reshape document", so the meaning is spelled and not inherited from a copy.
- [ ] `test_boot_tolerance.py:15-20` module docstring ("the other two surfaces have empty ledgers today") goes stale — rewrite it.
- [ ] `make cl`, `make cmig`, `make tb`, and the targeted e2e (`.venv/bin/pytest -x -q tests/e2e/pipelex/cli/test_migrate_commands.py`) green.
- [ ] Commit C1 and C2 **separately**: `inference-backend@2: delete prompting_target from every root table (pre-history)` and `pipelex-config@2: the prompting-style half of #1104 accounted for; the reshape becomes @3`.

### ⛔ CHECKPOINT CP2 — MANDATORY STOP

- [ ] Status: both entries recorded (ids, ops, declared paths), the `umig` byte-purity diff shape, anything the checks refused and how it was answered.
- [ ] Gates 1–5 green; committed; not pushed.
- [ ] STOP. Next session starts at Phase D.

## Phase D — boot tolerance at the fourth loader, and the end-to-end proof

- [ ] **Red first**: in `tests/unit/pipelex/system/configuration/test_boot_tolerance.py` (or a new `tests/unit/pipelex/cogt/model_backends/test_backend_boot_tolerance.py`): a temp `backends_dir_path` seeded from the kit's backend directory with `prompting_target` planted back into `openai.toml`'s `[defaults]` and into two of `portkey.toml`'s model tables (build the fixture by editing the kit copy programmatically — never from `~/.pipelex/`); `InferenceBackendLibrary.load(...)` over it **succeeds** in both `lenient` modes, the loaded model specs carry no `prompting_target` and everything else intact, and a stale-configuration warning is produced that names both files and `pipelex migrate` (in reach: the temp dir is passed as a walked root); a file with a *user's own* unknown key (`foo = 1` on a model) still fails exactly as before — tolerance is not leniency, and the ledger explains only what we removed; a stale `pipelex_gateway.toml` local-override file is repaired the same way (the served specs are untouched). Nothing is written to disk by the load (assert bytes unchanged).
- [ ] The implementation, following the four-step pattern of `pipelex_service_config.py:59-75` / `config_loader.py:459-487`: in `InferenceBackendLibrary`, when a LOCAL_FILE model-spec table is rejected (`key_split.rejected`) **or** the merged blueprint fails validation, and only then, replay the ledger over **that one file** (`replay_surface_files_in_memory(surface_id=INFERENCE_BACKEND_CONFIG_SURFACE_ID, paths=[path])` — one path per call, since the helper deep-merges its paths and backend files are independent documents; if a single-file variant reads better, add it beside the helper rather than bending the loop) — then re-run the loader's own steps over what comes back (variable substitution, `[defaults]` pop, `split_model_spec_keys`, the merge, `model_validate`) and, if that succeeds, keep the result and **park** the warning; if it does not, the original error stands (raise exactly what was raised before). Do the retry per file, once, and structure it so the happy path never imports the migration engine (the contract's "the healthy path is untouched" — `replay_surface_files_in_memory` already defers the engine import).
- [ ] **Where the warning goes.** `model_spec_keys.py` documents that this code runs before `runtime_hub.set_config()` on some paths, and `config_loader` parks its warning for the same reason. Recommendation: `InferenceBackendLibrary` gets `take_stale_configuration_warning()` on the same pattern as `ConfigLoader:62-70`, and `runtime_boot.py` emits it right after models-manager setup (find the spot beside `:210-211`; `doctor_cmd.py:1053-1054` likewise, and the `show_cmd.py:72-76` caller decides for itself — probably emit). `walked_dirs` = `config_manager.existing_config_dirs` (the roots — Phase A made `_is_within` resolve the surface's subdirectory from the ledger). Mind the layered resolution: `backends_dir_path` picks *one* directory (project wins the whole directory), so the files the loader replays are the ones under that directory; the walk still repairs both roots — the warning is about the files that were loaded, the command repairs everything it reaches. Say that in one docstring sentence.
- [ ] `runtime_boot.py:488-500` (`InferenceBackendLibraryNotFoundError` / `…ValidationError` → `PipelexSetupError`): unchanged in kind — the retry sits *inside* `load` and either recovers or gets out of the way.
- [ ] Update the `load` docstring (`backend_library.py:78-83`): leniency still covers credentials only; a stale file the ledger explains is now carried forward with a warning; a malformed file the ledger cannot explain stays fatal in both modes.
- [ ] **End to end, both loops** (`tests/e2e/pipelex/cli/test_migrate_commands.py`, a new class beside `TestAPreReshapeMachine`, e.g. `TestAStaleBackendDirectory`): temp HOME with `~/.pipelex/inference/backends/` seeded from the kit + `prompting_target` planted (and a `pipelex_gateway.toml` in it, so the collision is exercised on the real walk); `pipelex doctor` shows the pending-migrations row naming the backend files; `pipelex migrate --dry-run` lists them with op kinds only (**no value from the file rendered** — the standing rule); `pipelex migrate` rewrites each in place with **one backup per file beside it** (`openai.toml.bak.<stamp>`), a second run is a no-op with no new backup, the `.md` files and `inference/backends.toml` are never touched, and a subsequent boot is clean with no warning; the agent loop (`pipelex-agent migrate`, JSON) reports the same files and op kinds. Mutation-test the "no value rendered" assertion.
- [ ] Gates 1–5 (the `cli-docs` contract opens if `doctor_cmd.py` is touched — review `docs/tools/cli/` for real), full `agent-test`. Commit: `A stale backend directory boots with a warning and pipelex migrate repairs it in place`.

## Phase E — docs, changelog, skills, the stale sentence, and the plan records

- [ ] `docs/migration-ledger.md`: § Surfaces — the table gains the fourth row (id, files, defaults-layer kind, subdirectory) and describes the (directory, name) claim rule; `:11` ("a new inference backend … content sync is out of scope") — sharpen: *the content* of a backend file (which models, which endpoints) is still content and out of scope; its *schema* (which keys a table may carry) is now a surface; `:374` "all three configuration surfaces" → four (or "every surface"; never a count that will go stale — write it so it does not need editing next time); `:513` (done in Phase A — re-read); `:548` "the three loaders" → the loaders (name the fourth); `:560` "an inference backend file … none of those has a ledger" → it has one now; the chain diagram `:446` unchanged in shape; § The fingerprint — the open root; § The defaults layer — the third kind, in one paragraph, honestly ("copied, not layered").
- [ ] `docs/configuration/config-technical/inference-backend-config.md`: one short section — *when you upgrade and a backend file is out of date*: what the boot says, that `pipelex migrate` reaches `inference/backends/`, what it will and will not touch (your own unknown key is yours), backups beside the file. Keep it in the page's existing voice; `docs/configuration/index.md:35` gets the one-clause note that backend files are migratable. `make docs-check`.
- [ ] `CHANGELOG.md` (Unreleased): `:10` — rewrite the "not recursively, because a subdirectory such as `inference/backends/`…" sentence to the new rule; `:20` — "each of the three configuration surfaces … all three start at schema version 1" → mention the fourth (`inference/backends/*.toml`) and its ledger; `:31` — the reshape bullet's entry id → `pipelex-config@3` (done in C2, re-check); add two `**Migration:**` bullets in the condensed house style (bold label + 2–4 sentences): the backend entry `inference-backend@2` — *run `pipelex migrate` after upgrading; a stale backend directory boots with a warning until you do* — and `pipelex-config@2` for the prompting-style half of the templating change (and one clause that the reshape entry is now `@3`). Do not mention `wip/` docs.
- [ ] `.claude/skills/add-migration/SKILL.md:17-22` — the surface list gains the fourth (or stops enumerating); § *Special case: a change that predates the first fingerprint* — add the one procedure it lacks and this session performed: **inserting a pre-history entry below existing entries** (renumber, `git mv` the goldens, copy the frozen link, hand-author `before@N`, `umig` must then be a byte-pure no-op on the head — that no-op is the proof). `.claude/skills/release/SKILL.md` step 3b: nothing to change unless the entry-id diff logic assumes ids never move — read `:76-83` and decide; if a renumbered id would read as "a new breaking entry without a bullet", say in the skill that a renumber shows up as two ids and both need the changelog to mention them.
- [ ] `pipelex/system/configuration/config_surface.py:4-5, 100-102` ("all three", "the same three") — the module docstring and the constants comment.
- [ ] `tests/unit/pipelex/migration/test_transform_check.py:242` docstring ("all three surfaces are in today") — reword.
- [ ] `wip/prompting-style/templating-style-implementation-plan.md:184` § *Out of scope here*: since `#1109` a leftover per-model `prompting_target` is a **hard error** (`NOT_HEADER_SHAPED`, fatal even leniently), not a silent outbound header — correct the sentence, and add that the sweep's scope is the repositories we ship, while a user's machine is what the `inference-backend@2` ledger entry now repairs.
- [ ] Gates 1–8, including `make check` and the **full** `agent-test`. Commit: `Docs and changelog for the fourth surface and the two pre-history entries`.

### ⛔ CHECKPOINT CP3 — MANDATORY STOP (pre-PR)

- [ ] Status: every phase ticked; the S7b **Record** fields drafted here — decisions D1–D4 as taken; whether the registry-driven machinery absorbed the fourth surface without edits (name the exceptions); anything the tool got wrong on the *fixture* backend directory; the read-only `pipelex migrate --dry-run` report shape against **this laptop's real** `~/.pipelex/inference/backends/` (paths and op kinds only — never values; `--dry-run` only, the real run is S7's first-machine step).
- [ ] The workspace plan docs updated and named in the hand-off (not committed by this session): `wip/migrator-3/sequencing.md` S7b row + a session record; `wip/migrator-3/backend-config-surface.md § Decisions owed`; `wip/migrator-3/README.md` — the `Scope` row of the key design decisions amended per R10 (backends are a surface; language and plugins still separate projects); `wip/migrator-3/plan.md § Checkpoint D` gains the S7b record placeholder.
- [ ] `git rev-list --left-right --count origin/dev...HEAD` re-checked; if `dev` moved, `git merge origin/dev` (never rebase), gates again.
- [ ] STOP. Louis' go to push and open the PR — everything before this line is local; the push is the first outward-facing action.

## Phase F — the PR, the review rounds, the merge

- [ ] `git push -u origin feature/Backend-surface`; open the PR **against `dev`** titled *The inference-backend surface: a fourth migration surface, and the two pre-history entries #1104 left behind* — description opens with why it blocks the release (one paragraph from the top of this file), then the four decisions as taken, then the byte-purity proof of the renumber, then the first-machine dry-run shape.
- [ ] Bot review rounds (Codex, Greptile, Cubic — the pattern of #1113–#1115): fix real findings on the branch, answer and resolve every thread; **defer** design-tradeoff findings into `wip/migrator-3/` (workspace) or `wip/` here with an owner, per the standing feedback (a deferral is a recorded item, not an open thread nobody will read again). Re-run gates 1–8 after each round.
- [ ] `make check` and full `agent-test` green on the final head; PR `MERGEABLE`/`CLEAN`.

### ⛔ CHECKPOINT CP4 — MANDATORY STOP **(Louis go: merge)**

- [ ] Louis merges (squash) to `dev`. Record the PR number and the `dev` merge commit in Status and in `wip/migrator-3/sequencing.md`'s S7b row; verify `git diff --stat origin/dev feature/Backend-surface` is empty after the squash; delete the branch local and remote with `-d` after that check; **the worktree `_backend/` may be removed** (its two records live on `dev` once merged — this file included, if kept; otherwise archive this file into `wip/migrator-3/` per the house rule that the root `TODOS.md` belongs to the work in flight).
- [ ] STOP. **S7's release step resumes** in `pipelex/` on `dev` — its first line is still restoring this laptop's two `*.pre-reshape.bak` files, and it now has a second thing to prove: the global `inference/backends/` directory migrated **by the tool**, then a clean boot. That is a different session, in a different venue, on Louis' go.

## Known traps and open risks (read before each phase)

- **The pre-history checks skip the walk** (`coverage.py:143-146`, `ledger_check.py:157-163`) — that is why a `move_key` onto a destination the frozen previous fingerprint already has does not trip `DESTINATION_OCCUPIED`, and why every op is instead checked against `declared_removed_paths` at its **literal, pre-entry path** (`_check_pre_history_source_is_declared_material`, no rename chaining). Declare every path an op addresses.
- **`PRE_HISTORY_HAS_A_DIFF`** fires if the fingerprint pair around a pre-history entry shows any removal or narrowing. For `pipelex-config@2` the pair is (`@1`, a copy of `@1`) — trivially no diff. For `inference-backend@2` it is (`@1`, `@2`) both cut on today's projection — same. If either fires, the goldens were not what the plan says; do not `umigf` your way past it.
- **`umig` rewrites the head only and refuses a destructive overwrite**; after the renumber the head `@3` must come out byte-identical to the moved `@2` files, and after the backend cut `@2` must equal `@1` but for the version label. Any other diff is a finding.
- **`replay_surface_files_in_memory` deep-merges its paths** — one path per call for backend files.
- **`_is_within` resolves the parent, never the file** — keep the S7 asymmetry when it learns about subdirectories (a symlinked backend file out to a dotfiles repo is in reach).
- **`backends_dir_path` is resolved per directory, project wins the whole directory** — the walk repairs both roots; the loader loads one. Not a bug; a sentence in a docstring.
- **`extra="allow"` keys are invisible to the fingerprint**, a `RootModel` projects a phantom `root.`, and `compute_fingerprint` seeds `prefix=()` — the open-root seed is the smallest change that makes the paths honest; measure before building.
- **`InferenceModelSpecBlueprint.sdk` is required** — a `MODEL_DEFAULTS` surface on it raises `MigrationRegistryError` at registry build (`surfaces.py:149-163`); the projection is what makes the surface buildable.
- **Two `.md` files live in the backend directory** (`pipelex_gateway_models*.md`) and `pipelex_inference.toml` (not kit-shipped) is beside them on this laptop — the glob `*.toml` and the (directory, name) rule handle both; the tests must include both shapes.
- **`config_surface.py` may not import `surfaces.py`** (kernel-layer closure; `check-hub-layering` chases `interpreter_hub` only, so only the full `agent-test` would see it — see the memory note). Read the subdirectory from the ledger, not the registry.
- **`.claude/skills/add-migration` says never hand-edit a golden** — copying `fingerprint@1 → @2` and `git mv @2 → @3` is the one sanctioned exception, and its proof is that `umig` is a no-op afterwards; write that into the skill (Phase E) so the next person is not left to argue it.
- **No hardcoded counts** in the docs you touch — "the four surfaces" is the same trap "the three surfaces" was; write "every surface" where the number is not the point.
