# Validation-error reporting & surfacing — improvement plan

## Goal (use this as the /goal)

Perfect, clear validation-error reports and fix reports on every surface: every validation error must state — in MTHDS author syntax, never internal repr — what is wrong, where it is, and exactly what to write to fix it; and every surface (human `pipelex validate` / `pipelex fix`, agent `pipelex-agent validate` / `pipelex-agent fix` in markdown and JSON, the API `/validate` payload) must present that explanation at full strength, with an actionable next step, verified hands-on against the `pipelex-demos/bad_bundles` playground.

## Where this work lives

- Branch/worktree: `feature/Autofix` in `_autofix/` (this directory). The autofix track's steps 1–5 are done (rules, loop, agent + human `fix bundle` CLIs, `💡 Suggested fix:` lines in validate); step 6 (release train) is pending — see `wip/autofix/master-plan.md` and `wip/autofix/step5-human-cli-surfacing.md`. This plan is a follow-on quality track born from dogfooding the playground.
- Related deferred items to fold in opportunistically: `wip/autofix/deferred-checkpoint-e-review-items.md` (validate footer over-count; cross-CLI duplications) and the step-5 doc's explicitly-deferred "richer still-invalid markdown for the agent command".

## The test bed (built 2026-07-09, session before this plan)

`/Users/lchoquel/repos/Pipelex/pipelex-demos/bad_bundles/` — a playground of deliberately broken `.mthds` bundles, one scenario per directory, covering every autofix rule plus an unfixable-collision safety case and a kitchen-sink combo. Its `README.md` documents every scenario and the exact commands; `_pristine/` holds backups and `./restore.sh` re-breaks everything, so it can be exercised repeatedly. **This is where every before/after comparison in this plan gets verified.**

Run everything from the `pipelex-demos` repo root with its own venv (the user fixed it; it has the `fix` command):

```bash
cd /Users/lchoquel/repos/Pipelex/pipelex-demos
source .venv/bin/activate
export PIPELEX_NO_DECK_NOTICE=1
pipelex validate bundle bad_bundles/01_wrong_sequence_output/bundle.mthds
pipelex-agent validate bundle bad_bundles/01_wrong_sequence_output/bundle.mthds
pipelex fix bundle bad_bundles/01_wrong_sequence_output/bundle.mthds --diff
./bad_bundles/restore.sh
```

## Confirmed diagnosis (from the dogfooding session — reproduce on scenario 01)

Comparing `pipelex validate`, `pipelex-agent validate`, and `pipelex fix --diff` on `bad_bundles/01_wrong_sequence_output` showed the three surfaces are *consistent* (one engine, zero drift) but the explanation quality is carried almost entirely by the `💡 Suggested fix:` line — the core message is weak, and the agent surface doesn't even show that line as prose. Specifics, with pointers:

1. **Messages leak internal repr instead of author syntax.** The sequence-multiplicity raise site (`pipelex/pipe_controllers/sequence/pipe_sequence.py:114-118`) says `declares output multiplicity=None, but the last step ... has output multiplicity=True` — Python internals (`None` = single, `True` = list). An author writes `StoryIdea` vs `StoryIdea[]`. The message never shows the solution.
2. **The `(required: [...], provided: ...)` suffix is misleading on multiplicity errors.** Appended generically in `categorize_pipe_validation_with_libraries_error` (`pipelex/core/pipes/handle_pipe_errors.py:193`) by raw-repr-ing `required_concept_codes` (a Python list) and `provided_concept_code` (a string). For `INADEQUATE_OUTPUT_MULTIPLICITY` the concepts are identical by definition (the concept check passed just above), so it prints two same-looking refs — and the list-repr brackets `['story_studio.StoryIdea']` accidentally mimic MTHDS `[]` multiplicity syntax. It reads like a multiplicity statement it is not making.
3. **The correct solution string exists at the raise site and is thrown away by the message.** `expected_output_ref` is computed as exactly `"StoryIdea[]"` (`pipe_sequence.py:86-90`, via `StuffSpec.to_bundle_representation`) but flows only into the fix-planner enrichment (the `💡` line). Any surface showing only `message` (agent top line, API consumers) gets the `None`/`True` riddle.
4. **The agent markdown under-delivers on the workspace's own "format follows consumer" doctrine.** `pipelex-agent validate` markdown = a title, a *truncated* top message (drops the required/provided detail the item has), a boilerplate hint ("Check the validation_errors array for specific issues", from `pipelex/pipeline/exceptions.py:92`), then a raw JSON dump of `validation_errors`. The per-item prose rendering the human renderer got in step 5 (titled error, field lines, `💡 Suggested fix:` line, actionable footer naming the fix command) never got mirrored to the agent markdown. Doctrine reference: workspace-root `CLAUDE.md` § "Surface output conventions" and `docs/specs/pipelex-mthds-protocol.md` § "Presentation vs contract".
5. **The human footer is the model to generalize:** `💡 N of these errors can be fixed automatically — run: pipelex fix bundle <path>` — "a hint needs an action behind it". The agent surfaces have no equivalent (neither validate nor the hint text), even though the payload carries `suggested_fix` with `safety: "safe"`.

## Key code map

| Concern | Where |
| --- | --- |
| Raise sites of pipe validation errors (message wording) | `pipelex/pipe_controllers/sequence/pipe_sequence.py` (multiplicity + concept + taint); sibling controllers/operators under `pipelex/pipe_controllers/`, `pipelex/pipe_operators/` |
| Error-type enum + `is_*` properties | `pipelex/core/pipes/exceptions.py` (`PipeValidationErrorType`, `INADEQUATE_OUTPUT_MULTIPLICITY` at :108) |
| Categorizer that builds error data + appends the required/provided suffix | `pipelex/core/pipes/handle_pipe_errors.py:180-209` |
| Shared items builder (single source of truth; attaches `suggested_fix`) | `pipelex/pipeline/validation_errors.py:24` (`build_validation_error_items`) |
| Fix planner (rules, descriptions, `KNOWN_FIX_CODES`) | `pipelex/pipeline/fixes/planner.py` |
| Fix loop + result model | `pipelex/pipeline/fixes/fix_loop.py` |
| Human validate/fix rendering (items renderer, 💡 lines, footer) | `pipelex/cli/error_handlers.py` (`display_validation_error_items`, `handle_validate_bundle_error`); `pipelex/cli/commands/fix/` (`_fix_core.py`, `_diff_sandbox.py`) |
| Agent CLI output plumbing (markdown/JSON two-stream, error envelope) | `pipelex/cli/agent_cli/commands/agent_output.py` (`_render_error_markdown` at :289); `pipelex/cli/agent_cli/commands/fix/`; agent CLI conventions in `pipelex/cli/agent_cli/CLAUDE.md` |
| Agent/API fix markdown renderer (CLI-free) | `pipelex/pipeline/fixes/fix_render.py` (`format_fix_markdown`) |
| API error envelope (`ValidateBundleError.to_error_report`, boilerplate hint) | `pipelex/pipeline/exceptions.py` (hint at :92, report at ~:154) |
| Tests pinning current wordings/renderings | `tests/unit/pipelex/cli/test_validate_suggested_fix_rendering.py`, `tests/unit/pipelex/cli/commands/fix/test_fix_bundle_human_format.py`, `tests/integration/pipelex/cli/test_fix_bundle_human.py`, `tests/integration/pipelex/pipeline/test_fix_convergence_loop.py`, e2e under `tests/e2e/pipelex/cli/` and `tests/e2e/agent_cli/` |
| Cross-repo wording corpus (regen after wording changes) | `conformance/validate-error-qa/` — the generated error-explanation QA corpus, freshness-tested against the live API (see workspace-root `CLAUDE.md`) |

## Guardrails

- **Presentation vs contract:** wordings, markdown shape, exit codes, and hints are presentation — free to improve. The structured fields (`is_valid`, `error_type`, `category`, `suggested_fix`, `validation_errors[]`) are the machine contract — additive changes only, and any addition ripples to the API report and conformance fixtures (step-6 already owes a `suggested_fix` fixture regen; coordinate, don't collide).
- **One engine, every surface:** improvements go into the message at the raise site or the shared items builder/renderers — never into per-surface private wording that can drift.
- Wording changes WILL break string-pinning tests; update the pins in the same change (that's the point of them).

## Phase 2 design notes (gathered from reading the renderers — not yet implemented)

The intended architecture for Phase 2 (agent surfaces catch up), converged after reading the code:

- **Add a shared, CLI-free markdown renderer** for validation-error items — e.g. `format_validation_errors_markdown(items, *, bundle_path, fix_command, header)` in a non-CLI module (put it beside `format_validate_markdown` in `pipelex/pipeline/validation_render.py`, or in `validation_errors.py`). It mirrors the *structure* of the human `display_validation_error_items` (`cli/error_handlers.py:266-328`) but emits markdown (`**bold**`/backticks) instead of Rich markup: group by category, per item render humanized `error_type` title + identity fields (Pipe/Concept/Domain/Field/Missing*/Variables) + the message + `💡 Suggested fix: <description>` + source, then a fix-aware footer. Reuse it for (a) agent validate markdown, (b) agent fix still-invalid markdown (`fix_render.py`), (c) API `rendered_markdown` in Phase 3.
- **Agent validate error path**: mirror the human `handle_validate_bundle_error`. Cleanest is a dedicated `agent_error_validate_bundle(exc, *, bundle_path, library_dirs, allow_signatures)` (in `agent_cli/commands/agent_output.py`) that, for **markdown**, prints the prose (title `# Bundle validation failed`, bundle path, prose items via the shared renderer, fix-aware footer naming `pipelex-agent fix bundle <path>`) and for **JSON** emits the *exact same envelope as today* (`_agent_error_json` with `is_valid`/`bundle_path`/`validation_errors`). Call it from `validate/bundle_cmd.py:194` and `validate/method_cmd.py:119` in place of the generic `agent_error(...)`. **The JSON contract must not change** — hooks pin `--format json` and branch on structured fields (`mthds-plugins/docs/mthds-agent-output-audit.md`).
- **Fix-aware footer (disease E)**: replace the boilerplate hint `"Check the validation_errors array for specific issues"` (`ValidateBundleError.user_action` at `pipeline/exceptions.py:90-93`) *for the agent markdown presentation only* — the footer names `pipelex-agent fix bundle <path>` when N items carry a `safe` suggested_fix, mirroring the human footer (`error_handlers.py:367-388`). Keep `user_action.detail` as the JSON `hint` (or improve it too — it's presentation, not contract).
- **Truncated top-line + pydantic leak (disease C)**: the agent prose body shows the *items*, not `exc.message`, so the pydantic `Value errors: '<field>': Value error,` leak won't appear in prose. To also clean the JSON `message` field, fix it at the source where `ValidateBundleError.message` is built from the pydantic `ValidationError` — `pipeline/validate_bundle.py:~142` `_translate_to_validate_bundle_error` (summarize from items: count + first clean message). `message` is presentation, safe to improve; verify no hook branches on it.
- **Fix still-invalid markdown (`fix_render.py` `format_fix_markdown`)**: render `remaining_errors` (already `ValidationErrorItem`s) as prose via the shared renderer with their 💡 lines — this is the step-5 deferred "richer still-invalid markdown".
- Tests: agent-CLI unit + e2e under `tests/e2e/agent_cli/`; keep the JSON snapshot contract byte-stable; only the markdown stream changes.

## Phases

### Phase 0 — Cold-start orientation — DONE

- [x] Read this file top to bottom; skim `wip/autofix/step5-human-cli-surfacing.md` (design decisions D5.4/D5.5 explain the human renderer) and `pipelex/cli/agent_cli/CLAUDE.md` (two-stream output conventions).
- [x] Run the playground: `./bad_bundles/restore.sh`, then all surfaces on scenario 01 (commands above) and capture the current outputs as the "before" baseline. Captured **all** scenarios × five surfaces → `wip/validation-reporting/before/` (regen: `scratchpad/capture_before.sh`).
- [x] Sweep the remaining scenarios (02–10) across the surfaces and note, per scenario, every message that fails the bar "states the problem AND the concrete author-syntax action". → **inventory in [`wip/validation-reporting/phase0-inventory.md`](wip/validation-reporting/phase0-inventory.md)** (diseases A–F, per-scenario verdict table, Phase 1 scope).

### Phase 1 — Message quality at the source (the engine, so every surface benefits) — DONE

- [x] Rewrite the sequence multiplicity-mismatch message (`pipe_sequence.py:114`) in author terms using the already-computed `expected_output_ref` and a `to_bundle_representation` rendering of the declared output. Now: *"the sequence 'brainstorm' declares its output as 'StoryIdea', but its last step 'gen_ideas' yields 'StoryIdea[]'. Update the sequence's output to 'StoryIdea[]' (or change the last step)."*
- [x] Rewrite the `input_stuff_spec_mismatch` message (`pipe_abstract.py:361-373`) — the worst offender (4 lines of `Concept(...)`/`PresenceMarker` repr) → *"In pipe 'make_recipe', input 'dish' is declared as 'Number' but its step needs 'Text'. Update the input to 'Text'."* Removed the `mismatch_details` machinery.
- [x] Fix the required/provided suffix (`handle_pipe_errors.py:192`): suppress it for multiplicity errors (new enum property `is_inadequate_output_multiplicity`), and for the cases where it stays, render author-syntax refs joined without Python list-repr brackets.
- [~] Audit every other pipe/blueprint validation message for the same diseases. Done: the messy ones (A) are the two above. **Disease F (`invalid_pipe_code_syntax` "Must be in snake_case" on a dotted code) DEFERRED** — low value (the 💡 line already says "Strip the same-domain prefix"), and it would touch the well-tested categorizer/message contract (typed error categorizes structurally, but 4 test files pin the wording). Native-redecl / missing-input / concept-mismatch messages already meet the bar. Disease C (pydantic top-line leak) is Phase 2 (summarize-from-items).
- [x] Update string-pinning tests: new `tests/unit/pipelex/core/pipes/test_handle_pipe_errors_suffix.py` (suffix suppression/reformatting), strengthened `test_pipe_sequence_output_multiplicity.py` (author-syntax pin + no repr leak), strengthened `test_controller_inputs_enrichment.py` (stuff-spec message pin). `make agent-check` green; targeted suites green (core + pipe_controllers + pipes + pipeline = 2010 passed/1 pre-existing xfail; cli = 820 passed).
- [x] **CHECKPOINT 1** — re-captured all scenarios → `wip/validation-reporting/after/`. Exactly 01/02/03/08 changed (all disease-A/B); diseases A+B markers now **absent** from every after-capture; verified cascade (02: `Idea`→`Idea[]`) and multi-file (08: `Quote`→`Quote[]`) render correctly. Good handoff point.

### Phase 2 — Agent surfaces catch up to the human renderer (markdown presentation) — DONE

- [x] `pipelex-agent validate` markdown: renders `validation_errors` items as prose (humanized `error_type` title, message, per-field lines, `💡 Suggested fix:` line), not a JSON dump. New shared CLI-free renderer `format_validation_error_items_markdown(items)` in `pipeline/validation_render.py` mirrors the human `display_validation_error_items` structure; wired via new dispatcher `agent_error_validate_bundle(exc, *, bundle_path, library_dirs, allow_signatures)` in `agent_output.py`, called from `validate/bundle_cmd.py` + `method_cmd.py`. JSON stream kept byte-identical (verified: all 10 scenarios' AGENT-json capture unchanged).
- [x] Truncated/leaky top-line: the markdown prose shows the *items*, not `exc.message`, so the pydantic `Value errors: '<field>': Value error,` leak (disease C) is **gone from the markdown surface** (verified 05/06/09/10). **Deferred to Phase 3:** cleaning the JSON top-level `message` field at the source (`_translate_to_validate_bundle_error` / interpreter) — doing it CLI-only would create CLI≠API drift, so it belongs with Phase 3's "API carries improved messages" (one-engine). JSON `message` intentionally left as-is here to keep the JSON contract byte-stable.
- [x] Boilerplate hint (disease E) replaced for the **markdown** presentation with a fix-aware footer: when N items carry a suggested fix, names the exact `pipelex-agent fix bundle <path>` command (with `-L`/`--allow-signatures` echo, via shared `build_fix_command`); else "no automatic fix" pointer. JSON `hint` kept unchanged ("Check the validation_errors array…") per the contract.
- [x] `pipelex-agent fix` still-invalid markdown: new `format_fix_still_invalid_markdown(result, *, bundle_path)` in `fix_render.py` renders applied fixes + bail reason + remaining errors as prose (with 💡 lines) + context tip; wired in `fix/bundle_cmd.py` (markdown branch), JSON `FixBundleError` envelope unchanged. (The step-5-deferred "richer still-invalid markdown".)
- [x] Human `handle_validate_bundle_error` refactored to use the shared `build_fix_command` (drift reduction; output byte-identical — verified HUMAN-validate capture unchanged across all 10).
- [x] Tests: new `test_validate_bundle_error_format.py` (dispatcher: markdown prose + fix-aware footer, JSON envelope byte-stable); strengthened `test_validation_render.py` (shared items renderer + `build_fix_command`), `test_fix_render.py` (still-invalid prose + tip branches), `test_fix_format.py` (command-level markdown still-invalid). `make agent-check` green; cli+pipeline+e2e suites green (2054+ passed). Hook JSON contract untouched (see audit note below).
- [x] **CHECKPOINT 2 CLEARED** — re-captured all 10 scenarios × 5 surfaces → `wip/validation-reporting/after/`. Per-section diff vs Phase-1: **AGENT validate markdown CHANGED on all 10** (JSON dump → prose), **AGENT validate json SAME on all 10** (contract preserved), **HUMAN validate + HUMAN fix --diff SAME on all 10** (footer refactor byte-stable), **AGENT fix markdown CHANGED only on 09** (the sole still-invalid case). No `Value errors:` / `multiplicity=None` / json-fence leak in any markdown surface. Good handoff point.

### Phase 3 — JSON / API parity and the fix-report surfaces

- [ ] **Carry-over from Phase 2 (disease C, JSON `message`):** clean the leaky top-level `message` (`Validation error(s):\n\nValue errors: '<field>': Value error, …`) at its **source** so *every* surface benefits (one-engine), not CLI-only. Sources: blueprint path = `interpreter_error.message` (interpreter); pydantic path = `report_validation_error` (`core/validation.py`) called from `_translate_to_validate_bundle_error` (`validate_bundle.py:164-181`). Cleanest = summarize from the built items (count + first clean item message). Verify no hook branches on `message` (they branch on `is_valid`/`error_domain`/`validation_errors[]` — `mthds-plugins/docs/mthds-agent-output-audit.md`). This changes the API report `message` too → coordinate with the conformance corpus regen (Phase 4).
- [ ] Verify the API `/validate` payload (`ValidateBundleError.to_error_report`) carries the improved messages verbatim and the hint improvement lands there too; a produced verdict stays a 200/`is_valid` discriminated body — no contract change. Consider reusing the shared `format_validation_error_items_markdown` for the API `rendered_markdown` (the design note's consumer (c)); today the API invalid-arm uses the older `render_invalid_validation_markdown` (dict-based, no 💡 lines) in `validation_render.py` — unify them.
- [ ] Human + agent fix reports: apply the Phase 1 message bar to `bail_reason` wordings and the "still shows a suggested fix that was not applied" tip; confirm `--diff` preview wording matches the real run.
- [ ] If any structured field is worth adding for consumers (e.g. `expected_output_ref` / declared-vs-expected as data), do it additively and note the wire addition for the step-6 changelog + conformance fixture regen.
- [ ] **CHECKPOINT 3** — update this doc.

### Phase 4 — Full-sweep QA and closeout

- [ ] Restore the playground and run every scenario × every surface (`validate` human/agent-md/agent-json, `fix` human/agent, `--diff`, and the collision bail of scenario 09); each output must meet the goal bar with no regressions.
- [ ] Regenerate/refresh the `conformance/validate-error-qa/` corpus against the new wordings (coordinate with step 6's release ordering — corpus freshness-tests against the *live* API).
- [ ] Docs: update the error-reporting docs in `docs/` (and `pipelex-demos/bad_bundles/README.md` if commands or outputs changed); CHANGELOG entry under `[Unreleased]`.
- [ ] `make agent-check` + `make agent-test`; final status update in this doc.

## Status log

- 2026-07-09 — Plan written from the dogfooding session (playground built + three-surface diagnosis on scenario 01). No implementation started.
- 2026-07-09 — **Phase 0 DONE.** Confirmed the step-5 code is present on `feature/Autofix` (came in via the `dev` merge `ec477700f`); demos venv points at this worktree, so code changes dogfood live. Captured every scenario × five surfaces to `wip/validation-reporting/before/`. Inventory in `phase0-inventory.md`: 6 diseases (A internal-repr messages, B misleading required/provided suffix, C pydantic top-line leak, D agent-md JSON-dump-not-prose, E boilerplate agent hint, F snake_case wording). Raise sites confirmed: `pipe_sequence.py:114` (multiplicity), `pipe_abstract.py:361-373` (stuff-spec-mismatch — worst), `handle_pipe_errors.py:192` (suffix). `StuffSpec.to_bundle_representation(relative_to_domain=…)` is the author-syntax renderer for Phase 1.
- 2026-07-09 — **Phase 2 DONE (CHECKPOINT 2 cleared).** Agent surfaces now render prose. New shared CLI-free renderer `format_validation_error_items_markdown(items)` + `build_fix_command(executable, *, bundle_path, library_dirs, allow_signatures)` in `pipeline/validation_render.py` (mirror the human `display_validation_error_items` / footer). New dispatcher `agent_error_validate_bundle(exc, *, bundle_path, library_dirs, allow_signatures)` in `agent_output.py` (markdown prose + fix-aware footer; JSON envelope byte-identical) wired into `validate/bundle_cmd.py` + `method_cmd.py`. New `format_fix_still_invalid_markdown(result, *, bundle_path)` in `fix_render.py` wired into `fix/bundle_cmd.py` markdown branch (JSON `FixBundleError` unchanged). Human `handle_validate_bundle_error` refactored to the shared `build_fix_command` (byte-identical). Disease D (JSON-dump→prose) + E (boilerplate hint→fix-aware footer) cured on the markdown surface; disease C cured in markdown (items shown, not `exc.message`) — **JSON `message` cleanup deferred to Phase 3** (fix at source, one-engine). Tests: +1 new module (`tests/unit/pipelex/cli/agent_cli/test_validate_bundle_error_format.py`), strengthened `test_validation_render.py` / `test_fix_render.py` / `test_fix_format.py`. `make agent-check` green; cli+integration-pipeline+e2e suites green. Re-captured `wip/validation-reporting/after/` (regen script: `scratchpad/capture_after.sh` — runs human `pipelex` with `--no-logo`, strips banner/telemetry-DEBUG noise; demos venv points at this worktree so changes dogfood live). Per-section verification: AGENT-md CHANGED×10, AGENT-json SAME×10, HUMAN-validate/fix SAME×10, AGENT-fix-md CHANGED×1 (09 only). NEXT = **Phase 3** (JSON/API parity + the disease-C JSON-`message` carry-over at source; fix reports' `bail_reason` wording; consider additive structured fields).
- 2026-07-09 — **Phase 1 DONE (CHECKPOINT 1 cleared).** Rewrote the two repr-leaking messages (A) at their raise sites + suppressed/reformatted the misleading suffix (B) in the categorizer (new `is_inadequate_output_multiplicity` enum property). Every surface benefits (one engine) — verified the agent JSON/markdown carry the new message with zero `multiplicity=None/True` or `(required: [` leaks. Deferred disease F (snake_case wording) — low value, categorizer/test-contract risk. C stays for Phase 2. Tests: +1 new module, 2 strengthened; `make agent-check` green, targeted suites green. before/after captures in `wip/validation-reporting/`. NEXT = **Phase 2** (agent `validate`/`fix` markdown → prose items + 💡 lines + fix-aware footer; fix the pydantic top-line leak C + boilerplate hint E). Code map: `agent_output.py` `_render_error_markdown` (~:289), `pipeline/exceptions.py:92` (hint), `fix_render.py` (still-invalid md).
