# Optionals phase 1 — live tracker

Branch `feature/Optionals` (worktree `_optionals`). This file is the single live tracker: progress checkboxes, checkpoint log, decisions/deviations. Reference docs: `wip/optionals-design.md` (decisions D1–D11, all decided) and `wip/optionals-plan.md` (the narrative plan this tracker implements — steps A–F map one-to-one).

Working rules: TDD (red tests first per step), `make agent-check` after every code change, full `make agent-test` at each checkpoint, **one commit per checkpoint**, no backward compatibility (breaking changes go in the changelog under `[Unreleased]`).

## Cold-start context (update at every checkpoint)

- **Status:** Step E DONE (Checkpoint E cleared — SHAs in the checkpoint log). An absent result is a first-class success everywhere in-repo. Protocol bump landed in one motion: `optional: bool` on `PipeInputContract`/`PipeOutputContract` (from `presence.is_optional`), `warnings: list[ValidationErrorItem]` on `PipelexValidationReport` (assembled in `build_validation_report`, populated by the new `pipeline/optionality_warnings.py::build_optionality_warnings` in `validate_in_process`, the agent-CLI whole-bundle validate cores, the bare CLI (`_echo_optionality_warnings`, bundle arm + `--all`), and the three bundle-context builder `validate_ops` twins — all through the single-walk collector `pipeline/controller_taint.py::collect_controller_taint_analyses`, which also feeds `build_liftable_pipes`; markdown gets a "Warnings" section in `validation_render.py`). The useless-`!` lint (`OPTIONAL_FORCE_REDUNDANT`, advisory-only enum value): the taint walks classify every `!` consumption (asserting = slot tainted, redundant = slot guaranteed) via `scan_taint_triggers` + new `ForceConsumptionInfo` carriers on both analyses; the builder aggregates per (pipe_ref, variable) ACROSS flows — one asserting flow silences redundant observations, and a `!` never observed in any flow is never flagged. Boundaries: `resolve_main_stuff_root_key` returns the declared slot name for a recorded main absence (message swept to the new invariant), `hydrate_working_memory` round-trips the `absences` ledger (malformed → loud `PipeJobError`), OTel captures absence documents on inputs and outputs, bare CLI prints/saves the absence artifact (no viewer), agent CLI returns the absence document as the compact result. Shared artifact renderers: `core/memory/absence_render.py::build_absence_payload/build_absence_json/build_absence_markdown/build_absence_html` (delivery executor + both CLI run cores render through them). Graph: `NodeSpec.skip_reason` + `skipped` status via the new `on_pipe_end_skipped` tracer hook (protocol + noop + manager + tracer + `pipe_end_skipped` event + assembler), skip reason from the shared `PipeAbstract._make_skip_reason`; a skipped pipe with a PLURAL output registers its real empty-list output spec (and plural companion slots) so downstream DATA edges resolve; `EdgeSpec.optional` on data edges whose producer registered `extra={"optional": True}` on its output IOSpec (set in the run epilogue from `output.presence.is_optional`; both builders share `graphspec.py::output_digest_is_optional`). **Bonus fix (load-bearing):** `StuffArtefact.__bool__` — Jinja2 truth tests (`{% if var %}`, `@?`) on a PRESENT singular value crashed via the list-only `__len__` fallback; now truthy, with empty lists falsy per D4. End-to-end pin: `test_absent_main_output_surfaces.py` (execute → response slot name, provenance chain to NOT_PROVIDED origin, graph skipped nodes + serialized).
- **Next action:** Step F — specs, conformance, docs, release prep: `docs/specs/` protocol spec (optional flag, warnings, absence records, new error categories incl. `optional_force_redundant`), conformance fixtures + error-QA rows + `make check-spec-links`, regenerate MTHDS JSON schema + error pages, optionality guide + pipe-page updates + `@?` docs reconciled, changelog finalize, full gates, FINAL CHECKPOINT.
- **Cross-repo hand-off notes (accumulating for the wave):** (1) mthds-python `DictWorkingMemoryAbstract` must gain the `absences` ledger (extra=forbid blocks it today) — then `_run_core_api.py`'s gated absent arm can land (comment in code marks the spot); (2) mthds-ui renders `skipped` nodes + `optional` edges + `skip_reason` detail; (3) pipelex-api response models surface `warnings` + contract `optional` flags.
- **Open questions for Louis:** none blocking. Standing flagged compromise: `continue` pass-through breaks in phase 1 while its ergonomic replacement (`??` coalescing) is phase 2 — restructure only if Louis asks.

## Checkpoint protocol — MANDATORY STOP at every CHECKPOINT

At each checkpoint the agent MUST stop forward progress and run this sequence, in order. No rolling into the next step with the checkpoint half-done.

1. **Verify:** full `make agent-check` + `make agent-test` green (`make tb` too if config files were touched). Fix failures before anything else.
2. **Commit:** one commit for the step (changelog entry included when the step is user-visible/breaking).
3. **Update this file:** tick the boxes, fill the checkpoint log row (SHA), and refresh "Cold-start context" — decisions taken, deviations from the plan, open questions, exact next action — so a brand-new session can resume efficiently from this file alone. If a design decision changed, update `wip/optionals-design.md`/`wip/optionals-plan.md` too.
4. **Fan out a context-free code review:** spawn a **Sonnet-5 sub-agent with NO inherited context** (general-purpose agent, `model: sonnet` — never a fork) whose prompt is ONLY a pointer to the changes under review plus the instruction to run the `/code-review` skill. Example prompt: *"In /Users/lchoquel/repos/Pipelex/_optionals, run the /code-review skill on `git diff <previous-checkpoint-SHA>..HEAD`. Report findings."* Do **not** pass the plan, the design doc, the rationale, or your own conclusions — the reviewer judges the code cold. Review bar: clean solid software, not over-engineering.
5. **Triage findings:** correctness bugs → fix now (amend or follow-up commit, re-run gates). Design-tradeoff findings → do not reflexively apply; record in a deferred-items doc under `wip/optionals/` and move on. Over-engineering findings → simplify now.
6. Only then proceed to the next step.

## Prep

- [x] Commit this tracker (+ staged old-TODOS deletion). Record the SHA as the Step-A review base in the checkpoint log. *(Turned out already done: the tracker landed in `eace1d607`.)*

## Step A — grammar and carriers (parse `?` / `!`, no behavior change)

- [x] Red tests first: parser unit tests for every marker × multiplicity combination; blueprint + spec accept/reject fixtures.
- [x] Extend the marker grammar (D1) in `pipelex/core/pipes/variable_multiplicity.py` (+ its inline twin), the looser regex in `input_stuff_specs_factory.py`, and the naive `[`-splitter in `concepts/helpers.py`. Fix the `MUTLIPLICITY_PATTERN` typo while there.
- [x] `StuffSpec`/`NamedStuffSpec` gain the presence marker (a `PresenceMarker` StrEnum: plain / optional / force); `InputStuffSpecs.required_names` splits into required vs declared.
- [x] Blueprint-parse-time grammar errors (`OPTIONAL_MARKER_INVALID`): `?` on plurals, `!` on outputs or plurals, markers on concept definitions / `refines` / structure-field refs / package refs (D1, D4).
- [x] Builder specs mirror the blueprint rules (`to_blueprint()` passes markers through); `contract_match` canonicalization compares presence markers (D5).
- [x] **CHECKPOINT A** — markers parse, validate, and round-trip; zero runtime behavior change; run the full checkpoint protocol above.

## Step B — absence at runtime (ledger + trichotomy + lifting)

- [x] Red tests first: lift-skip chain with provenance, absorb, force-failure UX (assert the message names variable, pipes, and reason), plural normalization, optional method inputs, run-report absence enumeration. All tests sink absence before the method boundary (a downstream `?` input) — end-to-end absent-main-output coverage is Step E's.
- [x] WorkingMemory gains the absence ledger (D2): `AbsenceRecord` (`variable_name`, `producing_pipe`, `kind` = declared-absent | skipped | not-provided, `reason`, upstream chain). New tri-state resolved accessor (Stuff | AbsenceRecord) for post-run reads — do **not** resurrect `optional_main_stuff` (design §14).
- [x] The three runtime miss-gates learn the trichotomy (D3): `PipeAbstract.validate_before_run` (fix the "Dry run of ..." message bug on the live path), `SubPipe`, PipeCondition's branch check. Plain input fed absence → **skip** (lift) + record skipped-absence for the pipe's output with provenance; `?` input → run; `!` input absent → `OptionalValueAbsentError` (D9: new error class in the relevant `exceptions.py`, provenance chain, RFC 7807 + error page via `pipelex-dev generate-error-pages`).
- [x] The per-pipe graph-tracer epilogue in `pipe_abstract.py` (success path reads `working_memory.get_main_stuff()` directly) learns the resolved-as-absent arm **here, not in Step E** — it fires on every pipe run, so the first skipped pipe would crash it.
- [x] Skipped plural output normalizes to empty `ListContent` + ledger note (D4); taint stops.
- [x] Optional method inputs (D5): omitting a `?` input in `inputs.json` / the execute request → not-provided record instead of `PipeRunInputsError`; the required-input error message gains the "these optional inputs may be omitted" hint.
- [x] Mock seeding / dry-run sweep learn optional slots (dry run stays all-present per D6): mock seeding already enumerates every declared input including `?` ones (all-present); pinned by test, and not-provided records are only written for slots the mock pass left empty.
- [x] **CHECKPOINT B** — absence exists, propagates, and fails loudly only through `!`; run the full checkpoint protocol.

## Step C — controllers: `continue` replacement + parallel combine (D11)

- [x] Red tests first: continue-absent (live + dry), parallel with absorbed/omitted/required-absent branches, batch compaction if shipped.
- [x] **PipeCondition `continue`** (design §14, breaking): replace pass-through-or-error with "declared output absent, memory otherwise unchanged" — record the absence (kind = declared-absent, reason = the evaluated expression/outcome), stamp no main stuff, return success. Same in dry run (the all-special-outcomes guard becomes: legal iff output declared `?`). Rewrite `test_pipe_condition_continue_delivery.py` to pin the new semantics (keep the run-id stamping assertions).
- [x] These tests exercise the delivery boundary, so `delivery_executor.py` (typed + raw arms: absent main output delivers an explicit absence artifact, not an error) lands **here, not in Step E**.
- [x] Document the migration idiom (previous value stays in memory under its own name; coalescing is the phase-2 answer) in docs + changelog (breaking).
- [x] **PipeParallel combine under absence** (D11): absent branch result → structured output field with `required = false` absorbs as field-`None`; `Composite` output omits the component + ledger note. Runtime guard raises a typed error if a required field's branch is absent (statically unreachable after Step D, but the runtime must not feed `combine_stuffs` a hole). **Checkpoint-B review pinned the entry points:** `pipe_parallel.py` `_run_branches_and_combine` (`pipe_output.main_stuff` — the raising accessor) crashes today on a lifted branch (`WorkingMemoryStuffNotFoundError`, reproduced); `pipe_batch.py` has the identical shape at its branch-collect loop and in `_dry_run_controller_pipe`. Both must move to the tri-state `resolve_main_stuff()` with the D11 policy.
- [x] PipeBatch compaction (D4 bonus): absent branch results dropped from the aggregated list — SHIPPED here (it fell out of the tri-state collect loop naturally; no `?`-output gating needed at runtime since a list cannot hold a hole).
- [x] **CHECKPOINT C** — controllers speak absence; the #1014 `continue` tests replaced; run the full checkpoint protocol.

## Step D — static validation: the taint pass + template lint

- [x] Red tests first: taint fixtures (valid + each error type), guard-lint accept/reject, `@?` detection.
- [x] Absence-propagation pass over controller dataflow (D6): per-slot presence computed (`guaranteed` / `maybe-absent`) by walk methods on the controllers (`PipeSequence.analyze_taint`, `PipeParallel.analyze_branch_taint`); shared carriers + presence-resolution helper in `pipe_controllers/absence_taint.py`.
- [x] New `PipeValidationErrorType`s: `OPTIONAL_NOT_HANDLED` (taint reaches a non-optional boundary — message names source, path, three fixes), `OPTIONAL_OUTPUT_REQUIRED` (`continue`-reachable condition without `?` output), `OPTIONAL_BRANCH_REQUIRED_FIELD` (the D11 static error, extending `PipeParallel.validate_output_with_library`).
- [x] Template guard-lint (D7): `OPTIONAL_INPUT_UNGUARDED` via the new guard walker (`tools/jinja2/jinja2_optional_guards.py`) + shared `lint_optional_input_guards` helper, wired into PipeLLM prompts/system prompts, PipeCompose templates, and PipeCondition expressions. `@?` fix: declared-optional inputs are no longer presence-required by the SubPipe / PipeCondition miss-gates.
- [x] Liftable-pipe inventory as structured data on the valid report (`liftable_pipes` beside `pipe_io_contracts`), built by `pipeline/liftable_pipes.py` from the same taint analyses.
- [x] **CHECKPOINT D** — validation proves the safety theorem ("every absence source reaches an explicit sink"); full checkpoint protocol run (see the checkpoint log).

## Step E — wire, boundaries, and graph

- [x] Red tests first: end-to-end absent-main-output run over each surface (execute response, CLI output cores, telemetry, hydration round-trip), graph skipped-state serialization, contracts/warnings, redundant-`!` lint.
- [x] Protocol bump, one motion (D6 + D8): `optional: bool` on `PipeInputContract`/`PipeOutputContract`, the general `warnings` array on the validation report (same item shape as errors, never flips `is_valid`), absence records on run results. Neutral MTHDS naming, no `pipelex_` prefixes.
- [x] Populate `warnings` with the useless-`!` lint (`optional_force_redundant`). The over-claimed-`?` lint is deliberately **not** emitted in phase 1 (D5 blesses over-claiming as evolution room; phase 2 makes `?` on operator outputs meaningful).
- [x] The remaining post-run boundaries learn the resolved-as-absent arm: `resolve_main_stuff_root_key` (absent arm returns the declared slot name), `hydrate_working_memory` (absences ledger round-trips, malformed record fails loud), `otel_factory` (inputs AND outputs capture), bare CLI run core (pretty print + `--save-main-stuff` absence artifact + `--save-csv` message), agent CLI run core (absence document as result payload). Shared renderers in `core/memory/absence_render.py`; delivery executor refactored onto them. **Agent-API run core (`_run_core_api.py`) stays gated on the cross-repo mthds bump** — the SDK wire models (`DictWorkingMemoryAbstract`, extra=forbid, root+aliases only) cannot carry absences yet; noted in code, hand-off item for the cross-repo wave.
- [x] Graph (D8): `NodeSpec` gains `skip_reason`, lifted pipes end their node `skipped` (new tracer hook `on_pipe_end_skipped` + `pipe_end_skipped` trace event + assembler handler), `EdgeSpec.optional` marks data edges fed by `?`-declared outputs (producer output IOSpec `extra["optional"]`). (mthds-ui rendering is the cross-repo wave.)
- [x] Sweep the invariant prose to "a pipe run always resolves its declared output: a value or a recorded absence".
- [x] **CHECKPOINT E** — an absent result is a first-class success everywhere in-repo; full checkpoint protocol run (see the checkpoint log).

## Step F — specs, conformance, docs, release prep

- [ ] `docs/specs/` protocol spec: `optional` flag, `warnings` array, absence records, new validation-error categories; matching `conformance/` fixtures (`optional_not_handled.mthds` etc.) + error-QA corpus rows; `make check-spec-links` green (run in `conformance/`).
- [ ] Regenerate the MTHDS JSON schema (`pipelex-dev generate-mthds-schema`) and error pages; docs: an optionality guide beside `multiplicity.md`'s pipelex counterpart, pipe-page updates (PipeCondition, PipeParallel, PipeBatch), `@?` docs reconciled.
- [ ] Changelog under `[Unreleased]`: breaking — `continue` semantics, `?`/`!` grammar, protocol additions.
- [ ] Full `make agent-test` + `make tb`.
- [ ] **FINAL CHECKPOINT** — phase 1 complete; run the full checkpoint protocol. Hand off: cross-repo wave (after the Required-main-stuff Phase 3 sweep) and phase 2 (LLM maybe-wrapper). Fold or close this tracker and `wip/optionals-plan.md`.

## Checkpoint log

| Checkpoint | Commit SHA | Review verdict | Notes |
|---|---|---|---|
| Prep | `eace1d607` | n/a | Tracker was committed alongside the plan update; serves as the Step-A review base. |
| A | `ae4ace79e` | Clean — no findings at reporting confidence | Cold Sonnet review of `eace1d607..HEAD`. Two sub-threshold doc-in-code notes applied in the follow-up commit (PipeSpec authoring-surface descriptions teach `?`/`!`; enum comment covers all three trigger cases); third note (factory doesn't re-enforce mutual exclusion) = deliberate layering, see deviations log. |
| B | `6c258e2ba` | 4 findings, all triaged | Cold Sonnet review of `80c4e56f6..HEAD`. (1)+(2) PipeParallel/PipeBatch crash (`get_main_stuff` raise) on a lifted branch — CONFIRMED reachable, but it is exactly the already-planned Step C work (D11 combine-under-absence + batch compaction); pinned explicitly in the Step C bullets. (3) delivery executor generic error on an unsunk lift chain — the plan's explicit Step C boundary (delivery arms land there). (4) duplicated miss-gate check → hoisted to `WorkingMemory.list_missing_names` in the checkpoint commit. |
| C | `0df759bd6` | 10 findings: 1 bug fixed, rest triaged | Cold Sonnet review of `7d2ed6bf4..HEAD`. (1) **Stale-value masking — CONFIRMED bug, fixed in the checkpoint commit:** recording an absence under a name that already held a `Stuff` left the stale value winning `resolve_stuff`; fix = new `record_resolved_absence` (removes value + alias under the name — the mirror of `set_stuff`'s value-supersedes-record), used by `record_new_main_absence` and the parallel branch arm; `record_absence` stays the pure NOTE write (D4 plural note depends on it). Regression tests added. (2)–(4) OTel + three CLI run cores raise on absent main — already-planned Step E work, now pinned as REACHABLE in the Step E bullet (Checkpoint-B precedent). (5) `hydrate_working_memory` drops the absences ledger — added to the Step E bullet (was missing from the checklist). (6) non-`None` field default absorbs as the default — deliberate (proto-coalescing); changelog + docs wording corrected to say "field default". (7)–(9) dedup/nesting tradeoffs → `wip/optionals/deferred-absence-handling-dedup.md`. (10) informational. |
| D | `f3d58d104` (+ triage follow-up commit) | 10 findings: 8 fixed, 2 deferred | Cold Sonnet review of `2d4211420..HEAD`. **Fixed in the triage commit:** (1) lifted `add_each_output` parallel left branch slots neither-value-nor-record at runtime while the static pass blessed the flow — new `PipeAbstract.lifted_companion_slots()` hook + `_make_lifted_output` resolves each companion (singular → SKIPPED record with provenance, plural → empty list per D4); the sequence walk's lifted-parallel arm now keys on the same hook (one source of truth); end-to-end static+runtime agreement test added. (2) walker skipped subtrees under unbuildable paths (`{{ x[0].f }}`) → now recurses inward. (3)+(7) `{% set %}` handling rebuilt as sequential body-walking: read-before-set is flagged, nested sets shadow correctly. (4) `Jinja2DetectVariablesError` leaked raw from construction → lint skips unparseable templates (parsing gates own syntax errors). (5) PipeSearch + PipeImgGen (prompt + negative_prompt) wired into the guard-lint. (6) `and` short-circuit recognized (`{% if var and var.attr %}` clean). (8) `variable_names` now carries `SlotTaint.origin_slot_name`, not prose. (10) duplicated taint bookkeeping factored into `scan_taint_triggers` + `is_plural_step_result` in `absence_taint.py`. **Deferred → `wip/optionals/deferred-step-d-review-tradeoffs.md`:** (9) cross-package liftable inventory scoping (decide at the Step E/F protocol bump), taint-analysis recompute/double-parse efficiency, `_build_full_path` duplication, inverted-guard shapes. |
| E | `2f55da612` (+ checkpoint commit) | 10 findings: 1 bug + 5 consistency/cleanup fixed, 4 deferred | Cold Sonnet review of `26738d440..HEAD`. **Fixed in the checkpoint commit:** (1) **lifted-plural producer registration — CONFIRMED regression, fixed:** the skipped early-return never registered the lifted pipe's output IOSpec, but a lifted PLURAL output writes a real empty-list Stuff (D4) that downstream pipes consume — its digest never entered either graph builder's producer map, silently dropping the DATA edge. Fix: `on_pipe_end_skipped` (protocol/tracer/manager/event/assembler) now carries `output_spec` + `output_concept_data` and registers them like a success output; the epilogue computes the spec on both arms; plural companion slots register via `register_controller_output`. Unit tests on both builders. (3) useless-`!` lint was unreachable from the human `pipelex validate` → `_echo_optionality_warnings` on the bundle arm + `--all` sweep. (4) builder `validate_ops` twins drifted → `warnings` added to the three bundle-context ops. (7) taint walk ran twice per validate → shared `collect_controller_taint_analyses` pass feeds both projections (`pipelex/pipeline/controller_taint.py`); both builders now take analyses. (8) `_is_optional_output_digest` duplicated → shared `output_digest_is_optional` in `graphspec.py`. (10) absence-artifact serializer divergence → `build_absence_json`/`build_absence_html` in `absence_render.py`, all three call sites routed through them. (9) missing docs → graph-tracing page (SKIPPED + skip_reason + optional edges) and agent-cli page (validate `warnings` shape vs init/doctor warnings shape). **Deferred → `wip/optionals/deferred-step-e-review-tradeoffs.md`:** (2) API-runner absent arm (cross-repo gated, already in hand-off notes), (5) builder runner_code codegen crashes on absent optional output (pre-existing, builder-layer pass), (6) `\|length` on singular still raises (D4 keeps length a list concept; candidate guard-lint extension), + reviewer's below-cut latent facade gap and pre-existing no-span-on-lifted-leaf. |
| F (final) | — | | |

## Decisions & deviations log

(fill at checkpoints — anything done differently from `wip/optionals-plan.md`, with why)

- **Step A — parser is combo-permissive; validators own the mutual-exclusion rule.** `parse_concept_with_multiplicity` accepts `X[]?` (multiplicity + marker) and returns both; the D1 v1 rule (markers mutually exclusive with multiplicity) is enforced at the blueprint/spec layer where the input-vs-output context is known, raising the typed `OPTIONAL_MARKER_INVALID`. Rejecting in the parser would have degraded those cases to untyped syntax errors.
- **Step A — `OPTIONAL_MARKER_INVALID` is a `PipeValidationErrorType` raised as `PipeValidationError` inside blueprint validators**, riding the existing pydantic-unwrap machinery (`extract_wrapped_pipe_validation_error`) exactly like `BATCH_ITEM_NAME_COLLISION`. No categorizer change needed.
- **Step A — the naive `[`-splitter was renamed** `strip_multiplicity_from_concept_ref_or_code` → `strip_markers_from_concept_ref_or_code` (it now strips presence markers too; the old name would have lied). Internal helper, single caller (`pipe_factory`).
- **Step A — runtime gates pinned to `declared_names`.** The three `pipe_abstract` enumeration sites + `pipe_extract.required_variables` now use `declared_names` (all inputs), so an input declared `?` is still enforced-present at run time in Step A — exact current behavior preserved; Step B flips the gates to the trichotomy.
- **Step A — derived batch-list needs stay presence-plain.** In `pipe_parallel`/`pipe_sequence`, the computed list-input specs that override `multiplicity=True` (batch `input_list_name` derivation) do not carry the inner spec's presence (a plural + marker combo is exactly what D4 forbids); all pass-through `add_stuff_spec` sites carry `presence=stuff_spec.presence`.
- **Step A — two stale fixtures updated**, since a trailing `!` is now legal grammar: `test_pipe_blueprint.py` invalid-syntax cases switched to genuinely malformed specs (`Invalid@Format`, `Invalid@Concept`).
- **Step B — the lift gate reads the pipe's OWN declared presence, not the needed-aggregation.** A controller's `needed_inputs()` carries its children's markers (a sequence over a step needing `X` plain aggregates `X` plain even when the sequence declares `X?`). Lifting on the aggregated marker would skip the whole controller instead of letting its steps lift. `_scan_input_presence` therefore prefers `self.inputs.root[name].presence` (the boundary contract, D5 "boundaries explicit") and falls back to the needed spec's marker. Consequence: whole-controller lifting still happens when the author declares the input plain at the controller boundary — which is the intended semantics.
- **Step B — the declared-vs-needed controller input check no longer compares presence markers.** With presence on `StuffSpec`, the old full-model `!=` would have rejected exactly the legal D3 shapes (controller `X?` over child-needed `X` plain) with an empty mismatch summary. The check now compares concept + multiplicity explicitly; presence relationships across the boundary are all legal per the trichotomy (Step D's taint pass reasons about them).
- **Step B — `validate_before_run` returns the presence scan** (`InputPresenceScan`: missing / forced-absent / liftable) instead of `None`; it owns all three raise cases (force → `OptionalValueAbsentError`, missing-no-record → `PipeRunInputsError` with the run-mode-correct label + optional-inputs hint) and skips resource checks + per-pipe `_validate_before_run` when the pipe is about to be lifted (pipe-specific validation would inspect inputs the pipe never consumes — e.g. PipeCondition's expression render would crash on the absent var).
- **Step B — value-supersedes-record invariant:** `set_stuff` pops the absence record under the written name; `set_new_main_stuff` also pops the positional `main_stuff` record. The D4 plural note is written AFTER the empty list so it survives as an observability note (stuff wins in `resolve_stuff`).
- **Step B — lifting applies in dry mode too** (uniform gate): records never exist in today's dry sweeps (mock seeding is all-present per D6), so this is unreachable until Step C's continue-under-dry work, which will pin the dry semantics.
- **Step B — sequence test builds pipes in code** (`get_pipe_library().add_new_pipe`) instead of an `.mthds` fixture: a bundle declaring `PipeFunc`s can't validate standalone (the functions aren't registered in the validation hook's process).
- **Step C — dry-run `continue` parity keys on "no mapped pipes", not "no main stuff".** The dry sweep runs every mapped outcome, so when `pipe_dependencies()` is non-empty the mapped pipes' stamped outputs stand (the continue arm is Step D's static concern). Only an all-special-outcomes condition records the declared-absent output in dry — including over a pre-existing main stuff, mirroring the live supersede rule.
- **Step C — batch compaction shipped (D4 bonus), ungated.** The tri-state collect loop drops any absent branch result — no check that the inner output is declared `?`. Rationale: a `ListContent` cannot hold a hole, so compaction is the only lossless runtime behavior; whether the author *may* rely on it is Step D's static story. The old xfail `test_pipe_condition_continue_output_type` ("identifies a bug") is un-xfailed and pins the fixed semantics: live = compacted list, dry = every mapped outcome modeled (one mock item per batch item).
- **Step C — parallel ledger note is re-keyed to the branch result name.** `_make_lifted_output` records the branch absence under the sub-pipe's `output_name` already; the combine defensively re-keys via `model_copy(update={"variable_name": ...})` if a record ever arrives under another name, so the parent ledger always speaks in result names.
- **Step C — delivery absence artifact shape:** `main_stuff.json` = `{"absent": true, ...AbsenceRecord dump...}` (explicit discriminator so consumers can tell an absence doc from a value dump), `main_stuff.md` = human-readable summary + provenance chain, `main_stuff.html` = escaped `<pre>` of the JSON. `main_stuff_viewer.html` is NOT produced for absences (nothing to view). Raw arm reads the serialized `absences` ledger; a malformed raw record degrades to the loud contract-violation error.
- **Step C — PipeCondition docs page was stale** (`pipe_map` / `default_pipe_code` — fields that don't exist in the language; it's `outcomes` / `default_outcome`, and `default_outcome` is required). Fixed while documenting the new `continue` semantics + migration idiom on that page.
- **Step C (checkpoint triage) — the ledger API now distinguishes NOTE from RESOLUTION.** `record_absence` = pure ledger note beside a possible value (D4 plural note); `record_resolved_absence` = the slot's resolution, removing any stale value/alias under the name (mirror of `set_stuff`'s value-supersedes-record). `record_new_main_absence` and the parallel branch arm go through the resolution form. Found by the Checkpoint C cold review (stale value outranked a fresh absence under a colliding name).
- **Step C (checkpoint triage) — non-required structured fields absorb absence as their DEFAULT, not hardcoded `None`.** Deliberate: an author-declared default on an optional field is per-field coalescing avant la lettre (phase-2 `??` preview). Docs + changelog say "field default (`None` unless the author declared another default)".
- **Step D — the taint pass is a separate walk, not a presence dimension inside `needed_inputs`.** The plan wording ("`needed_inputs`/`generated_outputs` gain the presence dimension") would have entangled taint state with the input-aggregation used by unrelated validation. Instead: `PipeSequence.analyze_taint()` / `PipeParallel.analyze_branch_taint()` re-use `needed_inputs()` per step and track presence in their own `slot_taints` map — same bookkeeping, isolated concern, and the liftable-pipe inventory reuses the identical analyses.
- **Step D — `OPTIONAL_OUTPUT_REQUIRED` fires at pipe construction** (`validate_output_static`, inside the pydantic model validator, wrapped like `OPTIONAL_MARKER_INVALID`), not at library validation: outcome map and output declaration are both local to the pipe, so the earliest gate wins.
- **Step D — guard-lint coverage extended beyond the plan's "prompts":** PipeLLM (prompt + system_prompt), PipeCompose (template mode), PipeCondition (expression), PipeSearch (prompt), PipeImgGen (prompt + negative_prompt) — every pipe rendering authored templates over its inputs. PipeCompose construct mode composes structured fields, not authored templates: not linted.
- **Step E — the agent-API run core's absent arm is GATED on the cross-repo mthds bump.** The mthds SDK wire models (`DictWorkingMemoryAbstract`: root + aliases, `extra="forbid"`) cannot carry the absences ledger, so `_run_core_api.py` keeps its contract-violation raise (message swept to the new invariant wording) with a comment marking the gate. Everything else on the §14 checklist landed.
- **Step E — `warnings` also ride the agent-CLI whole-bundle validate surfaces**, not just the protocol report: `validate_bundle_core` and `validate_pipe_in_bundle_core` attach `warnings` (dumped `exclude_none`, mirroring the error-items projection); `validate_pipe_core` (no bundle context) does not. Rationale: the lint's cross-flow aggregation needs the whole loaded bundle, which those surfaces have.
- **Step E — redundant-`!` aggregation is deliberately conservative.** A warning fires only when the (pipe, variable) has ≥1 redundant observation and 0 asserting observations across ALL analyzed flows; a `!` input on a pipe referenced by no controller yields no observation and no warning (no flow context to judge it in). This guarantees the lint never tells an author to remove a `!` some flow relies on.
- **Step E — otel INPUTS capture also learned the absence arm** (site not on the §14 checklist): `make_inputs_json` hard-read every needed input and crashed on a running controller whose `?` input was absent (found by the new end-to-end test). Value → as before; recorded absence → absence payload; neither → still the loud hard-miss raise.
- **Step E — pre-existing truthiness bug fixed (load-bearing for D7):** `StuffArtefact` had no `__bool__`, so Jinja2's truth test (`{% if var %}` — exactly the guard idiom the Step D lint recommends) fell through to the list-only `__len__` and raised `TypeError` on a PRESENT singular value. Found when the new `@?hint` contract fixture failed its dry run. Fix: present non-list artefact is truthy; list artefacts follow emptiness (`[]` falsy, D4). Changelog entry under Fixed.
- **Step E — the optional-edge marker is producer-side data:** the run epilogue stamps `extra={"optional": True}` on the output IOSpec when the pipe declares a `?` output; edge generation (tracer + assembler) reads the producer's registered output spec for the digest (shared `output_digest_is_optional` helper since the checkpoint triage). A skipped node with a SINGULAR output registers no output spec (a recorded absence has no payload), so no data edge leaves it; a skipped node with a PLURAL output registers its real empty-list output (see the triage entry below), so its data edges resolve like any produced value.
- **Step E (checkpoint triage) — skipped nodes register real outputs.** `on_pipe_end_skipped` (protocol, NoOp, tracer, manager, `PipeEndSkippedEvent`, assembler) gained optional `output_spec`/`output_concept_data`, registered in the producer map exactly like a success output. The run epilogue now computes the output spec on BOTH arms (a lifted plural output is a real empty-list Stuff per D4) and, on the lifted arm, also registers plural companion slots via `register_controller_output` (the companion-slot registration is exercised only under an `add_each_output` lift with tracing on — covered by the code path, no dedicated test; the main-output arm is unit-tested on both builders).
- **Step E (checkpoint triage) — one taint walk per validate pass.** `collect_controller_taint_analyses` (`pipelex/pipeline/controller_taint.py`) runs each controller's analysis once; `build_liftable_pipes` and `build_optionality_warnings` now take the analyses, not pipes. Every warnings call site (protocol report, agent-CLI cores, bare CLI, builder validate_ops) goes through the collector. Deeper memoization (recompute inside `validate_output_with_library` / enclosing walks) stays deferred (Step D item 2, carried into the Step E deferred doc).
- **Step E (checkpoint triage) — the lint now reaches all four validate surfaces.** Bare `pipelex validate` prints warnings in yellow (bundle arm + `--all` sweep; advisory, never the exit code); the three bundle-context builder `validate_ops` twins carry the same `warnings` key as their agent-CLI cores. Single-pipe surfaces stay lint-less on every CLI (no flow context).
- **Step E (checkpoint triage) — absence artifact renderers centralized.** `build_absence_json` (clean_json_dumps, indent=2) and `build_absence_html` (escaped `<pre>`) joined `absence_render.py`; the bare CLI (`--save-main-stuff`), the agent CLI envelope, and the delivery executor all render through them — no per-surface serializer drift.
- **Step D (checkpoint triage) — lifting learned companion slots.** `_make_lifted_output` (a Step B site) only resolved the pipe's own output slot; a lifted `add_each_output` parallel left its branch slots neither-value-nor-record — a hard miss downstream on flows the new static pass declares safe. New `PipeAbstract.lifted_companion_slots()` hook (overridden by PipeParallel) resolves each branch slot on lift (singular → SKIPPED record, plural → empty list), and the sequence taint walk keys its lifted-parallel arm on the same hook so static and runtime cannot diverge.
