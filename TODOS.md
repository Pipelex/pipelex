# Drift Hunt — campaign tracker

A campaign, not a feature: sweep every hand-written doc page for code↔doc drift, fix what's stale, and come out with per-section defect-density evidence. The drift-contracts system (`docs/contribute/drift-contracts.md`, design at `wip/drift-contracts-design.md`) enforces the floor *going forward*; this hunt backfills the floor once. Promoted from `wip/kick-off-drift-hunt.md` (now a pointer stub) — this file is the live tracker.

## Cold-start context (update at every checkpoint)

- **Status:** **✅ CAMPAIGN DONE — CHECKPOINT 2 CLOSE-OUT EXECUTED (2026-07-12), STOPPED for Louis' final review.** The record: `wip/drift-hunt/findings/SUMMARY.md` (close-out section: coverage disclosure, Stage-2 resolution table, ranked drift-contract shortlist for the Phase 3 verdict, lessons) + `inventory.md`; the handoff: `deferred-code-findings.md` (10 items). Full `make agent-test` GREEN on the reconciled branch. `wip/drift-hunt/` pruned (removed files at `git show drift-hunt-archive:wip/drift-hunt/<path>` — pushed tag pinning `9a6201a5e`). ⚠ Record correction: the observer-provider-injection.md "partially fixed" note below was a **stale-base artifact** — the page is fully fixed on dev (S2-3/#1042 fixed Part-2 F38–F42; re-verified against live code at close-out), no follow-up needed. Everything below this point is historical narrative. — Historical: **ALL FOUR STAGE-2 PRs MERGED TO DEV (2026-07-12)** — squash SHAs: **#1042 (S2-3) `9814e2f4c`, #1043 (S2-1) `126616a88`, #1044 (S2-2) `46511ce9c`, #1045 (S2-4) `cb13e5b33`**. dev tip = `cb13e5b33`. All four PR branches deleted, remote AND local (local tips verified ancestors of the merged `refs/pull/N/head` before force-delete — squash merges always fail `git branch -d`'s ancestry check, that refusal is expected, not a danger signal). Merges were forced sequential by branch protection (head must be up to date with dev): after each squash the next PR went BEHIND → `gh pr update-branch` + squash auto-merge + wait for CI, three times. The main checkout `../pipelex` is back on `dev` at the tip; this `_drift` worktree stays on `docs/Drift-hunt`. Pre-merge, PR #1045 was finalized: Louis's review edits + bot-triage fixes landed as PR-branch commit `1b776eca5` (configure-ai-providers.md local-overrides/init/telemetry.toml notes; `generate-mthds-schema` added to the kit-rules no-make fallback list — verified against `Makefile:882` `plxt-lint: env generate-mthds-schema-quiet`, regenerated via `make rules`; data-extraction.md explicit `storage_dir` for pre-boot `LocalObserver` — verified against `local_observer.py:19`; observer-provider-injection.md JSONL filenames corrected to the `LocalObserverEventType` values); all 8 bot threads (greptile/cubic/codex) confirmed-and-fixed or answered, replied + resolved; CI green on that head. ⚠ observer-provider-injection.md is now only PARTIALLY fixed out-of-batch (JSONL names done; the broken `from pipelex import Pipelex` import at line 209 and pre-rename hook prose remain — follow-up stands, smaller). **Campaign branch reconciled with dev** (merge `4094fc050`, Phase-H style): 8 files conflicted (the two `CLAUDE.md`/`AGENTS.md` generated pair, kit agent_rules ×2, data-extraction, python-classes, PipeSequence, configure-ai-providers), all resolved by taking dev's reviewed superset — residual diff vs dev is campaign artifacts only (TODOS.md + `wip/drift-hunt/` + the kick-off stub). Earlier same day: **S2-4 DONE (2026-07-12)** (branch `docs/Drift-hunt-s2-4`, off dev `15e0a94ea`; campaign-branch mirror `5e15bec08` + records commit). All 23 features+tail findings fixed, **0 rejected-at-fix-time**; all cross-cutting sweeps landed (PipeStructure roster ×3 incl. the landing page — count-free phrasing per the no-hardcoded-counts rule; `agent-cli.md` `--mock-inputs` carry-over; D13 heartbeat everywhere). **+F11 (features file) and F14–F22 (tail file) fix-time drift found & fixed (per D16, adversarially verified), 3 candidates refuted** (Linkup/pypdfium2 extract-list "omissions" — correctly SCOPED lists, the scope qualifier is part of the claim; the debugging-page marker expression — provably selects the identical test set). **Two lessons with teeth:** (1) repo-root `CLAUDE.md`/`AGENTS.md` are GENERATED from `pipelex/kit/agent_rules/` — a direct edit failed `make check-rules` mid-batch; the D13 fixes were applied at the source templates + `make rules` (AGENTS.md picked up the fixes for free). (2) The batch's own new `LocalObserver` snippet was written with a broken import (`from pipelex import Pipelex`; real: `from pipelex.pipelex import Pipelex`) — caught pre-commit by import-checking every new snippet (the S2-1 lesson applied). The workspace-root `CLAUDE.md:160` heartbeat claim was fixed in ITS OWN repo (single-file commit `cc2b68c` on `feature/Follow-ups`). Empirical re-verification throughout (`pipelex init config` re-run sandboxed; the `uv` commands run live; pytest `-k` semantics proven by collection). 1 new code-side deferral (`deferred-code-findings.md` item 10: shipped `plxt.toml` "built-in default" comment contradicts its own `array_auto_collapse` value). 1 out-of-batch record for Louis: **`docs/advanced/observer-provider-injection.md` (+ `advanced/index.md:18`) still carry pre-rename observer hooks, wrong JSONL filenames, and the broken `Pipelex` import** — survived Part 2's stub classification and S2-3; one-page follow-up. Gates green (docs-check strict + full `make check`). **Blind post-fix review (2 no-context Sonnet reviewers on `66b1d6ebd`): ZERO defects.** `docs/setup/telemetry.md` untouched as ordered. Resolution records: banners + end sections of `findings/features-reliability.md` and `findings/the-tail.md`. Burn: ~1.5M tokens (13 fix + 6 verify + 2 review agents). ⟶ **NEXT = CHECKPOINT 2 (campaign close-out — STOP for Louis): full `make agent-test` on final state, `findings/SUMMARY.md` close-out (incl. the cookbook-unswept D12 disclosure + contract-candidate shortlist), hand off `deferred-code-findings.md` (10 items), prune `wip/drift-hunt/`.** Earlier: **S2-2 DONE (2026-07-12), PR #1044 → dev OPEN** (branch `docs/Drift-hunt-s2-2`, off dev, docs-only, 10 pages; campaign-branch commits `8f03eb6f2` + `1824f2db4`). All 14 concepts + top-level findings fixed, **0 rejected-at-fix-time**; **+7 fix-time drift found & fixed (F55–F61, per D16, adversarially verified) + 1 stale section pointer, and 6 candidates refuted** — notably the multi-line inline TOML tables candidate (the shipped tomli 2.4.1 accepts TOML 1.1; testing against the ACTUAL loader, not a generic parser, decided it) and the domain "three properties" claim (`main_pipe` is bundle/crate metadata the runtime `Domain` drops). Every changed example re-extracted from the pages and re-validated (cross-bundle via `-L` scaffolds); the libraries.md `execute()` pattern and the F3 Document Extraction sequence executed live in dry-run. **Blind post-fix review (2 no-context Sonnet reviewers on `8f03eb6f2`): ZERO defects — first Stage-2 batch to survive clean.** 2 new code-side deferrals (`deferred-code-findings.md` items 8–9: `tomli>=2.3.0` floor predates the TOML 1.1 syntax the docs/kit rely on; `build structures` opaque-placeholder shadowing after a class migration). 1 out-of-batch defect recorded for Louis: **`pipes/pipe-operators/PipeLLM.md:126-135` inline `@document` survived S2-1** (adversarially confirmed; one-line fix, could ride PR #1043's branch). Convention proposal for Louis (D11-adjacent): elided sequence sketches are elision-by-design iff the snippet validates once placeholder sub-pipes are scaffolded. Also: the globally-installed `pipelex-agent` shim backing the marketplace validate-mthds hook is broken in this workspace (`ModuleNotFoundError: pipelex_tools`) — hit independently by multiple agents. The PR carries this page set's unshipped Stage-0 fixes (native-concepts triple-quote + adapt-to-llm-prompting-style), self-contained on dev. Gates green (docs-check strict on both branches; `make check` green — check-urls flaked once, passed on retry). Resolution record: banner + end sections of `findings/building-methods.md`. Burn: ~1.5M tokens (8 fix + 4 verify + 2 review agents). ⟶ **NEXT = S2-4 (the last part), ⛔ needs Louis' per-part greenlight (D18).** Earlier: **S2-1 DONE (2026-07-12), PR #1043 → dev OPEN** (branch `docs/Drift-hunt-s2-1`, off dev, docs-only, 17 pages; campaign-branch commits `7942339cc` + `dd6174ee6`). All 30 `pipes/` findings fixed, **0 rejected-at-fix-time**; **+10 fix-time drift found & fixed (F45–F54, per D16, adversarially verified) and 2 candidates refuted** (the PipeCondition fallback fragment — fragment standard; the PipeExtract Overview one-liner — the code's own taxonomy covers web pages under "document processing"). Every changed bundle example re-validated against the real parser incl. dry-run; the blind post-fix review caught **2 defects in the batch's own text** — the "run a bundle" Python snippets on TWO pages never ran (`bundle_uris` is only a dedup key; `execute()` needs `pipe_code` or `mthds_contents` — inherited from the original docs, the Stage-1 kwarg-rename finding masked the broken call pattern) and the F45 dedup claim missed its `bundle_uris` gate. Corrected snippets executed live in dry-run. 2 new code-side deferrals (`deferred-code-findings.md` items 6–7: CSV codec datetime inconsistency; concept-factory `refines` asymmetry). Gates green (docs-check strict on both branches + `make check`). Resolution record: banner + end sections of `findings/building-methods.md`. Burn: ~1.7M tokens (10 fix + 3 verify + 2 review agents). ⚠ S2-2 lesson: **a Stage-1 finding scoped to a kwarg can hide a non-functional surrounding example — validate the whole snippet, not just the finding's clause.** ⟶ **NEXT = S2-2 (recommended), ⛔ needs Louis' per-part greenlight (D18).** Earlier: **S2-3 DONE (2026-07-12), PR #1042 → dev OPEN** (branch `docs/Drift-hunt-s2-3`, off dev, docs-only). All 42 under-the-hood+advanced findings fixed, **0 rejected-at-fix-time**; +4 fix-time drift found & fixed (F43–F46, per D16, adversarially verified) and **2 fix-time candidates REFUTED — the campaign's first refutations** (fix-time finders over-flag; the D6 verify gate is what keeps D16 honest). 2 code-side items deferred (`deferred-code-findings.md` items 4–5), F24 fixed at its real site + class label corrected, `setup/telemetry.md` untouched. The blind post-fix review (2 no-context Sonnet reviewers on the commit SHA) caught 3 defects in the batch's own new text — including one **inherited from the Stage-1 finding's Reality text itself** (F36's "packaged kit configs" fallback; real fallback is global `~/.pipelex/`) — all folded in before the PR. ⚠ Lesson for S2-1/2/4: D6's "code is the authority" applies to the finding's *Reality* text too, and the blind post-fix review earns its keep — keep both. Gates green (docs-check strict on both branches + `make check`). Resolution record: banner + end section of `findings/under-the-hood-advanced.md`. Burn: ~2.4M tokens (14 fix + 2 verify + 2 review agents). ⟶ **NEXT = S2-1 (recommended), ⛔ needs Louis' per-part greenlight (D18).** Earlier: **🎉 STAGE 1 COMPLETE — PART 4 DONE (2026-07-12).** The tail (18 pages: get-started 3, contribute 3, tools non-CLI 2, setup 1, viewpoint 1, root index/license 2, D9 additions 6) swept via the wave Workflow (`wf_0c7af057-a6e`): 244 claims checked, **12 confirmed / 0 rejected**, **+1 more found by the post-run false-negative check** = **13 drift**, 0 reclassified. Severity: 2 breaks-a-user / 9 wrong / 2 misleading / 0 stale; **density 0.72 (9/18 dirty)**. Findings in `wip/drift-hunt/findings/the-tail.md`; `SUMMARY.md` now carries the closed Stage-1 scoreboard. No fixes applied (fixes = Stage 2). **STAGE 1 TOTAL: 104 pages, 1,439 claims, 109 drift, 3 convention, 0 rejected, density 1.05.** ⟶ **NEXT = CHECKPOINT 1 (STOP for Louis).** Earlier: Parts 1–3 recaps below; CHECKPOINT 0 CLOSED (D7–D10), Phase H DONE, Phase B DONE, Stage 0 DONE, gates green.
- **Part 4 recap + what it taught (read before Stage 2):** the tail is mostly **contributor / process / meta** pages, so its dominant claim type was one the campaign had never met — **repo tooling** (make targets, generated file trees, `mkdocs.yml`, `robots.txt`, the Python floor). Four new review rules were added on top of the reused Part-3 block: **H** make-targets-and-tooling (grep the Makefile *and read the recipe*), **I** process/policy prose is out of scope (zero findings on a code-of-conduct is the *correct* outcome), **J** version requirements (`requires-python = ">=3.11,<3.15"`; 3.10 dropped — fired zero times, `CONTRIBUTING.md` is correct), **K** docs-site meta-claims (vs `mkdocs.yml` / workflows / Makefile). **Three headline results:** (1) **the drift correlates with audience, not complexity** — every maintainer-facing page came back clean (drift-contracts, keyword-only, telemetry setup), every page explaining tooling *to someone else* drifted; (2) **docs edited in the same commit as their code do not drift** (proven: `d3550330a`, `15e0a94ea` each touched doc+code together) — direct evidence for the drift-contracts mechanism; (3) **the tooling class wants a CONTRACT, not a check** — every documented make target *exists*, so an existence script scores zero; what drifted is *behavior under a stable name* (`agent-test` grew a heartbeat 3.5 weeks after the page was written on the premise it was silent).
- **⚠ False-negative discipline — IT FINALLY CAUGHT SOMETHING (Part 4).** The rule (a *clean* result on a page seeded as high-risk is where the campaign most likely fools itself) held in Parts 1–3 every time. In Part 4 it **broke a clean verdict**: the language tutorial's four bundles all pass `pipelex validate bundle` *and* dry-run — and the page still teaches a **false rule** about the `@` vs `$` sigils (F13: it claims they differ by *data provenance*, input-vs-working-memory; they actually differ by *rendering shape*, block-on-its-own-line vs inline, and both resolve against the same `declared_inputs`). **Lesson with teeth for Stage 2 and for the contract design: a snippet validator verifies the snippet, never the sentence beside it.** With 109 confirmed and **0 ever rejected**, the pipeline's error mode is proven to be **false negatives, not false positives** — so keep the FN spot-check as a standing step.
- **⚠ Correction to the campaign's own record (fix at Stage 2):** Part 2's **F24 is right as written** (`pipelex-agent init --config` doesn't accept a `telemetry_mode` field) but its **class label — "fabricated `telemetry_mode` values" — is WRONG**. The values are real (`PostHogMode` StrEnum = `off`/`anonymous`/`identified`, `telemetry_config.py:27-32`) and `docs/setup/telemetry.md` documents them **correctly** (it FN-checked clean, decisively). A Stage-2 fixer trusting the class label could gut an accurate page. Fix F24 at `init-cli-flows.md:404`; leave `setup/telemetry.md` alone; correct the label in `under-the-hood-advanced.md`. **✅ DONE in S2-3 (all three parts honored).**
- **Part 2 recap (DONE 2026-07-12):** under-the-hood (19) + advanced (7) = 26 pages, 435 claims, 42 confirmed / 0 rejected / 0 reclassified (10 breaks / 23 wrong / 4 misleading / 5 stale; density 1.62, or 2.0 vs the 21 substantive pages — 5 `advanced/*-injection.md` are Under-Construction stubs). Findings in `wip/drift-hunt/findings/under-the-hood-advanced.md`. Dominant class: renamed Python API never propagated into docs.
- **Part 1 recap (DONE 2026-07-12):** building-methods (35 pages), 452 claims, 47 confirmed / 0 rejected → **3 reclassified as authoring convention (D11) → 44 drift** (25 breaks / 11 wrong / 6 misleading / 2 stale; density 1.26). Findings in `wip/drift-hunt/findings/building-methods.md`.
- **Next:** **NOTHING — campaign closed at Checkpoint 2 (2026-07-12).** Awaiting Louis' final review of the close-out. Campaign branch: do NOT push unless Louis asks (origin copy is stale at `2cb9c5957`, branch is ahead — expected). Post-campaign follow-ups live outside this tracker: the drift-contracts Phase 3 verdict consumes the shortlist in `findings/SUMMARY.md`; the 10 deferred code findings are ordinary follow-ups from `deferred-code-findings.md`.
- **Carry-overs into Stage 2:** ✅ ALL DONE in S2-4 — `docs/tools/cli/agent-cli.md:38` fixed; the `PipeStructure` roster fixed on all 3 pages (incl. a new PipeStructure section on `features/pipe-operators.md`). (The F24 class-label correction was ✅ done in S2-3.)
- **Reusable wave script (for Stage C, the last fan-out):** `/Users/lchoquel/.claude/projects/-Users-lchoquel-repos-Pipelex--drift/268d8cee-6e55-4669-9dff-33187cf92158/workflows/scripts/drift-hunt-tail-part4-wf_0c7af057-a6e.js` — start from **this** one (not Part 3's): it carries the full rule stack (Part-3's A–G + Part-4's H–L) and the D11 carve-out. Swap `PAGES` + seeds. For Stage C, add the D3 ruling (cookbook ground truth is `../pipelex-cookbook`, resolve references there) and **budget for the FN spot-check as a first-class step, not an afterthought**.
- **Phase H carved out (2026-07-12, Louis-approved):** the drift-tool hardening shipped separately as **PR #1041 → dev MERGED** (`15e0a94ea`, branch `fix/Drift-ack-hardening`). **dev merged into this branch 2026-07-12** (`19f991300`): the 4 drift-ack files conflicted (not the "content-identical, no conflict" originally predicted — #1041 landed as a *reviewed superset* of the branch's early `8c7413afc`, adding the post-verify recheck + staging-failure rollback), all resolved by taking dev's superset, so the drift area is now byte-identical to dev. Gates green (agent-check clean, `test_drift_ack_cmd.py` 18 passed incl. #1041's 2 new tests). Branch is now 0 behind dev.
- **Branch / worktree:** `docs/Drift-hunt` off `dev`, in the `_drift` worktree (treat as repo root). Do NOT push unless Louis asks. Commits so far: tracker `5e3699c64`, hardening `8c7413afc`, backfill `4093b1628`, then Stage 0 artifacts + fixes, then **dev merge `19f991300`** + a `ruff-format` normalization `86b569acf`, then **Stage 1 Part 1 findings** (see git log).
- **Working artifacts (post-prune):** `wip/drift-hunt/` — `findings/SUMMARY.md` (the campaign record + close-out), `inventory.md` (the denominator), `deferred-code-findings.md` (the handoff). Everything else pruned at Checkpoint 2; recover via `git show drift-hunt-archive:wip/drift-hunt/<path>` (pushed tag pinning `9a6201a5e`).
- **Decisions taken:** D1–D10 below, plus H-A2 implemented (narrow warn→fail escalation — see Phase H).
- **Key learning (D6 vindicated):** `pipelex validate --all` was initially "confirmed dead" from `--help` (no group-level `--all`) and a fix was drafted — live execution proved a shortcut layer forwards it to `validate pipe`; fix reverted. Never conclude a CLI form is dead from `--help` alone; run it.
- **Open questions:** none — all rulings taken (D1–D19), campaign closed. The only pending act is Louis' final review.

## Decisions

- **D1 — cocode is out of scope.** Not ready for this task (user call, 2026-07-05). The whole hunt runs on the pre-screen script + Claude agent fan-outs; do not reach for cocode even for sub-steps.
- **D2 — scope = hand-written pages under `docs/`.** Excluded as *generated*: `docs/errors/` (regenerated by `make gep`) and the generated gateway-models doc(s). Excluded as *freshly reviewed*: `docs/configuration/` and the CLI docs (`docs/tools/cli/` + `pipelex/cli/agent_cli/CLAUDE.md`) — they got a genuine review as the drift-contracts seed acks; re-sweeping them now would measure nothing. Everything else under `docs/` is in: building-methods, features, cookbook, under-the-hood, tools (non-CLI), advanced, reliability, get-started, contribute, setup, viewpoint, index/license.
- **D3 — cookbook ground truth is a sibling repo.** `docs/cookbook/` pages reference examples that live in `../pipelex-cookbook` (read-only ground truth), not in this tree. Reviewers and the pre-screen's path-existence check must resolve cookbook references there; never conclude "referenced example missing" from this tree alone.
- **D4 — fixes land in reviewable batches.** One commit per docs section at minimum; separate PRs per section if a batch grows beyond comfortable review size. Default base: `dev`.
- **D5 — no manifest growth during the hunt.** The drift-contracts dogfood freeze stays in force: the hunt produces *evidence* for which sections might earn a contract later; it does not add contracts to `drift.toml`.
- **D6 — findings must survive adversarial verification before anything is fixed.** No finding is acted on unless a second, independent agent confirms it against the code with `file:line` evidence. Fix agents re-verify at fix time; a finding that doesn't hold up gets recorded as rejected, not silently dropped.
- **D7 — cookbook cross-check is its own greenlight-gated stage (Louis, 2026-07-12).** Comparing `docs/cookbook/` pages against `../pipelex-cookbook`'s actual files runs as Stage C, separate from Stage 1 and ONLY on Louis' explicit greenlight — the cookbook may change soon. Cookbook pages are out of the Stage 1 fleet.
- **D8 — cross-repo references: minimal only; code defects get deferred, not fixed (Louis, 2026-07-12).** A short, named pointer to another repo's own docs is fine; workspace-relative paths a docs-site reader can't resolve, or restating another repo's content at length, is drift — inline the short essence here, leave the detail in its home repo. Code defects surfaced by the hunt are not fixed mid-campaign: record them in `wip/drift-hunt/deferred-code-findings.md`.
- **D9 — scope edges: all in (Louis, 2026-07-12).** The four pages D2 didn't name (`docs/agents/debugging-hanging-pytest-runs.md`, `docs/analytics/data-extraction.md`, `docs/distributed-execution/index.md`, `docs/CLAUDE.md`) AND the root-level `CONTRIBUTING.md` + `CODE_OF_CONDUCT.md` (the include-targets of the docs stubs) join Stage 1. Note: the two root files got no Stage 0 pre-screen (it scanned `docs/` only) — their review agents start without seed rows.
- **D11 — scaffolding omissions in teaching examples are authoring convention, not drift (Louis, 2026-07-12).** Omitting a top-level `domain`, a pipe `description`, or the `prompt` body in a focused example is NOT drift — a reader fills it in (with a placeholder) before running. **Still drift** (survives scaffolding): a written prompt that ignores its own declared input; a functionally-required input that IS the subject (e.g. PipeExtract's Image/Document); wrong field names / output types / enum values / defaults. Applies to ALL Stage-1 parts and Stage C — **seed every future review agent with this carve-out** alongside the FP do-not-flag list. Consequence for the derived-check candidate: `validate-doc-bundle-examples` must inject placeholder `description`/`domain` and tolerate an omitted prompt before running `pipelex validate bundle`, so it only flags defects that survive scaffolding.
- **D12 — Stage C (cookbook) is DEFERRED (Louis, 2026-07-12).** Not part of this campaign's remaining arc; supersedes D7's greenlight gate. The 21 cookbook pages stay unswept — say so plainly in the close-out (Stage 1 covered 104 of the 125 in-scope pages).
- **D13 — the `make agent-test` heartbeat claim gets fixed EVERYWHERE, in Stage 2 (Louis, 2026-07-12).** `agent-test` is no longer silent on success (start line + 20 s heartbeat + completion line — `Makefile:732-739`, `:49-65`, `:27`). Fix Part-4 F8's doc page **and** the two agent-facing instruction files that are its source: `CLAUDE.md:52` ("Zero output on success") and the **workspace-root `CLAUDE.md:160`** ("silent on success"). ⚠ **The workspace-root file is in a DIFFERENT GIT REPO** (`/Users/lchoquel/repos/Pipelex` is its own repo; this worktree is `pipelex`) — it cannot ride this branch. It needs its own commit over there, in the same S2-4 session.
- **D14 — Stage 2 = 4 parts, ONE PR PER PART to `dev`, one cold-start session each (Louis, 2026-07-12).** Independent, not stacked: the parts touch disjoint page sets, so they can land in any order. Each part resumes cold from its own findings file alone.
- **D15 — fixers MAY restructure a broken teaching example (Louis, 2026-07-12).** When a correct fix needs more than a one-line patch (e.g. a prompt that ignores its own declared input), the fixer may rewrite the example so it is correct and actually runs. **Bounded:** no new content, no editorial rewrites, and the example must **re-validate against the real parser** before the fix counts. This is what many of the ~18 building-methods bundle-example findings actually require.
- **D16 — drift spotted DURING a fix: fix in place, if it clears the same bar (Louis, 2026-07-12).** Fix agents re-verify against the code (D6), so they will find drift Stage 1 missed — F13 proves the sweep is not exhaustive. If the new drift is **on a page already in the batch** and clears the same evidence bar (`file:line` on both sides, adversarially verified), fix it in the same batch and **append it to the findings file** (the audit trail must stay complete). Drift on an out-of-scope page is **recorded, not fixed**.
- **D17 — Stage 2 stays DOCS-ONLY (Louis, 2026-07-12).** No derived checks, no new `drift.toml` contracts (D5 holds). The contract/check shortlist is handed to the drift-contracts Phase 3 verdict at Checkpoint 2. Keeps every Stage-2 PR pure-docs and the campaign's evidence honest — and Part 4 is the cautionary tale: a green snippet check reads as a clean page when it is not (F13).
- **D18 — ⛔ NO Stage-2 part starts without Louis' explicit greenlight (Louis, 2026-07-12).** Same gate as D10 governed Stage 1: per part, he says go. Do not begin fixing because the plan looks settled.
- **D19 — elided sequence sketches are authoring convention, not drift (Louis, 2026-07-12).** A sequence sketch whose step pipes are absent from the snippet is elision-by-design **iff the snippet validates once placeholder sub-pipes are scaffolded** (and no prose claims it is complete or runnable). Ruled during S2-2 (the native-concepts "In Complex Methods" refutation; same pattern at `working-memory.md:24-27`). D11-adjacent: seed every future review/fix/verify agent with this carve-out alongside D11's, so the pattern is ruled uniformly instead of re-litigated per page. Still drift: a sketch that fails even with placeholder sub-pipes scaffolded (wrong step syntax, bad concept refs, sequence-output mismatch).
- **D10 — Stage 1 runs as 4 parts, one cold-start session each (Louis, 2026-07-12).** Full fan-out, worst-first, but split so context is cleared between parts and each part resumes cold from this tracker: Part 1 building-methods (35) → Part 2 under-the-hood + advanced (26) → Part 3 features + reliability (25) → Part 4 the tail: get-started, contribute, tools non-CLI, setup, viewpoint, root index/license, the D9 additions (18). Per part: run the wave Workflow, commit `findings/`, update Cold-start context (findings count + token burn), report to Louis — he greenlights the next part (or stops) before a new session picks it up.

## What "drift" means here (and what it doesn't)

In scope per page: statements the code can contradict — commands, flags, config keys, defaults, file paths, API/class/function names, described behaviors and sequences, code snippets that should run or parse. Out of scope: editorial quality, tone, structure, SEO, completeness wishes. This is a drift hunt, not a rewrite; standing repo rules apply to fixes (docs = current reality, no hardcoded counts, MkDocs conventions).

## Checkpoint protocol (applies to every checkpoint below)

1. **Gates:** `make agent-check` (any code change), `make docs-check` (mkdocs strict — any doc change), full `make agent-test` at checkpoints.
2. **Commit** the phase's work (per D4 granularity for doc fixes). No push.
3. **Update the Cold-start context** section above (status, commit SHAs, decisions, open questions) so a fresh session can resume cold.
4. **Fan out `/code-review`** on the phase's changes: Sonnet-5 sub-agent(s), spawned with **no inherited context** — hand each one only a pointer to the changes (commit SHA or `git diff <base>..HEAD` range), never the plan, the rationale, or our own conclusions. Triage findings for clean-solid-not-over-engineered; apply what survives.
5. **STOP for Louis** where marked — Checkpoints 0, 1, and 2 are user-review gates.

## Deferred drift-contracts items (track here, do NOT do during the hunt)

- [ ] **Phase 3 — pilot verdict:** per-contract keep / narrow / mechanize / drop decision, driven by `wip/drift-contracts/dogfood-log.md`. Gated on the dogfood period verdict; the hunt feeds it evidence (Stage 1 logs observations; Checkpoint 2 hands over the contract-candidate shortlist). **Working doc drafted 2026-07-13: `wip/drift-contracts/phase-3-verdict.md` — awaiting Louis' rulings on its decision boxes.** (Original phase breakdown: the `docs/Update` branch is gone; the surviving source is `wip/drift-contracts-design.md` → Rollout plan.)
- [ ] **lint-drift promotion into lint-all / required CI check:** gated on the open-PR backlog landing (a required check would fail every open PR that predates its acks). Track only.
- [x] **PR #1040 hardening findings (A1/A2, `wip/drift-contracts/pr-1040-review-notes.md`):** resolved in Phase H below.

---

## Phase H — hardening pass (drift tool, from PR #1040 review notes)

- [x] **H-A1 — `drift ack` auto-stages the ack file it writes.** `git add .drift/acks/<id>.toml` right after `save_ack`, via a new `stage_file` git-adapter function (subject grant recorded BEFORE running checks). Makes the local gate honest: the ack lands in the same index `drift check` reads. Temp-repo test: ack path appears in `git diff --cached --name-only` after `drift ack`.
- [x] **H-A2 — DECISION: implement the narrow warn→fail escalation.** When a contract has `verify_commands` AND an unstaged-modified or untracked file matches its triggers, `drift ack` fails instead of warning — that is the one case where the verify guarantee is meaningfully weakened (verify ran on working-tree content the digest does not cover). Check runs BEFORE the verify commands (fail fast, don't burn a `make tb`). No-verify contracts keep the warn-only behavior. Tests: verify-contract + dirty trigger → `DriftAckError`, ack not written, verify not run; no-verify contract keeps warning + writing.
- [x] Update `docs/contribute/drift-contracts.md` + `.claude/skills/drift-review/SKILL.md` (ack now auto-stages; warn→fail rule) + `CHANGELOG.md` [Unreleased].
- [x] Gates: `make agent-check`, targeted tests (`tests/unit/pipelex/cli/dev/`), `make docs-check`.

## Phase B — dogfood-log backfill

- [x] Backfill the missing mandatory entry in `wip/drift-contracts/dogfood-log.md` for the `config-docs` ack recorded 2026-07-11 during the subject-grants regrind (ack rationale: Python-3.10 drop / StrEnum swap in `img_gen_job_components.py` via the Phase-M dev merge — clean-pass). Mark it honestly as a backfill.

### CHECKPOINT H — verify + review (no user stop)

- [x] Checkpoint protocol steps 1–4 (commit Phase H + B; `/code-review` fan-out on the hardening commit only — Phase B is a log entry). Apply surviving findings, then proceed.

---

## Stage 0 — inventory + mechanical pre-screen

Deterministic checks that need no judgment, to make the semantic pass sharper and cheaper. The seed-ack dogfood showed this class is rich (dead config section, phantom CLI flag, dead config field — all mechanically detectable).

- [x] Build the inventory: every in-scope `.md` page (per D2), grouped by section, committed as `wip/drift-hunt/inventory.md` with a page count per section — the denominator for defect density later. **Scope note for Checkpoint 0:** D2's list doesn't name `docs/agents/`, `docs/analytics/`, `docs/distributed-execution/`, nor root-level `changelog.md` / `contributing.md` / `CODE_OF_CONDUCT.md` — inventory them as in-scope-pending-confirmation (hand-written, not generated, not freshly reviewed) and let Louis rule at Checkpoint 0.
- [x] Write the pre-screen script at `wip/drift-hunt/prescreen.py` (throwaway campaign tooling — self-contained, stdlib + repo venv only). Checks, each producing `(page, claim, verdict, evidence)` rows:
    - Referenced repo file paths exist in the tree (special-case `docs/cookbook/` → resolve against `../pipelex-cookbook`, per D3).
    - Mentioned config keys exist in `pipelex/pipelex.toml` / the `configs.py` model tree.
    - Mentioned `pipelex` / `pipelex-dev` CLI commands and flags exist in the actual `--help` surfaces.
    - Fenced `toml` blocks parse; fenced `python` blocks compile (`ast.parse`).
    - Internal doc links resolve (complementing, not re-implementing, the mkdocs strict build).
- [x] Run it; triage the raw hits into `wip/drift-hunt/suspects.md` — per section: confirmed-mechanical defects vs needs-judgment flags vs false positives (note false-positive patterns; they tune Stage 1 prompts).
- [x] Fix the confirmed mechanical defects immediately (they need no Stage 1) — one commit, gate: `make docs-check` green.

### CHECKPOINT 0 — STOP (suspect list in hand)

- [x] Checkpoint protocol steps 1–4: full `make agent-test` green, Stage 0 outputs + fixes committed, Cold-start context updated, `/code-review` fan-out on the fix commit came back clean (all five fixes verified accurate; one adjacent staleness found in the same table — image_reference.py → shared/ — applied as a follow-up commit).
- [x] **Louis' triage rulings (2026-07-12):** judgment seeds resolved and FP patterns confirmed — recorded as D7/D8, in `suspects.md`, and in `deferred-code-findings.md`.
- [x] **Louis' scope + shape rulings (2026-07-12):** scope edges all in (D9); Stage 1 = full fan-out, worst-first, in 4 cold-start parts (D10). **Checkpoint 0 CLOSED — each Stage 1 part still needs its own explicit go.**

---

## Stage 1 — semantic review fan-out — ✅ **COMPLETE (all 4 parts, 2026-07-12)**

Four parts, one cold-start session each (D10), 104 pages total. Cookbook pages excluded (Stage C, per D7). **Final: 1,439 claims, 109 drift, 3 convention, 0 rejected, density 1.05.** Burn: Part 1 ≈ 4.5M + Part 2 ≈ 4.8M + Part 3 ≈ 2.7M + Part 4 ≈ 2.7M tokens.

- [x] **Part 1 — building-methods (35 pages). DONE 2026-07-12.** 47 confirmed / 0 rejected → **44 drift** after 3 convention reclassifications (D11). → `findings/building-methods.md`, scoreboard `findings/SUMMARY.md`. Dominant class: doc bundle examples that fail `pipelex validate bundle` even after scaffolding (~18). Strongest drift-contract candidate: `validate-doc-bundle-examples` (with placeholder injection, per D11).
- [x] **Part 2 — under-the-hood + advanced (26 pages). DONE 2026-07-12.** 42 confirmed / 0 rejected / 0 convention-reclassified → 42 drift (10 breaks / 23 wrong / 4 misleading / 5 stale; density 1.62, or 2.0 vs 21 substantive pages — 5 `advanced/*-injection.md` are Under-Construction stubs). → `findings/under-the-hood-advanced.md`. Dominant class: renamed Python API/method never propagated into docs (the #1 cross-part class now). Strongest new drift-contract candidates: `doc-python-snippets-import` (import+getattr on doc symbols), `doc-python-signatures-vs-live` (keyword-only drift), `doc-toml-config-vs-shipped`.
- [x] **Part 3 — features + reliability (25 pages). DONE 2026-07-12.** 10 confirmed / 0 rejected / 0 convention-reclassified → 10 drift (2 breaks / 7 wrong / 1 misleading / 0 stale; **density 0.40 — the cleanest section**, 8/25 dirty; all 4 reliability pages clean). → `findings/features-reliability.md`. Dominant class: stale CLOSED enumerations (a list written once, the registry grew, the list never revisited). Strongest new drift-contract candidate: `doc-enumerations-vs-registry`. Two new review rules (exhaustive-vs-illustrative lists; floor counts) — reuse in Part 4. Carry-over → `docs/tools/cli/agent-cli.md:38`.
- [x] **Part 4 — the tail (18 pages). DONE 2026-07-12.** 12 confirmed by the wave + 1 by the post-run FN check / 0 rejected / 0 reclassified → **13 drift** (2 breaks / 9 wrong / 2 misleading / 0 stale; density 0.72, 9/18 dirty). → `findings/the-tail.md`. Dominant class: **repo-tooling drift** (make targets, generated trees, `mkdocs.yml`, `robots.txt`) — and crucially **not** a phantom-target class: every documented target exists; what drifted is *behavior under a stable name*. Strongest new drift-contract candidate: **`make-targets-and-tooling` — a contract, NOT a derived check** (nothing mechanizable would have caught any of it). Two new rules added (H make-targets, K docs-site meta) plus two scope rules (I process prose out of scope, J version floors). **STAGE 1 COMPLETE ⟶ Checkpoint 1.**

Per part (the wave protocol) — **all four parts complete:**

- [x] Per page, a review agent: read the page, extract its checkable claims, verify each against the current code, report findings with severity (`breaks-a-user` / `wrong` / `misleading` / `stale-detail`) and `file:line` evidence for both the doc claim and the contradicting code. Seed each agent with the page's Stage 0 suspect rows and the Checkpoint-0-blessed do-not-flag list (`suspects.md` FP patterns).
- [x] Adversarial verify pass (D6): every finding independently re-derived by a verifier agent prompted to refute it; only confirmed findings survive. Run as a pipeline (page-review → verify per finding), not a barrier. **Result across Stage 1: 109 confirmed, 0 ever rejected.**
- [x] **FN spot-check (added in Part 3, promoted to a standing step in Part 4):** hand-verify the *clean* verdicts on the pages seeded as highest-risk — that is where the campaign is most likely to fool itself. It held in Parts 1–3 and **broke a verdict in Part 4** (F13). With 0 rejections across the whole of Stage 1, the pipeline's error mode is **false negatives, not false positives**. Keep this step in Stage C.
- [x] Aggregate into `wip/drift-hunt/findings/<section>.md` (findings ranked by severity), building toward `findings/SUMMARY.md`: per-section counts, defect density (findings / pages), rejected-findings list with reasons.
- [x] Log observations relevant to the drift-contracts dogfood — they feed the Phase 3 verdict, per D5. **Headline: drift correlates with *audience*, not complexity** (author-is-the-reader pages stay current; explain-it-to-someone-else pages drift), and **docs edited in the same commit as their code do not drift at all** — which is the drift-contracts thesis, observed in the wild.
- [x] End of part: commit the findings, update the Cold-start context, report to Louis.

### CHECKPOINT 1 — ✅ CLOSED (Louis, 2026-07-12)

- [x] Checkpoint protocol steps 1–4 (findings committed `32792618d`; full `make agent-test` green; review fan-out n/a — no code/tooling changed).
- [x] **Louis' rulings — recorded as D12–D17 below.** Stage C deferred; the `agent-test` heartbeat gets fixed everywhere (incl. both `CLAUDE.md`s) in Stage 2; Stage 2 = 4 parts, one PR each; fixers may restructure a broken example; mid-fix drift is fixed in place on the same page; Stage 2 stays docs-only.

---

## Stage C — cookbook cross-check — ⏸️ **DEFERRED (Louis, 2026-07-12 — D12)**

Not part of this campaign's remaining arc. The 21 `docs/cookbook/` pages stay **unswept**; note that in the close-out so the campaign's coverage claim stays honest (Stage 1 covered 104 of the 125 in-scope pages). If it is ever revived: compare each page against `../pipelex-cookbook` (read-only ground truth, D3), reuse the Part-4 wave script (it carries the full rule stack), and budget the FN spot-check as a first-class step.

---

## Stage 2 — fix in batches (shape settled by D14; **4 parts, one cold-start session each**)

Mirrors Stage 1's session-sharding: each part resumes cold from **its own findings file alone**, lands as **one PR to `dev`** (D14), and touches a page set disjoint from the other parts — so the four can land in any order without conflicts.

**Recommended order: S2-3 → S2-1 → S2-2 → S2-4.** S2-3 first because it is the highest volume but the *lowest effort per fix* (mostly one-line renamed-API substitutions) — it shakes out the fix loop cheaply. S2-1 second, while attention is freshest, because it is the hardest. S2-4 last because it absorbs every cross-cutting sweep.

- [x] **S2-1 — building-methods / `pipes/`** (30 findings, 17 pages). **DONE 2026-07-12 — PR #1043 MERGED to dev (squash `126616a88`)** (branch `docs/Drift-hunt-s2-1`, deleted; campaign-branch commits `7942339cc` + `dd6174ee6`). All 30 fixed, 0 rejected-at-fix-time; every changed bundle example re-validated against the real parser (incl. dry-run), broken examples restructured per D15 (PipeSequence, PipeBatch, PipeParallel combine, PipeCondition, multiplicity, pipes/index). +F45–F54 fixed per D16 (11 candidates → 10 confirmed, 2 refuted). Blind post-fix review caught 2 defects in the batch's own text (the non-running `bundle_uris` run-a-bundle snippets on two pages; the F45 dedup gate) — fixed, snippets executed live in dry-run. 2 code-side deferrals recorded (items 6–7). The PR carries this page set's previously-unshipped Stage-0 fix, self-contained on dev. Full fix-time record in `findings/building-methods.md`.
- [x] **S2-2 — building-methods / concepts + top-level** (14 findings, 9 pages). **DONE 2026-07-12 — PR #1044 MERGED to dev (squash `46511ce9c`)** (branch `docs/Drift-hunt-s2-2`, deleted; campaign-branch commits `8f03eb6f2` + `1824f2db4`). All 14 fixed, 0 rejected-at-fix-time; every changed example re-validated against the real parser (libraries.md `execute()` + F3 sequence live in dry-run). +F55–F61 fixed per D16 (12 candidates → 7 confirmed + 1 out-of-definition stale pointer, 6 refuted). Blind post-fix review: zero defects. 2 code-side deferrals (items 8–9); 1 out-of-batch defect recorded (PipeLLM.md inline `@document`, S2-1 page). The PR carries this page set's previously-unshipped Stage-0 fixes (incl. `adapt-to-llm-prompting-style-*.md`), self-contained on dev. Full fix-time record in `findings/building-methods.md`.
- [x] **S2-3 — under-the-hood + advanced** (42 findings, 26 pages). **DONE 2026-07-12 — PR #1042 MERGED to dev (squash `9814e2f4c`)** (branch `docs/Drift-hunt-s2-3`, deleted; campaign-branch commits `10b4bae0a` + `af7106342`). All 42 fixed, 0 rejected-at-fix-time; +F43–F46 fixed per D16 (2 more candidates refuted — first refutations of the campaign); F24 handled per the Cold-start note; 2 code-side deferrals recorded. The PR also carries these pages' previously-unshipped Stage-0 fixes, keeping it self-contained on dev. Full fix-time record in `findings/under-the-hood-advanced.md`. **Post-PR bot-review triage (2026-07-12):** greptile+cubic left 6 unresolved threads → deduped to 4 issues, all 4 confirmed against the code (2 incomplete config tables now presented-as-exhaustive, 1 table row contradicting the corrected `--graph-no-data` info block, 1 "copied once" vs full-reset wording), fixed in PR-branch commit `532725251`, mirrored here via cherry-pick; all 6 threads replied + resolved.
- [x] **S2-4 — features + reliability + the tail + ALL cross-cutting sweeps** (23 findings, 25 pages + the sweeps). **DONE 2026-07-12 — PR #1045 MERGED to dev (squash `cb13e5b33`)** (branch `docs/Drift-hunt-s2-4`, deleted; finalization commit `1b776eca5` = Louis's review edits + 8 bot threads triaged; campaign-branch mirror `5e15bec08`). All 23 fixed, 0 rejected-at-fix-time; all four cross-cutting items landed (PipeStructure roster ×3, `agent-cli.md:38`, D13 heartbeat everywhere — incl. the `pipelex/kit/agent_rules/` SOURCE templates + `make rules` regeneration of `CLAUDE.md`/`AGENTS.md` after `check-rules` caught the direct edit, and the workspace-root `CLAUDE.md` in its own repo, commit `cc2b68c` on `feature/Follow-ups`; `docs/setup/telemetry.md` untouched). +F11 + F14–F22 fixed per D16 (13 candidates → 10 confirmed, 3 refuted). Blind post-fix review: zero defects; one snippet-import defect in the batch's own text caught pre-commit. 1 code-side deferral (item 10); 1 out-of-batch record (observer-provider-injection.md + advanced/index.md, S2-3 pages). Full fix-time record split across `findings/features-reliability.md` and `findings/the-tail.md`.

Per batch:

- [ ] Fix highest severity first within the part. Every fix **re-verified against the code at fix time** (D6) — the finding is the pointer, **the code is the authority**. A finding that no longer reproduces is marked rejected-at-fix-time, not silently patched.
- [ ] Gates: `make docs-check` (mkdocs strict) + `make check`. ⚠ **`docs-check` does NOT cover `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md` or the `CLAUDE.md`s** — they are not mkdocs pages. Verify those by hand.
- [ ] Fixing docs does NOT reopen drift contracts (docs are review targets, not triggers). If a fix touches a trigger file, resolve via the `drift-review` skill.
- [ ] Mark resolved findings in the findings files as batches land (mark, don't delete — audit trail until close-out).
- [ ] Per-batch `/code-review` fan-out (checkpoint protocol step 4) before the PR is finalized.
- [ ] Open the PR to `dev` (D14), then report to Louis. Session ends; the next part runs cold.

### CHECKPOINT 2 — ✅ EXECUTED (2026-07-12) — STOPPED for Louis' final review

- [x] All batches landed (all four PRs merged to dev 2026-07-12, campaign branch reconciled, `make docs-check` + `make check` green on the reconciled branch) — **full `make agent-test` on this branch: GREEN** (and the run displayed the start-line + heartbeat lines the D13 fixes document).
- [x] Close-out written in `findings/SUMMARY.md`: coverage disclosure (**104 of 125 in-scope pages swept; the 21 cookbook pages were NEVER swept, D12**), Stage-2 resolution table (109/109 fixed, 0 rejected-at-fix-time; +31 fix-time findings + 1 stale pointer fixed, 13 refuted), the ranked drift-contract shortlist handed to the Phase 3 verdict (per D5/D17), and the campaign's lessons.
- [x] `wip/drift-hunt/deferred-code-findings.md` (10 items) handed off — kept in place, header updated. **The "partial out-of-batch observer page" turned out to be a stale-base artifact, NOT a real remainder:** verified at close-out, `docs/advanced/observer-provider-injection.md` (and `advanced/index.md`) is fully fixed and byte-identical to dev — Part 2's F38–F42 covered those exact defects and S2-3 (PR #1042, squash `9814e2f4c`) fixed them; S2-4's session recorded them as "remaining" because its base predated #1042's merge. Verified against live code: hooks `observer_protocol.py:17-31`, payload keys `pipe_router_protocol.py:18-47`, JSONL names `local_observer.py:11-14`, import + `Pipelex.make` kwargs + `hub.set_observer` (`hub.py:263`). No follow-up needed.
- [x] `wip/drift-hunt/` pruned: kept `findings/SUMMARY.md` + `inventory.md` (the record) + `deferred-code-findings.md` (the handoff); removed the four per-section findings files, `prescreen.py`, `prescreen-raw.md`, `suspects.md`, `checkpoint-0-decision-brief.md` — all recoverable at `git show drift-hunt-archive:wip/drift-hunt/<path>` (pushed tag pinning `9a6201a5e`).
- [x] Cold-start context updated to DONE.
- [ ] **STOP for Louis:** final review of the close-out; decide what happens to the campaign branch (do NOT push unless asked).

---

## Out of scope (explicit)

- cocode, in any role (D1).
- Growing `drift.toml` (D5 — evidence now, contracts later).
- Editorial/style/structure rewrites, new content, SEO.
- Generated docs (`docs/errors/`, gateway models) — fix the generator if one of those is wrong.
- `docs/configuration/` + CLI docs (just reviewed via seed acks) — unless a Stage 1 finding elsewhere points back into them.
- Other repos' docs (read `../pipelex-cookbook` as ground truth; don't fix it — hand defects off via SUMMARY.md).
