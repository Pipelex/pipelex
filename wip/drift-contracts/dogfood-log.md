# Drift-contracts dogfood log

One entry per ack. Verdicts: **real-catch** (the review found actual staleness), **clean-pass** (genuinely reviewed, nothing was stale), **friction** (the contract opened on changes that could not have affected the targets — candidate for narrowing or mechanizing).

- **2026-07-10 · keyword-only-convention · real-catch** — the guard gained the subject-grant registry (new violation kinds, literal-subject ban, symmetric staleness); the convention doc was materially stale and was rewritten as part of the same change — exactly the coupling this contract exists to enforce.
- **2026-07-10 · cli-docs · clean-pass** — the keyword-only sweep touched many pipelex/cli files (kwonly'd internal signatures, two escape hatches, call-site keywords); reviewed docs/tools/cli/ and the agent-CLI CLAUDE.md — no command, flag, or output changed, both targets stay accurate.
- **2026-07-11 · config-docs · clean-pass** — opened by dev's Python-3.10 drop reaching the branch via the Phase-M merge (StrEnum import swap in `img_gen_job_components.py`); reviewed docs/configuration/ — no config field, default, or documented behavior changed. *(Backfilled 2026-07-12: the ack was recorded during the subject-grants regrind without its mandatory log entry — process slip, not a review slip.)*
- **2026-07-16 · config-docs · clean-pass** — opened by the pipe_func config wiring in `configs.py` (PipeFuncConfig import + `pipe_func_config` field on the Pipelex model) plus comment-only wording fixes in `pipelex.toml` + `pipe_func_config.py` (dropped the stale core-`local_sandbox` mode claim, "our Daytona plugin" naming); reviewed docs/configuration/ — no default or documented behavior changed, pipe_func_config deliberately has no page, and the stale mode was never documented there. Note: the trigger correctly caught a *comment* fix in the shipped defaults file, which is user-readable surface — not friction. *(Amended 2026-07-17: the ack rationale originally described the trigger diff as comment-only, omitting the `configs.py` field wiring — caught by a PR-review bot on #1052; review conclusion unchanged.)*
- **2026-07-26 · config-docs · friction** — opened by the `pipelex.hub` → `pipelex.runtime_hub` split, which rewrote one import line in `config_check.py` and `aws_config.py`. Neither file's *configuration surface* moved at all — no model field, default, or TOML key — so no `docs/configuration/` page could have gone stale. Narrowing that would have prevented it: the trigger set matches whole files under the config packages, so any edit to them (including a pure import-path swap) opens the contract; scoping the trigger to the files that actually define settings (`configs.py`, `*_config.py`, `pipelex.toml`, the shipped `.pipelex/` defaults) and dropping incidental members like `config_check.py` / `aws_config.py` would keep the real coupling and drop this class of opening.
- **2026-07-26 · cli-docs · clean-pass** — the same split rewrote import lines across 28 `pipelex/cli/**` files plus an internal `PipelexHub` → `RuntimeHub` rename inside doctor's private setup helper. Reviewed `docs/tools/cli/` and `pipelex/cli/agent_cli/CLAUDE.md`: no command, flag, help text, output string, or exit code changed, and neither target mentions the hub. Logged as clean-pass rather than friction on purpose — a 28-file sweep across the CLI package is exactly the shape that *could* have moved the surface, so the contract opening was proportionate even though the verdict was "nothing stale". This is the second consecutive clean-pass for cli-docs on a mechanical sweep (cf. 2026-07-10); a third would be worth weighing against narrowing.
- **2026-07-26 · keyword-only-convention · friction** — opened by the hub split renaming two *docstring* mentions of `pipelex.hub` inside `keyword_only_guard.py`; no guard behavior, violation kind, registry schema, or flag changed, and the convention doc refers to the presentation layer only generically ("Typer/hub import"). Worth noting what the contract did *not* fire on: the same change re-recorded all 54 `pipelex/hub.py` grants under the two new modules — a 55-entry registry rewrite — because `subject_grants.toml` is not a trigger, and correctly so (a grant migration is data, not convention). Narrowing that would have prevented this opening: the trigger matches the whole guard module, so a comment-only edit opens it; restricting the digest to the guard's *code* (ignoring docstrings/comments) — or accepting that this module is small enough that prose edits are rare — is the tradeoff to weigh.
- **2026-07-26 · config-docs · clean-pass** — opened by H-4/H-3 landing: `configs.py` swapped its `TemplatingStyle` import from `pipelex.cogt.templating` to `pipelex.tools.templating` (the Phase 3 placement fix), one line, every other trigger file byte-identical. Reviewed `docs/configuration/` — no page documents an import path, every Python path the docs do cite still resolves, and the `pipe_func` example is untouched by the Phase 4 `concept_provider` injection. Logged clean-pass, not friction, on purpose: `configs.py` is the module that *defines* every setting, so an edit there is the shape that genuinely could move the documented surface. Worth recording against the narrowing proposed in the entry above — scoping the trigger to "files that actually define settings" would **not** have prevented this opening, because `configs.py` is squarely inside that narrowed set. File-level triggers cannot distinguish an import swap from a field change; only a content-aware digest could.
- **2026-07-26 · cli-docs · clean-pass** — opened by the Phase 4 renderer regroup: 10 CLI command-core files, each changed by exactly one import line (`core.pipes.inputs.input_renderer` / `core.pipes.output.output_renderer` → `core.pipes.rendering.*`). Reviewed both targets and verified the documented surface against the *live* CLI (`pipelex build inputs pipe`, `pipelex build output pipe`, `pipelex-agent inputs pipe`) rather than by reading alone: arguments, flags, format values and defaults all match `docs/tools/cli/`; `agent_cli/CLAUDE.md`'s layout map lists only `agent_cli`'s own modules, none of which moved. Also checked the behavior-adjacent half of Phase 4 — no CLI doc cites a Python import path or calls a factory that gained a required `concept_provider`, and the runner codegen emits none. **This is the third consecutive clean-pass for cli-docs on a mechanical sweep** (cf. 2026-07-10, 2026-07-26), which the previous entry said would be worth weighing. Weighed: the shape of these three openings is not "CLI files changed" but "a repo-wide refactor rewrote import lines", and that is now visible across *three different contracts* in four openings. The narrowing that would have prevented all of them is not a trigger-glob edit — it is a content-aware digest that ignores changes confined to import statements (and, for `keyword-only-convention`, to comments/docstrings). That is one mechanism, applicable manifest-wide, versus three separate glob surgeries; recommend it be evaluated as such before any per-contract narrowing.
- **2026-07-26 · config-docs · friction** — opened by the runtime/interpreter rename, which rewrote exactly one import line in `config_check.py` and `aws_config.py` (`pipelex.service_hub` → `pipelex.runtime_hub`). This is a **verbatim repeat** of the first `config-docs` friction entry above: same two files, same incidental-member shape, same pure import-path swap, same "no configuration surface could have moved" verdict. The narrowing proposed there (drop incidental members like `config_check.py` / `aws_config.py`) *would* have prevented this one — unlike the `configs.py` opening logged between them, which it would not have. Together the three entries bound the problem precisely: a glob narrowing fixes the incidental-member case only, and a content-aware digest fixes all of them.
- **2026-07-26 · cli-docs · clean-pass** — opened by the runtime/interpreter rename: 28 `pipelex/cli/**` files, each changed only by hub import lines. Reviewed both targets; neither `docs/tools/cli/` nor `pipelex/cli/agent_cli/CLAUDE.md` contains a single reference to any hub module, so the rename is structurally invisible to them. **Fourth consecutive clean-pass on a mechanical sweep.** One methodological note worth keeping: the review was cheap and *mechanical* this time — diffing all 28 triggers together and filtering out every changed line naming a hub left nothing, which proves import-only in one command rather than by reading 28 diffs. That check is precisely the predicate a content-aware digest would automate, which is further evidence for the recommendation in the entry above rather than a new argument.
- **2026-07-26 · keyword-only-convention · friction** — opened by the runtime/interpreter rename touching **the same two docstring lines** in `keyword_only_guard.py` that the hub split touched (the "no `rich`, `pipelex.<hub>`, or `typer`" stdlib-only note, and the presentation-layer pointer). Same contract, same two lines, same non-reason, second time in one refactor track — the strongest single piece of evidence in this log that file-level digests over prose are the wrong granularity. The convention doc needed no edit for the same reason as last time: it names the presentation layer only generically. Note the asymmetry this exposes: the change *did* re-key all 55 hub subject grants (paths, `ServiceHub.`/`MethodHub.` qualnames, and two `set_instance` param names), which is the registry migration the doc actually specifies — and `subject_grants.toml` is correctly not a trigger, so the contract fired on the prose it could ignore and stayed silent on the data it describes.
- **2026-07-26 · hub-layering-convention · clean-pass (initial)** — **new contract**, added on Louis' explicit say-so after being built, verified, and deliberately reverted at H-2. Triggers are the layering guard plus both hub modules; review target is `docs/contribute/hub-layering.md`. No verify command: `check-hub-layering` already gates `make check` and CI, so re-running it pre-ack would add the unstaged-file hard error without adding a guarantee. Initial review was mechanical rather than a read-through — extracted all 65 public module-level symbols from the two hubs and confirmed every one appears in the doc's partition tables, then imported `RUNTIME_LAYER_PACKAGES` from the guard and confirmed all 11 declared packages are named in Enforcement. Both checks are cheap and repeatable; whoever reviews this contract next should run them rather than re-read the tables. Deliberately **not** made a trigger: `tests/unit/pipelex/test_runtime_layer_import_closure.py`, even though the doc has a section on it — that stays within the scope Louis approved, and it is the obvious first candidate if this contract ever proves under-triggered. Watch item for the pilot verdict: both hub modules are type-annotated with most of the tree, so a *type move* elsewhere edits their import blocks and opens this contract — the same import-churn shape logged four times above. If that is what its first few openings look like, this contract is evidence for the content-aware digest, not against it.
- **2026-07-27 · hub-layering-convention · real-catch** — first opening after the initial baseline, and it **answers that entry's watch item in the negative**: this was not the import-churn shape. Both triggers changed semantically — the guard's declared runtime layer gained `pipelex.runtime_hub` (closing a gap where the module at the centre of the rule was exempt from it), and `runtime_hub.py` shed write-only class-registry state. The review found four stale claims in `docs/contribute/hub-layering.md`, none of which the contract's two prescribed mechanical checks can see: they verify *coverage* (every hub symbol appears, every declared package is named) and are blind to a covered claim being **false**. Three of the four were exactly that — a compliance claim that read as a claim about the property when the guard only checks direct imports; a "the one module not covered" sentence wrong on both halves; and a leaf-import rule whose stated criterion contradicts two of its three in-tree importers. That is the case *for* a prose review target with a human-or-agent reader, and the sharpest evidence in this log that the mechanical checks are a floor, not the review. Note also what this says about the content-aware-digest recommendation running through the entries above: it would have opened this contract too (the trigger diffs are code, not imports), so the recommendation costs nothing here — the two are not in tension.
- **2026-07-27 · hub-layering-convention · real-catch (second)** — opened by a *behavioral* guard change, the second consecutive non-import-churn opening for this contract: `_is_type_checking_test` matched `ast.Attribute(attr=...)` with an unconstrained receiver, so any `settings.TYPE_CHECKING:` block claimed the layer-rule exemption; the attributed form is now rooted at `typing`. The doc's carve-out bullet was under-specified rather than false, and needed the constraint stated. **The more interesting half is what the review caught that the triggers cannot see.** The same commit hardened both test modules the review target has sections on — the closure test gained a negative control and a subprocess timeout, the lifecycle test now pins the real `Pipelex.teardown` instead of a stand-in — and *neither test file is a trigger*, deliberately, per the initial baseline entry above. So the contract fired on the guard and the review then found staleness caused by changes it is structurally blind to. Two readings, and the honest one matters: this is **not** an argument for adding the test modules as triggers (the initial entry's scoping decision was Louis', and the spillover was caught anyway), but it *is* evidence that a review target's value is bounded by what a reviewer reads, not by what opened the contract. A narrower trigger set that had stayed closed here would have shipped two stale doc sentences.
- **2026-07-27 · cli-docs · clean-pass** — opened by a single trigger, `pipelex/cli/commands/plugins_cmd.py`, and this one is **not** the import-churn shape the four entries above complain about: the command's call actually changed, `build_registrar(config=…)` → `build_registrar(config=…, builtin_plugins=…, core_unconditional_plugin_names=…)`, because discovery stopped importing the built-in list so the runtime-layer plugins package could stay importable without the interpreter. A command module that changes how it builds what it prints is squarely the coupling this contract exists for, so the opening was proportionate — and a content-aware digest would rightly have opened it too. Reviewed against the *live* CLI: `pipelex plugins --help` still exposes only `list` with no options, and `plugins list` still prints the same five columns with all 19 built-ins in the same order and the same core-unconditional set as before the split, so nothing about the surface moved. Both targets carry zero references to the plugins command or to any Python import path. **Fifth consecutive clean-pass for cli-docs**, but the first on a genuinely behavior-adjacent trigger rather than a sweep — which cuts *against* narrowing this contract: the openings worth preventing are the sweeps, and this was not one.
- **2026-07-27 · hub-layering-convention · real-catch (third)** — third consecutive opening for this contract, third that is not import churn, and the first where **the change's whole purpose was to falsify the review target**. The guard gained a transitive rule (resolve the module-level import graph of `pipelex/`, flag any runtime-layer module that *reaches* `interpreter_hub`), which retires two things the doc said in prose: the paragraph conceding the guard "does not follow the import graph", and the Known-inversions block measuring four `pipelex.plugins` modules that breached through it. Both were accurate when written and became false the moment the remedy landed — so the contract worked here as **promise retirement**: a doc that honestly records a known defect *and* the planned fix accrues a debt that only a trigger can call in. Neither of the contract's two prescribed mechanical checks can see that class of staleness (they were re-run and both passed, unchanged: 33 + 32 hub symbols covered, all 12 declared packages named) — same finding as the previous two entries, now three for three. Worth noting for the pilot verdict: this opening also came with real spillover the triggers cannot see — the closure test gained an entry point and `PipelexPlugin`'s docstring gained the one-plugin-one-layer contract, neither file a trigger — and again it was caught only because the reviewer read past the diff.
- **2026-07-27 · hub-layering-convention · real-catch (fourth)** — opened by a code review hardening the transitive rule the previous entry logged, and it is the first opening where the staleness ran in **both** directions at once. Two doc claims were false because the *code* was weaker than the prose: the page promised rule 3 has "no escape hatch" while a `# hub-layering: ignore` on a direct import silently exempted the whole module from it, and it described a graph that models what an import loads while the graph skipped every ancestor `__init__.py` Python executes on the way. Neither was fixed by editing the doc — the code moved to meet it (the marker now removes the edge; ancestors are edges), and the doc gained only the sentences needed to state the new truth precisely. A third change went the other way: the rule now raises rather than reporting zero when it cannot find the hub module in the graph, which is a *new* guarantee the doc had no claim about, and got a paragraph. The review also caught two errors in prose the previous ack had signed off on — a registry attributed to the wrong hub, and an H2 inserted mid-section that reparented core's provider-injection convention under the plugins heading. **That is the finding worth recording for the pilot verdict.** Three consecutive acks on this contract have now been recorded by an agent, each with an honest, detailed rationale, and this one still found two errors inside the immediately preceding one. The rationale field is a genuine review record, but it is not self-checking: nothing in the drift machinery re-reads a rationale against the page it signs. The mechanical checks passed unchanged for the fourth time running (33 + 32 hub symbols covered, all 12 declared packages named) and were blind to all of it, as in the three entries above. Related process note: this ack's `reviewed_by` had come out as the git user (`make drift-ack` defaults to `get_git_user_name`), attributing an agent review to a human; corrected here and on `cli-docs` to match the other two acks in the registry. If the pilot keeps agents in the loop, `--by` should probably not default to the committer.
- **2026-07-27 · hub-layering-convention · real-catch (fifth)** — opened by a fix for a PR-review bot finding, and notable for *where in the pipeline the finding came from*. The four openings above were all driven by a human-or-agent review of the change itself; this one came from Greptile on the PR, after local gates, CI and a full `/code-review` pass had all gone green — a fourth defect of the same class (rule 3 green while the boundary is broken), this time a module-level `importlib.import_module("pipelex.interpreter_hub")` in an intermediary creating no graph edge. Two readings worth keeping. First, the contract behaved correctly as a *ratchet*: the fix touched a trigger, the contract opened, and the doc genuinely did need three edits, because the page states rule 3's carve-outs and its string-matching scope in prose that the fix falsified. Second, and more useful for the pilot verdict: **five consecutive real-catches for this contract, and none of them was the import-churn shape the earlier entries in this log complain about.** Whatever the case for a content-aware digest across `config-docs` / `cli-docs` / `keyword-only-convention`, it does not extend here — `hub_layering_guard.py` is a module whose every substantive edit changes what the review target claims, which is the shape a drift contract is *for*. If the pilot narrows contracts, this one should be left alone.
- **2026-07-27 · hub-layering-convention · real-catch (sixth)** — opened by M3 of the modularity track: the pipe-kind registration manifest moved out of `core/` into a new top-level `pipelex/pipe_machinery/`, which edits only the guard's `RUNTIME_LAYER_PACKAGES` *note* (the declaration tuple is unchanged). By the taxonomy running through this log that is the weakest opening shape yet — a comment-only trigger diff, the shape the `keyword-only-convention` entries above call friction — and it still turned up four stale places, three of them **outside** the paragraph the trigger diff touched: the "Where core splits" Pipe-machinery list and the Known-inversions line both still named `core.registry_models` as a module that imports the interpreter directly, and, more importantly, two *enumerations of the interpreter packages* went stale — the "`runtime_hub` must not name anything from …" list under "Why the boundary exists", and the `INTERPRETER` set inside the page's own verification snippet. That second one is the finding worth recording: the snippet is the doc's copy of a predicate that also exists in the closure test and in the design doc's classification snippet, and all three must gain a new interpreter package together or a check passes vacuously. A reader following the page's instructions with the stale set would have measured zero interpreter modules and believed it. So the verdict is real-catch, not friction, and the reason is a property of the *target*, not the trigger: this page embeds executable predicates, which go stale on any change to the set of interpreter packages regardless of how small the trigger diff was. The two prescribed mechanical checks passed unchanged for the fifth consecutive time (33 + 32 hub symbols covered, all 12 declared packages named) and were blind to all four, consistent with every entry since the baseline. **Sixth consecutive real-catch for this contract**; the recommendation in the entry above — leave this one alone if the pilot narrows contracts — now has a case where even a comment-only diff justified the opening.
- **2026-07-27 · cli-docs · clean-pass** — opened by M1a of the modularity track: 17 `pipelex/cli/**` triggers, every one changed by import rewrite alone as the MTHDS parser left `core/`. This is the sweep shape the entries above argue a content-aware digest should suppress, and the verdict is the sixth consecutive clean-pass for this contract. Reviewed against the *live* CLI rather than by reading: `pipelex-agent validate bundle <broken>.mthds --format json --error-format json` still emits the documented envelope unchanged. **One finding worth recording even though nothing was stale.** The change renames an error class, and `error_type` is the field clients branch on — so the natural worry is that a documented wire value moved. It did not, on this channel: the parser error is translated at `pipeline/validate_bundle.py:132`, so `error_type` is `ValidateBundleError` before and after, and the renamed `MthdsParserError` surfaces only in `error_source`, the internal stack-frame array both targets already describe as JSON-only. That is still a wire-visible rename on a documented field, and it is *not* the one the release sweep is tracking (four TypeScript consumers branch on the untranslated channel's value) — so the review's value here was establishing which channel breaks and which does not, a question neither target answers and neither trigger diff shows.
- **2026-07-27 · hub-layering-convention · real-catch (seventh)** — opened by M1a hoisting `core/interpreter/` + `core/bundles/` into `pipelex/mthds_parsing/`, again a note-only trigger diff (the declaration tuple is unchanged), and again — exactly as the sixth entry predicted — the *enumerations* were the staleness. Four of the page's five edits are enumeration sites: the must-not-name list, the verification snippet's `INTERPRETER` set, the "Where core splits" Pipe-machinery list, and Known-inversions. The prediction in the previous entry ("expect the same pair in M1a") held verbatim, which makes this the first opening on this contract that was *anticipated in the log and then confirmed* — evidence the failure mode is structural to the page, not incidental. **The new half is a claim retirement the trigger diff does not contain.** The transitive-rule paragraph listed `core.bundles.exceptions` among the homes "the predicate deliberately cannot name yet", because its validation-error data landed in every runtime closure via `pipeline/`. M1a fixed that straddler rather than inheriting it: `PipelexBundleBlueprintValidationErrorData` moved to `pipelex.core.exceptions` beside its two sibling error-data models, which let `INTERPRETER_CORE_EXCLUDED` be deleted outright and `mthds_parsing` be named in the predicate with no exclusion. So the doc had to *retire a conceded defect*, the same promise-retirement shape as the third entry above — and, as there, neither prescribed mechanical check can see it (both passed unchanged for the sixth consecutive time: 33 + 32 hub symbols covered, all 12 declared packages named). Methodological note for whoever holds this contract next: the matched triple was verified **by script** this time — extract `INTERPRETER_PACKAGES` from the closure test's dedented script string, `INTERPRETER` from this page's snippet, `I` from the design doc's, assert set equality — after the sixth entry found they had silently disagreed on `pipe_signature`. Do that rather than reading three lists side by side; it is ten lines and it is the only check that catches the failure this page keeps having.
- **2026-07-27 · cli-docs · clean-pass** — second opening from the same refactor track and the same cause as the entry above: 11 `pipelex/cli/**` triggers, one import line each, this time the renderers following `core.pipes.rendering` → `pipe_machinery.rendering`. **Seventh consecutive clean-pass**, and the sharpest data point yet for the content-aware-digest recommendation: the *same* mechanical sweep opened this contract twice in one afternoon, on two disjoint file sets, for two verdicts of "nothing stale". Reviewed against the live CLI for the affected command families rather than by reading (`pipelex build inputs --help`, `pipelex codegen inputs --help`), which is the only check that could have caught a real regression, and it cost more than the finding was worth — the honest cost/benefit for this contract on refactor sweeps, recorded as such.
- **2026-07-27 · hub-layering-convention · real-catch (eighth)** — the trigger diff this contract was designed for: `RUNTIME_LAYER_PACKAGES` itself changed, six `pipelex.core.*` entries collapsing to one `pipelex.core`. Five stale places, and the pattern from the previous two entries held for the third time running — **the enumerations are where this page rots**, and two of the five were not in the paragraph the trigger diff touches: "The layer rule" under Enforcement still listed all six core entries and closed by pointing at an explanation of why `core/` is listed package-by-package, and the must-not-name list still trailed "…or the Pipe-touching modules of `core.pipes`", now an empty set. **The half worth recording is what M1c did about it rather than what it found.** The gap this contract's own sixth entry flagged — that nothing binds the three copies of the interpreter-package set, which is why they had silently disagreed on `pipe_signature` — is now closed by two tests instead of a drift contract: they parse `INTERPRETER_PACKAGES` back out of the dedented script string and assert every token is a real package directory, and that the set equals this page's snippet. Both mutation-checked red. That is a better answer than adding the closure test as a trigger, because it converts a review obligation into a mechanical one, and it retires the standing worry that `DIRTY_ENTRY_POINT` only ever proved *some* token matched. Note the boundary of the fix: the design doc's third copy is not asserted, since `wip/` gets archived at track end, so that one stays a manual concern — a real limit, recorded rather than papered over. Eight consecutive real-catches; the recommendation to leave this contract alone if the pilot narrows contracts stands.
- **2026-07-28 · hub-layering-convention · real-catch (ninth)** — the M1 checkpoint review round, and the first opening in this track where **`runtime_hub.py` is itself in the trigger diff**. That matters because of what the *previous* ack missed. The rule is stated in two places — `docs/contribute/hub-layering.md` (the review target) and `runtime_hub.py`'s docstring (a trigger) — and the eighth entry above rewrote the doc copy while the source copy stayed stale, naming two packages M1a had deleted and omitting the four current ones. It stayed stale for a structural reason worth naming: **a trigger file is never a review target**, so the source-side twin of a documented rule carries no review obligation at all; it only gets read when some *other* change happens to touch it. Three of the four checkpoint reviewers found it independently. Second finding, and the sharper one: the previous ack's own edit to the guard note **introduced** a false claim — "measured: every `pipelex.core.*` module loads zero interpreter modules" — where the pre-M1 note had said the honest opposite. Measured: `core.pipes.pipe_output` loads `pipeline.pipeline_models`, `runtime_hub` also loads `pipe_run.pipe_run_mode`; the claim holds only under the closure predicate's deliberate carve-out, which line 228 of the review target states plainly two paragraphs away. So an ack rewriting a trigger to match its target wrote a claim the target already contradicted. This is the **second** time an ack's own edits contained an error caught by a later review (cf. the fourth entry, "nothing in the drift machinery re-reads a rationale against the page it signs") — and this time the error was not in the rationale but in the *code comment the ack edited*, which is worse, because the rationale is read once and the comment is read forever. The two prescribed mechanical checks passed unchanged for the seventh consecutive time and were blind to all of it. **Ninth consecutive real-catch**; the standing recommendation to leave this contract alone if the pilot narrows contracts stands, but the log now also supports a narrower proposal: when a review target's claim is mirrored in a trigger file's prose, the ack should be required to diff *both* copies, because only one of them is ever reviewed.
- **2026-07-28 · config-docs · friction (candidate)** — opened by M2 moving the seventeen built-in vendor adapters out of `pipelex/plugins/` into `pipelex/providers/`, which re-pathed exactly four import lines in `config_cogt.py`. No config field, default, validator, TOML key or section name changed; the four vendor config classes moved intact rather than being edited. The review target is `docs/configuration/**/*.md`, and grepping the whole tree for both module paths and all four config class names returns **zero hits** — those pages document TOML sections and keys and cite no Python import path anywhere, so an import-path move is invisible to them *by construction*, not by luck. That is the distinction between this entry and the clean-passes above: a clean-pass means the review could have found something and did not, this one means the trigger and the targets cannot interact. Second such opening on this contract from a pure refactor sweep (cf. 2026-07-11), and both were import-line-only. Concrete narrowing proposal, now that there are two data points: this contract's digest could ignore diffs confined to `import`/`from` statements, which would have suppressed both openings without weakening it — every real catch it has recorded involved a field, default or key.
- **2026-07-28 · hub-layering-convention · real-catch (tenth)** — M2, and the first opening whose trigger is a package being **added** to `RUNTIME_LAYER_PACKAGES` rather than removed or collapsed. Five stale places, and for the fourth consecutive time **the enumerations were the rot** and two of them sat well away from the paragraph the diff touched: the layer-rule enumeration under Enforcement listed seven packages where there are now eight, and the fixed worked example under Known inversions still pointed at `plugins/builtins.py`. The load-bearing edit was different in kind, though, and it is the one to record: "Where the built-in plugins split" asserted that `pipelex/plugins/` is where the runtime-port adapters live — a sentence M2 makes flatly false — and rewriting it forced the page to separate **two splits it had been conflating**. Mechanism-vs-adapters does not cross a layer at all (both packages are runtime-layer; the split exists to make the one-way dependency legible), while runtime-vs-interpreter does. A page about layering had been using one section to describe a non-layer boundary, which is exactly the confusion that makes a reader mis-place the next plugin. **The finding no check could see, and the reason it was found.** `_SUBSYSTEM_SECTIONS` in `error_pages_generator.py` keys on a module's *second dotted segment*, so moving the vendors from `plugins` to `providers` silently reclassified every vendor error page out of "Inference & providers" into the fallback "Platform & tooling" macro — a user-facing regression, wire-invisible, with `make gep` exiting 0 and producing a plausible diff either way. This is the **identical shape** to the `mthds_parsing` regression M1's review round found three weeks ago, and it was caught this time only because M1's recorded lesson was to *read* the generated diff instead of trusting the exit code. A lesson written down and then paying off on the next occurrence of its own failure mode is the strongest evidence in this log that the review obligation is worth its cost. The three prescribed mechanical checks (8 declared packages named under Enforcement, 27 + 22 hub accessors covered, matched triple equal by script) passed unchanged and were blind to all six findings — eighth consecutive time. **Tenth consecutive real-catch.**
- **2026-07-28 · cli-docs · real-catch** — first opening for this contract that was *not* a mechanical sweep, and the first that caught real staleness. Triggers were two `build output` help strings: the group help in `build/app.py` advertised "(JSON, Python, or TypeScript)" while `--format` accepts `json|python|schema` (passing `typescript` errors out), and `method_cmd.py`'s `--format` help lacked the TypeScript/Zod parenthetical its `pipe`/`bundle` siblings carry. Reviewing `docs/tools/cli/` against them found `docs/tools/cli/build/index.md` describing the command as "Generate example output JSON for a pipe" — written before `python` and `schema` existed, and stale in the same direction as the help string that opened the contract. `build/output.md` was already accurate (all three subcommands list the three formats; TypeScript is consistently framed as a downstream use of `--format schema`), and `agent_cli/CLAUDE.md` has no `build` command at all, so it was structurally out of scope. Both stale strings fixed before the ack. Worth recording against the four consecutive clean-passes above: this contract's value shows up precisely when a trigger diff is a *user-visible string* rather than an import line — which is the same distinction the content-aware-digest recommendation draws, and further evidence it would preserve the openings that matter while dropping the ones that don't. Note the finding arrived from outside the contract (a developer reported the `typescript` help lie); the contract's contribution was pulling the neighbouring doc table into the same fix instead of leaving it to rot another cycle.
- **2026-07-29 · keyword-only-convention · real-catch (eleventh)** — FU-3, and the first opening on this contract where the review target's **prose was the specification the code was changed to satisfy**, not the record that had to catch up. Line 37 had asserted since the registry landed that "the registry is machine-written and sorted by key"; nothing checked it, and the modularity track broke it three times, each found by hand well after the fact. The guard gains an `unsorted-grant` kind and the doc's claim stops being a claim. Five edits, and for the fifth consecutive entry across this log's two active contracts **the enumerations were where the page had to change and three of the five sat away from the paragraph the work is about**: the violation-kinds table under Enforcement, the hook bullet's parenthetical listing which violations are full-scan-only, and the auto-fix paragraph's list of what is never rewritten (which was already carrying only `grant-param-mismatch` and silently omitting `dead-grant`, a pre-existing gap this pass closed as a side effect). The fifth is the one worth naming: the page opens with "A signature is a violation when any of these holds", and registry order is **not** a property of any signature, so adding a sixth bullet there would have been wrong — the honest edit was a pointer line saying the registry carries one rule that is not about a signature at all. A doc whose framing sentence silently excludes a rule it later documents is a shape no mechanical check can see, and it only surfaced because the rule being added did not fit the list. **Eleventh consecutive real-catch on this contract** — but the recorded verdict is narrower than the streak suggests: the doc edits here were going to happen with or without the contract, since the whole item was "enforce what the docs claim". What the contract bought was the *sweep* — the three enumerations, which the implementing change had no reason to visit.
- **2026-07-29 · cli-docs · clean-pass** — the first entry in this log for an opening caused by a **merge** rather than by a change, and it exposes a property of the ack model nothing above has hit. Merging `dev` into `refactor/Hub-2` conflicted on `.drift/acks/cli-docs.toml` itself: both branches had acked the same contract, the file holds exactly one ack, and the two sides carried different digests *and* different trigger blobs. Resolving to either side leaves the digest wrong, because the merged trigger content is a third thing — so the contract necessarily reopens on the union of both branches' changes, and the whole modularity sweep (26 `pipelex/cli/**` files) re-presents as unreviewed despite three prior acks on this branch having covered it. **The ack registry has no merge semantics**: an ack is a claim about one linear history, and a merge invalidates both parents' claims by construction. That is not a bug — reopening is the conservative answer — but it means any long-lived branch pays the full review cost of a contract again at every merge, which belongs in the pilot verdict next to the content-aware-digest recommendation the entries above build. Note the two interact: a digest that ignored import-only diffs would have made this reopening nearly free, since 19 of the 26 triggers changed by import line alone. The review itself was cheap and mechanical, per the method recorded above — filtering import lines out of the combined diff left three real changes, all previously reviewed (the `PipelexInterpreter`→`MthdsParser` and `PipelexInterpreterError`→`MthdsParserError` renames, and `build_registrar`'s new arguments) — then both sides were checked live: dev's corrected `build output` help now agrees with the `docs/tools/cli/build/index.md` line dev fixed with it, and the agent-CLI error envelope still reports `error_type: ValidateBundleError` with `MthdsParserError` confined to `error_source`, as the target describes. One durable finding worth keeping beyond the merge question: `agent_cli/CLAUDE.md` survives an error-class rename untouched **because it names the `AGENT_ERROR_HINTS` dict instead of copying its keys** — the exact opposite of the enumeration-copying that the `hub-layering-convention` entries above show rotting on every single opening. Two targets, two contracts, same log: the page that points at its source is the one that never goes stale.
- **2026-07-29 · cli-docs · friction** — the third import-line-only opening on this contract and the second one in two days, so the narrowing proposal recorded above is now backed by more data than the openings it would suppress. Trigger was a single file, `validate/bundle_cmd.py`, and the entire diff is two `from` lines: `generate_graph_for_bundle` / `generate_view_for_bundle` moved to `pipelex.pipeline.bundle_graph_rendering` when `graph_rendering.py` was split along the hub layer boundary. No option, default, enum member, output shape, or error class moved. Both targets were opened and grepped for every moved symbol and both module paths — zero hits, and structurally so: neither `docs/tools/cli/` nor `agent_cli/CLAUDE.md` cites a Python import path anywhere, which is the same *by construction* argument the `config-docs` friction entry makes. Verified live anyway (`--graph-format` still `mermaidflow|reactflow|both`, default `reactflow`, matching both targets verbatim). Recorded as **friction** rather than clean-pass on the distinction this log has already drawn: a clean-pass means the review could have found something and did not; here the trigger and the targets cannot interact. **Narrowing that would have prevented it:** the content-aware digest proposed on 2026-07-28 — ignore diffs confined to `import` / `from` statements. That single change would have suppressed this opening, the `config-docs` one, and made the merge-induced reopening nearly free, while preserving every real-catch this contract has recorded (each involved a user-visible string, never an import).
- **2026-07-29 · hub-layering-convention · real-catch (eleventh)** — the first opening whose trigger is packages being added to `RUNTIME_LAYER_PACKAGES` *for the sake of what the omission was hiding*, and it turns the contract's own recurring failure mode into the finding. For the **fifth consecutive opening** the enumeration under Enforcement was stale — eight packages listed where there are now twelve — and once again it sat in a section the trigger diff never touched. But the substantive edit is the one to record, because it generalizes the page's existing worked example rather than repeating it: `graph/graph_rendering.py` had been reaching `interpreter_hub` through `pipeline.dry_run_pipeline` with every gate green, for the reason the guard's own note states and the page had not drawn a lesson from — rules 1 and 3 both filter candidates through `is_runtime_layer`, so an **undeclared** package is excluded from the rules' domain rather than reported by it. The `pipelex.plugins` breach the transitive rule was built for was a declaration that overreached; this one is a package with no declaration at all, which is strictly harder to see and which no gate in the repo could have surfaced. The page now carries it under Known inversions with the generalized rule — *an undeclared package is not neutral, it is unpoliced* — and the same conclusion under The rule, as the reason to declare a measured-clean package rather than leave it alone. The two prescribed mechanical checks (twelve declared packages named under Enforcement, 32 + 31 hub accessors covered) passed unchanged and were blind to the enumeration rot, **ninth consecutive time**. Method note worth keeping: the negative control was run rather than reasoned about — re-adding the evicted import makes the guard fail with `interpreter-hub-transitive`, where before the declaration it produced no finding at all — which is what turns "the declaration matters" from an argument into a measurement. **Eleventh consecutive real-catch.**
- **2026-07-29 · cli-docs · friction** — fourth consecutive import-line-only opening, and the clearest one yet: two of the three triggers change a single `from` line each (`PipeRunMode` moving to `pipelex.system.pipe_run_mode`), and the third changes a *comment* — the guard's declaration rationale, which no CLI doc has ever described or could. Targets grepped for all four moved symbols and their old module paths: zero hits. Verified the affected surface live anyway (`run pipe --help` still lists `--dry-run` / `--mock-inputs` exactly as `agent-cli.md` lines 37-38 describe). The narrowing case is now overdetermined — the content-aware digest proposed on 2026-07-28 would have suppressed this, the two before it, and made the merge-induced reopening nearly free, while preserving every real-catch this contract has recorded (all of which touched a user-visible string). One further refinement this opening suggests: `pipelex/cli/dev_cli/**` arguably should not be a trigger for this contract at all — the dev CLI is not distributed with the package and neither review target covers it, so it can only ever produce friction here. It already has its own contract in `hub-layering-convention`, which is where its changes were genuinely reviewed.
- **2026-07-29 · hub-layering-convention · real-catch (twelfth)** — the opening that closes the thing this whole log has been watching rot. The caveat that two interpreter-named packages could not be named in the layer predicate was written into **six** places, and the leaf models that made it true moved this phase, so all six went false at once. Four were on the review target and all four were edited: the must-not-name list, the deliberately-absent paragraph, the `core.pipes.pipe_output` parenthetical under Where core splits, and "what the predicate still cannot name" under The property. **Sixth consecutive opening where an enumeration was the rot** — but this one carries the counter-example, and it is the most useful finding in the entry. The page's runnable `INTERPRETER = {…}` snippet is the one enumeration that *cannot* go stale, because `test_the_interpreter_package_set_matches_the_documented_one` compares it against `INTERPRETER_PACKAGES` by machine: it would have gone red on this change whether or not anyone reviewed the page. Every other enumeration on the page is prose, and every one of them has now rotted at least twice. That is a concrete, generalizable remedy for this contract's recurring failure — a doc enumeration that duplicates a code constant should either point at it (the `AGENT_ERROR_HINTS` pattern the 2026-07-29 `cli-docs` entry praised) or be machine-compared to it; prose duplication is the failure mode, not prose itself. The two prescribed mechanical checks passed and were blind to all six sites, **tenth consecutive time**, which is by now less a caveat than a measurement: they check *presence*, and every finding this contract produces is about *content*. Outcome verified rather than asserted — measurement across all nine runtime entry points returns zero `pipeline` and zero `pipe_run` modules, and the closure test is green with both names added. **Twelfth consecutive real-catch.**
- **2026-07-29 · hub-layering-convention · real-catch (thirteenth)** — third opening on this contract in one day, and the one that found a live counter-example to the page's own central claim rather than a stale sentence. Two of the three findings are the usual shape, now thoroughly established: the Enforcement enumeration was stale *again* (sixth consecutive opening, same enumeration as the previous two), and the exclusion sentence — "kit is data files and language has interpreter edges, so both stay out" — read exhaustive while accounting for two of the tree's twenty-seven top-level packages. Enumerating all of them and running the measurement over each turned up `test_extras`: clean, shipped, imported at boot, and undeclared. **The third finding is a new shape and the reason to read this entry.** Five built-in vendor adapters imported `MissingDependencyError` from `pipelex/exceptions.py` — the *public all-errors aggregate*, which re-exports every interpreter package's exceptions — instead of from its definition site. Each therefore loaded 15 interpreter modules, inside `pipelex.providers`, a package the guard's own tuple already declared clean. Both gates were blind by construction: the guard's rules only chase `interpreter_hub` and an aggregate of exception classes never reaches it, while the closure test's `providers.builtins` entry point misses these modules because the vendor plugins import them *inside a function* — a deferred import hides a module from the static graph and from the aggregator's runtime closure simultaneously. So the failure needed two independent blind spots to survive, and it had them. Fixed at the import site (the repo's existing "direct full-path imports everywhere" rule already forbade it), with one new entry point and a negative control. **The generalization worth carrying forward: a module that re-exports across layers is a layer boundary with the sign filed off.** `pipelex/exceptions.py` is a deliberate public convenience, so the answer is not to delete it — it is that runtime-layer code must import from definition sites, and that a lazily-imported module is invisible to every static gate in this repo. Two prescribed mechanical checks passed and were blind to all three findings, **eleventh consecutive time**; the counting is no longer interesting, the pattern is — they verify *presence*, and every finding this contract has ever produced concerns *content*. **Thirteenth consecutive real-catch.**
- **2026-07-30 · hub-layering-convention · real-catch (fourteenth)** — opened by adding `pipelex.runtime_boot` to `RUNTIME_LAYER_PACKAGES`, the guard's tuple, when `pipelex.py` was split into a runtime-layer composition root and an interpreter-layer boot. The catch is the same enumeration for the third consecutive round: the prescribed mechanical check ("every declared package is named under 'The rule'") **failed** on `pipelex.runtime_boot`, so the page's own definition of the runtime layer would have shipped one entry short of the tuple it claims to describe. Seven consecutive rounds now where an enumeration on this page was the rot, and three in a row where it was *this* enumeration. Two observations worth carrying forward. First, the enumeration is the highest-value target on the page and the cheapest to check — the check is four lines of shell against the imported tuple, it has caught something every time it has been run, and it is still not automated; a test asserting the doc names every declared package would convert this recurring real-catch into a gate and retire the manual step. That is now the clearest candidate the pilot has produced. Second, the other prescribed check (hub accessor coverage) passed and was again blind to the finding — it has now been blind on every round it has been run, which is a signal about the check rather than about the page. Beyond the enumeration, six prose sites were genuinely stale because they named `Pipelex.setup` / `Pipelex.teardown` as doing things that now belong to the runtime half, and one — "a process that only ever builds a `RuntimeHub` (the doctor path, most unit tests)" — went from describing a degradation nobody could reach to describing a first-class, tested entry point. The contract opening was proportionate: the trigger was a one-line tuple edit, and the review turned up eight edits.
