# Drift-contracts dogfood log

One entry per ack. Verdicts: **real-catch** (the review found actual staleness), **clean-pass** (genuinely reviewed, nothing was stale), **friction** (the contract opened on changes that could not have affected the targets — candidate for narrowing or mechanizing).

- **2026-07-10 · keyword-only-convention · real-catch** — the guard gained the subject-grant registry (new violation kinds, literal-subject ban, symmetric staleness); the convention doc was materially stale and was rewritten as part of the same change — exactly the coupling this contract exists to enforce.
- **2026-07-10 · cli-docs · clean-pass** — the keyword-only sweep touched many pipelex/cli files (kwonly'd internal signatures, two escape hatches, call-site keywords); reviewed docs/tools/cli/ and the agent-CLI CLAUDE.md — no command, flag, or output changed, both targets stay accurate.
- **2026-07-11 · config-docs · clean-pass** — opened by dev's Python-3.10 drop reaching the branch via the Phase-M merge (StrEnum import swap in `img_gen_job_components.py`); reviewed docs/configuration/ — no config field, default, or documented behavior changed. *(Backfilled 2026-07-12: the ack was recorded during the subject-grants regrind without its mandatory log entry — process slip, not a review slip.)*
- **2026-07-16 · config-docs · clean-pass** — opened by the pipe_func config wiring in `configs.py` (PipeFuncConfig import + `pipe_func_config` field on the Pipelex model) plus comment-only wording fixes in `pipelex.toml` + `pipe_func_config.py` (dropped the stale core-`local_sandbox` mode claim, "our Daytona plugin" naming); reviewed docs/configuration/ — no default or documented behavior changed, pipe_func_config deliberately has no page, and the stale mode was never documented there. Note: the trigger correctly caught a *comment* fix in the shipped defaults file, which is user-readable surface — not friction. *(Amended 2026-07-17: the ack rationale originally described the trigger diff as comment-only, omitting the `configs.py` field wiring — caught by a PR-review bot on #1052; review conclusion unchanged.)*
- **2026-07-26 · config-docs · friction** — opened by the `pipelex.hub` → `pipelex.service_hub` split, which rewrote one import line in `config_check.py` and `aws_config.py`. Neither file's *configuration surface* moved at all — no model field, default, or TOML key — so no `docs/configuration/` page could have gone stale. Narrowing that would have prevented it: the trigger set matches whole files under the config packages, so any edit to them (including a pure import-path swap) opens the contract; scoping the trigger to the files that actually define settings (`configs.py`, `*_config.py`, `pipelex.toml`, the shipped `.pipelex/` defaults) and dropping incidental members like `config_check.py` / `aws_config.py` would keep the real coupling and drop this class of opening.
- **2026-07-26 · cli-docs · clean-pass** — the same split rewrote import lines across 28 `pipelex/cli/**` files plus an internal `PipelexHub` → `ServiceHub` rename inside doctor's private setup helper. Reviewed `docs/tools/cli/` and `pipelex/cli/agent_cli/CLAUDE.md`: no command, flag, help text, output string, or exit code changed, and neither target mentions the hub. Logged as clean-pass rather than friction on purpose — a 28-file sweep across the CLI package is exactly the shape that *could* have moved the surface, so the contract opening was proportionate even though the verdict was "nothing stale". This is the second consecutive clean-pass for cli-docs on a mechanical sweep (cf. 2026-07-10); a third would be worth weighing against narrowing.
- **2026-07-26 · keyword-only-convention · friction** — opened by the hub split renaming two *docstring* mentions of `pipelex.hub` inside `keyword_only_guard.py`; no guard behavior, violation kind, registry schema, or flag changed, and the convention doc refers to the presentation layer only generically ("Typer/hub import"). Worth noting what the contract did *not* fire on: the same change re-recorded all 54 `pipelex/hub.py` grants under the two new modules — a 55-entry registry rewrite — because `subject_grants.toml` is not a trigger, and correctly so (a grant migration is data, not convention). Narrowing that would have prevented this opening: the trigger matches the whole guard module, so a comment-only edit opens it; restricting the digest to the guard's *code* (ignoring docstrings/comments) — or accepting that this module is small enough that prose edits are rare — is the tradeoff to weigh.
