# Drift Hunt — Stage 1 Part 1 findings: building-methods

Section: **building-methods** (35 pages). Generated by the wave Workflow (`drift-hunt-building-methods`, run `wf_d707ca81-495`, 2026-07-12): one review agent per page (Sonnet-5, high effort) → adversarial verify per finding (D6). Every finding was **independently reproduced against the real parser / runtime** by a second agent prompted to refute it; the recurring load-bearing rules were additionally hand-spot-checked against code (pipe `description`/`output` required & `inputs` optional; inline `@`-sigil rule; PipeLLM unused-input & no `nb_output`; native-concept set = 15; PipeExtract `output=Page[]`; shorthand→required; dpi default=72; PipeFunc async-not-required).

> **Scope ruling applied (Louis, 2026-07-12).** Omitting *scaffolding* fields in a focused teaching example is authoring convention, **not drift**: (1) a missing top-level `domain`, (2) a missing pipe `description`, and (3) an omitted `prompt` body are all things a reader fills in (with a placeholder) before running. The future `validate-doc-bundle-examples` derived check should **inject placeholder `description`/`domain` and tolerate omitted prompts** before validating, so it only flags defects that survive scaffolding. A written prompt that *ignores its own declared input* is still drift (it teaches broken wiring). Three findings were reclassified out as convention; three were re-pointed to the defect that survives scaffolding. See the reclassified section at the bottom.

## Summary

- **Pages reviewed:** 35  ·  **claims checked:** 452
- **Confirmed findings (drift):** 44  ·  **reclassified as authoring convention:** 3  ·  **rejected by adversarial verify:** 0
- **Defect density:** 1.26 findings/page; 26/35 pages carry ≥1 finding, 9/35 clean

| Severity | Count | Meaning |
|---|---|---|
| 🔴 breaks-a-user | 25 | documented command/snippet fails or misfires if followed verbatim (surviving scaffolding) |
| 🟠 wrong | 11 | factually incorrect statement about code/behavior |
| 🟡 misleading | 6 | technically defensible but leads a reader astray |
| ⚪ stale-detail | 2 | outdated minor detail, low user impact |

## Findings by page (ranked by severity within each page)

### `concepts/native-concepts.md` — 3 finding(s)

**F1 · 🔴 breaks-a-user**  <sub>(review id [3])</sub>
- **Doc:** `docs/building-methods/concepts/native-concepts.md:302-308`
- **Claim:** [pipe.analyze_document] / type = "PipeLLM" / description = "Analyze a document" / inputs = { document = "Document" } / output = "Text" / prompt = "Analyze this document and provide a summary"
- **Reality:** PipeLLM validation requires every declared input to be referenced in the prompt (via $var/@var). The snippet declares `document` but never references it in the prompt text, so validating this exact bundle fails.
- **Code evidence:** pipelex/pipe_operators/llm/pipe_llm_blueprint.py:113-131 (unused-input check); ran `.venv/bin/pipelex validate bundle` on the exact snippet -> "Extraneous Input Variable ... Pipe: analyze_document ... Unused input variable(s): 'document'. These variables are declared in inputs but not referenced in the prompt or system_prompt."
- **Verified:** Wrote the exact snippet to a scratch .mthds file and ran `.venv/bin/pipelex validate bundle <file>`; it failed with the unused-input-variable error, tracing to the enforcement code in pipe_llm_blueprint.py. — adversarial verify: **CONFIRMED**

**F2 · 🔴 breaks-a-user**  <sub>(review id [4])</sub>
- **Doc:** `docs/building-methods/concepts/native-concepts.md:313-319`
- **Claim:** [pipe.process_image] / type = "PipeLLM" / description = "Describe an image" / inputs = { photo = "Image" } / output = "Text" / prompt = "Describe what you see in this image"
- **Reality:** Same class of bug as analyze_document: input `photo` is declared but never referenced ($photo/@photo) in the prompt, so the exact documented snippet fails real bundle validation.
- **Code evidence:** pipelex/pipe_operators/llm/pipe_llm_blueprint.py:113-131 (unused-input check); ran `.venv/bin/pipelex validate bundle` on the exact snippet -> "Extraneous Input Variable ... Pipe: process_image ... Unused input variable(s): 'photo'."
- **Verified:** Wrote the exact snippet to a scratch .mthds file and ran `.venv/bin/pipelex validate bundle <file>`; validation failed with the same unused-input-variable error class. — adversarial verify: **CONFIRMED**

**F3 · 🔴 breaks-a-user**  <sub>(review id [5])</sub>
- **Doc:** `docs/building-methods/concepts/native-concepts.md:386-412`
- **Claim:** "Document Extraction" pattern: `[pipe.extract_pages]` (output = "Page[]") + `[pipe.analyze_page]` (inputs = { page = "Page[]" }, prompt references `@pages`) + `[pipe.extract_and_analyze]` (PipeSequence step calls `pipe = "analyze_pages"`)
- **Reality:** The self-contained 3-pipe example has two internal reference bugs, either of which fails bundle validation verbatim: the prompt variable (`@pages`) doesn't match the declared input name (`page`), and the PipeSequence step references a pipe (`analyze_pages`) that doesn't exist — only `analyze_page` is defined.
- **Code evidence:** Ran `.venv/bin/pipelex validate bundle` on the exact snippet: (1) "Missing Input Variable ... Pipe: analyze_page ... Variables: pages ... used in the prompt ... but not declared in inputs" (prompt uses `@pages` but the declared input is `page`, singular); (2) after fixing the prompt var, a second run surfaces "Unresolved Pipe Dependency ... Pipe: extract_and_analyze ... Missing Pipe: analyze_pages ... Pipe 'analyze_pages' not found" (the PipeSequence step calls `analyze_pages`, but the pipe defined two blocks earlier is named `analyze_page`, singular)
- **Verified:** Wrote the exact snippet to a scratch .mthds file and ran `.venv/bin/pipelex validate bundle <file>` twice (once as-is, once with the prompt var fixed to isolate the second bug); both runs produced hard validation failures on real code paths. — adversarial verify: **CONFIRMED**

### `pipes/executing-pipelines.md` — 3 finding(s)

**F4 · 🔴 breaks-a-user**  <sub>(review id [18])</sub>
- **Doc:** `docs/building-methods/pipes/executing-pipelines.md:38-40, 50-51, 63`
- **Claim:** runner = PipelexMTHDSProtocol(bundle_uri="path/to/my_bundle.mthds") — and again at line 63: "If you provide both `bundle_uri` and `pipe_code`, the explicit `pipe_code` takes priority over `main_pipe`."
- **Reality:** The constructor parameter is `bundle_uris` (plural, a list of strings), not `bundle_uri` (singular string). Every Python snippet in the 'Simplest Approach' section (both the main_pipe example and the specific-pipe example) uses the wrong, non-existent kwarg and raises a TypeError if run verbatim.
- **Code evidence:** pipelex/pipeline/runner.py:108-121 — PipelexMTHDSProtocol.__init__ only accepts `bundle_uris: list[str] | None = None` (plural, list). Ran the exact doc snippet: `.venv/bin/python -c "from pipelex.pipeline.runner import PipelexMTHDSProtocol; PipelexMTHDSProtocol(bundle_uri='x')"` → `TypeError: PipelexMTHDSProtocol.__init__() got an unexpected keyword argument 'bundle_uri'. Did you mean 'bundle_uris'?`
- **Verified:** Read pipelex/pipeline/runner.py constructor signature; reproduced the exact TypeError by running the doc's own code with .venv/bin/python. — adversarial verify: **CONFIRMED**

**F5 · 🔴 breaks-a-user**  <sub>(review id [19])</sub>
- **Doc:** `docs/building-methods/pipes/executing-pipelines.md:87 and 104-122`
- **Claim:** `library_dirs`: ... If not specified, Pipelex falls back to the `PIPELEXPATH` environment variable, then to the current working directory. (Also embodied by the "Basic usage (loads from current working directory)" heading and its code sample.)
- **Reality:** There is no current-working-directory fallback for library_dirs resolution. When library_dirs is unset and PIPELEXPATH is unset (and no Pipelex.make(library_dirs=...) default was set), the effective directory list is empty, not cwd — so pipes in the working directory are NOT auto-discovered, contrary to both the explicit 'then to the current working directory' claim and the 'Basic usage (loads from current working directory)' example.
- **Code evidence:** pipelex/hub.py:699-730 resolve_library_dirs() resolution order is: per-call arg → Pipelex.make() instance default → PIPELEXPATH env var → else returns `[]` ("none configured"); no cwd fallback exists anywhere in pipelex/system/environment.py:59-71 (get_pipelexpath_dirs) or pipelex/pipeline/execution_seams.py. Empirically reproduced: ran the doc's own 'basic usage' pattern (PipelexMTHDSProtocol() with no library_dirs, PIPELEXPATH unset, cwd containing a valid pipe) and got `PipeNotFoundError: Pipe 'greet' not found...` instead of a successful run.
- **Verified:** Read pipelex/hub.py:resolve_library_dirs and pipelex/system/environment.py:get_pipelexpath_dirs; reproduced the doc's own basic-usage snippet against a real .mthds file in cwd with PIPELEXPATH unset and got PipeNotFoundError instead of success. — adversarial verify: **CONFIRMED**

**F6 · 🔴 breaks-a-user**  <sub>(review id [20])</sub>
- **Doc:** `docs/building-methods/pipes/executing-pipelines.md:168-187`
- **Claim:** MTHDS content snippet under 'Using MTHDS Content Directly' (the `prompt = "\nProduct Description: $description\n\n...\n"` block using a plain double-quoted TOML string spanning multiple lines) is presented as runnable content passed to `mthds_contents=[my_pipe_content]`.
- **Reality:** TOML basic strings (single double-quotes) cannot contain literal newlines — that requires a multi-line basic string (triple double-quotes). The doc's snippet uses a single-quoted basic string with embedded raw newlines, which the real MTHDS/TOML parser rejects outright; copy-pasting this exact bundle content fails to parse.
- **Code evidence:** Wrote the exact MTHDS content to /private/tmp/.../scratchpad/executing-pipelines-mthdscontent.mthds and ran `.venv/bin/pipelex validate bundle <file>` → `❌ Bundle validation failed ... TOML syntax error at line 14, column 11: Illegal character '\n'` (the `prompt = "` basic string containing a literal newline).
- **Verified:** Wrote the doc's exact MTHDS content to a scratch file and validated it with the real parser via `.venv/bin/pipelex validate bundle`, per the assignment's required verification method for TOML/MTHDS snippets. — adversarial verify: **CONFIRMED**

### `pipes/pipe-controllers/PipeSequence.md` — 3 finding(s)

**F7 · 🔴 breaks-a-user**  <sub>(review id [29])</sub>
- **Doc:** `docs/building-methods/pipes/pipe-controllers/PipeSequence.md:48-52`
- **Claim:** Example step `[pipe.extract_text_from_image]` (type = "PipeExtract") sets `output = "Text"`
- **Reality:** PipeExtract hard-requires its `output` to be exactly `Page[]`; the documented example sets it to `Text`, so the copy-pasted snippet fails validation immediately.
- **Code evidence:** pipelex/pipe_operators/extract/pipe_extract_blueprint.py:32-35 (`validate_output`: `if self.output != "Page[]": raise ValueError(f"PipeExtract output should be a 'native.Page[]' concept, but is {self.output}")`). Confirmed by running `.venv/bin/pipelex validate bundle` on the doc's snippet reproduced verbatim: `PipeExtract output should be a 'native.Page[]' concept, but is Text`.
- **Verified:** Read pipe_extract_blueprint.py's validate_output(), then ran `.venv/bin/pipelex validate bundle` against the doc's example snippet reproduced in a scratch .mthds file; the real parser rejected it with the exact error quoted above. — adversarial verify: **CONFIRMED**

**F8 · 🔴 breaks-a-user**  <sub>(review id [30])</sub>
- **Doc:** `docs/building-methods/pipes/pipe-controllers/PipeSequence.md:48-52`
- **Claim:** Example step `[pipe.extract_text_from_image]` (type = "PipeExtract") declares no `inputs` key at all
- **Reality:** PipeExtract requires exactly one declared input (an Image or Document concept); the documented step omits `inputs` entirely, which is a second, independent way the same copy-pasted example fails to validate.
- **Code evidence:** pipelex/pipe_operators/extract/pipe_extract_blueprint.py:20-32 (`validate_inputs`: raises when `self.inputs is None` or `nb_inputs < 1`, requiring exactly one Image/Document input). Confirmed via `.venv/bin/pipelex validate bundle`: `Exactly one input must be provided for the PipeExtract, and it must be a Document or an Image or a concept that refines one of them.0 inputs were provided.`
- **Verified:** Read pipe_extract_blueprint.py's validate_inputs(), then ran `.venv/bin/pipelex validate bundle` on a minimal reproduction of the doc's extract_text_from_image step; the real parser rejected it with the exact error quoted above. — adversarial verify: **CONFIRMED**

**F9 · 🟠 wrong**  <sub>(review id [32])</sub>
- **Doc:** `docs/building-methods/pipes/pipe-controllers/PipeSequence.md:38 (`| \`result\` | string | The name to give to the output of this step in the working memory. | Yes |`)`
- **Claim:** Step Configuration table: the `result` key is `Required: Yes`
- **Reality:** `result` is an optional field on `SubPipeBlueprint` (defaults to `None`), not a required key; a `steps` entry omitting `result` passes real validation.
- **Code evidence:** pipelex/pipe_controllers/sub_pipe_blueprint.py:14 (`result: str | None = None`) — an optional field with no required-key validator elsewhere in the class. Confirmed by running `.venv/bin/pipelex validate bundle` on a PipeSequence step written as `{ pipe = "step_one" }` (no `result` key): the bundle validated successfully (`Successfully validated bundle ...`).
- **Verified:** Read SubPipeBlueprint's field definitions in sub_pipe_blueprint.py, then ran `.venv/bin/pipelex validate bundle` on a scratch .mthds file containing a PipeSequence step with no `result` key; validation succeeded. — adversarial verify: **CONFIRMED**

### `pipes/understanding-multiplicity.md` — 3 finding(s)

**F10 · 🔴 breaks-a-user**  <sub>(review id [45])</sub>
- **Doc:** `docs/building-methods/pipes/understanding-multiplicity.md:277-289 ([pipe.generate_subject_lines] section: output = SubjectLine, nb_output = 3, prompt containing Generate $_nb_output compelling subject lines for this email.)`
- **Claim:** Use Case 2 example: a PipeLLM pipe can set nb_output = 3 directly on the pipe (alongside output = SubjectLine) and reference it in the prompt via $_nb_output to generate a fixed number of outputs.
- **Reality:** A PipeLLM blueprint (and the base PipeBlueprint) forbids extra fields and has no nb_output attribute, so this exact snippet fails bundle validation with 'Extra forbidden fields'. The doc's own earlier section (Fixed output) already documents the actual mechanism for a fixed count - output = Concept[N] bracket notation - which this example contradicts by using output = SubjectLine (singular) plus a nonexistent nb_output field instead.
- **Code evidence:** pipelex/core/pipes/pipe_blueprint.py:191 (PipeBlueprint model_config = ConfigDict(extra=forbid)) and pipelex/pipe_operators/llm/pipe_llm_blueprint.py (PipeLLMBlueprint declares no nb_output field). Ran .venv/bin/pipelex validate bundle on the exact snippet: output was 'Extra forbidden fields: pipe.generate_subject_lines.PipeLLM.nb_output'. The only real nb_output field lives on SubPipeBlueprint (pipelex/pipe_controllers/sub_pipe_blueprint.py:14), i.e. it is a steps=[...] entry key inside a PipeSequence (correctly documented that way in docs/cookbook/design-slides.md:63), not a top-level PipeLLM field.
- **Verified:** Read pipelex/core/pipes/pipe_blueprint.py and pipelex/pipe_operators/llm/pipe_llm_blueprint.py; grepped for nb_output usage; wrote the doc's exact TOML to scratch/understanding-multiplicity-usecase2.mthds and ran '.venv/bin/pipelex validate bundle <file>', which failed with 'Extra forbidden fields: pipe.generate_subject_lines.PipeLLM.nb_output'. — adversarial verify: **CONFIRMED**

**F11 · 🔴 breaks-a-user**  <sub>(review id [46])</sub>
- **Doc:** `docs/building-methods/pipes/understanding-multiplicity.md:258-266 ([pipe.process_invoice_batch] section: output = InvoiceData, steps = [{ pipe = extract_single_invoice, batch_over = invoice_images, batch_as = invoice_image, result = all_invoice_data }])`
- **Claim:** Use Case 1 example: a PipeSequence step with batch_over/batch_as over an InvoiceImage[] input, with the sequence pipe declaring output = InvoiceData (singular), correctly processes a batch of invoices.
- **Reality:** Batching a single-item pipe (extract_single_invoice, output InvoiceData) over a list with batch_over makes the step's effective output a list (InvoiceData[]), so the enclosing PipeSequence's declared output = InvoiceData is inconsistent and the bundle is rejected by the validator as written; it must be output = InvoiceData[].
- **Code evidence:** Ran '.venv/bin/pipelex validate bundle' on the exact snippet (scratch/understanding-multiplicity-usecase1.mthds); output: 'PipeSequence output multiplicity mismatch: the sequence process_invoice_batch declares its output as InvoiceData, but its last step extract_single_invoice yields InvoiceData[]. Update the sequence's output to InvoiceData[] (or change the last step).'
- **Verified:** Wrote the doc's exact TOML to scratch/understanding-multiplicity-usecase1.mthds and ran '.venv/bin/pipelex validate bundle <file>'; validation failed with the PipeSequence output multiplicity mismatch error shown above. — adversarial verify: **CONFIRMED**

**F12 · 🔴 breaks-a-user**  <sub>(review id [47])</sub>
- **Doc:** `docs/building-methods/pipes/understanding-multiplicity.md:187-189 ([concept] Document = A written or printed record, Summary = ...) and lines 218-220 ([concept] Image = A visual image file, Comparison = ...)`
- **Claim:** Two of the doc's illustrative examples define concepts named Document (Input Multiplicity section 2, Variable list) and Image (Input Multiplicity section 3, Fixed count) as ordinary custom concepts.
- **Reality:** Document and Image are reserved native concept codes; declaring [concept] Document = ... or [concept] Image = ... is a hard validation error (Native Concept Redeclaration), so both snippets fail bundle validation as written.
- **Code evidence:** pipelex/core/concepts/native/concept_native.py:23-38 lists NativeConceptCode including IMAGE = Image and DOCUMENT = Document. Ran '.venv/bin/pipelex validate bundle' on both exact snippets (scratch/understanding-multiplicity-summarize_all.mthds and .../understanding-multiplicity-compare2images.mthds); both failed with 'Native Concept Redeclaration: Cannot declare a concept named [Document|Image] because it is natively available in Pipelex'.
- **Verified:** Grepped pipelex/core/concepts/native/concept_native.py for the native concept enum; wrote both doc snippets verbatim to scratch files and ran '.venv/bin/pipelex validate bundle <file>' on each - both rejected with Native Concept Redeclaration errors. — adversarial verify: **CONFIRMED**

### `configure-ai-llm-to-optimize-methods.md` — 2 finding(s)

**F13 · 🔴 breaks-a-user**  <sub>(review id [10])</sub>
- **Doc:** `docs/building-methods/configure-ai-llm-to-optimize-methods.md:24-34 (`2. **An alias** ... defined in the `[aliases]` section:` followed by a  block starting `[aliases]`)`
- **Claim:** An alias is a user-defined shortcut that maps to a model name, defined in the `[aliases]` section — with example TOML showing a top-level `[aliases]` table.
- **Reality:** The correct TOML section for LLM aliases is `[llm.aliases]`, not a bare top-level `[aliases]`. Pasting the doc's example verbatim into any real deck file (e.g. x_custom_llm_deck.toml) fails validation with a pydantic extra_forbidden error because ModelDeckBlueprint forbids unknown top-level keys.
- **Code evidence:** pipelex/cogt/models/model_deck.py:45-82 (aliases is a field of LLMDeckBlueprint, itself nested under the `llm` key of ModelDeckBlueprint, which has `model_config = ConfigDict(extra="forbid")` via ConfigModel at pipelex/system/configuration/config_model.py:12); real deck file pipelex/kit/configs/inference/deck/1_llm_deck.toml:33 uses `[llm.aliases]`. Ran `ModelDeckBlueprint.model_validate({'aliases': {...}})` in the venv — result: `aliases / Extra inputs are not permitted [type=extra_forbidden]` plus missing-field errors for llm/extract/img_gen/search.
- **Verified:** Read pipelex/cogt/models/model_deck.py and pipelex/system/configuration/config_model.py; read the real kit deck file 1_llm_deck.toml; ran ModelDeckBlueprint.model_validate() in .venv/bin/python3 against the doc's exact snippet content and confirmed rejection with extra_forbidden. — adversarial verify: **CONFIRMED**

**F14 · ⚪ stale-detail**  <sub>(review id [11])</sub>
- **Doc:** `docs/building-methods/configure-ai-llm-to-optimize-methods.md:110-115 and 128-132 (bullet list and directory tree, both closed lists with no ellipsis)`
- **Claim:** The Model Deck 'consists of numbered deck files loaded in order' and both the bullet list and the Directory Structure tree enumerate exactly 1_llm_deck.toml, 2_img_gen_deck.toml, 3_extract_deck.toml, x_custom_llm_deck.toml as the complete deck/ contents.
- **Reality:** The doc's deck-file list omits `4_search_deck.toml` (search model config, loaded by the same numbered-deck mechanism the doc describes) and `x_custom_extract_deck.toml` (custom extract overrides, same mechanism as the documented x_custom_llm_deck.toml). The list is presented as exhaustive (no '...' or 'e.g.'), unlike the adjacent backends/ tree entry in the same code block which does use '...' to signal more files exist.
- **Code evidence:** `find pipelex/kit/configs/inference/deck` shows 6 files: 1_llm_deck.toml, 2_img_gen_deck.toml, 3_extract_deck.toml, 4_search_deck.toml, x_custom_extract_deck.toml, x_custom_llm_deck.toml. pipelex/kit/paths.py:22-23 lists both x_custom_llm_deck.toml and x_custom_extract_deck.toml as kit-managed custom-override files; 4_search_deck.toml exists on disk and is an established file (git log: added by commit 7c128dec8 'Rename search presets to provider-agnostic names').
- **Verified:** Ran `find` over pipelex/kit/configs/inference; grepped pipelex/kit/paths.py for x_custom_* file registrations; checked git log to confirm 4_search_deck.toml is an established, non-transient file, not a work-in-progress addition. — adversarial verify: **CONFIRMED**

### `libraries.md` — 2 finding(s)

**F15 · 🔴 breaks-a-user**  <sub>(review id [15])</sub>
- **Doc:** `docs/building-methods/libraries.md:106-129 (mthds_content snippet, especially lines 116-120)`
- **Claim:** The 'Loading from MTHDS content' example (Option B) shows a runnable MTHDS content string that can be passed to `mthds_contents` and executed: `[pipe.my_pipe]` with `type = "PipeLLM"`, `inputs = { desc = "ProductDescription" }`, `output = "Tagline"`, `prompt = "Generate a tagline for: @desc"` (no `description` field).
- **Reality:** The snippet as written in the doc does not parse/validate: `PipeLLM` requires a `description` field that is missing, and the inline `@desc` reference in the prompt is rejected by the template-sigil parser (must be `$desc` for an inline value, or `@desc` alone on its own line).
- **Code evidence:** Ran `.venv/bin/pipelex validate bundle <scratch>.mthds` against the doc's exact snippet content: first failure — 'Missing required fields: pipe.my_pipe.PipeLLM.description'; after adding a description, second failure — 'Value error, Template sigil error in PipeLLM prompt: Inline `@desc` is not allowed on line 1: the `@` sigil produces tag-wrapped block content and must appear alone on its own line. Did you mean `$desc` (inline value)...'
- **Verified:** Wrote the doc's exact MTHDS content to /private/tmp/.../scratchpad/libraries_doc_snippet.mthds and ran the real parser via `.venv/bin/pipelex validate bundle <path>`; confirmed two independent real-parser rejections. — adversarial verify: **CONFIRMED**
- **Reclassified (Louis, 2026-07-12):** operative defect is the inline `@desc` sigil (survives a `description` placeholder); the missing-`description` half is authoring convention.

**F16 · 🟠 wrong**  <sub>(review id [14])</sub>
- **Doc:** `docs/building-methods/libraries.md:34 (table row) and lines 38-39 ('!!! info "Future Change"' admonition)`
- **Claim:** Pipes are 'Unique per library' with a footnote: 'Pipe uniqueness will soon be scoped per domain instead of per library, allowing different domains within the same library to have pipes with the same code.' (presented as a Future Change)
- **Reality:** Pipe uniqueness is ALREADY scoped per domain (via the domain-qualified `pipe_ref`), not per library — this shipped in v0.22.0. The doc presents it as a not-yet-implemented future change, when the described capability (same pipe code in different domains within one library) already works today.
- **Code evidence:** pipelex/core/pipes/pipe_abstract.py:98-101 (`pipe_ref` property = `f"{self.domain_code}.{self.code}"`); pipelex/libraries/pipe/pipe_library.py:39-47 (`add_new_pipe` collides on `pipe.pipe_ref`, i.e. domain-qualified, not bare code); CHANGELOG.md:867 ('Domain-Qualified Pipe References (`pipe_ref`): Added a `pipe_ref` property (domain.code) as the primary pipe index, allowing pipes with the same code to coexist across different domains' — shipped in v0.22.0, 2026-03-25). Empirically verified: built two .mthds files (domain_a, domain_b) each declaring `pipe.generate_tagline`, loaded together via `.venv/bin/pipelex validate pipe --all -L <dir>` — output: 'Validating 2 pipes ... Setup sequence passed OK, config and pipelines are validated.' No collision.
- **Verified:** Read pipe_abstract.py and pipe_library.py source; grepped CHANGELOG.md for the shipping changelog entry; empirically ran `.venv/bin/pipelex validate pipe --all -L <scratch dir>` against two same-coded, different-domain pipes and confirmed no collision/error. — adversarial verify: **CONFIRMED**

### `pipes/pipe-controllers/PipeBatch.md` — 2 finding(s)

**F17 · 🔴 breaks-a-user**  <sub>(review id [23])</sub>
- **Doc:** `docs/building-methods/pipes/pipe-controllers/PipeBatch.md:50, 52-57`
- **Claim:** batch_params (table dict, Optional) — 'An optional table to provide more specific names for the batch operation.' With sub-keys input_list_stuff_name (Optional, defaults to the name of the PipeBatch's main input) and input_item_stuff_name (Required).
- **Reality:** There is no `batch_params` table on the PipeBatch MTHDS blueprint (or the authoring spec, pipelex/builder/pipe/pipe_batch_spec.py:36-49) - an extra field is rejected outright. The real field names are the flat, both-required `input_list_name` and `input_item_name` directly on the pipe, not `input_list_stuff_name`/`input_item_stuff_name` nested under `batch_params`. (Those latter names belong to the internal `BatchParams` runtime object and to the unrelated `SubPipe.batch_params` used inside PipeParallel/PipeSequence - not to PipeBatch's own MTHDS syntax.)
- **Code evidence:** pipelex/pipe_controllers/batch/pipe_batch_blueprint.py:9-14 (PipeBatchBlueprint has flat required fields `input_list_name: str` and `input_item_name: str`, no `batch_params` field); confirmed by running `.venv/bin/pipelex validate bundle` on a `.mthds` snippet using `[pipe.x.batch_params]` with `input_list_stuff_name`/`input_item_stuff_name` -> `Missing required fields: 'pipe.summarize_all_articles.PipeBatch.input_list_name', 'pipe.summarize_all_articles.PipeBatch.input_item_name'` and `Extra forbidden fields: 'pipe.summarize_all_articles.PipeBatch.batch_params'`.
- **Verified:** Read pipe_batch_blueprint.py and pipe_batch_spec.py; grepped for input_list_stuff_name/input_item_stuff_name usage across pipelex/; ran real parser (.venv/bin/pipelex validate bundle) on a scratch .mthds file using the doc's documented batch_params syntax and got a hard validation failure naming both the missing flat fields and the forbidden batch_params table. — adversarial verify: **CONFIRMED**

**F18 · 🔴 breaks-a-user**  <sub>(review id [24])</sub>
- **Doc:** `docs/building-methods/pipes/pipe-controllers/PipeBatch.md:73-81`
- **Claim:** Worked example TOML: PipeBatch pipe `summarize_all_articles` sets `input_item_name = "ArticleText"` and never sets `input_list_name`.
- **Reality:** `input_list_name` is a required field on PipeBatchBlueprint (pipelex/pipe_controllers/batch/pipe_batch_blueprint.py:13) with no default and no fallback-to-main-input logic anywhere in the factory/blueprint code (pipe_batch_factory.py:28-31). The example as written fails bundle validation.
- **Code evidence:** Ran `.venv/bin/pipelex validate bundle` on the doc's example verbatim (fixing only an unrelated prompt-variable typo) -> `Missing required fields: 'pipe.summarize_all_articles.PipeBatch.input_list_name'`.
- **Verified:** Wrote the doc's example TOML verbatim to a scratch .mthds file and ran `.venv/bin/pipelex validate bundle` on it; observed the real parser reject it for the missing `input_list_name` field. — adversarial verify: **CONFIRMED**

### `pipes/pipe-controllers/PipeCondition.md` — 2 finding(s)

**F19 · 🔴 breaks-a-user**  <sub>(review id [26])</sub>
- **Doc:** `docs/building-methods/pipes/pipe-controllers/PipeCondition.md:39-88`
- **Claim:** The "Simple routing based on category" example (a complete, self-contained `.mthds` snippet with `domain`, `[concept]`, and all pipes defined) is presented as a working example of PipeCondition routing.
- **Reality:** The doc's `CategoryInput` concept is declared as a plain unstructured text concept (`CategoryInput = "Input with a category field"`), which has no `category` field. Because the expression `input_data.category` accesses that field, and dry-run mock-generates `input_data` from the concept's declared structure, the mock has no `.category` attribute and the expression renders empty, causing the run to fail dry-run validation via `pipelex validate bundle`. Copy-pasting this complete example verbatim into a `.mthds` file and validating it (the documented validation workflow) fails.
- **Code evidence:** Ran `.venv/bin/pipelex validate bundle <scratch-file with the exact snippet>` — dry-run fails with: "PipeCondition 'route_by_category': Conditional expression returned no result" (raised by pipelex/pipe_controllers/condition/pipe_condition.py:238-244 `_validate_before_run`, which also executes during dry-run per pipelex/core/pipes/pipe_abstract.py:511-514). Re-running the identical pipeline but with `CategoryInput` declared as a structured concept (`[concept.CategoryInput.structure] category = "text"` instead of the doc's plain-string concept `CategoryInput = "Input with a category field"`) validates successfully.
- **Verified:** Wrote the exact doc snippet to a scratch .mthds file and ran the real `pipelex validate bundle` CLI; confirmed the failure is caused by the concept's missing structure by re-running with a structured concept definition, which passes. — adversarial verify: **CONFIRMED**

**F20 · 🟠 wrong**  <sub>(review id [25])</sub>
- **Doc:** `docs/building-methods/pipes/pipe-controllers/PipeCondition.md:34`
- **Claim:** `add_alias_from_expression_to`: "the string result of the expression evaluation is added to the working memory as an alias with this name" (i.e. the parameter's value becomes the new alias's name, holding the evaluated expression result).
- **Reality:** It is backward: the NEW alias name created is the evaluated expression's string result itself (e.g. "small"), and `add_alias_from_expression_to`'s value is the pre-existing TARGET name it must point to (which must already exist in working memory) — not the name under which the result is stored. A user who follows the table's literal description (setting `add_alias_from_expression_to` to a not-yet-existing name, expecting it to be created and hold the expression result) gets `WorkingMemoryConsistencyError: Cannot create alias to non-existent target '<name>'` at runtime. Note the separate bullet list at lines 207-214 ("Requires the target to exist in working memory beforehand") is consistent with the real code and does not have this problem — only the parameter-table row (line 34) states the backward semantics.
- **Code evidence:** pipelex/pipe_controllers/condition/pipe_condition.py:271-276 calls `working_memory.add_alias(alias=evaluated_expression, target=self.add_alias_from_expression_to)`; pipelex/core/memory/working_memory.py:248-253 `add_alias(self, alias, *, target)` creates `self.aliases[alias] = target` and requires `target` to already exist in `self.root` (raises `WorkingMemoryConsistencyError` otherwise, working_memory.py:243-245).
- **Verified:** Read pipe_condition.py call site and working_memory.py add_alias/set_alias implementation; confirmed parameter semantics via param names (alias vs target) and the pre-existing-target guard. — adversarial verify: **CONFIRMED**

### `pipes/pipe-controllers/PipeParallel.md` — 2 finding(s)

**F21 · 🔴 breaks-a-user**  <sub>(review id [28])</sub>
- **Doc:** `docs/building-methods/pipes/pipe-controllers/PipeParallel.md:63-94`
- **Claim:** The worked 'Example: Extracting different details from a text' TOML snippet (concept ProductAnalysis with structure fields `features`/`sentiment` of `type = "text"`, combined from two PipeLLM branches with plain/unstructured outputs `ProductFeatures`/`ProductSentiment`) is presented as a runnable configuration for `analyze_product_in_parallel`.
- **Reality:** The page's only full worked example does not validate/run when copied verbatim: it either fails immediately on unused-input-variable validation (no prompt given) or, once a prompt is added, fails at the combine step because a `type = "text"` (plain str) structure field cannot receive the `TextContent` produced by an unstructured PipeLLM branch output.
- **Code evidence:** Copied the snippet verbatim into a scratch bundle and ran `.venv/bin/pipelex validate bundle /private/tmp/.../pipeparallel_doc_example_exact.mthds`. It fails at blueprint-validation time with two 'Extraneous Input Variable' errors ('description' declared in inputs but not referenced in prompt/system_prompt) because the doc's `extract_features`/`analyze_sentiment` pipes declare no `prompt` at all. Fixing that by adding a prompt referencing `$description` (model $testing-text) still fails, now at the dry-run combine step, with a pydantic error: "features: string_type: Input should be a valid string / sentiment: string_type: Input should be a valid string" — because `type = "text"` structure fields (pipelex/core/concepts/concept_structure_blueprint.py:38,126-135) generate plain `str` pydantic fields, but the branch pipes' outputs (`ProductFeatures`/`ProductSentiment`, declared with no `.structure`) resolve to `TextContent` objects, not bare strings, when combined via `StuffFactory.combine_stuffs` (pipelex/core/stuffs/stuff_factory.py:116-136, `the_subclass.model_validate(obj=stuff_contents)`).
- **Verified:** Wrote the doc's snippet verbatim to /private/tmp/claude-501/-Users-lchoquel-repos-Pipelex--drift/6df8efb5-54e5-4a4e-a1e2-51eecce3f7f4/scratchpad/pipeparallel_doc_example_exact.mthds and ran `.venv/bin/pipelex validate bundle <file>`; also ran a corrected variant with prompts added (pipeparallel_doc_example.mthds) and observed the downstream combine ValidationError. Cross-checked the field-type mapping in pipelex/core/concepts/concept_structure_blueprint.py and the combine implementation in pipelex/core/stuffs/stuff_factory.py. — adversarial verify: **CONFIRMED**
- **Reclassified (Louis, 2026-07-12):** kept for the dry-run combine type-mismatch (`type="text"` str field fed a `TextContent`). The branch pipes' missing-prompt / unused-input half is authoring convention (omitted-prompt).

**F22 · 🟠 wrong**  <sub>(review id [27])</sub>
- **Doc:** `docs/building-methods/pipes/pipe-controllers/PipeParallel.md:45`
- **Claim:** In the 'MTHDS Parameters' table, `inputs` (dictionary mapping input names to concept codes) is marked Required: Yes.
- **Reality:** `inputs` is optional at the PipeBlueprint model level (and PipeParallelBlueprint adds no requirement for it); a PipeParallel with no `inputs` key parses and validates fine. The table's 'Required: Yes' is factually wrong.
- **Code evidence:** pipelex/core/pipes/pipe_blueprint.py:185 declares `inputs: dict[str, str] | None = None` (optional, no PipeParallel override). Confirmed by running `.venv/bin/pipelex validate bundle` on a scratch PipeParallel pipe with no `inputs` key at all — blueprint validation passed with zero complaint about a missing `inputs` field (it only failed later, at the unrelated dry-run combine step). File: /private/tmp/claude-501/-Users-lchoquel-repos-Pipelex--drift/6df8efb5-54e5-4a4e-a1e2-51eecce3f7f4/scratchpad/pipeparallel_no_inputs.mthds
- **Verified:** Read pipelex/core/pipes/pipe_blueprint.py and pipelex/pipe_controllers/parallel/pipe_parallel_blueprint.py; ran `.venv/bin/pipelex validate bundle` against a scratch .mthds omitting `inputs` on the PipeParallel pipe and observed no missing-field error. — adversarial verify: **CONFIRMED**

### `pipes/pipe-operators/PipeCompose.md` — 2 finding(s)

**F23 · 🔴 breaks-a-user**  <sub>(review id [36])</sub>
- **Doc:** `docs/building-methods/pipes/pipe-operators/PipeCompose.md:36`
- **Claim:** In the Template Mode Configuration table, `output` (string, "The concept for the output") is marked Required: No
- **Reality:** `output` is actually a required field for PipeCompose in template mode (and construct mode, where the same table correctly marks it Yes) -- omitting it fails bundle validation, not optional as the table states.
- **Code evidence:** pipelex/core/pipes/pipe_blueprint.py:186 (`output: str` with no default, so Pydantic treats it as required); confirmed by running `.venv/bin/pipelex validate bundle` on a PipeCompose template-mode pipe with `output` omitted -> `Missing required fields: 'pipe.compose_greeting.PipeCompose.output'`
- **Verified:** Wrote /private/tmp/.../scratchpad/pipecompose_no_output.mthds (valid PipeCompose template-mode pipe with `output` omitted) and ran `.venv/bin/pipelex validate bundle <file>`; it failed with 'Missing required fields: ...PipeCompose.output'. Also read pipelex/core/pipes/pipe_blueprint.py:186 confirming `output: str` has no default at the blueprint level. — adversarial verify: **CONFIRMED**

**F24 · 🟠 wrong**  <sub>(review id [37])</sub>
- **Doc:** `docs/building-methods/pipes/pipe-operators/PipeCompose.md:35 and :124`
- **Claim:** In both the Template Mode Configuration table and the Construct Mode Configuration table, `inputs` (table, "Input variables needed for the template/construct") is marked Required: Yes
- **Reality:** `inputs` is optional at the blueprint level and a PipeCompose pipe (either mode) validates successfully with the `inputs` key omitted entirely, contradicting the 'Required: Yes' marking in both tables.
- **Code evidence:** pipelex/core/pipes/pipe_blueprint.py:185 (`inputs: dict[str, str] | None = None`); confirmed by running `.venv/bin/pipelex validate bundle` on a PipeCompose template-mode pipe with no `inputs` key at all -> 'Successfully validated bundle', and again on a construct-mode pipe with an all-fixed-value construct and no `inputs` key -> 'Successfully validated bundle'
- **Verified:** Wrote and validated two scratch bundles: pipecompose_no_inputs.mthds (template mode, no `inputs` field, static template) and pipecompose_construct_no_inputs.mthds (construct mode, no `inputs` field, all-fixed construct fields) via `.venv/bin/pipelex validate bundle`; both returned 'Successfully validated bundle'. Also read pipelex/core/pipes/pipe_blueprint.py:185 confirming `inputs` has an Optional/None default. — adversarial verify: **CONFIRMED**

### `pipes/pipe-operators/PipeExtract.md` — 2 finding(s)

**F25 · 🔴 breaks-a-user**  <sub>(review id [39])</sub>
- **Doc:** `docs/building-methods/pipes/pipe-operators/PipeExtract.md:105 (referring to the example pipe at lines 97-103, which declares `inputs = { article_url = "Document" }`)`
- **Claim:** Pass a web URL as the `document_uri` when running the pipe. PipeExtract fetches the page and extracts its content into `Page` objects...
- **Reality:** The example pipe's declared input name is `article_url`, not `document_uri`. Supplying an input keyed `document_uri` (as the doc instructs) is rejected by the runtime because it does not match the pipe's declared input.
- **Code evidence:** Ran `.venv/bin/pipelex run bundle <scratch>.mthds --pipe extract_web_article --inputs <inputs.json with {"document_uri": ...}> --dry-run` → `Failed to execute pipeline 'extract_web_article': Input 'document_uri' is not declared by this pipe. Declared inputs: 'article_url'.`
- **Verified:** Wrote the doc's example pipe to a scratch .mthds file, validated it with `pipelex validate bundle`, then ran it with `pipelex run bundle --pipe extract_web_article --inputs ... --dry-run` supplying the input key literally named in the doc's prose; the CLI errored, confirming the key mismatch. — adversarial verify: **CONFIRMED**

**F26 · 🟠 wrong**  <sub>(review id [38])</sub>
- **Doc:** `docs/building-methods/pipes/pipe-operators/PipeExtract.md:61`
- **Claim:** `page_views_dpi` ... The resolution (in Dots Per Inch) for the generated page views when processing a PDF. Defaults to `150`.
- **Reality:** When `page_views_dpi` is omitted from the pipe (as in the doc's own example blueprint), the actual default resolution used is 72 DPI, not 150.
- **Code evidence:** pipelex/pipelex.toml:258 `default_page_views_dpi = 72`; consumed at pipelex/cogt/content_generation/content_generator.py:262 `page_views_dpi = job_params.page_views_dpi or get_config().cogt.extract_config.default_page_views_dpi`
- **Verified:** Grepped pipelex.toml and configs.py for the config key, then traced its consumption in content_generator.py to confirm it is indeed the fallback applied when the MTHDS-level `page_views_dpi` field is unset. — adversarial verify: **CONFIRMED**

### `domain.md` — 1 finding(s)

**F27 · 🔴 breaks-a-user**  <sub>(review id [12])</sub>
- **Doc:** `docs/building-methods/domain.md:162 (also :174, :204)`
- **Claim:** prompt = "Process this invoice: @invoice" (also line 174: "Reconcile this invoice: @invoice", and line 204: "Extract the primary diagnosis: @record")
- **Reality:** The `@` sigil cannot be used inline within a prompt string next to other text — it must be alone on its own line (block content); inline variable interpolation requires the `$` sigil instead. All three PipeLLM prompt examples on this page use the now-invalid inline `@` form and would fail validation if copy-pasted verbatim.
- **Code evidence:** pipelex/cogt/templating/template_preprocessor.py:122-136 raises TemplateSigilSyntaxError for inline `@identifier` when the identifier is a declared input; ran `.venv/bin/pipelex validate bundle` on the doc's exact snippet (domain+concept+pipe with prompt = "Process this invoice: @invoice") and got: "Value error, Template sigil error in PipeLLM prompt: Inline `@invoice` is not allowed on line 1: the `@` sigil produces tag-wrapped block content and must appear alone on its own line. Did you mean `$invoice` (inline value)...". Changing to `$invoice` validates successfully.
- **Verified:** Wrote the doc's exact domain+concept+pipe snippet to a scratch .mthds file and ran `.venv/bin/pipelex validate bundle <file>`; reproduced the TemplateSigilSyntaxError, then fixed it by swapping `@invoice` for `$invoice` and confirmed the bundle then validates successfully. — adversarial verify: **CONFIRMED**

### `pipelex-bundle-specification.md` — 1 finding(s)

**F28 · 🔴 breaks-a-user**  <sub>(review id [16])</sub>
- **Doc:** `docs/building-methods/pipelex-bundle-specification.md:83-93`
- **Claim:** Example bundle showing bundle-level `system_prompt`: a `[pipe.extract_diagnosis]` section with `type = "PipeLLM"`, `inputs`, `output`, `prompt` (no `description` field) is presented as a working snippet illustrating that the pipe automatically inherits the bundle's system_prompt.
- **Reality:** The snippet is missing the mandatory `description` field on the pipe. Every other pipe example on this same page (extract_invoice, validate_invoice, reconcile_payment, process_vendor_invoice, schedule_payment) correctly includes `description`, but this one omits it and is not marked as elided (no `#...` or checkmark decoration) - a reader who copies it verbatim gets a hard validation failure.
- **Code evidence:** pipelex/core/pipes/pipe_blueprint.py:184 (`description: str` — required, no default, on the shared `PipeBlueprint` base that `PipeLLMBlueprint` inherits, no override in pipelex/pipe_operators/llm/pipe_llm_blueprint.py); ran `.venv/bin/pipelex validate bundle <scratch>/bundle-spec-system-prompt.mthds` on the doc's exact snippet (plus a domain-valid concept section) -> "Bundle validation failed ... Missing required fields: 'pipe.extract_diagnosis.PipeLLM.description'"
- **Verified:** Read pipe_blueprint.py to confirm `description` has no default; wrote the doc's exact snippet to a scratch .mthds file and ran the real `pipelex validate bundle` command against it, which failed with the missing-field error. — adversarial verify: **CONFIRMED**
- **Reclassified (Louis, 2026-07-12):** the real defect is inline `@record` in the prompt (`…: @record` at :93 — must be `$record`, or `@record` alone on its own line). Validation short-circuited at missing-`description` first and masked it; that half is authoring convention.

### `pipes/index.md` — 1 finding(s)

**F29 · 🔴 breaks-a-user**  <sub>(review id [21])</sub>
- **Doc:** `docs/building-methods/pipes/index.md:102-105 ([concept] table declares only ProductDescription and Tagline) and :122-126 (pipe.extract_keywords_from_tagline sets output = "Keyword[]")`
- **Claim:** The two-step 'Multi-Step Methods' example (marketing_pipeline.mthds, lines 97-144) is presented as a complete, valid pipeline: 'This defines a two-step pipeline. The PipeSequence controller description_to_tagline takes a ProductDescription as input and outputs a Tagline...'
- **Reality:** The snippet as written in the doc does not validate: pipe.extract_keywords_from_tagline declares output = "Keyword[]" but the [concept] table only defines ProductDescription and Tagline, and Keyword is not a native concept, so the real pipelex validator rejects the bundle with an Unresolved Concept error.
- **Code evidence:** Ran `.venv/bin/pipelex validate bundle --allow-signatures <scratch copy of the exact doc snippet>` -> '❌ Bundle validation failed ... Unresolved Concept: Keyword ... Concept 'Keyword' in pipe.extract_keywords_from_tagline.output is not declared in domain 'marketing' and is not native.' Also confirmed via pipelex/core/concepts/native/concept_native.py:23-38 (NativeConceptCode enum: Dynamic/Text/Image/Document/Html/TextAndImages/Number/YesNo/Date/Time/Page/JSON/SearchResult/Anything/Composite) that 'Keyword' is not a native concept.
- **Verified:** Wrote the exact doc snippet to a scratch .mthds file under the scratchpad dir and ran the real CLI validator `pipelex validate bundle --allow-signatures`; cross-checked the native concept list in pipelex/core/concepts/native/concept_native.py. — adversarial verify: **CONFIRMED**

### `pipes/pipe-operators/PipeImgGen.md` — 1 finding(s)

**F30 · 🔴 breaks-a-user**  <sub>(review id [41])</sub>
- **Doc:** `docs/building-methods/pipes/pipe-operators/PipeImgGen.md:61-63`
- **Claim:** Common image generation model handles: `base-img-gen`: Base image generation model (alias for flux-pro/v1.1); `best-img-gen`: Best quality image generation model (alias for flux-pro/v1.1-ultra); `fast-img-gen`: Fast image generation model (alias for fast-lightning-sdxl)
- **Reality:** These three model handles no longer exist in the model deck at all (neither as aliases nor presets); the deck was restructured around nano-banana/gpt-image models (consistent with the rest of the same page's own size table). A user following this doc and setting `model = "base-img-gen"` (or best-img-gen/fast-img-gen) gets a hard validation error instead of the described Flux/SDXL routing.
- **Code evidence:** pipelex/kit/configs/inference/deck/2_img_gen_deck.toml:33-39 (current aliases are best-gpt, best-gemini, default-general, default-premium, default-small — none named base-img-gen/best-img-gen/fast-img-gen); ran `.venv/bin/pipelex validate bundle` on a PipeImgGen pipe with `model = "base-img-gen"` → raised `ModelChoiceNotFoundError: Model handle 'base-img-gen' was not found in the model deck`
- **Verified:** Grepped the entire repo for base-img-gen/best-img-gen/fast-img-gen — found only in CHANGELOG.md history, none in current TOML config; read the current img_gen deck TOML; ran the real CLI (`pipelex validate bundle`) against a scratch .mthds file using model = "base-img-gen" and got a ModelChoiceNotFoundError with the actual available handle list, confirming it fails as written. — adversarial verify: **CONFIRMED**

### `pipes/pipe-operators/PipeStructure.md` — 1 finding(s)

**F31 · 🔴 breaks-a-user**  <sub>(review id [43])</sub>
- **Doc:** `docs/building-methods/pipes/pipe-operators/PipeStructure.md:103-105`
- **Claim:** prompt = """\nRead this invoice and produce a faithful textual transcript of every line item, total, and metadata: @invoice_pdf\n"""
- **Reality:** The MTHDS template engine requires the `@` block-sigil to appear alone on its own line; it cannot be used inline within a sentence as the doc example shows. The doc's snippet, if run as written, fails validation.
- **Code evidence:** Ran `.venv/bin/pipelex validate bundle <scratch>/pipestructure_ex3_invoice.mthds` (full example reproduced from the doc, with a `domain` header added since the doc snippet is a fragment like all examples on this page) — output: "Value errors: 'pipe.extract_invoice_text.PipeLLM': Value error, Template sigil error in PipeLLM prompt: Inline `@invoice_pdf` is not allowed on line 1: the `@` sigil produces tag-wrapped block content and must appear alone on its own line. Did you mean `$invoice_pdf` (inline value), or move `@invoice_pdf` onto its own line?". Moving `@invoice_pdf` to its own line (verified in a second scratch file) makes the same bundle validate successfully.
- **Verified:** Reconstructed the full example into a scratch .mthds file and ran the real parser via `.venv/bin/pipelex validate bundle`; confirmed the fix (moving the sigil to its own line) resolves it. — adversarial verify: **CONFIRMED**

### `pipes/run-modes-and-backends.md` — 1 finding(s)

**F32 · 🔴 breaks-a-user**  <sub>(review id [44])</sub>
- **Doc:** `docs/building-methods/pipes/run-modes-and-backends.md:59-68`
- **Claim:** From Python, the same knobs live on the runner: `from pipelex.pipeline.runner import PipelexRunner` ... `runner = PipelexRunner(bundle_uri="path/to/my_bundle.mthds", pipe_run_mode=PipeRunMode.DRY,)` ... `response = await runner.execute_pipeline()`
- **Reality:** The class is named `PipelexMTHDSProtocol`, not `PipelexRunner`; it takes `bundle_uris` (a list), not `bundle_uri`; and its execution method is `execute()`, not `execute_pipeline()`. Copy-pasting the snippet raises an ImportError.
- **Code evidence:** pipelex/pipeline/runner.py:100 defines `class PipelexMTHDSProtocol(MTHDSProtocol["PipeOutput"])`, not `PipelexRunner`; `grep -rn "PipelexRunner" pipelex/` returns zero matches; `git log -S "class PipelexMTHDSProtocol"` shows commit 9a64896a3 "feat(protocol)!: PipelexRunner → PipelexMTHDSProtocol — implement the MTHDS Protocol locally (#985)" performed the rename. Constructor at runner.py:108-121 takes `bundle_uris: list[str] | None` (plural), and the execution method at runner.py:137 is `async def execute(...)`, not `execute_pipeline()`.
- **Verified:** Read pipelex/pipeline/runner.py in full; grepped pipelex/ for `PipelexRunner` and `execute_pipeline` (no matches); ran `git log -S "class PipelexMTHDSProtocol"` to find the rename commit and confirm the doc was never updated after PR #985. — adversarial verify: **CONFIRMED**

### `concepts/refining-concepts.md` — 2 finding(s)

**F33 · 🟠 wrong**  <sub>(review id [9])</sub>
- **Doc:** `docs/building-methods/concepts/refining-concepts.md:149`
- **Claim:** Each inherits `ImageContent` structure (url, caption, base_64, etc.) with specific semantic meaning.
- **Reality:** ImageContent has no `base_64` field. Base64-encoded image data is carried as a data URL inside the existing `url` field, not as a separate `base_64` field.
- **Code evidence:** pipelex/core/stuffs/image_content.py:19-29 -- ImageContent fields are url, public_url, source_prompt, source_negative_prompt, caption, mime_type, width, height, filename; no `base_64` field exists anywhere in the class or its base (`grep -rni base_64 pipelex/core/stuffs/` finds no such field, only doc-string mentions of 'a base64 data URL' describing the `url` field's possible content).
- **Verified:** Read pipelex/core/stuffs/image_content.py and grepped pipelex/core/stuffs/ for base_64/base64 field definitions. — adversarial verify: **CONFIRMED**

**F34 · 🟡 misleading**  <sub>(review id [8])</sub>
- **Doc:** `docs/building-methods/concepts/refining-concepts.md:84-96`
- **Claim:** When you refine a concept, you cannot add an inline structure or specify a `structure_class_name`. ... Not allowed: `structure_class_name = "InvoiceModel"` # ❌ Not allowed
- **Reality:** `structure_class_name` is not a real TOML field on a concept under any circumstance -- it is rejected as an unknown/extra field even without `refines` present. The doc presents it as a field that becomes invalid specifically 'when you refine a concept', implying it is otherwise valid syntax. The actual field for naming an explicit structure class is `structure` used as a string (e.g. `structure = "InvoiceModel"`), which the code derives into an internal `structure_class_name` attribute (pipelex/core/concepts/concept_factory.py:309-319) -- that internal attribute name leaked into the doc's TOML example as if it were the wire field.
- **Code evidence:** pipelex/core/concepts/concept_blueprint.py:12-20 (ConceptBlueprint fields are only source, description, structure, refines, with model_config = ConfigDict(extra="forbid")); reproduced with `.venv/bin/pipelex validate bundle` on a concept with `structure_class_name = "InvoiceModel"` and no `refines` at all -> error: "Concept validation error at 'concept.Invoice.structure_class_name': Extra inputs are not permitted"
- **Verified:** Read pipelex/core/concepts/concept_blueprint.py for the ConceptBlueprint field set; ran `.venv/bin/pipelex validate bundle` against a scratch .mthds file containing `structure_class_name = "InvoiceModel"` with no `refines`, confirming it fails as an extra/unknown field regardless of refinement. — adversarial verify: **CONFIRMED**

### `concepts/define_your_concepts.md` — 1 finding(s)

**F35 · 🟠 wrong**  <sub>(review id [1])</sub>
- **Doc:** `docs/building-methods/concepts/define_your_concepts.md:150`
- **Claim:** Pipelex includes these built-in native concepts: Text, Image, Document, Html, TextAndImages, Number, Page, Dynamic, JSON, SearchResult, and Anything.
- **Reality:** The code defines 15 native concepts, not 11 as the doc states; YesNo, Date, Time, and Composite are real, usable native concepts (documented on the sibling native-concepts.md page) that this page's enumeration omits entirely.
- **Code evidence:** pipelex/core/concepts/native/concept_native.py:23-38 defines NativeConceptCode(StrEnum) with 15 members: DYNAMIC, TEXT, IMAGE, DOCUMENT, HTML, TEXT_AND_IMAGES, NUMBER, YES_NO="YesNo", DATE="Date", TIME="Time", PAGE, JSON, SEARCH_RESULT, ANYTHING, COMPOSITE="Composite"; sibling page docs/building-methods/concepts/native-concepts.md:28-41 documents all 15 including YesNo, Date, Time, Composite (added via commit de69c95ed, PR #1028)
- **Verified:** Read pipelex/core/concepts/native/concept_native.py (full NativeConceptCode StrEnum member list); cross-checked against docs/building-methods/concepts/native-concepts.md which documents all 15 members; ran `git log --oneline -- pipelex/core/concepts/native/concept_native.py` confirming YesNo/Date were added in commit de69c95ed (#1028, Smart Inputs track). — adversarial verify: **CONFIRMED**

### `concepts/inline-structures.md` — 1 finding(s)

**F36 · 🟠 wrong**  <sub>(review id [2])</sub>
- **Doc:** `docs/building-methods/concepts/inline-structures.md:79-86`
- **Claim:** "When you write `field_name = \"description\"`, Pipelex automatically creates a text field... This will put the field as optional by default, and not required."
- **Reality:** The shorthand `field_name = \"description\"` syntax makes the field REQUIRED, not optional. The doc's own Quick Example later in the same page (`invoice_number = \"The unique invoice identifier\"`) is generated as `invoice_number: str = Field(..., description=...)` (required), which is inconsistent with the tip's claim just 30 lines earlier.
- **Code evidence:** pipelex/core/concepts/helpers.py:81-88 (`normalize_structure_blueprint`): `if isinstance(field_value, str): # Simple string descriptions default to required=True (shorthand syntax implies required)` sets `required=True`. Verified end-to-end: ran `.venv/bin/pipelex build structures` on a bundle with `[concept.Task.structure]\ntitle = \"Task title\"` and the generated `structures.py` emitted `title: str = Field(..., description=\"Task title\")` — `Field(...)` with no default means the field is required, not optional.
- **Verified:** Read pipelex/core/concepts/helpers.py:81-88 and ran a real `pipelex build structures` invocation on a scratch .mthds file using only the shorthand syntax, inspecting the generated Pydantic field to confirm it is required (`Field(...)`), not `Optional`/`Field(default=None)`. — adversarial verify: **CONFIRMED**

### `pipes/csv-input-and-output.md` — 1 finding(s)

**F37 · 🟠 wrong**  <sub>(review id [17])</sub>
- **Doc:** `docs/building-methods/pipes/csv-input-and-output.md:19`
- **Claim:** `date` → `datetime.datetime` (an ISO `YYYY-MM-DD` value is accepted and parsed as midnight)
- **Reality:** A concept field with `type = "date"` coerces a CSV cell to Python `datetime.date`, not `datetime.datetime`. A distinct `datetime` field type (ConceptStructureBlueprintFieldType.DATETIME) maps to `datetime.datetime`, but the doc's flat-concept-rule table is describing the `date` type and mislabels its Python target. The 'parsed as midnight' framing is also inapplicable to a `date` (no time component exists to set to midnight).
- **Code evidence:** pipelex/codegen/resolved_fields.py:139-140 maps ConceptStructureBlueprintFieldType.DATE -> ResolvedTypeKind.DATE; pipelex/core/concepts/structure_generation/generator.py:330-332 maps ResolvedTypeKind.DATE -> python `date` (from datetime import date), NOT `datetime`; confirmed at runtime by tests/unit/pipelex/tools/tabular/test_csv_codec.py:337-338 (`assert item.born == date(1815, 12, 10)`; `assert isinstance(item.born, date)`) and the accept-table row at line 368 (`(DateRow, "1815-12-10", date(1815, 12, 10))`).
- **Verified:** Read concept_structure_blueprint.py and resolved_fields.py/generator.py to trace type -> ResolvedTypeKind -> Python annotation; cross-checked against the pinned unit-test contract in tests/unit/pipelex/tools/tabular/test_csv_codec.py which asserts the concrete runtime type is `date`. — adversarial verify: **CONFIRMED**

### `pipes/pipe-operators/PipeFunc.md` — 1 finding(s)

**F38 · 🟠 wrong**  <sub>(review id [40])</sub>
- **Doc:** `docs/building-methods/pipes/pipe-operators/PipeFunc.md:26`
- **Claim:** Function Eligibility Requirements: "**Must be an async function** (defined with `async def`)"
- **Reality:** A plain `def` (non-async) function decorated with `@pipe_func()` that otherwise meets the signature/return-type criteria IS eligible for registration and IS actually invoked (via `asyncio.to_thread`) at run time. The async requirement stated by the doc is not enforced anywhere in the registration or execution path.
- **Code evidence:** pipelex/system/registries/func_registry.py FuncRegistry.check_function_eligibility() (used by the real discovery path pipelex/system/registries/func_registry_utils.py:148-172, called from pipelex/libraries/library_manager.py:324) never calls inspect.iscoroutinefunction or otherwise checks async-ness — it only checks param count/name, WorkingMemory type annotation, and StuffContent-subclass return type. Verified live: `.venv/bin/python -c "...@pipe_func()\ndef sync_func(working_memory: WorkingMemory) -> TextContent: ...\nfunc_registry.check_function_eligibility(sync_func)"` printed `eligibility_error: None` and `is_eligible: True`. Also pipelex/pipe_operators/func/pipe_func.py _live_run_operator_pipe explicitly branches on `inspect.iscoroutinefunction(function)` and runs a sync function via `asyncio.to_thread(...)`, confirming sync functions are a first-class supported case, not an oversight.
- **Verified:** Read pipelex/system/registries/func_registry.py and func_registry_utils.py in full, traced the real discovery call chain (library_manager.py -> register_funcs_in_folder -> check_function_eligibility), then ran a live Python check via .venv/bin/python confirming a sync function passes eligibility; cross-checked pipe_func.py's execution branch that explicitly supports sync via asyncio.to_thread. — adversarial verify: **CONFIRMED**

### `pipes/pipe-operators/index.md` — 3 finding(s)

**F39 · 🟡 misleading**  <sub>(review id [33])</sub>
- **Doc:** `docs/building-methods/pipes/pipe-operators/index.md:16-23 and :27-34`
- **Claim:** docs/building-methods/pipes/pipe-operators/index.md:16-23 (Core Operators) and :27-34 (Overview) both enumerate 'the pipe operators available in Pipelex' (PipeLLM, PipeExtract, PipeImgGen, PipeSearch, PipeFunc, PipeCompose) and neither lists PipeStructure.
- **Reality:** PipeStructure is a real, documented, directly-usable pipe operator (own class, own blueprint, own doc page, own nav entry as a sibling of this page) that this index page's two enumerations of 'the pipe operators available in Pipelex' both fail to list.
- **Code evidence:** pipelex/pipe_operators/structure/pipe_structure.py:41 defines `class PipeStructure(PipeOperator[PipeStructureOutput])`, a full peer of the other six operator classes. mkdocs.yml:255 and :470 place `PipeStructure.md` directly beside `PipeLLM.md`, `PipeExtract.md`, etc. in the same 'Pipe Operators' nav section as this very index page, and docs/building-methods/pipes/pipe-operators/PipeStructure.md is a complete existing page describing it as directly user-callable (echoed at docs/building-methods/pipes/pipe-operators/PipeLLM.md:19: "call PipeStructure directly").
- **Verified:** Read pipelex/pipe_operators/*/*.py and enumerated all `class Pipe*(PipeOperator[...])` classes (7 found: PipeCompose, PipeExtract, PipeFunc, PipeImgGen, PipeLLM, PipeSearch, PipeStructure); grepped mkdocs.yml nav to confirm PipeStructure.md is a sibling page in the same nav section as this index. — adversarial verify: **CONFIRMED**

**F40 · 🟡 misleading**  <sub>(review id [35])</sub>
- **Doc:** `docs/building-methods/pipes/pipe-operators/index.md:19`
- **Claim:** docs/building-methods/pipes/pipe-operators/index.md:19: "[**`PipeExtract`**](./PipeExtract.md): Performs Optical Character Recognition (OCR) on images and PDF documents to extract text and embedded images."
- **Reality:** PipeExtract is not limited to OCR on images/PDFs — it also fetches and extracts content from web-page URLs, a capability this index page's one-line summary omits entirely, underselling the operator's actual scope relative to both the code and PipeExtract's own doc page.
- **Code evidence:** pipelex/cogt/extract/extract_worker_abstract.py:42-43 defines `is_web_page_supported()` on the extract worker, and pipelex/pipe_operators/extract/pipe_extract_blueprint.py has `render_js`/`include_raw_html` fields specific to web-page fetching; the operator's own detail page (docs/building-methods/pipes/pipe-operators/PipeExtract.md:3,7,10) states PipeExtract 'extracts structured content from documents (PDF, images, web pages)' and 'For web pages, it fetches and extracts page content,' listing `linkup-fetch` and `@default-extract-web-page` as a real extraction backend/alias.
- **Verified:** Read pipelex/cogt/extract/extract_worker_abstract.py and pipelex/pipe_operators/extract/pipe_extract_blueprint.py; cross-checked against docs/building-methods/pipes/pipe-operators/PipeExtract.md, which documents the same operator in full and explicitly covers web-page extraction. — adversarial verify: **CONFIRMED**

**F41 · ⚪ stale-detail**  <sub>(review id [34])</sub>
- **Doc:** `docs/building-methods/pipes/pipe-operators/index.md:27-34 vs :18-23 on the same page`
- **Claim:** docs/building-methods/pipes/pipe-operators/index.md:27-34, 'Overview' section: "Pipelex provides the following pipe operators:" then lists PipeLLM, PipeExtract, PipeFunc, PipeImgGen, PipeSearch — omitting PipeCompose, which the same page's 'Core Operators' section (index.md:18-23, directly above) does list.
- **Reality:** Two enumerations of the same operator set on the same page disagree with each other: 'Core Operators' includes PipeCompose, 'Overview' does not.
- **Code evidence:** pipelex/pipe_operators/compose/pipe_compose.py:39 confirms `class PipeCompose(PipeOperator[PipeComposeOutput])` is a real operator with its own doc page (docs/building-methods/pipes/pipe-operators/PipeCompose.md); the index page also has no '## PipeCompose' detail section below (unlike the other five operators, each of which gets one at index.md:35-98).
- **Verified:** Compared index.md's two lists directly line-by-line and confirmed PipeCompose is a real registered operator via pipelex/pipe_operators/compose/pipe_compose.py:39. — adversarial verify: **CONFIRMED**

### `concepts/python-classes.md` — 1 finding(s)

**F42 · 🟡 misleading**  <sub>(review id [7])</sub>
- **Doc:** `docs/building-methods/concepts/python-classes.md:109-110, :134, :171`
- **Claim:** "If you need custom validation or computed properties, you have two options: 1. Generate then copy: Run `pipelex build structures`, then copy the generated class into your own module and customize it there" (also repeated at line 134 'Copy the generated class into your own module...' and line 171 'the easiest migration path is to use `pipelex build structures` to generate the Python class, then copy it into your own module and customize it there').
- **Reality:** The doc's recommended workflow (copy the generated class's field definitions into a hand-written module) directly conflicts with the codegen tool's own embedded guidance, which recommends subclassing the generated class in a sibling module instead — because a copy silently drifts from the source concept on the next `pipelex build structures` regeneration, whereas a subclass import stays in sync.
- **Code evidence:** pipelex/codegen/emitters/python_common.py:26-27 — the generated file's own header explicitly says: "To customize a generated type, do NOT edit this file. Create a sibling module and subclass — subclasses survive regeneration". Confirmed by actually running `pipelex build structures` in scratchpad/pyclasses: the emitted structures.py header instructs subclassing, not copying, as the customization pattern.
- **Verified:** Ran `.venv/bin/pipelex build structures` against a scratch .mthds bundle and inspected the generated structures.py header comment; traced the comment text to its source in pipelex/codegen/emitters/python_common.py:18-33. — adversarial verify: **CONFIRMED**

### `pipes/pipe-controllers/index.md` — 1 finding(s)

**F43 · 🟡 misleading**  <sub>(review id [22])</sub>
- **Doc:** `docs/building-methods/pipes/pipe-controllers/index.md:23-28 ('Pipelex provides the following pipe operators: - PipeSequence ... - PipeCondition') vs docs/building-methods/pipes/pipe-controllers/index.md:10 ('While Pipe Operators perform the work, pipe controllers define the method and manage the execution logic')`
- **Claim:** "Pipelex provides the following pipe operators:" followed by a list of PipeSequence, PipeParallel, PipeBatch, PipeCondition
- **Reality:** PipeSequence/PipeParallel/PipeBatch/PipeCondition are pipe controllers (subclass PipeController, live under pipelex/pipe_controllers/), not pipe operators (subclass PipeOperator, live under pipelex/pipe_operators/). The Overview section mislabels them, directly contradicting the same page's own intro paragraph two sections above.
- **Code evidence:** pipelex/pipe_controllers/sequence/pipe_sequence.py:36 `class PipeSequence(PipeController)`; pipelex/pipe_controllers/parallel/pipe_parallel.py:49 `class PipeParallel(PipeController)`; pipelex/pipe_controllers/batch/pipe_batch.py:48 `class PipeBatch(PipeController)`; pipelex/pipe_controllers/condition/pipe_condition.py:33 `class PipeCondition(PipeController)` — all subclass PipeController (pipelex/pipe_controllers/pipe_controller.py:16), distinct from PipeOperator (pipelex/pipe_operators/pipe_operator.py:22) in the separate pipe_operators package
- **Verified:** Grepped class definitions in pipelex/pipe_controllers/ and pipelex/pipe_operators/ to confirm the four classes subclass PipeController, not PipeOperator, and reside in the pipe_controllers package. — adversarial verify: **CONFIRMED**

### `pipes/pipe-operators/PipeLLM.md` — 1 finding(s)

**F44 · 🟡 misleading**  <sub>(review id [42])</sub>
- **Doc:** `docs/building-methods/pipes/pipe-operators/PipeLLM.md:237`
- **Claim:** `reasoning_effort` | string | Reasoning depth: `"none"`, `"minimal"`, `"low"`, `"medium"`, `"high"`, `"max"`.
- **Reality:** The enum has seven valid values, including "xhigh" between "high" and "max". The PipeLLM.md table lists only six, omitting xhigh entirely. The sibling page docs/under-the-hood/reasoning-controls.md documents xhigh explicitly (with provider mapping details), confirming it's a real, supported value that this page's table simply drops.
- **Code evidence:** pipelex/cogt/llm/llm_job_components.py:10-17 — class ReasoningEffort(StrEnum): NONE="none", MINIMAL="minimal", LOW="low", MEDIUM="medium", HIGH="high", XHIGH="xhigh", MAX="max"
- **Verified:** Read pipelex/cogt/llm/llm_job_components.py ReasoningEffort StrEnum definition and cross-checked docs/under-the-hood/reasoning-controls.md which documents xhigh with provider mapping details. — adversarial verify: **CONFIRMED**

## Reclassified as authoring convention — not counted as drift

Marked, not deleted (audit trail). Per the scope ruling above.

- ~~**[6] breaks-a-user · `concepts/python-classes.md`**~~ — "Step 1: Start with inline structure" / "Step 4: Update your .mthds file" show `.mthds` TOML content
    - Doc: `docs/building-methods/concepts/python-classes.md:116-124 and :160-163` · **Convention:** Missing top-level `domain` scaffolding only — validates clean once a placeholder `domain` is added. Authoring convention, not drift (Louis, 2026-07-12).
- ~~**[13] breaks-a-user · `domain.md`**~~ — [pipe.process_invoice]\ntype = "PipeLLM"\ninputs = { invoice = "Invoice" }\noutput = "Text"\nprompt 
    - Doc: `docs/building-methods/domain.md:158-163 (also :170-175, :199-204)` · **Convention:** Missing pipe `description` scaffolding. Redundant anyway: the same blocks independently break on inline `@invoice` (kept as F for domain.md). Convention (Louis, 2026-07-12).
- ~~**[31] breaks-a-user · `pipes/pipe-controllers/PipeSequence.md`**~~ — Example steps `[pipe.summarize_text]` and `[pipe.translate_to_french]` (type = "PipeLLM") declare `i
    - Doc: `docs/building-methods/pipes/pipe-controllers/PipeSequence.md:54-64` · **Convention:** PipeLLM steps declare `inputs` but omit the `prompt` body entirely → 'unused input'. An omitted prompt body is scaffolding a reader fills in — Louis split-ruling: omitted-prompt = convention (2026-07-12).

## Pages reviewed clean (0 drift findings)

- `adapt-to-llm-prompting-style-openai-anthropic-mistral.md` (8 claims checked)
- `kick-off-a-methods-project.md` (7 claims checked)
- `packages.md` (14 claims checked)
- `pipes/pipe-operators/PipeSearch.md` (14 claims checked)
- `pipes/pipe-output.md` (14 claims checked)
- `pipes/provide-inputs.md` (22 claims checked)
- `pipes/signature-pipes.md` (14 claims checked)
- `pipes/understanding-optionality.md` (20 claims checked)
- `pipes/working-memory.md` (8 claims checked)

> Zero findings ≠ proof of perfection, but each of these pages had its checkable claims verified against current code (CLI `--help`, real parser via `validate bundle`, symbol/grep). The three Stage-0-fixed pages were re-checked; the Stage-0 fixes hold.

## Rejected findings

None. All 47 review-stage findings survived independent adversarial re-verification (0 refuted). Of the 47, 3 were later reclassified as authoring convention (above), leaving 44 counted as drift.
## Recurring defect classes (for Stage 2 batching)

Fix by class, not just page-by-page. Counts reflect the 44 drift findings (post-reclassification):

1. **Doc bundle example fails `pipelex validate bundle` even after scaffolding is filled in (~18 of the 25 breaks-a-user).** These survive placeholder `description`/`domain`/prompt injection — genuine drift:
    - Inline `@var` in a prompt string — must be `$var` inline, or `@var` alone on its own line (domain, libraries, pipelex-bundle-spec, PipeStructure).
    - Written prompt that never references a declared input → "Unused input variable" (native-concepts ×2 — the wrong-wiring case Louis kept as drift).
    - Redeclaring a native concept name (`Document`/`Image`) as custom (understanding-multiplicity).
    - Output concept used but never declared / not native (`Keyword[]` in pipes/index).
    - PipeExtract `output` ≠ `Page[]`, and its one required Image/Document input omitted (PipeSequence).
    - `nb_output` set on a PipeLLM — it is a `steps[]`/SubPipe key, not a PipeLLM field (understanding-multiplicity).
    - `batch_params` table on PipeBatch fabricated; real fields are flat `input_list_name`/`input_item_name`, and `input_list_name` omitted in the worked example (PipeBatch, 2 findings).
    - PipeSequence output-multiplicity mismatch under `batch_over` (understanding-multiplicity).
    - Single-quoted TOML basic string with literal newlines — needs `"""` (executing-pipelines).
    - Concept structure/type mismatch at dry-run combine (PipeParallel; PipeCondition unstructured concept).

2. **Parameter-table requiredness inverted vs `PipeBlueprint` (5).** `inputs` marked Required (optional: PipeParallel, PipeCompose); `output` marked optional (required: PipeCompose template mode); `result` marked required (optional: PipeSequence).

3. **Renamed Python API never propagated (3, across 2 pages).** `PipelexRunner`→`PipelexMTHDSProtocol`, `bundle_uri`→`bundle_uris`, `execute_pipeline()`→`execute()`. ⚠ **Cross-part leak:** same stale `execute_pipeline()` at `docs/under-the-hood/execution-graph-tracing.md:269` — a **Part 2** page; carry into Part 2's fix batch.

4. **Stale enumeration (5).** Native concepts 11 vs 15 (define_your_concepts); `reasoning_effort` omits `xhigh` (PipeLLM); pipe-operators/index omits PipeStructure and PipeCompose (2); deck-file list omits `4_search_deck.toml`/`x_custom_extract_deck.toml` (configure-ai-llm); img-gen handles `base-/best-/fast-img-gen` gone (PipeImgGen).

5. **Wrong default / behavioral claim (remaining).** `page_views_dpi` 150→72; PipeFunc "must be async" (sync supported via `asyncio.to_thread`); library_dirs "falls back to cwd" (no such fallback → `PipeNotFoundError`); pipe uniqueness billed as "Future Change" but shipped per-domain in v0.22.0; `add_alias_from_expression_to` semantics reversed; `structure_class_name` internal attr leaked as wire syntax; `ImageContent.base_64` field nonexistent; `[aliases]` should be `[llm.aliases]`; `date` maps to `datetime.date` not `datetime.datetime`; shorthand string field defaults to **required**, doc says optional.

## Authoring-convention carve-out (Louis, 2026-07-12)

Not drift; a reader fills these in before running. The future check must scaffold them, not flag them:

- Missing top-level `domain` in a focused example.
- Missing pipe `description` in a focused example.
- Omitted `prompt` body (the example teaches step/structure wiring, not the prompt).

**Still drift** (survives scaffolding): a written prompt that ignores its own declared input; a declared input that is functionally the subject (e.g. PipeExtract's required Image/Document); wrong field names, wrong output types, wrong enum/default values.

## Dogfood observations (feed the drift-contracts Phase 3 verdict, per D5)

- **Top derived-check candidate: `validate-doc-bundle-examples`** — extract every fenced `toml`/`mthds` bundle example under `docs/`, **inject placeholder `description`/`domain` and tolerate an omitted prompt**, then run `pipelex validate bundle`. With the scaffolding normalized, it isolates exactly the ~18 genuine breaks-a-user drifts and none of the convention cases. The placeholder-injection step is what makes it precise (Louis's insight) — without it the check would false-positive on every teaching fragment.
- **Why Stage 0's prescreen missed all of these:** `prescreen.py` only checked that fenced TOML *parses* (`tomllib`) plus config-key/path existence. These examples parse fine but fail *semantic* bundle validation. "Parses" vs "validates" is exactly where building-methods drift lives.
- **Second candidate (mechanical): `operator-param-table-vs-blueprint`** — diff each operator doc's parameter table against the pydantic model's required/optional fields (catches the 5 requiredness inversions). Needs a table parser.
- **Third: `doc-python-snippets-import`** — import-and-construct smoke check for Python snippets (catches the renamed-API class); the "run it, don't read it" discipline D6 mandates for CLI forms, applied to Python.
- **Origin skews to language-surface churn:** required-`description`, the `@`/`$` sigil split, per-domain pipe uniqueness, the runner rename, `nb_output` placement, native-concept additions — all recent changes whose examples were never re-validated. building-methods is the language reference, so it absorbs the most churn — consistent with the worst-first prediction.
