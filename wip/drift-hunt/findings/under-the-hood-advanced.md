# Drift Hunt — Stage 1 Part 2 findings: under-the-hood + advanced

> **✅ RESOLVED — Stage 2 batch S2-3 (2026-07-12).** All findings below (F1–F42) were re-verified against the code at fix time (D6) and **fixed; none was rejected-at-fix-time**. Fix-time additions (new drift F43–F45 fixed per D16, two refuted candidates, entangled corrections, one code-side deferral) are recorded in the "Stage 2 (S2-3) resolution" section at the end. ⚠ One label in the Dogfood section was corrected at fix time — see the F24 note there.

Section: **under-the-hood** (19 pages) + **advanced** (7 pages) = **26 pages**. Generated by the wave Workflow (`drift-hunt-uth-advanced`, run `wf_28527b4e-470`, 2026-07-12): one review agent per page (Sonnet-5, high effort) → adversarial verify per finding (D6). Every finding was **independently reproduced against the real code / runtime** by a second agent prompted to refute it; the D11 scaffolding carve-out and the Stage 0 blessed FP list were baked into both the review and verify prompts up front.

> **D11 carve-out — no reclassifications this part.** The scaffolding carve-out (omitted `domain` / `description` / `prompt` in a teaching bundle example is convention, not drift) reclassified 3 findings out of Part 1. It applies to **none** of Part 2: these are internals/injection pages, so their claims are Python API signatures, class/method/protocol names, module paths, config keys, enum values, and output filenames — none is a bundle-teaching example. Nothing to reclassify.

> **Load-bearing spot-checks (hand-verified against code after the run):** `execute_pipeline`→`execute` gone from `pipelex/pipeline/runner.py` (F10); `OrchestratorProtocol.execute` (not `run`) in `orchestrator_registry.py` + `direct_orchestrator.py` (F27); `pipelex.toml` google block `minimal = "minimal"` not `"low"` (F31); `PipelexHub.set_report_delegate` (not `set_reporting_delegate`) (F37); `ObserverProtocol.observe_before_run/observe_after_successful_run/observe_after_failing_run` (F38); `DataInclusionConfig` has 5th field `pipe_and_concept_registry` (F13). All held.

## Summary

- **Pages reviewed:** 26  ·  **claims checked:** 435
- **Confirmed findings (drift):** 42  ·  **reclassified as authoring convention:** 0  ·  **rejected by adversarial verify:** 0
- **Defect density:** 1.62 findings/page (42/26); 14/26 pages carry ≥1 finding
- **Clean-page caveat:** of the 12 clean pages, **5 are 8-line "Under Construction" stubs** (`advanced/{content-generator,pipe-router,reporting-delegate,secrets-provider,storage-provider}-injection.md` — frontmatter + an admonition, `claims=0`, nothing checkable). Excluding those non-pages, the *substantive* denominator is 21 pages: **14 dirty / 7 clean**, density 2.0.

| Severity | Count | Meaning |
|---|---|---|
| 🔴 breaks-a-user | 10 | documented command/snippet fails or misfires if followed verbatim |
| 🟠 wrong | 23 | factually incorrect statement about code/behavior |
| 🟡 misleading | 4 | technically defensible but leads a reader astray |
| ⚪ stale-detail | 5 | outdated minor detail, low user impact |

## Findings by page (worst-first; ranked by severity within each page)

### `under-the-hood/execution-graph-tracing.md` — 8 finding(s)

**F10 · 🔴 breaks-a-user** — *carry-in from Part 1, confirmed*
- **Doc:** `docs/under-the-hood/execution-graph-tracing.md:269`
- **Claim:** `response = await runner.execute_pipeline(...)` inside a `scoped_event_log` block.
- **Reality:** The runner method was renamed to `execute`; `execute_pipeline` no longer exists anywhere in `pipelex/`. Calling it on `runner` raises `AttributeError`. (The same page uses the correct `.execute` elsewhere, e.g. line 71.)
- **Code evidence:** `pipelex/pipeline/runner.py:137` defines `async def execute(...)`; `grep -rn "execute_pipeline" pipelex/` → zero matches.
- **Verified:** grep for the symbol (gone) + read `runner.py`. — adversarial verify: **CONFIRMED**

**F11 · 🔴 breaks-a-user**
- **Doc:** `docs/under-the-hood/execution-graph-tracing.md:291-298`
- **Claim:** `def copy_for_child(self, child_node_id: str, next_sequence: int) -> TraceContext:` — `next_sequence` shown as a plain positional parameter.
- **Reality:** Real signature is `copy_for_child(self, child_node_id: str, *, next_sequence: int)` — `next_sequence` is keyword-only (repo keyword-only convention). Calling it positionally raises `TypeError: ... takes 2 positional arguments but 3 were given`.
- **Code evidence:** `pipelex/graph/trace_context.py:66`; reproduced the `TypeError` via `.venv/bin/python`.
- **Verified:** read + ran the positional call form. — adversarial verify: **CONFIRMED** (verifier corrected the code line from :71 to **:66**)

**F12 · 🔴 breaks-a-user**
- **Doc:** `docs/under-the-hood/execution-graph-tracing.md:247-252`
- **Claim:** `manager.on_pipe_end_success(graph_id=trace_context.graph_id, node_id=..., ended_at=..., output_spec=IOSpec(...))`
- **Reality:** `GraphTracerManager.on_pipe_end_success` takes `lookup_key` (keyword-only), not `graph_id`. Following the doc raises `TypeError: ... got an unexpected keyword argument 'graph_id'`.
- **Code evidence:** `pipelex/graph/graph_tracer_manager.py:269-274`; reproduced the `TypeError`.
- **Verified:** read the signature + ran the doc's exact call. — adversarial verify: **CONFIRMED**

**F13 · 🟠 wrong**
- **Doc:** `docs/under-the-hood/execution-graph-tracing.md:38-39, 413-417`
- **Claim:** "full data … (`stuff_json_content`, `stuff_text_content`, `stuff_html_content`, `error_stack_traces` are all `true`)" with a TOML snippet listing only those four keys.
- **Reality:** `DataInclusionConfig` has a **fifth** required field `pipe_and_concept_registry: bool` (also `true` by default in `pipelex.toml`). Instantiating the config from exactly the doc's four-key block fails pydantic validation (missing required field).
- **Code evidence:** `pipelex/graph/graph_config.py:6-12`; `pipelex/pipelex.toml:305-310`; reproduced the `ValidationError`.
- **Verified:** read model + toml + constructed with only 4 fields. — adversarial verify: **CONFIRMED**

**F14 · 🟠 wrong**
- **Doc:** `docs/under-the-hood/execution-graph-tracing.md:90-95`
- **Claim:** Outputs table names files `_graphspec.json`, `_mermaid.mmd`, `_mermaid.html`, `_reactflow.html`.
- **Reality:** `save_graph_outputs_to_dir` writes `graphspec.json`, `mermaidflow.mmd`, `mermaidflow.html`, `reactflow.html` — **no leading underscore**, and the mermaid outputs are `mermaidflow.*` not `mermaid.*`.
- **Code evidence:** `pipelex/graph/graph_factory.py:157-178` (invoked from `pipelex run pipe --graph`, `pipelex/cli/commands/run/_run_core.py:255`).
- **Verified:** read the writer + traced its CLI call site. — adversarial verify: **CONFIRMED**

**F15 · 🟠 wrong**
- **Doc:** `docs/under-the-hood/execution-graph-tracing.md:430`
- **Claim:** `direction` — "Flowchart direction (TB, LR, BT, RL)".
- **Reality:** `FlowchartDirection` has exactly two members, `TOP_DOWN`/`LEFT_TO_RIGHT`, rendering only `TD`/`LR`. No BT or RL exists.
- **Code evidence:** `pipelex/tools/misc/chart_utils.py:4-13`.
- **Verified:** read the enum (single source of truth). — adversarial verify: **CONFIRMED**

**F16 · 🟡 misleading**
- **Doc:** `docs/under-the-hood/execution-graph-tracing.md:192-197`
- **Claim:** Edge Types table lists exactly four `EdgeKind` values (`CONTROL`, `DATA`, `CONTAINS`, `SELECTED_OUTCOME`).
- **Reality:** `EdgeKind` defines **seven** members — the table omits `BATCH_ITEM`, `BATCH_AGGREGATE`, `PARALLEL_COMBINE`, which a reader would treat the table as exhaustively covering when consuming/rendering a GraphSpec.
- **Code evidence:** `pipelex/graph/graphspec.py:65-73`.
- **Verified:** read the `EdgeKind` StrEnum. — adversarial verify: **CONFIRMED**

**F17 · ⚪ stale-detail**
- **Doc:** `docs/under-the-hood/execution-graph-tracing.md:454-464` (and line 199)
- **Claim:** `IOSpec` snippet lists `name, concept, content_type, preview, size, digest, data, data_text, data_html` with `data: Any | None`.
- **Reality:** Real `IOSpec` also has `extra: dict[str, Any] = Field(default_factory=dict)` — and that omitted field is exactly where the optional-presence marker the doc's own text describes lives (`output_digest_is_optional()` reads `output_spec.extra.get("optional")`). `data`'s type is narrower than `Any` (`str | dict | list[str] | list[dict] | None`).
- **Code evidence:** `pipelex/graph/graphspec.py:206-224, 227-236`.
- **Verified:** read the model + the optional-marker reader. — adversarial verify: **CONFIRMED** (verifier note: doc line 199 attributes `optional` to `EdgeSpec`; it never explains the value is staged via `IOSpec.extra` first — that omission is the defect. Fix: add `extra` to the snippet + correct `data`'s type.)

### `under-the-hood/init-cli-flows.md` — 5 finding(s)

**F22 · 🟠 wrong**
- **Doc:** `docs/under-the-hood/init-cli-flows.md:307-309`
- **Claim:** `find_project_root()` markers are `.git`, `pyproject.toml`, `setup.py`, `setup.cfg`, `package.json`, `.hg`.
- **Reality:** The real `PROJECT_ROOT_MARKERS` frozenset includes `.pipelex` **first** — a directory containing only `.pipelex/` IS treated as a project root, contradicting the documented list.
- **Code evidence:** `pipelex/system/configuration/config_loader.py:13,16`.
- **Verified:** read the constant definitions. — adversarial verify: **CONFIRMED**

**F23 · 🟠 wrong**
- **Doc:** `docs/under-the-hood/init-cli-flows.md:403`
- **Claim:** `accept_gateway_terms` "Sets gateway terms acceptance (defaults to `false` if omitted)".
- **Reality:** When omitted, the code does **not** write `false` — it skips `update_service_terms_acceptance()` entirely, leaving existing acceptance state untouched ("no write at all").
- **Code evidence:** `pipelex/cli/agent_cli/commands/init_cmd.py:218-223` (`if accept_terms is not None:`).
- **Verified:** read `_configure_backends()`. — adversarial verify: **CONFIRMED**

**F24 · 🟠 wrong**
- **Doc:** `docs/under-the-hood/init-cli-flows.md:404`
- **Claim:** `pipelex-agent init` `--config` accepts a `telemetry_mode` (str) field with values `"off"`/`"anonymous"`/`"identified"`.
- **Reality:** `telemetry_mode` appears nowhere in `init_cmd.py`; the command's own `--config` schema is `{backends, primary_backend, accept_gateway_terms}`. Passing `telemetry_mode` is silently ignored.
- **Code evidence:** `grep telemetry_mode pipelex/cli/agent_cli/commands/init_cmd.py` → 0 hits; docstring schema at lines 322-329/359-370.
- **Verified:** grep + read the command's own schema docstring. — adversarial verify: **CONFIRMED**

**F25 · 🟡 misleading**
- **Doc:** `docs/under-the-hood/init-cli-flows.md:408-421`
- **Claim:** Flow diagram places "Step 4: Configure telemetry — Copy template + apply mode" after routing, just before the JSON output.
- **Reality:** Telemetry-template copy happens **early** ("Step 1.6", before backend/routing config); the code's actual Step 4 is "mark inference setup completed" (unrelated to telemetry), and a Step 5 (remote-config cache priming) is missing from the diagram entirely. No "apply mode" behavior exists (no `telemetry_mode` is read — see F24).
- **Code evidence:** `pipelex/cli/agent_cli/commands/init_cmd.py:385-414`.
- **Verified:** read `agent_init_cmd()` body vs the mermaid diagram. — adversarial verify: **CONFIRMED**

**F26 · 🟡 misleading**
- **Doc:** `docs/under-the-hood/init-cli-flows.md:432`
- **Claim:** "Key Differences" table: `pipelex-agent init` output is "Structured JSON via `agent_success()` / `agent_error()`".
- **Reality:** By default `pipelex-agent init` emits **Markdown** (`# Pipelex initialized`), JSON only via `--format json`; and the function called is `agent_success_formatted()` — a distinct function from the always-JSON `agent_success()` the doc names.
- **Code evidence:** `pipelex/cli/agent_cli/commands/init_cmd.py:341-344, 428`; `agent_output.py:378-397`.
- **Verified:** read the `CliOutputFormat` default + call site + both functions. — adversarial verify: **CONFIRMED** (verifier suggested the row read: "Markdown by default (`agent_success_formatted()`/`agent_error()`), JSON via `--format json`")

### `under-the-hood/image-handling-in-llm-prompts.md` — 4 finding(s)

**F18 · 🟠 wrong**
- **Doc:** `docs/under-the-hood/image-handling-in-llm-prompts.md:281-311`
- **Claim:** `ImageRegistry.register_image` returns a 1-based number directly (`len(self._images) + 1`, tracked via `_url_to_number`); worked example returns 1, 1, 2.
- **Reality:** `register_image` returns a **0-based** index (`return len(self._images) - 1`); the doc's exact call sequence returns 0, 0, 1. The 1-based `[Image N]` display number is produced later at render time. `_url_to_number` doesn't exist on the class.
- **Code evidence:** `pipelex/tools/jinja2/image_registry.py:27-46`; ran the doc's exact sequence → `0, 0, 1`.
- **Verified:** read the class + ran the example. — adversarial verify: **CONFIRMED**

**F19 · 🟠 wrong**
- **Doc:** `docs/under-the-hood/image-handling-in-llm-prompts.md:136-155`
- **Claim:** `with_images` example output is an indented 4-line nested structure (`text_and_images:` / `  text: …` / `  images: [Image 1]` / `page_view: [Image 2]`).
- **Reality:** Actual output for that exact input is three un-indented lines with no `text:`/`images:` sub-labels (`text_and_images: Welcome to the guide` / `[Image 1]` / `page_view: [Image 2]`) — `TextAndImagesContent.render_with_images` overrides and never labels its sub-fields.
- **Code evidence:** `pipelex/core/stuffs/text_and_images_content.py:76-90`; `structured_content.py:52-79`; reproduced the exact objects + real render.
- **Verified:** ran the doc's exact example through the real renderer. — adversarial verify: **CONFIRMED**

**F20 · 🟡 misleading**
- **Doc:** `docs/under-the-hood/image-handling-in-llm-prompts.md:355-358`
- **Claim:** Factory-time validation table shows two distinct conditions with two distinct quoted error strings ("Cannot use with_images on direct Image" / "Type X has no nested image fields").
- **Reality:** Neither quoted string exists in the source, and there is exactly **one** validation branch — a direct `Image` and a no-nested-images type both raise the same `WithImagesFilterError` message.
- **Code evidence:** `pipelex/pipe_operators/shared/template_image_analyzer.py:99-114`; `shared/exceptions.py:4`; grep for both strings → 0 hits.
- **Verified:** grep for the quoted strings + read the single branch. — adversarial verify: **CONFIRMED**

**F21 · ⚪ stale-detail**
- **Doc:** `docs/under-the-hood/image-handling-in-llm-prompts.md:337-339`
- **Claim:** `StuffArtefact.render_with_images(self, registry, *, text_format)` with `registry` positional, delegating `content.render_with_images(registry, text_format=...)`.
- **Reality:** Real signature is `render_with_images(self, *, registry, text_format)` — `registry` is keyword-only — and it delegates `render_with_images(registry=registry, text_format=text_format)`.
- **Code evidence:** `pipelex/core/stuffs/stuff_artefact.py:363-389`; confirmed via `inspect.signature`.
- **Verified:** read + `inspect.signature`. — adversarial verify: **CONFIRMED**

### `advanced/observer-provider-injection.md` — 5 finding(s)

**F38 · 🔴 breaks-a-user**
- **Doc:** `docs/advanced/observer-provider-injection.md:28-39, 83-100, 162-170, 183-193`
- **Claim:** `ObserverProtocol` defines `before_run()` / `successful_run()` / `failing_run()`, and every example observer implements those names.
- **Reality:** The real hook names are `observe_before_run` / `observe_after_successful_run` / `observe_after_failing_run`. Because `ObserverProtocol` is subclassed as a plain base (not `@runtime_checkable`), a class built exactly as the doc instructs **silently no-ops** — the mis-named override is shadowed by the base stub, no exception raised. A user's logging/metrics/storage code never runs, with no error.
- **Code evidence:** `pipelex/observer/observer_protocol.py:17,24,31`; call sites `pipe_router_protocol.py:22,35,48`; reproduced the silent no-op via `.venv/bin/python`.
- **Verified:** read protocol + call sites + ran the doc's exact class. — adversarial verify: **CONFIRMED** (dogfood: a smoke test wouldn't catch this — needs to invoke the hook and assert a side effect fired)

**F39 · 🔴 breaks-a-user**
- **Doc:** `docs/advanced/observer-provider-injection.md:87,94,100,185,188-189,192-193`
- **Claim:** Observers access the pipe code as `pipe_job.pipe_code` (e.g. `f"Starting pipe: {pipe_job.pipe_code}"`).
- **Reality:** `PipeJob` has no `pipe_code` field/property; the code lives at `pipe_job.pipe.code`. Every example logging `pipe_job.pipe_code` raises `AttributeError`.
- **Code evidence:** `pipelex/pipe_run/pipe_job.py:13-23`; `pipe_abstract.py:92`; confirmed absence via `python -c`.
- **Verified:** read `PipeJob` + the pipe abstract. — adversarial verify: **CONFIRMED**

**F40 · 🔴 breaks-a-user**
- **Doc:** `docs/advanced/observer-provider-injection.md:209,214`
- **Claim:** `from pipelex import Pipelex` then `Pipelex.make()`.
- **Reality:** `pipelex/__init__.py` exports only `log, pretty_print, pretty_print_md, pretty_print_url` — no `Pipelex`. The import raises `ImportError`; the correct form (used elsewhere in the docs) is `from pipelex.pipelex import Pipelex`.
- **Code evidence:** `pipelex/__init__.py`; reproduced the `ImportError`; correct form at `docs/building-methods/pipes/executing-pipelines.md:32`.
- **Verified:** read `__init__.py` + ran the import. — adversarial verify: **CONFIRMED**

**F41 · 🟠 wrong**
- **Doc:** `docs/advanced/observer-provider-injection.md:143-147`
- **Claim:** `LocalObserver` writes `before_run.jsonl`, `successful_run.jsonl`, `failing_run.jsonl`.
- **Reality:** The event-type values are `before_run`, `after_successful_run`, `after_failing_run` → files `before_run.jsonl`, `after_successful_run.jsonl`, `after_failing_run.jsonl`. Only the first matches.
- **Code evidence:** `pipelex/observer/local_observer.py:11-14, 29`.
- **Verified:** read the enum + file-naming logic. — adversarial verify: **CONFIRMED**

**F42 · 🟠 wrong**
- **Doc:** `docs/advanced/observer-provider-injection.md:61-67`
- **Claim:** In the failing-run payload, "error details are handled separately by the pipe router" (implying the payload has no error).
- **Reality:** `_after_failing_run` puts the error directly in the payload under key `"error"` (`PayloadKey.ERROR`) before calling `observe_after_failing_run`. The doc's comment is wrong about the payload contents.
- **Code evidence:** `pipelex/pipe_run/pipe_router_protocol.py:37-48`; `observer_protocol.py:13`.
- **Verified:** read `_after_failing_run` + the `PayloadKey` enum. — adversarial verify: **CONFIRMED**

### `under-the-hood/stuffartefact-and-image-rendering.md` — 3 finding(s)

**F32 · 🔴 breaks-a-user**
- **Doc:** `docs/under-the-hood/stuffartefact-and-image-rendering.md:149-168` (+ 67, 185, 195, 212, 228)
- **Claim:** `ImageRenderable` protocol and all snippets show `render_with_images(self, registry, *, text_format)` with `registry` positional (e.g. `value.render_with_images(registry, text_format=format)`).
- **Reality:** `registry` is **keyword-only** in every real definition and call site (`render_with_images(self, *, registry, text_format)`). Calling it the documented way raises `TypeError`.
- **Code evidence:** `pipelex/tools/jinja2/image_renderable.py:36-42`; same shape across `image_content.py`, `list_content.py`, `text_and_images_content.py`, `structured_content.py`, `stuff_artefact.py`; reproduced the `TypeError`.
- **Verified:** read protocol + all impls + ran the positional call. — adversarial verify: **CONFIRMED**

**F33 · 🟠 wrong**
- **Doc:** `docs/under-the-hood/stuffartefact-and-image-rendering.md:51, 223-241, 335`
- **Claim:** A default field-iterating `render_with_images` lives on the `StuffContent` base class (`pipelex/core/stuffs/stuff_content.py`).
- **Reality:** `StuffContent` has no such method; it lives on the **`StructuredContent` subclass** (`structured_content.py:52-71`) with a materially different body (recurses into lists/tuples/dicts, calls `rendered_for_prompt` on nested content, not a flat `str()` fallback). A plain `StuffContent` subclass is not `ImageRenderable` (`isinstance(TextContent(...), ImageRenderable)` → `False`).
- **Code evidence:** grep `render_with_images` in `stuff_content.py` → nothing; `structured_content.py:52-71`.
- **Verified:** grep + read both files + isinstance check. — adversarial verify: **CONFIRMED**

**F34 · 🟠 wrong**
- **Doc:** `docs/under-the-hood/stuffartefact-and-image-rendering.md:212-221`
- **Claim:** `TextAndImagesContent.render_with_images` does `if self.text: parts.append(self.text.rendered_plain())`.
- **Reality:** The real code calls `self.text.rendered_for_prompt(text_format=text_format)` — it passes `text_format` through, so rendering is format-sensitive where the doc's `rendered_plain()` implies it isn't.
- **Code evidence:** `pipelex/core/stuffs/text_and_images_content.py:76-90`.
- **Verified:** read the method vs the doc snippet. — adversarial verify: **CONFIRMED**

### `under-the-hood/dry-run-mock-generation.md` — 3 finding(s)

**F5 · 🟠 wrong**
- **Doc:** `docs/under-the-hood/dry-run-mock-generation.md:166-171`
- **Claim:** `PipeSpec` example field `extract_talent: ExtractTalent | str = Field(..., examples=list(ExtractTalent))` demonstrates the `examples` mechanism.
- **Reality:** Neither `extract_talent` (field) nor `ExtractTalent` (type) exists anywhere. `PipeSpec`/`PipeExtractSpec` have no such field; importing `ExtractTalent` raises `ImportError`.
- **Code evidence:** grep across `pipelex/builder/pipe/*.py` + tree → no matches; `python -c` confirms field absent + import fails.
- **Verified:** grep + `PipeSpec.model_fields` check. — adversarial verify: **CONFIRMED**

**F6 · 🟠 wrong**
- **Doc:** `docs/under-the-hood/dry-run-mock-generation.md:97-104`
- **Claim:** `DryRunFactory.make_dry_run_factory(cls, object_class, snake_case_field_names=None, pascal_case_field_names=None)` with the two field-name params as positional-or-keyword.
- **Reality:** Real signature has a bare `*` — `snake_case_field_names`/`pascal_case_field_names` are keyword-only. The doc's implied positional call raises `TypeError: ... takes 2 positional arguments but 4 were given`.
- **Code evidence:** `pipelex/cogt/content_generation/dry_run_factory.py:339-345`; reproduced the `TypeError`.
- **Verified:** read signature + ran positional call. — adversarial verify: **CONFIRMED**

**F7 · ⚪ stale-detail**
- **Doc:** `docs/under-the-hood/dry-run-mock-generation.md:36-37`
- **Claim:** Mermaid flowchart node `dry_run_pipes` (between `pipelex validate --all` and `WorkingMemoryFactory.make_mock_inputs`).
- **Reality:** No `dry_run_pipes` symbol exists; the real intermediate step is `BundleValidator.validate_pipes()` — which the doc's own entry-point table (line 53) names correctly. The diagram node is internally inconsistent and non-greppable.
- **Code evidence:** grep `dry_run_pipes` → 0 hits; `pipelex/pipeline/bundle_validator.py:167`.
- **Verified:** grep + cross-check against the doc's own table. — adversarial verify: **CONFIRMED**

### `under-the-hood/build-time-elaboration.md` — 2 finding(s)

**F1 · 🟠 wrong**
- **Doc:** `docs/under-the-hood/build-time-elaboration.md:81`
- **Claim:** synthetic codes pass `is_pipe_code_valid` ("snake_case + length").
- **Reality:** `is_pipe_code_valid` only runs the snake_case regex — there is **no length check** (no min/max, no constant comparison) in it or its `is_snake_case` helper.
- **Code evidence:** `pipelex/core/pipes/validation.py:126-127`; `pipelex/tools/misc/string_utils.py:262-263`.
- **Verified:** read both functions + grep for any `len()`/`max_length` near the check. — adversarial verify: **CONFIRMED**

**F2 · 🟠 wrong**
- **Doc:** `docs/under-the-hood/build-time-elaboration.md:82`
- **Claim:** The post-elaboration re-validate wraps any `ValidationError` in `BundleElaboratorError` "with context naming the originating pipe".
- **Reality:** That `except ValidationError` raises `PipelexUnexpectedError` (not `BundleElaboratorError`, which is used only for the earlier synthesis-time checks in the same method), and the message names the bundle `domain` + the full sorted list of synthetic pipe codes, not a single originating pipe.
- **Code evidence:** `pipelex/core/interpreter/bundle_elaborator.py:93-100` (imports at :5 and :11).
- **Verified:** read the `elaborate` classmethod + both exception imports. — adversarial verify: **CONFIRMED**

### `under-the-hood/distributed-content-generation.md` — 2 finding(s)

**F3 · 🟠 wrong**
- **Doc:** `docs/under-the-hood/distributed-content-generation.md:54`
- **Claim:** On the worker, `model_class_from_json_schema()` (`schema_to_model_factory.py`) rebuilds a live Pydantic class.
- **Reality:** No module-level `model_class_from_json_schema()` exists; the real entry point is the classmethod `SchemaToModelFactory.make_from_json_schema(schema, *, class_name)`. Importing the doc's name raises `ImportError`.
- **Code evidence:** `pipelex/cogt/content_generation/schema_to_model_factory.py:82`; grep (only the doc matched) + reproduced `ImportError`.
- **Verified:** grep tree + read the class + ran the literal import. — adversarial verify: **CONFIRMED**

**F4 · 🟠 wrong**
- **Doc:** `docs/under-the-hood/distributed-content-generation.md:66`
- **Claim:** The schema cache uses "double-check locking".
- **Reality:** It holds a **single** mutex for the entire check-then-generate sequence (no unlocked pre-check / re-check) — the code's own comment documents this as a deliberate alternative to double-check locking (trading cross-schema cache-hit contention for killing same-schema thundering herd).
- **Code evidence:** `pipelex/cogt/content_generation/schema_to_model_factory.py:94-110`.
- **Verified:** read `make_from_json_schema` + its inline locking comment. — adversarial verify: **CONFIRMED**

### `under-the-hood/error-model.md` — 2 finding(s)

**F8 · 🟠 wrong**
- **Doc:** `docs/under-the-hood/error-model.md:196` (+ sample at 179-188)
- **Claim:** Render step is four functions `render_llm_error()` / `render_img_gen_error()` / `render_extract_error()` / `render_search_error()`, using `InferenceErrorFamily.LLM_COMPLETION`.
- **Reality:** There is exactly one function `render_inference_error(metadata, *, classification, family, model_desc, model_handle)` dispatching on `family` via internal lookup dicts — the four `render_*_error()` names don't exist. The sample's enum member `LLM_COMPLETION` also doesn't exist; the real member is `LLM`.
- **Code evidence:** `pipelex/cogt/inference/error_render.py:81, 29`; grep for the four names → 0 hits; real call sites use `render_inference_error(..., family=InferenceErrorFamily.LLM, ...)`.
- **Verified:** read `error_render.py` + grep the four names + real call sites. — adversarial verify: **CONFIRMED**

**F9 · ⚪ stale-detail**
- **Doc:** `docs/under-the-hood/error-model.md:229-231`
- **Claim:** `def fill_model_and_provider(self, model_handle, backend_name) -> None:` (both params positional).
- **Reality:** Real signature is `fill_model_and_provider(self, model_handle, *, backend_name)` — `backend_name` is keyword-only. The doc's snippet omits the `*`.
- **Code evidence:** `pipelex/cogt/exceptions.py:68`.
- **Verified:** read the full method definition. — adversarial verify: **CONFIRMED**

### `under-the-hood/orchestrator-plugins.md` — 2 finding(s)

**F27 · 🔴 breaks-a-user** — *page-wide rename*
- **Doc:** `docs/under-the-hood/orchestrator-plugins.md:11,13,24,39-45,47,55,85,201`
- **Claim:** `OrchestratorProtocol` blocking method is `run` — used in the protocol code block, the seam diagram (`orchestrator.run(...)`), prose ("`run` is the BLOCKING arm"), the lazy-import note, the validator docstring, and the worked Temporal example.
- **Reality:** The method is `execute`, not `run` — a rename that never propagated into this page. Every occurrence is wrong.
- **Code evidence:** `pipelex/plugins/orchestrator_registry.py:34-49`; `pipelex/runtime_bridge/direct_orchestrator.py:33`; `payloads.py:41-42`; the external `pipelex-temporal` reference plugin also uses `execute`.
- **Verified:** read all four in-tree sources + the reference plugin. — adversarial verify: **CONFIRMED**

**F28 · 🟠 wrong**
- **Doc:** `docs/under-the-hood/orchestrator-plugins.md:158`
- **Claim:** "Two slots take no explicit `setup()` parameter" (task manager + isolated-execution probe).
- **Reality:** **Three** slots skip the explicit-`setup()`-parameter step — `pipe_run` also has none (only `content_generator` and `pipe_router`, of five slots, take one). The section undercounts by one and implies `pipe_run` follows explicit-param→thunk→default precedence when it only ever resolves via thunk-or-default.
- **Code evidence:** `pipelex/pipelex.py:176-196` (setup signature), `:513-517` (comment + `pipe_run` resolution).
- **Verified:** read the full `Pipelex.setup()` body, comparing all five slot blocks. — adversarial verify: **CONFIRMED**

### `under-the-hood/pipe-routing-and-execution.md` — 2 finding(s)

**F30 · 🟠 wrong**
- **Doc:** `docs/under-the-hood/pipe-routing-and-execution.md:123-135`
- **Claim:** `PipeRouter._run_pipe_job(self, pipe_job, wfid=None)` calling `pipe_job.pipe.run_pipe(job_metadata=..., working_memory=pipe_job.working_memory, output_name=..., pipe_run_params=...)`.
- **Reality:** Real signature is `_run_pipe_job(self, pipe_job) -> PipeOutput` — **no `wfid` param** anywhere (`grep wfid pipelex/` → 0); it calls the hydration-safe `pipe_job.get_working_memory()` (not the possibly-`None` `.working_memory` field) and forwards an extra `library_crate=pipe_job.library_crate` kwarg the snippet omits.
- **Code evidence:** `pipelex/pipe_run/pipe_router.py:13-24`; grep `wfid` → 0 hits.
- **Verified:** read `pipe_router.py` + grep the invented param. — adversarial verify: **CONFIRMED**

**F29 · 🟠 wrong**
- **Doc:** `docs/under-the-hood/pipe-routing-and-execution.md:79-81`
- **Claim:** "Why Dynamic Classes Matter" box: when a PipeJob is serialized for Temporal, **Kajson** embeds `__class__`/`__module__`, and the receiver must have those registered to deserialize.
- **Reality:** For this exact path (dynamic-concept content crossing to a Temporal worker), the code **deliberately bypasses** Kajson's `__class__`/`__module__` and stamps pipelex-private `__pipelex_class__`/`__pipelex_module__` markers instead, resolved against the per-workflow `ClassRegistry` — "keeps kajson's universal decoder out of the loop at the Temporal data-converter boundary".
- **Code evidence:** `pipelex/core/memory/working_memory.py:571-621`.
- **Verified:** read `dump_for_transport` / `_encode_content_with_class_markers`. — adversarial verify: **CONFIRMED** (verifier precision: the evidence is specifically about `WorkingMemory` — carried on `PipeOutput`/`PipeJob.working_memory_raw` — which is exactly where the doc's "dynamic concept classes" live, so it's the right path to cite.)

### `under-the-hood/test-profile-configuration.md` — 2 finding(s)

**F35 · 🔴 breaks-a-user**
- **Doc:** `docs/under-the-hood/test-profile-configuration.md:205-219`
- **Claim:** The generated fixture file contains `LLM_MODEL_BACKEND_PAIRS: list[tuple[str, str]]`, `IMG_GEN_MODEL_BACKEND_PAIRS`, `EXTRACT_MODEL_BACKEND_PAIRS` (tuples).
- **Reality:** The generator emits `LLM_COMBOS` / `IMG_GEN_COMBOS` / `EXTRACT_COMBOS` / `SEARCH_COMBOS` as `list[ModelCombo]` (`ModelCombo('model','backend')` instances, not tuples), plus a `SEARCH_COMBOS` block the doc never mentions. Importing the doc's names raises `ImportError`.
- **Code evidence:** `pipelex/cli/dev_cli/commands/preprocess_test_models_cmd.py:522-556`; committed output `tests/integration/pipelex/fixtures/_generated_model_sets.py`.
- **Verified:** read the generator + the committed generated file. — adversarial verify: **CONFIRMED**

**F36 · ⚪ stale-detail**
- **Doc:** `docs/under-the-hood/test-profile-configuration.md:184`
- **Claim:** Preprocessing discovers models from backend TOMLs in `pipelex/backends/*.toml`.
- **Reality:** The real path is `inference/backends/*.toml` under the resolved config dir (project `.pipelex/` override → packaged `pipelex/kit/configs/`). No `pipelex/backends/` directory exists.
- **Code evidence:** `pipelex/system/configuration/config_loader.py:128-131`; `find` confirms `pipelex/backends/` absent, `pipelex/kit/configs/inference/backends/` + `.pipelex/inference/backends/` present.
- **Verified:** read `backends_dir_path` + the dir constants + `find`. — adversarial verify: **CONFIRMED**

### `advanced/index.md` — 1 finding

**F37 · 🔴 breaks-a-user**
- **Doc:** `docs/advanced/index.md:34`
- **Claim:** `hub.set_reporting_delegate(MyReportingDelegate())`
- **Reality:** `PipelexHub` has no `set_reporting_delegate`; the real setter is `set_report_delegate` (no "-ing"). The snippet raises `AttributeError`.
- **Code evidence:** `pipelex/hub.py:228`; confirmed via `hasattr` check.
- **Verified:** grep hub setters + ran the method-existence check. — adversarial verify: **CONFIRMED**

### `under-the-hood/reasoning-controls.md` — 1 finding

**F31 · 🟠 wrong**
- **Doc:** `docs/under-the-hood/reasoning-controls.md:244-253, 262-263`
- **Claim:** Google's `effort_to_level_map` maps `MINIMAL` → `"low"` (snippet `minimal = "low"`; note "MINIMAL and LOW both map to `low` … both produce `ThinkingLevel.LOW`").
- **Reality:** The shipped config maps google `minimal = "minimal"` (→ `ThinkingLevel.MINIMAL`), a genuinely distinct SDK level dispatched separately from LOW in `get_reasoning_level()`. (All other provider blocks in the snippet matched — only google's MINIMAL row drifted.)
- **Code evidence:** `pipelex/pipelex.toml:175-182`; `pipelex/plugins/google/google_config.py:17-20, 44-52`.
- **Verified:** line-by-line diff of the doc's TOML snippets vs `pipelex.toml` (all four provider blocks + both budget maps) + read the google enum/dispatch. — adversarial verify: **CONFIRMED**

## Clean pages (no findings)

Substantive, verified clean: `architecture-overview.md` (24 claims — pure directory/class-name enumeration, all matched), `codegen-projections.md` (26 — Stage-0 cross-repo fix held; every module path/CLI flag/exit-code/stamp checked out), `index.md` (under-the-hood), `inference-backend-plugins.md` (34 — plugin registry/factory names all current), `runtime-bridge-and-transport.md`, `secrets-provider-plugins.md`, `storage-provider-plugins.md`.

Non-pages (8-line "Under Construction" stubs, nothing checkable): `advanced/{content-generator,pipe-router,reporting-delegate,secrets-provider,storage-provider}-injection.md`.

## Stage 0 seeds — outcomes

- **`image-handling-in-llm-prompts.md`** (Stage 0 confirmed defect, fixed): the two dead paths (`llm_prompt_spec.py`, `template_image_analyzer.py`) are corrected — **not re-flagged**. But the review surfaced **four new, deeper** findings on the same page (register_image indexing, with_images output shape, single validation branch, keyword-only signature) that the Stage 0 path-existence prescreen could never reach.
- **`init-cli-flows.md`** (Stage 0 confirmed defect, fixed): the `mthds_schema.json` copy claim is gone — **not re-flagged**. Five *new* findings surfaced (project-root marker omission, terms-default misstatement, fabricated `telemetry_mode`, step-order diagram drift, output-format row).
- **`codegen-projections.md`** (Stage 0 judgment, fixed): the minimal-cross-repo-pointer fix **held** — 0 findings.
- **`execution-graph-tracing.md`** (Part 1 carry-in): F10 (`execute_pipeline`) **confirmed**, plus 7 more.

## Dogfood observations (feed the drift-contracts Phase 3 verdict, per D5)

The dominant drift classes here are all **mechanically detectable** — the reviewers repeatedly flagged this section as the strongest evidence yet for automated doc-vs-code contracts:

1. **Keyword-only-convention drift in doc code snippets** (F6, F9, F11, F21, F30, F32) — a doc shows a Python signature/call without the bare `*` the repo's own keyword-only convention now requires; the positional call form raises `TypeError`. The repo already has a `check-keyword-only` AST guard for *source*; a sibling check that parses fenced `python` blocks in `docs/under-the-hood/*.md` and validates call-sites/signatures against live `inspect.signature` would catch this whole class. **Strong new candidate: `doc-python-signatures-vs-live`.**
2. **Renamed API/method never propagated into docs** (F3, F8, F10, F27, F35, F37, F38, + the F30 invented param) — the single biggest class. A symbol-existence check on doc-referenced Python names (import-and-`getattr`) catches all of these. Extends the Part-1 `doc-python-snippets-import` candidate.
3. **Embedded `pipelex.toml` snippet vs shipped config** (F13 missing required field, F31 wrong value) — a line-diff of fenced TOML config blocks against the real `pipelex.toml` catches both. The `reasoning-controls.md` reviewer explicitly called this out ("doc TOML snippets vs shipped config" contract).
4. **Stale enumerations** (F15 direction values, F16 EdgeKind, F24 fabricated `telemetry_mode` *field*) — enum-membership drift; a check that resolves a documented enum's members against the StrEnum definition. *(Label corrected at fix time: F24's original "fabricated `telemetry_mode` values" was wrong — the values are real `PostHogMode` members (`off`/`anonymous`/`identified`, `telemetry_config.py:27-32`) and `docs/setup/telemetry.md` documents them correctly; what's fabricated is the FIELD on `pipelex-agent init --config`, which accepts no such key.)*
5. **Silent-no-op protocol drift** (F38) — the nastiest: a mis-named override against a non-`@runtime_checkable` protocol base neither errors nor type-checks; only invoking the hook and asserting a side effect catches it. A contract tied to `observer_protocol.py`'s signatures forcing a review of `observer-provider-injection.md` is the pragmatic mitigation.

## Rejected / reclassified findings (audit trail)

- **0 rejected** by adversarial verify (all 42 review-stage findings held; 6 carried minor evidence corrections, folded in above).
- **0 reclassified** as authoring convention (D11 doesn't apply to internals/injection pages — no bundle-teaching examples here).

## Stage 2 (S2-3) resolution — fix-time record (2026-07-12)

One fix agent per dirty page (Fable/Sonnet mix), each re-verifying every finding against the code before patching (D6). Gates: `make docs-check` + `make check`. Outcome:

- **F1–F42: all FIXED, 0 rejected-at-fix-time.** Every finding reproduced exactly as recorded; several verifier evidence lines were re-confirmed live (signatures via `inspect`, renders and imports via `.venv/bin/python`).
- **F24 fixed at its real site** (`init-cli-flows.md` `--config` table row removed); `docs/setup/telemetry.md` untouched, per the Checkpoint-1 record. Its class label in the Dogfood section above was corrected (field fabricated, values real).
- **Entangled corrections folded into parent fixes** (same snippet/section, would have left the block inconsistent): error-model's File→Purpose reference table carried the same four phantom `render_*_error()` names as F8; observer payload examples omitted the always-present `pipeline_run_id` and the closing example was missing its `get_pipelex_hub` import (F40/F42 blocks); stuffartefact's ListContent fallback (`str(item)` → `rendered_markdown()`) and the filter's `TextFormat` default (F32 blocks).

### New drift found at fix time (D16 — same pages, adversarially verified before fixing)

**F43 · 🟠 wrong — FIXED** — `docs/under-the-hood/image-handling-in-llm-prompts.md`, Runtime validation table: quoted error `"X does not implement the ImageRenderable protocol"` exists nowhere; the table's convention is verbatim quoting (its other rows match the code word-for-word). Real filter-path message is the `Jinja2ContextError` text at `pipelex/tools/jinja2/jinja2_with_images_filter.py:69-75`. Row now quotes it verbatim. (The verifier also noted a second, undocumented `TypeError` path via `StuffArtefact.render_with_images`, `stuff_artefact.py:383-388` — not added, since documenting a new path is new content, and the table is not framed as exhaustive.)

**F44 · 🟠 wrong — FIXED** — `docs/under-the-hood/execution-graph-tracing.md`, ReactFlow config table: `layout_direction` said "Dagre layout direction (TB, LR)", but the config value is a `FlowchartDirection` (`top_down`/`left_to_right`, `reactflow_config.py:38`, shipped default `left_to_right` in `pipelex.toml`); TB/LR are internal Dagre codes. A user following the row writes an invalid config value. Row fixed; Dagre conversion noted.

**F45 · 🟡 misleading — FIXED** — same page, Mermaid config table: `style.theme` enumerated "(default, dark, forest, neutral)" as the valid values, omitting `base` (`MermaidTheme`, `mermaid_config.py:9-14`). Added.

### Refuted at fix time (first refutations of the campaign — audit trail)

- **TraceContext snippet omits `tracer_key`/`emit_graph_events`/`emit_usage_events`** — REFUTED: the snippet is an illustrative excerpt, never framed as the full class; nothing the omission would falsify. Same judgment as the campaign's existing excerpt standard.
- **GraphSpec snippet omits `pipe_registry`/`concept_registry` + `to_json`'s `serialize_as_any=True`** — REFUTED: same excerpt standard; the page's `pipe_and_concept_registry` data-inclusion mentions are consistent with, not contradicted by, the omission.
- *(Methodology note: the fix-time finder over-flags relative to the Stage-1 pipeline — 2 of 5 fix-time candidates died in verification vs 0 of 109 in Stage 1. The D6 verify step is what keeps D16 honest.)*

### Recorded, not fixed

- **Code-side:** stale attribution in the `ImageRenderable` docstring (`pipelex/tools/jinja2/image_renderable.py:29` says "StuffContent (base)" — the implementation lives on `StructuredContent`) → `wip/drift-hunt/deferred-code-findings.md` item 4 (D17: Stage 2 is docs-only).
- **Unproven observation** (no in-tree evidence possible): `pipe-routing-and-execution.md`'s "Key components" bullet says the backend data converter relies on Kajson subclass preservation for `WorkingMemory` content — mildly in tension with the deferred-hydration design the page itself describes, but the data converter lives in the closed `pipelex-temporal` repo, so the claim can't be checked against code from here. Not a finding; noted for whoever next touches that page with `pipelex-temporal` open.
