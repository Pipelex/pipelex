# Datetime track — the native `Date` concept (as-built)

Status: **COMPLETE 2026-07-07.** All phases shipped, gates green. Design: `datetime-design.md` (approved, DT1–DT8). Based on main at v0.38.0. Track order (`README.md`): YesNo → **Datetime (this doc)** → Smart Inputs, one release wave. This doc is now the as-built record and the PR reviewer's guide; the phase-by-phase log and checkpoint states below capture exactly what landed and why.

## ⏩ Session handoff — resume here (cold start)

**Where the code lives.** The Date work is its own branch `feature/native-date` (NOT `feature/Smart-inputs`), tip **`c32c7595d`**. It is stacked: `feature/native-date` = the YesNo tip (`772c628c6`, = `origin/feature/Smart-inputs`) + the 6 Date phase commits (`0b6d201bb` → `2d7b3d503`) + the plan-doc rewrite (`dd3db9026`) + two bot-review fix commits (`978698b5c`, `c32c7595d`). Local `feature/Smart-inputs` was reset back to `origin` so it carries only YesNo. Everything is pushed.

**The PR.** **#1029** "Add native Date concept", base **`feature/Smart-inputs`** (so the diff is Date-only), head `feature/native-date`. It is **stacked on the still-open YesNo PR #1028** (base `dev`, head `feature/Smart-inputs`, still OPEN). ➜ **When #1028 merges to `dev`, retarget #1029 to `dev`** (GitHub usually auto-retargets on base-branch merge; verify).

**What's done.** All 5 phases + DT8 (checkpoints 1–4 below). Two bot-review rounds fully arbitrated and **all threads resolved (0 open)**:

- Round 1 (on `978698b5c`): fixed two real DT8-adjacent generator bugs (`build inputs` emitted an unparseable `"date_date"` placeholder for a Date input; temporal `default_value` rendered as module-qualified `datetime.datetime(...)` → `AttributeError` at eval) + three tightenings; dismissed the impossible bare-scalar Case-1 claim (no dead branch), deferred the real list-array gap to `case1-bare-date-arm-gap.md`.
- Round 2 (on `c32c7595d`): three test-oracle tightenings + one doc sigil fix (`@ticket` → `$ticket`, which the sigil validator rejects inline when the id is a declared input).

**CI.** Green on `978698b5c` across py3.11–3.14 tests + typechecks + both bot *checks* (Greptile pass, cubic pass). `c32c7595d` (test-only + one doc line) pushed after; **re-verify its CI went green** on resume.

**What remains (the goal loop's tail).** The finalize gstack `/review` **has now been run** (2026-07-07, Opus 4.8, testing + maintainability + adversarial-red-team fan-out with pointer-only context). It found one real defect the PR introduced and applied it plus low-risk polish; see the "gstack /review finalize round" subsection below. Only thing genuinely left: **retarget #1029 → `dev` once YesNo #1028 merges** (GitHub usually auto-retargets on base merge; verify). The optional bot re-trigger on the later commits was skipped as low-value (round-2/3 changes are test + doc + comment only; bots were already happy on `978698b5c`).

**Constraints (kept for any future sub-agent):** clean solid software, no over-engineering, don't guard impossible scenarios; on any doubt, defer as a new `.md` under `wip/inputs/`. For `/code-review` fan-outs specifically: pointer-only context (a commit SHA / diff / working-tree files), never the plan or rationale.

**Deferred notes (open):** `wip/inputs/case1-bare-date-arm-gap.md`, `scalar-envelope-arm-asymmetry.md`, `loader-vs-factory-date-split-duplication.md`, `container-default-temporal-codegen-gap.md`, `structure-field-fidelity-guard.md`; shared YesNo/Date LLM-output ergonomics; the release-wave cross-repo sweep (see the bottom section).

## As-built summary — for PR reviewers

**What shipped:** one new native concept `Date` — *"A calendar date, optionally with a time of day — as precise as its source states."* Content class `DateContent(date: datetime.date, time: datetime.time | None)`: a required calendar date plus an *optional* time (its UTC offset kept verbatim on the `time`'s tzinfo). The whole point is document-fidelity: an LLM producing a `Date` states the time only when the source does — no fabricated midnights — and the output's precision *is* the document's. See `datetime-design.md` §2 for the one-vs-three-concepts rationale.

**Where to look, by concern (all one commit per phase):**

- **Concept core** (`0b6d201bb`): `pipelex/core/stuffs/date_content.py` (the class + a `field_validator` that closes pydantic's lax temporal coercions — see the guard note below), `NativeConceptCode.DATE` wired into the four exhaustive enum matches + factory + registry, Number/YesNo-style accessors (`Stuff.is_date`/`as_date`, `WorkingMemory`/`PipeOutput.main_stuff_as_date`), a `datetime.time` arm in `html_rendering.py`. Tests pin the render matrix, the kajson + transport-hydration round-trips (offset preserved — the distributed-decode safety net), and `refines = "Date"`.
- **Inputs** (`3ca1068f9`): the inputs-file loader converts a **top-level** TOML date/datetime literal to a `DateContent` (it then rides the existing case-1.3 seam as a bare content instance — no mthds-python protocol change); a bare *time-of-day* anywhere is rejected (`InputsTimeOnlyNotSupportedError`, renamed from the wholesale `InputsDatetimeNotSupportedError`); nested date/datetime are left for the factory/pydantic. `StuffContentFactory` / `StuffFactory` gain Date arms (envelope + bare object + strict-ISO string, parsed **date-first** so a date-only string never fabricates a time). `DateContent.rendered_pretty` is overridden (the base crashed on real date objects). e2e bundle under `tests/e2e/pipelex/pipes/date/`.
- **LLM output** (`b735ebce8`): a test pins that `output = "Date"` takes the object path with `DateContent`'s schema (both field descriptions) — no new generation machinery (DT7 defers ergonomics to the shared YesNo/Date follow-up).
- **DT8 — the severable field-type fix** (`6dd17e452`): structure-field `type = "date"` used to generate `datetime.datetime` (forcing a fabricated time on every extracted date); now `date` → `datetime.date` and a new `type = "datetime"` → `datetime.datetime`. Mirrored across the blueprint layer (`concept_structure_blueprint.py`, `generator.py`) **and** the spec/authoring layer (`concept_spec.py`); default-value validation splits (a `date` field rejects a datetime default). Docs + breaking changelog entry.
- **Docs/wrap** (`2d7b3d503`): `native-concepts.md`, `provide-inputs.md`, CHANGELOG, bookkeeping.

**The one guard worth understanding** (`date_content.py` `_reject_lax_temporal`): pydantic's lax mode would silently corrupt data against the concept's own fidelity contract — a bare `int`/`float` or an **all-digit string** read as epoch seconds, and a `datetime` on the `date` field silently truncated (dropping its time + offset). The validator rejects all three (raising `ValueError` so pydantic wraps it into a `ValidationError` the input path catches). The factory's ISO parser rejects all-digit strings too, for consistency.

**Deferred (nothing lost):** `wip/inputs/scalar-envelope-arm-asymmetry.md` and `wip/inputs/loader-vs-factory-date-split-duplication.md` (design tradeoffs to resolve when Smart Inputs unifies the input paths); the shared YesNo/Date LLM-output-ergonomics follow-up; and the per-release cross-repo sweep (schema copies, MTHDS spec tables, mthds-python/mthds-js mirrors, the cross-repo spec suite, skills, cookbook `type = "date"` regen). See "Deferred to the release-wave sweep" at the bottom.

How the log below reads: each phase is checked off, and each CHECKPOINT's "state" line records what landed, the commit, decisions taken, and anything that fought back.

## 0. Cold-start context — read this first

**What ships:** one new native concept `Date` — "A calendar date, optionally with a time of day — as precise as its source states." Content = required `datetime.date` + optional `datetime.time` (UTC offset rides on the time's `tzinfo`). Rationale, rejected alternatives (one-vs-three), timezone policy, rendering, and non-goals are all in `datetime-design.md` — do not relitigate them here.

**Key mechanism facts** (verified 2026-07-07, line numbers approximate):

- **Adding a native concept** = enum value in `NativeConceptCode` (`pipelex/core/concepts/native/concept_native.py:20`) + arms in its exhaustive matches (`structure_class`, `is_composite`, `is_text_concept`, `is_dynamic_concept`) + a `case` in `ConceptFactory.make_native_concept` (`pipelex/core/concepts/concept_factory.py:98`) + content-class registration in `CoreRegistryModels.STUFF` (`pipelex/core/registry_models.py:92`). The library auto-loads natives from the enum (`concept_library.py:63`); reference validation and class-name lookups iterate the enum, so they pick the new value up automatically. Exhaustive `match/case` style means pyright walks you to every remaining site — no `case _` anywhere.
- **The content-class precedent** is `NumberContent` (`pipelex/core/stuffs/number_content.py`): one file per class in `pipelex/core/stuffs/`, overriding `short_desc`, `rendered_plain`, `rendered_html`, `rendered_markdown`, `rendered_json`. `structure_class_name` is auto-derived as `f"{enum_value}Content"` → the class MUST be named `DateContent`.
- **Bottom-up input dispatch** lives in `StuffFactory.make_stuff_from_stuff_content_or_data` (`pipelex/core/stuffs/stuff_factory.py:229`). Case 1.3 (bare `StuffContent` instance) infers the concept generically from the class name (`DateContent` → `native.Date`) — **works with zero code change** once the enum value exists. Case 2 (envelope): 2.1 str-content currently *requires* Text-compatibility (`stuff_factory.py:470`) — must learn the Date-compatible arm; content being a bare `datetime.date`/`datetime.datetime` object (TOML envelope) has no arm today and lands in the fallthrough.
- **`StuffContentFactory.make_content_from_value`** (`pipelex/core/stuffs/stuff_content_factory.py:12`) is where value→content conversion happens (special case: str + `TextContent`). Signature is typed `value: dict[str, Any] | str` — widens for temporal values.
- **Cross-repo gotcha:** `StuffContentOrData` / `PipelineInputs` live in the **mthds-python** package (`mthds/protocol/pipeline_inputs.py`, a pinned dependency — NOT in this repo). The union admits `str | Sequence[str] | StuffContentAbstract | Sequence[StuffContentAbstract] | dict[str, Any]` — bare `datetime` objects are NOT admitted. **Core-scope answer: don't touch the protocol.** The CLI loader converts TOML temporal literals into `DateContent` instances (admitted via case 1.3) before they hit the seam; datetime objects *nested inside* envelope/structured dicts pass fine (`dict[str, Any]`). Protocol widening to bare scalars rides Smart Inputs D10 with the rest of the downstream wave.
- **The error being retired:** `InputsDatetimeNotSupportedError` (`pipelex/cli/commands/run/exceptions.py:7`), raised by the recursive walk `_assert_no_datetime_values` (`pipelex/cli/commands/run/_inputs_file_loader.py:111`), caught at `_run_core.py:180` and `agent_cli/commands/run/stdin_resolver.py:200`. Tests: `tests/unit/pipelex/cli/commands/run/test_inputs_file_loader.py:99` (parametrized over the TOML temporal literal flavors) and `tests/unit/pipelex/cli/test_stdin_resolver.py:345`.
- **Already free, verify only:** LLM structured output ships `model_json_schema()` of the content class (`pipe_llm.py:249` dispatch, object path via `assignment_models.py:95`) — `output = "Date"` works day one, and DT2's field descriptions carry the anti-fabrication instruction into the schema. Dry-run mocks use polyfactory (`WorkingMemoryFactory.make_mock_content`, `working_memory_factory.py:174`), which synthesizes `date`/`time` natively. JSON cleaning already ISO-serializes all temporal types (`tools/misc/json_utils.py:58`). Structured-content HTML rendering ISO-renders `datetime`/`date` (`core/stuffs/html_rendering.py:53`) — check whether a `datetime.time` arm is missing.
- **Internal JSON serialization (kajson) — verified equipped, must be round-trip-tested.** Two different "JSON"s coexist and must not be confused. (a) The *inputs file* surface (`inputs.json`) is plain stdlib JSON — no kajson, no class metadata; bare ISO strings stay Text there until Smart Inputs (DT5's no-sniffing rule stands). (b) The *internal wire* is kajson (`../kajson` repo): `kajson/kajson.py` registers stock encoder/decoder pairs for the whole temporal family — `datetime.date`, `datetime.datetime`, `datetime.time`, fixed-offset `datetime.timezone`, `ZoneInfo`, `timedelta` — with offset-hardened wire handling for exactly DT2's subtle case (a bare `time` carrying a fixed-offset tzinfo; kajson's own comments note a ZoneInfo on a bare time destroys the offset, reinforcing DT3's offsets-only policy). So `DateContent` needs **no kajson changes** — but the round-trip must be pinned by tests because of the distributed stakes: per the Temporal work, a content class that fails payload *decode* inside a workflow does not fail loudly, it **hangs** (converter exceptions retry forever). The cross-process path: `WorkingMemory.dump_for_transport()` (`working_memory.py:551`) dumps in pydantic *python mode* — real `date`/`time` objects sit in the transport dict (its "JSON-safe" docstring is only true for kajson-or-pydantic-aware wires, not stdlib `json.dumps`) — then hydration goes `hydrate_working_memory` → `hydrate_content` → `model_validate` (`runtime_bridge/primitives/hydration.py:141`), which accepts both real temporal objects (kajson road) and ISO strings (pydantic json-mode road). Existing test module to extend: `tests/unit/pipelex/runtime_bridge/primitives/test_hydration.py`.
- **DT8 sites** (severable field-type fix): `ConceptStructureBlueprintFieldType.DATE` (`concept_structure_blueprint.py:44`) maps to Python `datetime.datetime` in the structure generator (`structure_generation/generator.py:375` and `:569`); default-value validator checks `isinstance(..., datetime)` (`concept_structure_blueprint.py:180`); stale TODO at `:64`. Docs using `type = "date"`: `docs/building-methods/concepts/inline-structures.md`, `define_your_concepts.md` (+ cookbook pages, cross-repo).

**House rules that bite here:** TDD (tests first, red→green); keyword-only args (bare `*` after the subject — `make cko` gates); error classes only in `exceptions.py` modules; no `case _` in enum matches; `make agent-check` after code changes; `make agent-test` before wrapping a session; `make tb` for a quick boot check; `make gep` regenerates error pages; no hardcoded counts in docs/comments; changelog entries under `[Unreleased]`.

## Phase 1 — Concept core (DT1–DT4)

Tests first, then make them green:

- [x] **Tests** in `tests/unit/pipelex/core/stuffs/date_content/` (split per the neighbouring `yes_no_content/` layout — one TestClass per module: `test_date_content_renders.py`, `test_date_content_smart_dump.py`, `test_date_content_serialization.py`, shared `test_data.py`): construction (date-only; date + naive time; date + offset time); ISO-string parsing on the dict path; the render matrix — `rendered_plain`/`rendered_markdown`/`rendered_html` give truncated ISO 8601 (`2026-07-07`, `2026-07-07T15:40:00`, `2026-07-07T15:40:00+02:00`), `rendered_json` gives the two-field form; `short_desc` distinguishes "a date (...)" vs "a date and time (...)"; `to_datetime()` on the with-time case and the raise on the no-time case; `model_json_schema()` contains both field descriptions.
- [x] **Serialization round-trip tests**: `kajson.dumps` → `kajson.loads` in each precision state, typed equality + class-registry resolution by name (`test_date_content_serialization.py`). Cross-process transport road added to `test_hydration.py`: a `Date` stuff, one inside a `ListContent` (marker path), offset preserved end-to-end, plus the ISO-string road (json-serialized transport dict → hydrate).
- [x] **`DateContent`** in `pipelex/core/stuffs/date_content.py`: `date: datetime.date` (required), `time: datetime.time | None = None`, `Field(description=...)` on both (DT2 anti-fabrication), `rendered_*` + `short_desc` (DT4), `to_datetime()`. Added a `field_validator("date","time", mode="before")` that rejects `int`/`float` — empirically the `time` field lax-coerces a bare int to seconds-of-day, so the DT6 no-epoch-seconds guard belongs on the class (raises `TypeError`, pydantic wraps to `ValidationError`).
- [x] **Enum + matches:** `DATE = "Date"` + arms in `structure_class`, `is_composite`, `is_text_concept`, `is_dynamic_concept` (pyright walked to each).
- [x] **Factory arm** with the DT1 description.
- [x] **Registry:** `DateContent` in `CoreRegistryModels.STUFF`.
- [x] **`html_rendering.py`:** added an explicit `case time():` ISO arm (was landing in the `case _:` fallback).
- [x] Number-mirrored accessors: `Stuff.is_date`/`as_date`, `WorkingMemory.get_stuff_as_date`/`main_stuff_as_date`, `PipeOutput.main_stuff_as_date`.
- [x] `make tb`, `make agent-check` (all green), targeted pytest green.

### CHECKPOINT 1 — concept exists and boots

State: **reached 2026-07-07.** `DateContent` exists, boots, round-trips through kajson and the transport hydration path with the offset preserved. `make agent-check` green (pyright 0 / mypy Success / ruff / plxt / cko), `make tb` green, full `make agent-test` green. Micro-decision settled: `to_datetime()` with `time is None` **raises** `DateContentError` (new class in `stuffs/exceptions.py`) — no silent midnight. Extra vs the original checklist: the DT6 lax-coercion guard was pulled forward into the content class, and the Number/YesNo-style typed accessors were added for family consistency. A fanned-out code review surfaced (and this checkpoint fixed) the full set of pydantic lax-coercion leaks the guard must close — see the micro-decision below.

## Phase 2 — Inputs (DT5): TOML literals in, rejection narrowed

- [x] **Tests first.** Reworked `test_inputs_file_loader.py`: rejection narrowed to bare time-of-day (top-level and nested); new cases assert top-level local date → date-only `DateContent`, local datetime → naive `DateContent`, offset datetime → offset preserved, and that a nested date/datetime is left in place. Factory unit tests in `test_stuff_content_factory.py` (date obj, datetime obj split, ISO date-only never fabricates a time, ISO datetime with offset, refining subclass, non-ISO rejected) and `data.py`/`test_stuff_factory_implicit_memory.py` envelope cases (2.15–2.20 + case 1.3, refining `DueDate`, error cases naming `native.Date` / `ISO 8601`). Updated `test_stdin_resolver.py`.
- [x] **`StuffContentFactory.make_content_from_value`:** widened `value` typing (`… | datetime.date`) and added a `DateContent` arm + `_make_date_content` / `_parse_iso_temporal` helpers — date obj; datetime obj split via `.date()`+`.timetz()` (offset kept); ISO `str` parsed **date-first** so a bare date never fabricates a midnight; dict rides `model_validate`.
- [x] **`StuffFactory` case 2 (envelope):** case 2.1e — a bare `datetime.date`/`datetime.datetime` routes through the resolved concept's content class (Date-compatible only, else a typed error naming `native.Date`); case 2.1f — a str under a Date-compatible (non-Text) concept builds `DateContent` from the ISO string; the str-arm error message now names both `native.Text` and `native.Date`. Case-list docstring updated.
- [x] **Loader** (`_inputs_file_loader.py`): replaced `_assert_no_datetime_values` with `_convert_temporal_inputs` — top-level date/datetime → `DateContent` (enters the seam as case 1.3), `_reject_time_of_day` rejects a bare time anywhere, nested date/datetime left in place. Docstrings updated.
- [x] **Error class:** renamed `InputsDatetimeNotSupportedError` → `InputsTimeOnlyNotSupportedError` (message: a time of day alone has no date to attach to — include the date or quote as a string). Updated docstring, `user_action`, both catch sites (`_run_core.py`, `stdin_resolver.py` incl. its `error_type` string), regenerated error pages (`make gep`).
- [x] **e2e:** new `tests/e2e/pipelex/pipes/date/date_departure/` bundle (Date input pipe + Date output pipe), `test_date_inputs_run.py` (`gha_disabled` subprocess feeding a TOML offset-datetime literal, offset preserved through the loader), `test_date_dry_run.py` (Date output mocked by polyfactory; Date input flows through).
- [x] **Dry-run mock check:** polyfactory synthesizes `DateContent` with no generator quirks — no fixes needed.
- [x] `make agent-check` (all green); targeted sweep 2058 passed.

### CHECKPOINT 2 — TOML temporal literals run end-to-end

State: **reached 2026-07-07.** Error class settled: `InputsTimeOnlyNotSupportedError`. No protocol-typing friction at the seam — the loader converting top-level literals to `DateContent` means they ride case 1.3 (a `StuffContentAbstract`, already admitted by `StuffContentOrData`), so **no mthds-python change was needed** (as §0 predicted). Notable finds fixed this checkpoint: (1) a `datetime.datetime.fromisoformat` on a date-only string fabricates a midnight, so the ISO parser must try `date.fromisoformat` first; (2) `pretty_print` of a `DateContent` crashed — the base `rendered_pretty` feeds `smart_dump()`'s real date objects to stdlib json — fixed by overriding `rendered_pretty` to render the ISO form (any displayed Date output would otherwise crash). `make gep` also surfaced a pre-existing, unrelated doc gap (the v0.38.0 storage/secrets provider error pages were never committed) — left out of scope; only the Date/time-only error pages are staged here. Fanned-out review round: fixed one real bug — the ISO parser's `date.fromisoformat` accepts the basic `YYYYMMDD` form, which let an all-digit epoch-looking string slip past `DateContent`'s no-epoch-seconds guard (now rejected in `_parse_iso_temporal` too, with a test). One duplication finding (the date/datetime split written in both the loader and the factory) deferred as a design tradeoff — see `wip/inputs/loader-vs-factory-date-split-duplication.md`.

## Phase 3 — LLM output verification (DT7)

- [ ] Locate how `output = "Number"` is covered in PipeLLM tests and mirror for `Date`: assert the object path is selected (not the Text path) and the schema handed to content generation is `DateContent`'s with both field descriptions.
- [ ] Optional, non-gating: one live-gateway smoke of an extraction that should produce date-only vs date+time (manual; not CI).

No new generation machinery: the leaner scalar-native generation form is explicitly deferred to the YesNo LLM-output-ergonomics follow-up (design DT7).

## Phase 4 — DT8: fix the `date` structure-field misnomer (severable — may be dropped without touching Phases 1–3)

- [x] **Tests first:** generator tests assert `type = "date"` emits `datetime.date` (schema `format: date`) and `type = "datetime"` emits `datetime.datetime` (`format: date-time`); blueprint tests assert default-value validation (date accepts a date and rejects a datetime; datetime requires one). Updated the pre-existing normalization test (`birthdate` now `date`).
- [x] `ConceptStructureBlueprintFieldType`: added `DATETIME = "datetime"`; pyright walked the exhaustive matches (blueprint validation arm, `_validate_default_value_type`, both generator mapping sites, the list-item recursive group).
- [x] `DATE` arms flip to `datetime.date` (generator import + `exec_globals` gain `date`; default-value check now `isinstance(date) and not isinstance(datetime)`); stale line-64 TODO removed (default-value type is validated).
- [x] Docs: `inline-structures.md` — split the `date` section into `date` (calendar date, `format: date`) and `datetime` (timestamp, `format: date-time`), added the field-type-vs-`Date`-concept note, fixed the generated-code example. `define_your_concepts.md`'s `issue_date` example is already a calendar date (correct post-DT8), detail lives on the linked page.
- [x] Regenerated the MTHDS schema (via `make agent-check`) — the `datetime` field-type enum lands in the gitignored `derived/mthds_schema.json`; downstream copies sync in the release wave.
- [x] Changelog: breaking `### Changed` entry — `date` fields no longer carry a time, `datetime` field type added. No in-repo `.mthds` uses `type = "date"`; cookbook improvements are noted for the cross-repo sweep (not edited here).

### CHECKPOINT 3 — DT8 landed or consciously dropped

State: **LANDED 2026-07-07.** DT8 shipped (not dropped). `type = "date"` → `datetime.date` (`format: date`), new `type = "datetime"` → `datetime.datetime` (`format: date-time`), default-value validation split accordingly, docs + breaking changelog entry in place. `make agent-check` green. Design decision honored: the DATE default-value check rejects a datetime (`isinstance(date) and not isinstance(datetime)`) — a datetime default would carry a time the `date` field silently drops, the exact fabrication DT8 exists to prevent. Stale line-64 TODO (`# TODO: date translator for default_value`) removed — defaults are validated as real date/datetime objects, no translator. Fanned-out review round: the flip left the **spec layer** (`pipelex/builder/concept/concept_spec.py`, the AI-authoring convenience format that mirrors blueprints via `to_blueprint()`) out of step — a reachable break where a spec `type = "date"` with a calendar-date default would fail. Mirrored the same change there (added spec `DATETIME`, flipped the DATE default-value check, walked pyright to every spec match site) with tests, since leaving the authoring path broken is not a complete DT8. Note for the release-wave sweep: cookbook `type = "date"` extractions (invoice/DPE) improve for free but must be re-generated; the downstream schema copies (mthds/vscode-pipelex/mthds-ui) pick up the new field-type enum.

## Phase 5 — Docs, changelog, wrap

- [x] `docs/building-methods/concepts/native-concepts.md`: `Date` table row + a `DateContent` section (date + optional time, fidelity note, `output = "Date"` example, `main_stuff_as_date` reader), mirroring Number/YesNo.
- [x] Inputs docs: `provide-inputs.md` warning box flipped to a tip — TOML date/datetime literals are native `Date` inputs, bare time-of-day is the remaining unsupported case (kept surgical; full rewrite rides Smart Inputs).
- [x] `CHANGELOG.md` under `[Unreleased]`: added native `Date` concept (pitch + TOML literal support + reserved-code breaking note); breaking `### Changed` for the narrowed inputs rejection (`InputsTimeOnlyNotSupportedError`); DT8 breaking entry already landed in Phase 4.
- [x] Track bookkeeping: `wip/inputs/README.md` (Datetime step → done, next = Smart Inputs) and this plan's checkpoint states updated.
- [x] Full gates: `make agent-check` + `make agent-test`.

### CHECKPOINT 4 — track complete, hand off to the next train car

State: **track complete 2026-07-07.** The native `Date` concept ships end to end on `feature/Smart-inputs` (Phases 1–4, commits `0b6d201bb` → `6dd17e452`, plus this Phase 5 docs/wrap commit). All gates green. Deferred (in `wip/inputs/`): the scalar-envelope arm asymmetry and the loader-vs-factory date-split duplication (both design tradeoffs to resolve when Smart Inputs unifies the input paths); the shared YesNo/Date LLM-output-ergonomics follow-up; and the release-wave cross-repo sweep (schema copies, MTHDS spec tables, mthds-python/mthds-js mirrors, the cross-repo spec suite, skills, cookbook `type = "date"` regen). Next train car: the Smart Inputs step (`smart-inputs-design.md`).

### PR round — #1029 (stacked on YesNo #1028), bot-review pass

The Date work went out as its own PR **#1029**, based on `feature/Smart-inputs` so the diff is Date-only (retargets to `dev` once YesNo #1028 merges). CI green across py3.11–3.14 + both bot *checks* pass. Greptile + cubic + Codex posted inline findings; a fanned-out arbitration pass verified each against the code and fixed the genuine wins, dismissed one impossible-scenario claim, and deferred one out-of-scope gap:

- **Two real generator bugs, both DT8-adjacent, fixed:** (1) `build inputs` emitted an unparseable placeholder (`"date_date"`) for a `Date` input because `ConceptRepresentationGenerator._generate_basic_value` had no temporal branch — the `build inputs` → `run` round-trip was broken; added `datetime.date`/`datetime`/`time` → ISO-example branches. (2) `StructureGenerator._format_default_value` rendered a temporal default via `repr()` → module-qualified `datetime.datetime(...)`, but generated code imports the *bare* classes (`from datetime import date, datetime`), so eval raised `AttributeError`; now emits `datetime.fromisoformat(...)`/`date.fromisoformat(...)` (offset-preserving, uses the imported bare names; `exec_globals` already carries both). The `date`-with-default case was pre-existing and is fixed too. Both pinned by round-trip tests.
- **Three low-risk tightenings:** the ISO-datetime factory test now also asserts the *date* component; `native-concepts.md` says "top-level" TOML literal (matching `provide-inputs.md`); the `InputsTimeOnlyNotSupportedError` hint unquotes its datetime example (a quoted value is a TOML string → Text, the opposite of "include the date") — error page regenerated via `make gep`.
- **One dismissed, one deferred:** a claimed missing bare-`datetime.date` arm in `StuffFactory` Case 1 is unreachable (the loader pre-converts top-level scalars to `DateContent`; the `StuffContentOrData` protocol union admits no bare datetimes) — no dead branch added. The one real gap it surfaced — a top-level TOML *array* of date literals errors cleanly instead of building `ListContent[DateContent]` — is unsupported-by-design list surface, deferred to Smart Inputs; captured in `wip/inputs/case1-bare-date-arm-gap.md`. No changelog entry: A/B are fixes inside the still-`[Unreleased]` Date feature, never shipped.

### gstack `/review` finalize round — 2026-07-07

The plan's finalize step ran: gstack `/review` (Opus 4.8) with a testing + maintainability + adversarial-red-team fan-out, each given pointer-only context (branch + base, computes its own diff — no plan/rationale, so the eyes stay independent). Scope check CLEAN; CI verified fully green; my own critical pass (SQL/race/shell/LLM-trust/enum-completeness) clean — enum consumers are all specific-value lookups or generic enum iterations that pick up `DATE` automatically, and the DT8 `exec()` codegen path is injection-safe (validated temporal objects → `.isoformat()` can't inject). One real defect + low-risk polish applied, two design tradeoffs deferred:

- **Real defect fixed — a stale, self-contradicting test.** `tests/unit/pipelex/cli/test_run_core_execution.py::test_toml_datetime_input_file_exits` asserted the *old* rejection behavior (a top-level TOML datetime → exit 1) that Phase 2 reversed (it's now converted to a `DateContent`). Untouched by the diff, it mocked no runner and passed only because execution failed 1 for an unrelated downstream reason — false coverage, and a docstring documenting the opposite of shipped behavior. Phase 2 had updated `test_inputs_file_loader.py` + `test_stdin_resolver.py` but this CLI-level test slipped. Repurposed to a bare time-of-day (`deadline = 12:00:00`) — the one temporal literal the loader still rejects — so it now exercises the real `InputsTimeOnlyNotSupportedError` catch site, and asserts the `"Failed to load input file"` framing to pin that the exit comes from that path (not a downstream failure). Renamed → `test_toml_time_only_input_file_exits`.
- **Polish.** Dropped a misleading parenthetical in `concept_representation_generator.py` (the temporal example arms are `is`-identity checks, not `isinstance`, so the "checked before date: datetime is a date subclass" ordering rationale was wrong/misleading). Added the two untested `_reject_lax_temporal` arms on the `time` field (datetime-on-time, all-digit-string-on-time — only the int arm was pinned). Added generator tests for `type = "list", item_type = "date"`/`"datetime"` (the DT8 list-item mapping was untested).
- **Deferred (design tradeoffs, per the "defer on doubt" rule):** `container-default-temporal-codegen-gap.md` (the DT8 scalar `fromisoformat` fix doesn't recurse into list/dict defaults — narrow, pre-existing, errors not silent) and `structure-field-fidelity-guard.md` (generated `date`/`datetime` structure fields don't carry the native `DateContent` anti-fabrication guard — likely an intentional non-goal, but the decision is now recorded). The red-team's third finding (top-level date-list gap) was already the existing `case1-bare-date-arm-gap.md` deferral — confirming that call. No changelog entry: all fixes are inside the still-`[Unreleased]` Date feature.

## Deferred to the release-wave sweep (NOT this plan's scope — listed so nothing is lost)

Shared per-release wave with YesNo + Smart Inputs (see `README.md`): MTHDS spec native-concepts tables (`mthds/docs/language/concepts.md`, `mthds/docs/spec/mthds-format.md`); schema-copy sync via the `mthds-schema-sync` skill; `mthds-python` protocol widening for bare temporal scalars (rides Smart Inputs D10) + any native-list mirrors; `mthds-js` mirrors; cross-repo spec-suite rows; `mthds-plugins` skills (`mthds-inputs`, `mthds-build`); editor tooling completion lists (vscode-pipelex). Plus the YesNo/Date shared follow-up: LLM-output ergonomics for scalar natives.

Our Temporal plugin (private repo): **no code change expected** — its codec serializes the `dump_for_transport` dict via kajson and binds classes through the registry, so `DateContent` flows in with the pipelex version-pin bump. At pin-bump time, add a `Date` case to its converter round-trip tests anyway: the failure mode over there is a hang, not an error, so coverage is cheap insurance.

## Micro-decisions log

- `to_datetime()` with `time is None`: **settled RAISE** (`DateContentError`, in `pipelex/core/stuffs/exceptions.py`) — a silent midnight is exactly the fabrication DT2 forbids.
- Final narrowed-error class name: **settled `InputsTimeOnlyNotSupportedError`** (Phase 2).
- ISO string parsing order: **settled date-first** — `datetime.fromisoformat` accepts a date-only string and fabricates a midnight, so `date.fromisoformat` is tried first; a bare time string fails both and correctly errors.
- `DateContent.rendered_pretty`: **settled override** — the base feeds `smart_dump()`'s real date/time objects to Rich's stdlib-json encoder and crashes; DateContent renders the ISO two-field JSON instead.
- DT6 lax-coercion guard placement: **settled ON THE CONTENT CLASS** (Phase 1, not Phase 2) — the guard is a class invariant, not just an input-boundary concern, because the dict-content envelope path reaches `model_validate` directly. One `field_validator("date","time", mode="before")` closes every verified pydantic lax leak: bare `int`/`float` (pydantic natively rejects on `date`, but lax-coerces `int`→`time` as seconds-of-day), an **all-digit string** (read as epoch — a real ISO date/time always carries `-`/`:`, so an all-digit string is only ever an epoch), and a **`datetime` on the `date` field** (silently truncated to the date, dropping its time + UTC offset — the exact fidelity loss DT3 forbids). Raises `ValueError` (not `TypeError`) so pydantic wraps it into a `ValidationError` the input path catches — a raw `TypeError` escapes `model_validate` uncaught.
- Note for Phase 4 (DT8): the older custom `type = "date"` structure-field path (`structure_generation/generator.py`) has the same unguarded epoch-int coercion — same bug class, addressed when DT8 flips that field type.
