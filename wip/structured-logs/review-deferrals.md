# Structured logs: findings deferred by review passes

Traces for what a review pass on a structured-logs member read as real but not worth fixing at that pass's bar. The sprint is L-260916-a8cdd2; the campaign documents live at the workspace root under `wip/structured-logs/`. An entry marked *unverified* rests on the reviewer's word alone: nobody reproduced it.

The sink member L-260916-aa6e58 took every entry below that named it: the traceback now rides the record's `exc_info`, the console flag and the poor branch retired with the log mode, the mutating formatter went with the poor handler, the class-owned attribute collision and the circular-reference and non-string-key raises are guarded and tested, the changelog bullet is folded, the fields-by-reference and `data` contracts are written down in the logging page, the `data` parse stays eager and the reason is recorded there, the `ErrorReport` comment names the payload fields alone, and the logger-attached filter advice is corrected in the changelog and the page. The entries stay here as the trace of the rounds that found them.

## L-260916-a1dbd5, round 1 (fields, context, no stack walk)

- **Caller attribution rides a fixed frame depth** (*unverified*, cubic). `CALLER_FRAME_DEPTH` in `pipelex/tools/log/log_dispatch.py` counts the frames between the frame lookup and the code that called `log.<level>(...)`, so a helper inserted into or removed from that call chain would attribute records to the wrong module and apply the wrong logger level. The tests in `tests/unit/pipelex/tools/test_log_caller_module.py` emit through every level and assert the caller's module, path and function, which is what catches such a change. A walk up the frames until one leaves the log package would remove the constant at the cost of a loop per line; the fixed depth stays until a second facade entry point makes the constant wrong for one of them.
- **A given `field_<name>` beside a colliding `<name>`** (*unverified*, cubic). Prefixing a colliding entry could overwrite an entry the caller gave under the prefixed name. Deferred here on the reading that the ordered attach kept both values; round 2 verified that it did so in one order only, and fixed it: the prefix is now applied until the destination is free.
- **The traceback rides the message rather than `exc_info`** (verified by inspection, code-review and the implementer). `include_exception` appends `traceback.format_exc()` to the message text in the dispatch. A structured sink wants the exception as `exc_info` or `exc_text`; setting it today would make the Rich handler render the traceback twice. The sink member L-260916-aa6e58 owns that decision, and the note on it carries the detail.

## L-260916-a1dbd5, round 2 (bar defects)

- **`is_console_logging_enabled = false` silences every handler, not the console** (*unverified*, code-review). The early return in `LogDispatch.dispatch` precedes `logger.handle(record)`, so a deployment that turns the console off to attach its own handler to the root logger gets no Pipelex record at all. The gate is the one the old console-only dispatch had, and it is the sink member's to replace: L-260916-aa6e58 selects sinks by configuration and retires the rich-or-poor mode, and the note on it carries this.
- **The formatter mutates `record.msg` in place** (*unverified*, code-review, outside the diff). `LevelAndEmojiLogFormatter.format` in `pipelex/tools/log/log_formatter.py` rewrites the record's message for WARNING and above, so a record formatted by more than one handler accumulates colour escapes. Pre-existing; it starts to matter when a second handler exists, which is the sink member's work, and the note on L-260916-aa6e58 carries it.
- **A `data` field is overwritten by structured content rather than prefixed** (*unverified*, code-review). `build_log_record_extra` lets the content's `data` win over `fields={"data": ...}` outright, the one place a value is discarded rather than carried under a prefix. Documented as deliberate in `docs/tools/logging.md`; no call site gives such a field, and a rule that prefixes it would be a change of contract rather than a fix.

## L-260916-a1dbd5, round 3 (bar necessity)

The bar admitted only what the round-2 pass itself introduced or what the branch could not ship without. The three entries marked *verified* were confirmed by reproduction and deferred because they predate that pass, no runtime caller reaches them, and the sink member L-260916-aa6e58 owns the same modules before the sprint's release; the note on that item carries each with its fix.

- **The collision guard reads only the record's instance dict** (*verified*, codex:review and codex:adversarial). `attach_log_record_extra` in `pipelex/tools/log/log_fields.py` tests `name in vars(record)`, which misses the attributes the `LogRecord` class owns: `fields={"__class__": ...}` and `{"__dict__": ...}` raise `TypeError` out of the log call, and `{"getMessage": ...}` shadows the method so the formatter fails and the stdlib's `handleError` drops the record. The fix is `hasattr(record, attribute)` in place of the `vars` lookup, keeping the `FORMATTER_OWNED_ATTRIBUTES` clause since `message` is not on a fresh record, with a regression test per key. No caller in `pipelex/` passes `fields=` yet; the exposure is a consumer forwarding a mapping whose keys it does not control, a kajson-encoded object among them.
- **A circular-reference content raises `ValueError` out of the facade** (*verified*, code-review). `_render_content` hands a `dict` or `list` to the `purify_json*` helpers, which catch `TypeError` only, and `json.dumps` raises `ValueError` on a cycle. Byte-identical on dev, where the raise is unconditional; the branch's docstring in `log_fields.py` is what promises a log call never raises. The fix is a guard on the log path, a `ValueError` caught beside the `TypeError` in the helpers or a fallback rendering in `_render_content`, and the promise narrowed if neither lands.
- **The changelog bullet is four sentences against the rule's three** (*verified*, code-review). The closing cross-reference is its own sentence since the branch's first commit; it folds into the previous one when the bullet is next touched.
- **A process entry cannot clear an inherited identifier** (*unverified*, cubic and code-review). `LogContext.merged_with` reads `None` as inherit, so `PipeRun.run` invoked inside an existing binding keeps the ambient `pipe_run_id` until `live_run_pipe` mints one; `request_id` and `pipeline_run_id` are always set by a run and cannot leak. No call site runs `PipeRun.run` inside a binding today; a replace-or-clear semantics on `bind_log_context` is the fix if an embedder or a nested run ever does.
- **`fields` values ride by reference while `data` is a snapshot** (*unverified*, code-review). A field given a live `dict` or `list` reads whatever the caller did to it by the time a queued sink serializes it. `docs/tools/logging.md` advises JSON-ready values as guidance rather than guarantee; whether the sink snapshots on emit or the contract states the requirement is the sink member's call.
- **`data` is re-parsed on every structured call before any sink reads it** (*unverified*, code-review). `json.loads(rendered)` in `_render_content` builds a second object graph per record at an enabled level; a lazy attribute or a sink-side parse is the sink member's design choice.
- **The reserved-name comment claims `ErrorReport` carries the identifiers** (*unverified*, code-review). The comment above the constants in `pipelex/tools/log/log_context.py` says a record and the `ErrorReport` agree on the key; `ErrorReport` carries none of the three, and the only `request_id` reachable from it is the provider's, a different identifier. The comment should name the run result and the payload fields alone.
- **The deleted gateway tests were the only mechanical guard on the no-log-on-load-path invariant** (*unverified*, code-review). Round 1 removed two tests that asserted the absence of a `RuntimeError` that no longer exists; the invariant they stood beside, that nothing logs on the gateway load path because a pre-configure line lands on stderr unformatted, is now prose only in `gateway_config.py`. A test that asserts no record is emitted on that path restores the guard.
- **Nits** (*unverified*, code-review): `pipe_run.py` still interpolates `pipeline_run_id` into five message strings while the same value rides the record as a field; `test_log_fields.py` hardcodes the DEV level as a number beside an imported constant; `bedrock_client_boto3.py` uses `run_in_executor`, which does not propagate contextvars, so a log call from that thread would lose the identifiers, though only a boto3 call runs there.

## L-260916-a1dbd5, round 4 (bar freeze)

The bar changed no code. Every reviewer raised the class-owned attribute collision again, and code-review the circular-reference raise, the console flag and the round-3 nits; those keep their entries above. What follows is new.

- **A filter attached to the `pipelex` logger no longer sees the records** (*unverified*, cubic). Python consults a logger's filters only for records originating on that logger, never while a descendant's record propagates, so a filter an integration attached to `logging.getLogger("pipelex")` stops running now that records originate on module loggers. No repository in the workspace attaches one. The changelog's "Loggers are named by module" bullet says a filter keyed on the bare package name must key on the prefix, which does not help a filter attached to the logger: such a filter must move to the handler, and the bullet and `docs/tools/logging.md` should say so.
- **A dict with a non-string key raises `TypeError` out of the facade** (*unverified*, code-review). `json.dumps` applies its `default` to values, not keys, so `log.info({(1, 2): "a"})` raises from the `purify_json*` fallback too. Byte-identical on dev; the same guard as the circular-reference entry above covers it.
- **The logging doc overstates what `data` is** (*unverified*, code-review). The "Structured content" section says any other object is carried as the dictionary or list its serialization produces; a scalar content lands as itself, an object that defeats both `json` and `kajson` lands as the `purify_json` warning wrapper, and a `float("nan")` survives the `json.loads` round trip as a float standard JSON cannot re-emit. A sink author iterating `record.data` meets a scalar. The sink member decides the shape and the page follows.
- **The caller-info test fixture's teardown is process-global** (*unverified*, code-review). `log_with_caller_info` in `tests/unit/pipelex/tools/test_log_caller_info.py` calls `Log.reset()`, which removes and closes every root-logger handler and calls `logging.shutdown()`. `Log.reset()` is pre-existing; the unit suite is green under random and fixed ordering, so this is a latent isolation hazard rather than a failure. A fixture that removes only the handler it added would remove the hazard.
- **The dev-side caller-info fix is filed under Changed** (*unverified*, code-review). `FUNC_MODULE` and `FUNC_MODULE_LINE` raised `KeyError: 'module'` on dev at the first log line after boot; the branch passes the module and covers it in `test_log_caller_info.py`, and the changelog folds that into the logger-naming bullet rather than a Fixed entry.
- **Out of scope, unverified: seven findings on the `main..dev` delta** (code-review, whose skill diffed `main...HEAD`). Files this branch does not touch, listed here so they can be chased and never as this branch's: `pipelex/pipeline/input_normalizer.py:232` (`HttpUrl`'s length cap rejects long signed URLs); `pipelex/tracing/ndjson_event_log.py:163` and `pipelex/tracing/dynamodb_event_log.py:170` (corrupt-vs-refused decided without checking `loc == ()`); `pipelex/tracing/ndjson_event_log.py:164` (raw `validation_error` interpolation leaks traced content); `pipelex/cli/commands/graph_cmd.py:59` (`json_invalid` misreported as a stale-file error); `pipelex/graph/mermaidflow/templates/_interactive_scripts.js.jinja2:275` (the JSON tab falls back without consulting `disabled`); `pipelex/tools/misc/http_utils.py:22` (whitespace accepted against the documented rule); `pipelex/runtime_hub.py:210` (`reset_boot_state` reverts the agent CLI's SILENT printer).

## L-260916-aa6e58, round 1 (bar open)

The pass fixed the nine confirmed findings: the OTLP handler's re-entrancy filter and its teardown guard, the mixed-type sequence, the JSON sink's non-finite floats and its reserved keys, the holding handoff, the console formatter's prefix under `exc_info`, the doctor's fallback sink and the migration entry's account of what a poor-mode or silenced file loses. The refuted finding (the override and kit configs carrying `sink` without `[runtime.log.otlp]`) was rejected: the kit carries the user-facing subset by design and the deep merge supplies the rest. What follows was read as real and left.

- **The held-record window spans the remote config fetch** (*unverified*, code-review). Between `Log.configure` and `log.install_sink` in `RuntimeBoot.setup`, the registrar is built and the remote config may be fetched, so a boot that logs verbosely on that path holds those records in memory up to the holding handler's capacity; beyond it the oldest are dropped. The capacity is generous for a boot and the window is documented in `docs/under-the-hood/log-sink-plugins.md`; a sink installed earlier would need the registrar earlier, which is the boot order's decision and not this member's.
- **The JSON sink binds its stream at factory time** (*unverified*, code-review). `pipelex/tools/log/json_log_sink.py` resolves `sys.stdout` or `sys.stderr` when the factory runs, so a consumer that swaps `sys.stderr` after boot, pytest's capture among them, keeps writing to the stream the sink captured. Resolving the stream per emit would follow the swap at the cost of a lookup per line; deliberate for now, and the test module patches the stream before installing.
- **A `console_log_target = "file"` raises a bare `ValueError`** (*unverified*, code-review, pre-existing). `stream_for_target` in `pipelex/tools/log/log_sink.py` inherits dev's handling of the third target value, which has no file path to open. The enum member predates this branch and no caller sets it; retiring the member or giving it a path is a config change of its own.
- **`redirect_to_stderr` imports Rich unguarded** (*unverified*, code-review). A server install without the CLI extra fails on that path. L-260916-fe7969 moves Rich behind the extra and adds the import guard, and the note on it carries this.
- **The Rich handler raises `MarkupError` out of a log call** (*verified by the round-1 verifier*, pre-existing). With `is_markup_enabled = true`, `RichHandler.emit` renders the message outside its own `try`, so a message whose text reads as a stray closing tag, `[/pipe]` for one, raises out of `log.info`. The holding drain now contains it for held records; a live record still raises. Dev's Rich mode did the same. A console handler subclass whose `emit` routes the error to `handleError` is the fix, owned by the console sink's next pass or by L-260916-fe7969 when it reworks the Rich path.
- **The doctor duplicates the log-sink composition root** (*unverified*, cubic). `doctor_cmd.py` performs discovery, registry construction, selection and installation that `RuntimeBoot.setup` also performs, and the fallback added this round widens the difference. Extracting one function both call is a refactor below this bar; it is worth doing before a third caller appears.
- **The migration deletes `log_mode` and `is_console_logging_enabled` without translating them** (*verified by the round-1 verifier*, cubic). A `remap_value` onto `sink` is refused by the ledger check because `sink` is an open token, an intermediate schema for a split into a safe and an unsafe entry has no golden a model ever had, and an `unsafe` entry blocks every full copy of an older base file at its defaults for the sake of the few that chose `poor` or `false`. The entry stays `safe`; what a person running `pipelex migrate` sees is the step's title, so the title names what a poor-mode file then sets by hand, and the description and guidance say what the deletion undoes for both keys. The engine gap underneath, that a safe entry's guidance is rendered nowhere, is L-260916-5d142f.
- **The migration coverage gate is blind to a deleted enum key** (*verified by the round-1 verifier*, tooling). `_check_enum_members_remapped` in `pipelex/migration/coverage.py` continues when a path does not survive into the new schema, which is how a `delete_key` on `log_mode` passed the gate. Filed as L-260916-0e1435.

## L-260916-aa6e58, round 2 (bar defects)

The pull request opened after round 1, so this round measured the sink seam alone against `feature/Log-fields-context`. Every reviewer raised the handoff's duplicate window and the flush that skips the close, and the adversarial reviewer the migration's silencing recipe; those were fixed. The concurrent `install_sink` race (cubic) was rejected: the boot configures logging once per process and `Log.configure` refuses a second call, so two installers is a state the runtime does not reach. What follows was read as real and left.

- **The structured-content guard is wider than the failure it catches** (*unverified*, code-review). `_render_content` in `pipelex/tools/log/log_dispatch.py` wraps both the `purify_json*` call and the `json.loads` re-read in one `except (TypeError, ValueError)`, so a re-read that failed after a successful purification would discard the truncated rendering for `repr(content)`, whole. No trigger is shown: the truncation happens inside the helpers on byte values, so what they hand back is loadable JSON. Narrowing the `try` to the helpers, or keeping the rendering when only the re-read failed, removes the question when the function is next touched.
- **The doctor keeps an already-configured sink without resolving the configured token** (*unverified*, cubic). When logging was configured before `setup_doctor_runtime` runs, an embedder or an interleaved test, the log-sink row reports healthy without asking the registry about `sink`. The CLI configures nothing before the command, so `pipelex doctor` always takes the other arm; an embedder calling the doctor in-process after its own boot keeps its own sink, which is the intent. Resolving the token in both arms is a small extension of the row, below this bar.
- **The migration's safe deletion of a silencing file** (adversarial, restating round 1). The round-1 entry above stands; this round corrected the recipe the entry names so that a person who follows it is silenced in fact.

## L-260916-aa6e58, round 3 (bar necessity)

The bar admitted what the round-2 pass's own changes introduced: the holding handler's replay and forward delivered records below the sink handler's level, since `Handler.handle` checks no level where `Logger.callHandlers` does, and the teardown's stderr diagnostic could itself raise on a closed stream and leave logging configured. Both were reproduced by the driver and fixed. What follows was read as real and left.

- **A sink whose handler build fails after its construction allocated resources is dropped without teardown** (*unverified*, cubic). `install_sink` reads `sink.handler` before recording the sink, deliberately, so that a failed build leaves the runtime untouched and the doctor's fallback can install; the cost is that a plugin sink whose factory opened a connection or started a thread and whose `make_handler` then raised has nothing that closes it, since the sink contract carries no teardown hook of its own. The built-in sinks allocate in the handler or, for `otlp`, in a provider that registers its own shutdown at exit. A `close` hook on the sink contract is the fix, and a contract change wants its own pass.
- **The migration's safe deletion of a silencing file, at boot** (adversarial, restating rounds 1 and 2 with one new fact). Boot tolerance replays the ledger in memory over a stale file, so a deployment that set `is_console_logging_enabled = false` resumes output on upgrade without running `pipelex migrate`. The boot's stale-configuration warning prints the titles it carried forward, so that deployment sees, on the console the boot re-enabled, the entry's title naming the by-hand edit; no repository in the workspace sets the flag. The round-1 entry stands, with L-260916-5d142f and L-260916-0e1435 carrying the engine gaps.
- **The doctor duplicates the plugin and sink composition root** (cubic, restating rounds 1 and 2). The fallback added in round 2 widened the difference; the entry above stands.
- **A processor that raises escapes the log call** (*verified by code-review's reproduction*). `_ProcessorFilter.filter` in `pipelex/tools/log/log_sink.py` runs each processor unguarded, and the stdlib runs a handler's filters outside every `try`, so a processor raising on a live record raises out of `log.info`; the same processor raising during the holding replay or forward is contained. Latent: nothing in `pipelex/` registers a processor yet. The scrub member L-260916-4837d4 registers the first one and owns the guard, a per-processor `except` routed to `handleError`; the note on it carries this.
- **The OTLP sink overwrites a carried `code.*` or `exception.*` field** (*verified by code-review's reproduction*). `_attributes` in `pipelex/tools/log/otlp_log_sink.py` assigns the semantic-convention keys after seeding from the carried attributes, so a field named `code.line.number` is discarded where every other sink re-prefixes; the JSON sink's prefix loop with those keys as its reserved set is the fix, below this bar.
- **Nits** (code-review): `OtlpLogHandler.close` calls the provider's shutdown before `super().close()`, so a shutdown that raises leaves the handler in the stdlib's handler list, contained by the teardown guard; the holding handler's drop at capacity is linear per record where a bounded deque would be constant, reached only by a process that never installs a sink.

## L-260916-aa6e58, round 4 (bar freeze)

The bar changed no code. The adversarial reviewer raised the migration's safe deletion of a silencing file a fourth time, and code-review the OTLP close order, the linear drop and the kit configs without `[runtime.log.otlp]`; the first three keep their entries above and the last was refuted in round 1. What follows is new. The two entries marked *verified* were reproduced by the driver and are filed as ledger bugs, since each is a one-line fix with a test that the branch below this one ships without.

- **A caller field named `_pipelex_forwarded` is read as the handoff mark** (*verified*, codex:review and code-review). `attach_log_record_extra` prefixes a name only when the record already owns it, and a fresh record does not own the mark, so `log.info("x", fields={"_pipelex_forwarded": True})` lands the attribute unprefixed and `ForwardedRecordFilter` drops the record from every sink, after the handoff too. The comment on `FORWARDED_MARK` claims the underscore keeps a caller from spelling it, which held while records went through `makeRecord(extra=...)` and no longer does. Filed as L-260916-40bf74: treat the mark as owned beside `FORMATTER_OWNED_ATTRIBUTES`, correct the comment, add the test.
- **The doctor's fallback install can itself raise** (*verified*, code-review). `install_doctor_log_sink` retries on any exception out of `install_sink` on the claim that a failed install leaves nothing installed; `install_sink` records the sink before the replay, deliberately, so a held record the handler cannot render whose `handleError` write meets a closed stderr raises `ValueError` with the sink recorded, and the fallback then raises `RuntimeError("already installed")`, which nothing in the doctor catches. Filed as L-260916-7e131d: gate the fallback on `log.sink is None`.
- **A caller's `fields={"data": ...}` is overwritten by structured content** (*unverified*, code-review). `build_log_record_extra` documents that structured content owns `data` outright, which makes it the one collision in the module that is not preserved under the `field_` prefix. A documented choice; prefixing the caller's entry instead would make the module's no-value-lost claim hold for every key, when the module is next touched.
- **Sink factories take the whole `LogConfig`** (*unverified*, cubic). The plugin contract hands every external factory the core configuration, which has no place for a plugin's own settings, so `docs/under-the-hood/log-sink-plugins.md` directs plugins to environment variables or private files. A narrow factory context carrying a plugin-owned payload is a contract change and wants its own pass, with the teardown hook the round-3 entry asks for.
- **Processors mutate the record every root handler shares** (*unverified*, cubic). `_ProcessorFilter` edits the original record, so a host-installed root handler sees it processed or not by its position in the handler list; a scrub that redacts reaches one handler and misses another. The scrub member L-260916-4837d4 registers the first processor and decides whether it runs on a copy or at one canonical point; the note on it carries this.
- **Held records pin live tracebacks** (*unverified*, code-review). `include_exception` now rides the record as `exc_info` rather than a formatted string, so a record held between `configure` and `install_sink` keeps its traceback, every frame and its locals, until the sink arrives or the boot dies. Bounded by the holding capacity and by a window of a few dozen lines; formatting `exc_text` eagerly when holding would cap it.
- **Nit** (code-review): `spell_non_finite` in `pipelex/tools/log/log_sink.py` catches `TypeError` and `ValueError` only, so a value nested deeper than the interpreter's recursion limit raises `RecursionError` past the docstring's never-raising claim; contained by `StreamHandler.emit`'s `handleError`, so the line is lost and not the process.

## L-260916-aa6e58, the migration finding, settled by the owner on 2026-09-17

**`pipelex-config@5` stays `safe`, and this finding is closed rather than deferred.** It was raised on every round of this member and once more by a Codex pass reading `origin/dev` during the Rich member's round 1, each time as the same reading: the entry deletes `is_console_logging_enabled = false` without translating it, and boot tolerance replays the ledger in memory, so an upgrade alone hands a silenced deployment its console back.

An implementer cured it on 2026-09-17 by making the entry `unsafe`, and that change was reverted the same day on the owner's decision. What the block cost is what decided it. `safety` is a property of an entry, entries are one per schema version and contiguous (`pipelex/migration/ledger.py:197`), and this version's link owns all four key removals, so the block cannot be narrowed to the case that matters. An `unsafe` entry is rehearsed and never applied (`pipelex/migration/engine.py:118`), so the retired keys survive the boot-tolerance replay, the re-validation fails again against a model that is `extra="forbid"` (`pipelex/system/configuration/config_model.py:12`), and `load_config_and_validate` re-raises (`pipelex/system/configuration/config_loader.py:530`). The block therefore converts "boots with a warning" into "fails to boot" for **every** file still carrying any of the four keys at their old defaults — every full copy of an older base file — for the sake of the few that chose otherwise.

The owner's reading is that nothing chose otherwise: no deployment set these keys, nothing depends on the flag outliving its removal, and this project keeps no backward compatibility. Round 3 had already recorded the corroborating fact, that no repository in the workspace sets the flag.

**Reopening this needs a new fact, not a new reading** — a configuration that actually sets one of the retired keys and a reason it may not be edited by hand. The rationale is recorded in the entry's own comment in `pipelex/migration/ledgers/pipelex-config.toml` so that it travels with the code rather than with this document.

Two corrections outlived the revert, both independent of the safety decision and both kept: the entry's guidance and `docs/configuration/config-practical/logging-config.md` had said `is_console_logging_enabled = false` "silenced every handler on the root logger", where `LogDispatch.dispatch` returned early and third-party loggers still reached the handlers, and the entry's comment had named `default_log_level = "OFF"` as the replacement where the base pins `pipelex` in `package_log_levels` and the root level alone silences none of Pipelex's records.

One finding from the reverted work is real and stays: `BlockedEntry` carries no `title` or `description` (`pipelex/migration/plan.py:72`), so a blocked entry reaches its reader through `guidance` and `detail` alone. It is latent — no shipped ledger entry is `unsafe` — and it is the mirror of L-260916-5d142f, which carries a safe step's unrendered guidance; the note on that item records it.

## L-260916-aa6e58, round 1 after the growth reset (bar open)

The branch grew past the growth line after round 4 — the forwarding-marker reservation and the OTLP reserved keys — so the ladder reset to round 1 at bar `open` and all four reviewers read the whole branch against `origin/dev` again. Twelve findings; eight went to a verifier against the checkout at `84c231c82`, and four were sorted as standing deferrals whose traces are above. **Not one of the twelve is about the lines the reset was triggered by**, which is worth recording: the new code is the part of this branch the rounds have now read most and found least in.

Five were confirmed and fixed. Each fix was mutation-tested — the fix reverted, its own guard watched going red, and no other test moving:

- **A non-finite float nested in a pydantic model cost the whole JSON line its structure.** `json_fallback` returned `model_dump(mode="json")`, which keeps the Python `nan` because pydantic's own spelling applies only where pydantic writes the text itself, so `json.dumps` refused the payload under `allow_nan=False` and `_json_line`'s recovery reprd every non-fixed key: an integer field became a string, a list its `repr`. The fallback now spells the dump the way a value handed in directly is spelled. Reachable with `sink = "json"`, which the hosted plane's runner and worker select, and the `otlp` sink's `render_json` took the same route.
- **`release_to` drained held records without marking them**, where the forward path marks. A thread descheduled inside `callHandlers` before the handoff can resume on the sink's handler carrying a record the drain already delivered, and the guard on that handler had nothing to read — a window distinct from the one the forwarding-marker commit closed, on the other side of the drain.
- **`close` dropped every held record below the last resort's own WARNING**, so a boot that died between `configure` and `install_sink` lost exactly the trail a verbose run was turned on to produce. The last resort's level is meant for a record emitted before anything was configured; these passed the level the configuration set.
- **A field spelling `data` beside structured content was destroyed** rather than moved aside under the `field_` prefix — the round-4 deferral above, taken rather than carried again, since the discipline it breaks is the one the module states everywhere else. The content keeps the name a sink reads it under and the field keeps its value, the prefix applied until the name lands where nothing sits.
- **The four `opentelemetry-*` dependencies carried no version specifier at all**, and no job resolves the floor. The SDK took a positional `emit(record)` in the versions the declaration permitted while the `otlp` sink passes the keyword form, so such a resolution made every record a `TypeError` that `handleError` turned into a traceback on stderr — nothing reaching the collector while the process reported success. Floors are now declared at the locked versions. A lowest-resolution job is still absent and would be the thing that keeps them honest.

Deferred, with what the verification established:

- **A processor that raises kills the live log call** (*verified*, codex:adversarial). `_ProcessorFilter.filter` runs the callbacks inside `Handler.handle`, which the stdlib calls before its own `try`, so the exception leaves `log.info` itself; the replay path swallows the same exception through `_deliver`'s `handleError`, so the two paths disagree about the same processor. No processor is registered anywhere in the tree, but `docs/under-the-hood/log-sink-plugins.md` tells readers to append a redaction one, so it is reachable for anyone following the documentation. It stays with the scrub member L-260916-4837d4, together with the shared-record mutation above, because the remedy is that member's contract choice rather than a review round's: a redaction processor that raises means the record is *unredacted*, so rendering it anyway leaks what it was meant to hide, while the class currently promises it never drops one.
- **The OTLP suppression guard rejects far more than the export path** (*verified*, code-review). `ExportPathFilter` returns on the context value alone after the name guard, so any record emitted while the SDK's batch processor holds `_SUPPRESS_INSTRUMENTATION_KEY` is dropped whatever its origin. The concrete loss is in this repo and the finding did not name it: `PostHogSpanExporter._do_export` logs its own failures on a `pipelex.*` logger inside a `BatchSpanProcessor` scope, so Pipelex's span-export diagnostics vanish from the `otlp` sink without trace. Deferred because the guard's breadth is what fixed a reproduced exit deadlock and the transport's logger names are open-ended, so narrowing it is design work — a diagnostics path that bypasses the export queue — and not a flip. Needs `sink = "otlp"` and telemetry both.
- **A boot that fails inside `__init__` after `configure` never closes the holding handler at all** (*verified*, found by the verifier rather than by a reviewer). `_release_after_failed_boot` is what reaches `log.reset()`, and `runtime_boot.py`'s own docstring records that both `make()`s construct outside their `try`, so on that path nothing is emitted anywhere — not even the stale-configuration warning. That is `runtime_boot`'s structure and not this module's, and the `close` fix above does not reach it.

One earlier entry is refined rather than repeated. **The doctor's fallback install** (filed as L-260916-7e131d) was confirmed as to ordering, and the verification moved where its fix belongs: `install_sink` records the sink before the replay *deliberately*, so `reset` can find and remove a handler already on the root logger, and the comment at the assignment says so. The defect is the doctor's `except` arm installing without clearing first, so the cure is in `doctor_cmd.py` and the ordering in `log.py` stays. The verifier also narrowed the trigger: every failure the arm was written for — a missing dependency, a console target no sink writes to, a factory that raises — happens at `sink.handler` before the assignment, so the fallback works for all of them, and reaching the double install needs the replay's own recovery to raise, which in practice means an unusable stderr or a third-party handler overriding `handleError`.

## L-260916-aa6e58, round 2 after the growth reset (bar defects)

Three reviewers of the four found only the standing deferrals — cubic four, `codex:review` two, `codex:adversarial` one, every distinct claim already traced above and none in the code the round's focus named. `code-review` was the one that found new ground, and what it found was the most serious thing six rounds on this member have turned up.

**A log call could raise on the default sink, which is the one thing the branch promises it never does.** The finding arrived as a claim about one error path; verifying it made it larger and moved its cause. `RichHandler` overrides `emit` and does not restore the `try`/`handleError` the stdlib's own handlers put around theirs, so anything raised while rendering leaves the log call — and Rich's markup parser raises on ordinary text. Its tag has to open with a lowercase letter, `#`, `/` or `@`, which the finding's own example got wrong: `[Errno 2]` is safe. What is not safe is the two shapes an error message is actually made of. `list[int]` in a type complaint loses the bracketed span silently. A bracketed path reads as a closing tag with nothing open and raises `MarkupError`. Confirmed end to end against the branch before the fix: `log.error("no such file: [/etc/pipelex.toml]")` raised out of the log call, and constructing a `FatalError` carrying such a message raised `MarkupError` **in place of** the failure it was reporting — the diagnostic destroyed and replaced by an unrelated exception. Fixed at both levels: the sink's handler restores the stdlib's guard, so a line Rich refuses costs at most its own rendering; and `TracebackMessageError` asks for its record verbatim through the per-record override Rich reads ahead of its handler's setting, so brackets and type names in an error reach the terminal as written. The mark is reserved in `log_fields` beside the forwarding marker, for the same two reasons: it is a console concern that rides no wire, and a caller's field spelling it must not decide how the console reads that line. Both arms of the error mode are marked — the `error` one this branch moved onto a propagating logger, and the `exception` one, which `origin/dev` already exposed.

**A guard the branch deleted is restored in a form that survives what retired it.** Two tests pinned that the gateway prune runs before the log hub is configured, by asserting that an unconfigured dispatch raised. A log call before `configure` is now handled rather than fatal, so they stopped failing for their own reason and were removed — while `drop_unknown_gateway_defaults` goes on stating the invariant in its docstring, "deliberately pure, and deliberately silent", with nothing holding it to it. The `defaults` guard now asserts the silence directly. Its per-model twin is deliberately **not** restored: nothing in that path's source claims silence, so pinning it would invent a contract rather than hold one, and a test asserting no record across a whole backend-library load would be brittle for the wrong reason.

Deferred, and filed where a trace alone would not be enough:

- **The doctor emits its own diagnostics through the configured sink** (L-260917-0be44e). With `sink = "otlp"` the doctor's stale-configuration warning is shipped to a collector and the terminal stays silent, while the Log Sink row reports healthy. Deferred because the remedy is contested *within this same round*: this finding wants the doctor to prefer a user-visible sink and merely report the configured one's resolvability, while cubic's standing finding wants it to duplicate less of `RuntimeBoot.setup` and reproduce the production path more faithfully. That tension is the useful part, and the narrower cure that needs neither is to carry the warning in the report rather than emit it through `log.*`.
- **The `json` sink writes ANSI escapes when a third-party library decorates a record** (L-260917-398eae, *unverified*: no uvicorn in this checkout, so the attribute name is the reviewer's own claim). `carried_attributes` makes any foreign decoration a field, and the module promises "no ANSI ever". It is the hosted plane's own configuration, so it is the case most likely to be seen. Deferred as a contract question: stripping escapes from every value is a scan on the hot path, a denylist of attribute names rots, and softening the promise gives it up — the reviewer offered all three and chose none.

Rejected, with the reason, so it is not re-raised as new:

- **`_render_content` discards a good rendering when its re-parse fails** (code-review). The claim is that a content whose purified rendering is not re-parseable falls to `repr` and loses a rendering that succeeded. That state cannot occur: `rendered` is `json.dumps` output of an already-purified structure, and the truncation the helpers apply runs on the structure before the dump rather than on its text, so the re-parse cannot fail where the dump succeeded. The `except` arm's `repr` fallback is reached only when `purify_json*` itself raised, which is the documented path and the correct outcome. The other half of the same finding — that the serialize-then-reparse round trip is paid on every structured call for a value the `console` sink discards — is a real cost and an improvement rather than a defect, so it is deferred at this bar and recorded here.
- **The holding handler drops the oldest record on overflow** (code-review). Offered as wrong for a failure-diagnosis buffer, on the argument that the earliest lines describe what first went wrong. The argument is not made out: the lines nearest a failure are at least as often the useful ones, the constant's own comment states the choice and its reason (bounded growth for a process that never installs a sink), and the same comment notes a boot holds a few dozen lines against a capacity of a thousand, so the path is not one a real boot reaches.

One thing noticed while verifying and left alone: `TracebackMessageError` in its `exception` mode calls `logger.exception` outside any `except` block, so a record built there appends `NoneType: None`. It predates the branch, `include_exception`'s own version of the same thing was fixed separately, and it is cosmetic noise rather than a loss.

## L-260916-aa6e58, round 3 after the growth reset (bar necessity)

All four reviewers ran over the whole branch against `origin/dev` at `2e81b2c68`, and the round changed no code. Nothing the bar admits was confirmed: the two findings Codex raised are standing deferrals already traced above, and the one new claim that would have been fixed if true turned out to be pre-existing.

**The processor seam came back for the fourth consecutive round, and this time from two reviewers.** `codex:adversarial` raised it at confidence 1.0 and `codex:review` at P2, both naming `pipelex/tools/log/log_sink.py:60-63` and both describing the mechanism the earlier rounds already verified: `_ProcessorFilter.filter` runs each callback where the stdlib runs a handler's filters, outside every `try`, so a processor raising on a live record raises out of `log.info` itself, while the same processor raising during the holding replay is contained by `_deliver`'s `handleError`. Nothing about the claim has changed and neither has the reason it stays here: the remedy is the scrub member L-260916-4837d4's contract choice rather than a review round's, because a redaction processor that raises means the record is *unredacted*, so rendering it anyway leaks exactly what it was meant to hide, while the class as written promises it never drops a record. No processor is registered anywhere in `pipelex/`, and the note on that member carries the finding. What is worth recording is the repetition itself: four rounds and two independent reviewers now agree it is real, so whoever merges this member should decide it deliberately rather than let it default through.

**The OTLP suppression guard was raised again, with the mechanism this document had already supplied.** `codex:review` named `pipelex/tools/log/otlp_log_sink.py:88-91` and reached PostHog by its own route — `BatchSpanProcessor` also sets `_SUPPRESS_INSTRUMENTATION_KEY`, so `PostHogSpanExporter._do_export`'s failure logs are rejected although they never came from this sink's export pipeline. That is the concrete loss round 2 recorded above and filed as L-260917-472a3e; the finding adds independent confirmation and no new ground.

**The one new claim the bar would have admitted was verified and refuted on attribution.** cubic raised at P1 that `configure()` overwrites the root level and every configured package logger's level while `reset()` restores none of them, so an embedder's logging stays changed after teardown. The mechanism is real and now carries a verified failing scenario, but it is not this branch's: `origin/dev` overwrites the same two families and additionally a third (the poor loggers), restores nothing either, and strips the host's own root handlers on top — so this branch strictly shrinks the residue. The new doctor `finally: log.reset()` sites do not widen the exposure, being guarded by `if not logging_was_configured`, which fires only where the doctor itself wrote those levels. Filed with the whole verification as L-260918-aee78b, together with the precedent that makes it cheap to specify: `reset_boot_state` already remembers and restores the pre-boot pretty-print mode.

Deferred, each new at this round and none of them admitted by `necessity`:

- **`pipelex doctor` can block for a minute or more on a machine configured for `otlp`** (*unverified*, code-review). `pipelex/tools/log/otlp_log_sink.py:158`: the doctor now installs the configured sink, both doctor commands reach `log.reset()` in a `finally`, and the flush is `force_flush(timeout_millis=5000)` whose own comment concedes the SDK discards that deadline — the real bound being `OTEL_EXPORTER_OTLP_TIMEOUT` plus the HTTP exporter's retry backoff. The case that stings is an operator running the doctor precisely because the collector is down. A doctor-specific short deadline, or skipping the flush when the doctor installed the sink itself, is the shape of the cure.
- **`stream_for_target`'s docstring promises a lazily-read stream that no caller reads lazily** (*unverified*, code-review). `pipelex/tools/log/log_sink.py:96` says the stream is "read at call time so a redirected stream is the one used", while `_make_json_log_sink` resolves it at boot and `ConsoleLogSink` binds it into `Console(file=...)` at handler build. An embedder swapping `sys.stderr` after `Pipelex.make()` gets records on the original stream. Either the docstring states build-time capture or the sinks resolve in `emit`; both are decisions rather than corrections, which is why it waits.
- **A `LogSink` instance is single-use and nothing says so** (*unverified*, code-review). `pipelex/tools/log/log_sink.py:82` caches the handler forever while `Log.reset()` closes it, so re-installing the same sink instance would hand back a closed handler, insert a second `ForwardedRecordFilter` into its filters and attach a dead handler to the root logger. No in-tree caller does this — boot and the doctor each build a fresh sink — but `LogSink` is the documented extension point, so either a `close()` hook clearing `_handler` or a line in the base class's docstring closes it.
- **A sink that fails to install is dropped with its thread still running** (*unverified*, code-review). `pipelex/cli/commands/doctor_cmd.py:1129`: for `sink = "otlp"` the factory constructs a `LoggerProvider` and a `BatchLogRecordProcessor` — an exporter thread and an `atexit` hook — before `log.install_sink` is reached, so an install that raises leaves them live while the `except` installs the console fallback and the later `reset()` tears down only that fallback. The window is narrow today because `OtlpLogHandler.__init__` is hard to make fail, but the shape applies to any plugin-contributed sink whose factory allocates.

Rejected, with the reason:

- **The holding handler drops the oldest record on overflow** (code-review), offered again and rejected again on the argument recorded under round 2 above: the lines nearest a failure are at least as often the useful ones, the constant's own comment states the choice and its reason, and a boot holds a few dozen lines against a capacity of a thousand. Nothing new was offered with it.
- **Caller attribution rides a fixed frame depth** (cubic), `pipelex/tools/log/log_dispatch.py:33`, already traced under the fields member's round 1 and belonging to that member's code rather than this one's. No wrapper exists today, so the claim is about a change nobody has made.

## L-260916-aa6e58, the processor seam, settled by the owner on 2026-09-18

Four consecutive rounds on this member raised the same finding, the last of them from two independent reviewers at confidence 1.0, and each round deferred it with a reasoned rationale rather than a shrug. Louis settled it instead of letting a fifth round take it: **the redaction member `L-260916-4837d4` owns the processor failure policy, and this member ships the seam as it stands.**

The defect is not in dispute and never was. `_ProcessorFilter.filter` runs each registered processor where the stdlib runs a handler's filters, which is outside every `try`, so a processor raising on a live record raises out of `log.info` itself and can abort the application work that made the call; the same processor raising during the holding replay is contained by `_deliver`'s `handleError`, so the two paths disagree about the same failure. Nothing in `pipelex/` registers a processor today, which is what makes shipping possible, but `docs/under-the-hood/log-sink-plugins.md` tells readers to append a redaction one, so the path is reachable for anyone following the documentation.

The reason it belongs there rather than here is that the cure is a contract choice and not a guard. A redaction processor that raises means the record is *unredacted*: rendering it anyway leaks exactly what the processor existed to hide, while dropping it contradicts `LogSink`'s promise that it never drops a record. Only the first registrant of a processor is in a position to take that decision, and it arrives with that member. What it owes is recorded on its item: decide the policy and say why, with fail-closed — drop the record and emit a minimal diagnostic carrying none of the unprocessed payload — as the shape the reviewers converged on; implement the guard at the processor boundary, routed to the handler's error path; and cover both the live and replay paths so they can no longer disagree.

A later round that re-raises this should be closed against this entry rather than argued again.

## L-260925-c9ab0d, round 1 (bar open)

The json sink's trace context, with the pipe and LLM spans made current. The pass fixed a held boot line taking the install's span rather than its own, which the verifier reproduced with a record logged on a second thread inside its own span: the holding handler now copies the context a record was emitted in and replays the record under it, for every sink. It also documented that the Gateway stream alone makes the spans current, and split the test module to one class each. What follows was read as real and left.

- **Under the Gateway stream alone, making the spans current gives the host the side effects without the join** (*verified*, cubic, with the verifier's experiments). The tracer exists whenever `custom_posthog.tracing.enabled or pipelex_telemetry_enabled` (`pipelex/system/telemetry/telemetry_manager.py`), and the Gateway stream is on automatically for a Gateway user outside a test run, so for an operator with no exporter of their own the spans go only to Pipelex's PostHog. Their json lines then name spans they never receive, their own instrumentation's spans are re-parented under a parent their backend never gets, and a `ParentBased` host sampler keeps those children whatever its ratio, since the run's parent is hard-coded `SAMPLED` (`pipe_abstract.py`, `llm_worker_abstract.py`). The claim that the host's spans are exported to Pipelex's PostHog was refuted: `make_ai_tracer` never registers its provider globally. The alternative is to make the spans current only when the operator has an exporter of their own (custom PostHog tracing, OTLP or Langfuse); it would also take the join away from any deployment, the hosted plane included, whose lines are meant to meet the traces on Pipelex's own stream. That is a product decision, not a defect, and this pass documented the behaviour in `docs/setup/telemetry.md` under "Pipelex's spans in your process" and in `docs/tools/logging.md` instead of choosing.
- **A Langfuse-only or OTLP-only telemetry configuration appears to create no tracer** (*verified by reading, not by running*, the verifier, outside the diff). The OTLP and Langfuse exporters are wired only inside `make_ai_tracer` (`pipelex/system/telemetry/otel_factory.py`), which runs only under the condition above, and `PostHogTracingConfig.enabled` defaults to `false`, so the Langfuse example in `docs/setup/telemetry.md` would export nothing without the Gateway stream or PostHog tracing. Pre-existing and outside this member; the orchestrator files it.
