# Phase 5 local cut-over — `/code-review` follow-ups

**Source:** xhigh `/code-review` on commit `a8382d1a1` (the `_plugins`/core side of the Temporal externalization — `git rm` of `pipelex/temporal/` + the `temporal` extra, pre-publish). This is the triage for that review (TODOS Checkpoint 5 → "Fan-out `/code-review` … Triage here").

**State that frames every decision below:** the cross-repo move is **already done locally** — `pipelex-temporal` is scaffolded at `../pipelex-temporal` (top-level `pipelex_temporal` package per D8), the temporal tree + tests + marker + `--temporal-server` conftest moved there, editable pins flipped, suites green on both sides. Nothing is published/merged/deployed. So "the following steps" = flip downstream consumer pins (`pipelex-worker`, `pipelex-api-hosted`), repoint `pipelex-mistralai-workflows`, run consumer suites, then publish/merge. None of the items below block those steps.

## Finalize-local pass (2026-06-20, session 2) — core dead-tooling removal + pipelex-temporal review triage

A second pre-publish local pass (still gated before publish). Goal: get **core clean of the moved Temporal dev-tooling** and review/triage the **pipelex-temporal side** of the cut-over (session 1 reviewed the core *deletion* side; this reviews the *additions* side). All changes local, uncommitted at time of writing, on top of the `a8382d1a1`/`27da150a5` cut-over commits (core) and `c39fbdc` (pipelex-temporal).

**Done:**

- **Both Temporal skills relocated to `pipelex-temporal` and removed from core.** Closes item 1 **and extends it** — alongside `temporal-e2e-validate` (the only one item 1 named), `temporal-test-crate` (`.claude/skills/temporal-test-crate/SKILL.md`, also dead in core: it pointed at the moved `tests/integration/pipelex/temporal/library_crate/`) was found and relocated too. Mechanical rewrite on the copies (`tests/{integration,unit}/pipelex/temporal/` → `tests/{integration,unit}/pipelex_temporal/`, `pipelex.temporal` → `pipelex_temporal`, and the one cross-repo `../pipelex/tests/` → `../pipelex-temporal/tests/`). Verified: residue grep clean (only the `.pipelex/temporal-payload-store/` runtime-dir false-positives remain — that dir name is config, not the module), the 3 scripts `py_compile`, and `submit_dry_validate.py`'s rewritten `pipelex_temporal.{exceptions,tprl_pipe.*}` imports resolve in pipelex-temporal's venv. Skills live in `.claude/` → **not** in the publishable wheel.
- **Core Makefile temporal targets removed** (the "or delete from core" half of item 2): help block, `.PHONY` entries, the `test-temporal`/`ttm` target + its `SRV`/`MODE`/`REG`/`TEMPORAL_*` vars, and `temporal-server`/`-stop`/`-worker`(+`-router`/`-runner`)/`-run`/`-run-dry`. Verified `make help`/`make -n` parse and every removed target reports "No rule to make target." The `agent-test-debug` target's temporal *comments* + the no-op `pkill temporal-sdk-python` were **left intentionally**, paired with the deferred `debugging-hanging-pytest-runs.md` (item 6) so the target and its doc de-temporalize together.
- **pipelex-temporal review (additions side): SHIP — clean.** `pr-review-toolkit:code-reviewer` built the wheel offline and verified empirically: `py.typed` packaged, `tests/` excluded, both entry points emitted, the `[tool.uv.sources] pipelex = {path=../_plugins}` pin does **not** leak into wheel METADATA (clean `Requires-Dist: pipelex>=0.35.0`), import-light proven at runtime (importing `temporal_plugin`/building the CLI leaves `temporalio` out of `sys.modules`), end-to-end entry-point discovery resolves, the namespace rewrite is logic-drift-free across the whole tree, suite collects standalone (0 errors). **No BLOCKER / SHOULD-FIX.** Two NITs:
  - **stray `.pipelex/traces/*.ndjson` (9 files) committed — FIXED.** Confirmed stray: tracing-guard tests write to `f"..._{uuid.uuid4().hex[:12]}"` dirs each run, so the committed fixed-uuid files are never read back. `git rm`'d + added `.pipelex/traces/` to `.gitignore`.
  - **shipped `test_extras/wf_test_*` back-import from the non-shipped `tests/` tree — DEFERRED.** `pipelex_temporal/test_extras/wf_test_content_generator_{child,pdf_page_views}.py` do `from tests.integration.pipelex_temporal.test_data import PipeTestCases`. Pre-existing (byte-faithful to `933b6c35c`) and dormant in production (gated behind `is_unit_testing` in `temporal_task_manager.run_worker`). Eventual fix: relocate `PipeTestCases` into `pipelex_temporal.test_extras` so nothing shipped depends on `tests/`. Not a cut-over blocker.

**Gates (this pass):** core `make tb` green + full `make agent-test` green (exit 0, HEAD `27da150a5`) + Makefile parses; pipelex-temporal `make agent-check` clean.

**Still open (added by this pass):**

- **pipelex-temporal Makefile lacks the Temporal dev targets** (`temporal-server`/`test-temporal`/`temporal-worker`/`temporal-run`) removed from core. Item 2's "move to pipelex-temporal's Makefile" half is **not** done — its Makefile delegates to `Makefiles/Makefile_basics.mk` and the targets can't be verified without a running Temporal server, so adding them was deferred rather than shipped unverified right before publish. Add when the repo's Temporal dev loop is next exercised.

## Fixed in session 1 (cut-over review, commit `a8382d1a1`)

Reachable breakages + cheap core-hygiene that stay in core and the cross-repo move would not otherwise touch:

- **kit `agent_rules/commands.md`** — removed the "Temporal Integration Test Options" section (pointed at the deleted `tests/integration/pipelex/temporal/`; shipped into client `.pipelex/`). Regenerated the derived `CLAUDE.md` via `pipelex-dev kit rules`; `check-rules` PASSED.
- **`uv.lock`** — regenerated; dropped the stale `temporal` extra (`temporalio`, `nexus-rpc`, `types-protobuf`). `uv lock --check` now clean.
- **`runtime_bridge/bridge.py`** docstring — "in-tree Temporal plugin" → external `pipelex-temporal`; dropped the "an in-tree orchestrator whose extra is absent" framing (no extra exists anymore).
- **`tests/unit/pipelex/system/test_config_temporal_optional_dep.py`** — docstring + assertion message no longer reference the deleted `temporal` extra (now: temporalio is not a core dep, ships only with `pipelex-temporal`).
- **5 plugin/cogt test stubs** (`test_plugin_discovery`, `test_inference_backend_coverage`, `test_model_lister_coverage`, `test_llm_worker_factory`, `test_img_gen_worker_factory`) — dropped the now-vestigial `temporal=SimpleNamespace(is_enabled=False)` stub field (no builtin's `register()` reads `config.temporal` post-cut-over — verified) and corrected the "TemporalPlugin is a builtin" comments.
- **Restored two core-coverage tests** for behavior that stayed in core but lost its test:
  - `test_trace_context_contract.py::test_non_direct_mode_nulls_host_trace_context` — pins the `trace_context if is_direct else None` guard in `bridge.py` (non-DIRECT modes must null the host trace_context; doubles as a check that no TEMPORAL orchestrator is registered in core).
  - `test_dispatch_options_no_temporalio.py` — `DispatchOptions` must be constructible without temporalio (complement to the AST scan, which cannot catch a regression of the `RetryPolicy = Any` placeholder to a bare forward ref → `PydanticUserError` at construction).

Gates after the fixes: targeted unit suites green (104 passed), `make agent-check` clean (pyright 0/0, mypy 0, ruff/plxt/keyword-only pass).

## Deferred — finalize with the cross-repo publish

These describe the Temporal surface that **moved** to `pipelex-temporal`. They don't bite core CI and don't block the publish steps; do them as part of finalizing the move (the package now exists locally to reference concretely).

1. **✅ DONE (session 2 — relocated, + `temporal-test-crate`; see *Finalize-local pass* above).** **Skill `temporal-e2e-validate`** (`.claude/skills/temporal-e2e-validate/`) — `scripts/submit_dry_validate.py` imports the deleted `pipelex.temporal.{exceptions,tprl_pipe.act_dry_validate,tprl_pipe.dry_validate_dispatch}` and its `DEFAULT_BUNDLE` points at the deleted `tests/integration/pipelex/temporal/...`. The whole skill exercises the Temporal arm, which now lives in `pipelex-temporal`. **Relocate the skill to `pipelex-temporal`** (and repoint imports to `pipelex_temporal.*`); it cannot run from the core venv (temporalio absent). Until then it is dead in core.

2. **✅ core-removed (session 2); ⏳ pipelex-temporal add still open (see *Finalize-local pass* above).** **Makefile temporal targets** (CI-isolated — `agent-test`/CI never invoke them; verified `agent-test:` deps don't reach them): `temporal-worker`/`tw` (runs deleted `pipelex.temporal.worker_cli`), `test-temporal`/`ttm` (`TEMPORAL_TESTS_DIR = tests/integration/pipelex/temporal/`, deleted), `temporal-run`/`temporal-run-dry`, `temporal-worker-router/-runner` (`twr`/`twn`), `temporal-server`/`ts`, and the debug-run guidance referencing the deleted temporal conftest. Move to `pipelex-temporal`'s Makefile (or delete from core). Also drop the temporal lines from the `make help` text and `.PHONY`.

3. **`docs/under-the-hood/orchestrator-plugins.md`** — rewrite Temporal as the canonical **external** orchestrator plugin (symmetric with `pipelex-mistralai-workflows`). Stale spots: frontmatter + line 10 ("in-tree Temporal plugin"), line 43/47 ("extra absent"; the install hint is rendered single-quoted `'pipelex-temporal'` while the code emits unquoted `pip install pipelex-temporal`), lines 82–89 (declares the `pipelex-temporal` console script in **core's** `pyproject.toml` — now in `pipelex-temporal`), lines 110–119 worked example ("in-tree today", "discovered through `BUILTIN_PLUGINS`", "Externalizing … (Phase 5)" as a *future* event — it has happened).

4. **`docs/under-the-hood/temporal-integration.md`** — the module map (~lines 106–276) enumerates concrete deleted `pipelex/temporal/**.py` files. Rewrite to point at `pipelex-temporal`, or reduce to an overview that links the external repo.

5. **`docs/errors/` temporal pages + `generate-error-pages`** — **do NOT run `generate-error-pages` in core until this is settled**: it deletes any page whose class is no longer importable from core, 404-ing the `type_uri` those errors emit. Per the cut-over (TODOS Phase-5 as-built), the split is:
   - **Stay in core** (classes in `system/configuration/{config_temporal,exceptions}.py`): `temporal-config-error.md` (`TemporalConfigError`), `worker-task-queue-unknown-error.md` (`WorkerTaskQueueUnknownError`). Pages valid; regen keeps them.
   - **Moved to `pipelex-temporal`**: `temporal-flow-error`, `temporal-server-error`, `unrecoverable-workflow-failure-error`, `workflow-execution-error`, `workflow-input-error`, `search-attribute-registration-error`, `worker-profile-config-error`, `worker-scope-config-error`, `content-generation-error`. Decide where these publish so `https://docs.pipelex.com/.../errors/<x>/` resolves (pipelex-temporal generates/publishes its own, or core retains authored stubs with `<!-- pipelex:authored -->`). Reconcile the links in `docs/errors/execution-and-runtime.md` accordingly.

6. **`docs/agents/debugging-hanging-pytest-runs.md`** (~lines 122, 161) — references the deleted `tests/integration/pipelex/temporal/` and its `boot_temporal` conftest teardown as "the known fixture-teardown hang point." That hang moved with the tests. Update or drop the temporal-specific example (the general methodology stays valid).

## Deferred — design decision (not a bug)

7. **Boot does not eagerly validate `temporal.is_enabled=true` when `pipelex-temporal` is absent.** With `TemporalPlugin` gone from `BUILTIN_PLUGINS`, nothing reads `is_enabled` or claims worker-runtime slots at boot; boot succeeds silently and the misconfiguration only surfaces at the first `TEMPORAL_*` dispatch (`MissingOrchestratorError`, which carries the install hint). When `pipelex-temporal` *is* installed, its entry-point plugin reads `is_enabled` and claims slots correctly. **Decide:** add an eager boot warning/validation for the enabled-but-not-installed case, or accept the lazy dispatch-time error. Note `Pipelex.setup` still accepts `temporal_enabled` and flips the config flag (`pipelex.py:204`) — keep or reconsider as part of the same decision.

## Deferred — cross-repo coupling guards (lower priority; review verifier marked "refuted" but the gap is real)

8. **`pipelex.toml` `non_retryable_error_types` pins externalized class-name string literals.** `worker_config.retry_policy_config.non_retryable_error_types` contains class names whose defining classes moved to `pipelex-temporal`. A rename there silently desyncs from the literal baked into core's `pipelex.toml`, re-making a deterministic failure retryable — the retry-forever family. `test_non_retryable_baseline_pins.py` only asserts the `DryRun*` entries, so this is unguarded. Add a cross-repo conformance pin (or move those literals to `pipelex-temporal`'s config) during the move.

9. **Error-report "parity" framing is now single-arm in core.** `tests/integration/pipelex/error_handling/test_data.py` (+ `test_error_report_local_full_chain.py`, `test_search_error_report_local_full_chain.py`) still frame the constants as driving "local / Temporal parity," but only the local arm runs in core (the Temporal arm moved). The shared constants are duplicated cross-repo with nothing enforcing they stay in sync. Reword the framing, or add a cross-repo guard.

## Deferred — harmless cleanup (do anytime / during the move)

10. **Dead crate fixture in core.** `tests/e2e/pipelex/cv_batch_screening/library_crate/cv_batch_screening_inputs.json` + `CvBatchScreeningTemporalTestData.INPUTS_FILE` (`test_data.py:18`) have zero consumers in this repo — the direct-mode e2e builds inputs inline and reads only `BUNDLE_FILE`. **Not deleted now on purpose:** `test_data.py`'s header documents an *intentional* self-contained crate mirror shared with `pipelex-temporal`'s suite + the skill. Decide during the move: (a) delete the JSON + constant from core (crate copy becomes bundle-only), or (b) keep it a complete mirror. Cosmetic also: `CvBatchScreeningTemporalTestData` is now a direct-mode-only fixture in core — consider dropping "Temporal" from the name.
