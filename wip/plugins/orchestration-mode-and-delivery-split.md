# Plan — split `PipelexExecutionMode` into `orchestration_mode` (open token) + `DeliveryMode` (closed enum)

**Status:** **ALL PHASES DONE — Phases 1 (core) + 2 (Temporal) + 3 (`pipelex-api`, THE gate) + 4 (Mistral plugin migration + bookkeeping) complete.** Phase 1: `_plugins` commit `bd4e52827` (+ docs `9c7a7785d`, + this tracker `919c09348`) on `feature/orchestration-mode-delivery-split`. Phase 2: `pipelex-temporal` commit `01d7dc3` on `feature/orchestration-mode-delivery-split`. Phase 3: `pipelex-api` commit `dd84a9a` on `feature/orchestration-mode-delivery-split` (cut off `06cd5e6`). All three with all gates green + clean-context `/code-review` at their checkpoints (Phase 3 review: **no must-fix issues**). The §8 `/start`-honesty conformance risk is **resolved** (honest 400 is safe, landed). **Phase 4 DONE** — the external `pipelex-mistralai-workflows` plugin migrated to the new core surface (commit `0eb3878` on `feature/Mistral-native`): deleted-enum import + registration under the deleted member + own `execution_mode` wire field + old `run` signature → `orchestration_mode`/`delivery`/new protocol, `supports_fire_and_forget=False`. Gates green + clean-context `/code-review` (one must-fix — a stale README — applied). **DEVIATION:** the plugin pins core editable at `../_workflows` (`feature/mistralai-2x-bump`), NOT the orchestration-split worktree, so Phase 4 first merged `feature/orchestration-mode-delivery-split` → `feature/mistralai-2x-bump` (`_workflows` merge `a1448ad8f`, conflict-free, core gates green). `make check-spec-links` was NOT triggered (no `docs/specs/` heading or `conformance/` test asserts mode values). See **"### Phase 4 — as-built"** in §10 for the full record. The one carry-forward from Phase 3 (the `pipelex-api` editable-pin → pushed-git-SHA flip before any PR) is still open and now joined by the `_workflows`/`_plugins` doc-reconciliation note (see §10 Phase 4 as-built). Design **locked** in discussion (2026-06-22). This is **Direction A** — the real two-axis split across all three repos (`pipelex` core → `pipelex-temporal` → `pipelex-api`), not the API-edge-only reshape (Direction B, rejected: it relocates the smell to the boundary instead of removing it). It **supersedes** the dual-mode framing in the two follow-on plans `wip/plugins/execute-per-request-execution-mode.md` and `wip/plugins/orchestrator-dispatched-validate.md` — both shipped a working surface on top of the *flat* `PipelexExecutionMode`; this plan removes the flatness those plans worked around (`_async_start_mode`, the `/execute` fire-and-forget 400, the `ApiConfig` f&f field-validator, the `/validate` double-registration). Tracker: `_plugins/TODOS.md`. Parent effort: `wip/plugins/orchestrator-agnostic-runner-and-flavors.md`.

> **START HERE (cold start).** Read this whole doc. The change is conceptual, not big-mechanical-only: today one enum `PipelexExecutionMode {DIRECT, TEMPORAL_BLOCKING, TEMPORAL_FIRE_AND_FORGET, MISTRAL_NATIVE}` conflates **two orthogonal axes** — *which orchestrator runs the pipe* and *whether the caller waits*. We split them: an **open string token** `orchestration_mode` (core owns only `"direct"`; plugins own `"temporal"`, `"mistralai-workflows"`) and a **closed enum** `DeliveryMode {BLOCKING, FIRE_AND_FORGET}` that endpoints set themselves and never receive from a caller. Delivery becomes a parameter to `OrchestratorProtocol.run`, so the two Temporal orchestrators collapse into one and the `/validate` double-registration disappears. Lands core → temporal → api, one repo / branch / commit per phase, clean-context `/code-review` at each checkpoint — same discipline as the rest of the effort.

---

## 1. The ask + locked decisions

**The ask (user):** the API surface for choosing *how* a call to `/execute`, `/start`, `/validate` runs is wrong. `PipelexExecutionMode` carries two things at once — the backend and the wait-semantics — and that conflation forced three separate workarounds (`_async_start_mode`, the `/execute` f&f 400, the `/validate` collapse). Re-cut it into the right enum(s) passed as args.

**Why the conflation is the root cause.** The blocking-vs-fire-and-forget decision is **already expressed by which endpoint you call** — `/execute` and `/validate` are synchronous, `/start` is fire-and-forget. Encoding it a *second* time inside the mode enum is the duplication, and every smell is a workaround for it:

- `_async_start_mode` (`pipeline.py:109-134`) maps `temporal_blocking → temporal_fire_and_forget` because `/start` has to re-derive the delivery axis the enum shouldn't carry.
- `/execute` rejects `temporal_fire_and_forget` with a 400 (`ErrorType.FIRE_AND_FORGET_NOT_SUPPORTED`) — refusing a delivery value the caller should never have been able to express.
- `ApiConfig._reject_fire_and_forget_default` (`api_config.py:54-74`) rejects a *configured* f&f mode — same problem on the config side.
- `TemporalPlugin.register` registers the **same** bundle validator under **both** temporal modes (`temporal_plugin.py:136-138`, decision D of the validate plan) — collapsing the axis because validate doesn't have it either.

Three endpoints, three kludges, one root cause: a wait-semantics dimension the caller/deployment should never receive.

**Locked decisions (do not re-litigate; raise explicitly before deviating):**

- **D-A — Two axes, two types.** Replace `PipelexExecutionMode` with `orchestration_mode` (the backend) + `DeliveryMode` (the wait-semantics). They are orthogonal: orchestration is per-call/config-chosen; delivery is endpoint-intrinsic.
- **D-B — `orchestration_mode` is an OPEN string token, not a closed enum.** Core defines only `"direct"` (its single built-in orchestrator). Each plugin defines and registers its own token *where its orchestrator lives* (`pipelex-temporal` owns `"temporal"`; `pipelex-mistralai-workflows` owns `"mistralai-workflows"`). The registry is keyed by `str`; validation is *"is a mode registered?"* — a lookup miss raises `MissingOrchestratorError` — never *"is it in an enum?"*. **This is the deliberate, justified exception to the repo's StrEnum-everywhere standard:** that standard exists to make the linter scream when a *closed* value is added and a branch forgotten; a plugin-contributed set is genuinely open, exhaustive matching is impossible, and the dict-lookup-or-raise *is* the correct unknown-handling. It is also the only choice faithful to the effort's north star (*the public base names no orchestrator*) — a closed `{DIRECT, TEMPORAL, MISTRAL}` enum would re-introduce the exact core→plugin coupling Phase C removed.
- **D-C — `DeliveryMode {BLOCKING, FIRE_AND_FORGET}` is a closed core StrEnum, endpoint-intrinsic, never on the wire or in config.** Endpoints set it: `/execute` → `BLOCKING`, `/validate` → `BLOCKING`, `/start` → `FIRE_AND_FORGET`. It stays a real StrEnum with exhaustive `match`/`case` (the standard applies — this axis is genuinely closed; a future `STREAMING` would be core's call, never a plugin's).
- **D-D — Delivery is a parameter to dispatch, not a registry key.** `OrchestratorProtocol.run(*, pipe_job, delivery_assignment, delivery: DeliveryMode)`. Each orchestrator honors `delivery` per its nature. This collapses the two Temporal orchestrators into one (`run` branches on `delivery`: `temporal_pipe_run.run(...)` for blocking vs `.start(...)` for f&f) registered **once** under `"temporal"`, and collapses the `/validate` double-registration to a single `"temporal"` validator.
- **D-E — `/start` is HONEST, not degrading.** An orchestrator advertises `supports_fire_and_forget: bool` (`direct`=False, `temporal`=True, `mistralai-workflows`=False). `/start` checks the *resolved* mode's orchestrator and, when it can't do genuine async, returns a 4xx — *"this deployment has no async-capable orchestration; use /execute"* — instead of silently running blocking and acking. (Replaces today's `_async_start_mode` identity-arm degrade.) **Consequence to verify (§8):** on the OSS base (`orchestration_mode = "direct"`) every `/start` now 4xxs — confirm the MTHDS-Protocol/conformance position on `/start` over a non-async backend before committing.
- **D-F — Generic `MissingOrchestratorError` / `MissingBundleValidatorError` message.** *"No orchestrator registered for mode '{mode}'; is its plugin installed?"* — fully decoupled. Drop the per-mode `match`-on-enum install hints (`requires_pipelex_temporal` etc.). Core no longer names `temporal`/`mistral` in its error text.
- **D-G — Rename the Mistral token `"mistral_native"` → `"mistralai-workflows"`** for consistency with the package name (`pipelex-mistralai-workflows`). Mistral is a deferred/secondary plugin; this is just the token string + any doc reference.
- **D-H — Rename the wire field + config key `execution_mode` → `orchestration_mode`** (and `allow_request_execution_mode_override` → `allow_request_orchestration_mode_override`). Breaking wire/config change — fine per the workspace "no backward compatibility" principle; note in changelogs.

---

## 2. Current state (anchors verified 2026-06-22)

**Core (`_plugins/pipelex/`):**

- `runtime_bridge/execution_mode.py` — `PipelexExecutionMode(StrEnum)`: the 4 members + three properties to delete: `requires_pipelex_temporal` (`30`), `requires_mistral_workflows_extra` (`38`), `is_fire_and_forget` (`46`).
- `plugins/orchestrator_registry.py` — `OrchestratorProtocol.run(self, *, pipe_job, delivery_assignment) -> PipelexPipeRunOutput` (`21`); `OrchestratorRegistry(orchestrators: dict[PipelexExecutionMode, OrchestratorProtocol])` with `get_optional(*, mode)` / `has(*, mode)` / `modes` (`24-42`).
- `plugins/bundle_validator_registry.py` — the `/validate` twin (`BundleValidatorProtocol`, `BundleValidatorRegistry`), same `dict[PipelexExecutionMode, …]` keying (per the validate plan's V0 as-built).
- `plugins/registrar.py` — stores `orchestrators` / `bundle_validators` typed `dict[PipelexExecutionMode, …]` (`111-112`) + the `_*_sources` mirrors (`122-123`); `add_orchestrator(*, mode, …)` (`167`) and `add_bundle_validator(*, mode, …)` (`179`).
- `runtime_bridge/exceptions.py` — `MissingOrchestratorError._build_message` (`27-40`) and `MissingBundleValidatorError._build_message` (`61-74`) both `match` on enum members for per-mode install hints (→ generic, D-F).
- `runtime_bridge/direct_orchestrator.py` — `DirectOrchestrator.run(*, pipe_job, delivery_assignment)` (`29`), returns `serialize_completed_output(pipe_output, workflow_id=None)` (`46-49`). Add `supports_fire_and_forget = False` + the `delivery` param (ignored — in-process always blocks).
- `plugins/direct/direct_plugin.py` — `register` calls `add_orchestrator(mode=DIRECT, …)` + `add_bundle_validator(mode=DIRECT, …)` (`20-22`). `DIRECT` becomes the `"direct"` string constant.
- `runtime_bridge/bridge.py` — `run_pipe_via_bridge` (`51`): `is_direct = input_payload.execution_mode is PipelexExecutionMode.DIRECT` (`85`); dispatch `get_orchestrator_registry().get_optional(mode=input_payload.execution_mode)` → `orchestrator.run(pipe_job, delivery_assignment)` (`92-95`); `_validate_input` checks `execution_mode is TEMPORAL_FIRE_AND_FORGET` for the delivery-target requirement (`164`). This is the **Tier-3 bridge** for *other* host runtimes (Mistral Workflows, raw Temporal) — pipelex-api's routes do **not** go through it (they hit the registry directly), but it must split consistently and pass `delivery`.
- `runtime_bridge/payloads.py` — `PipelexPipeRunInput.execution_mode: PipelexExecutionMode = DIRECT` (`30`). Splits into `orchestration_mode: str = "direct"` + `delivery: DeliveryMode = BLOCKING`. `PipelexPipeRunOutput` (`34-52`) already carries `workflow_id` / `is_completed` and needs no change.

**Temporal (`pipelex-temporal/pipelex_temporal/`):**

- `temporal_orchestrators.py` — `TemporalBlockingOrchestrator.run` calls `temporal_pipe_run.run(...)` (await completion, `58`) and reports `make_workflow_id(...)` (`69`); `TemporalFireAndForgetOrchestrator.run` calls `temporal_pipe_run.start(...)` (return immediately, `91`) and builds an `is_completed=False` output (`96-103`). **The only difference is `run` vs `start`** → one `TemporalOrchestrator` whose `run` branches on `delivery`. Both already guard the `pipelex[temporal]` extra via `_require_temporal_extra` (`32-40`).
- `temporal_plugin.py` — `register` (`127`): two `add_orchestrator` calls under the two temporal modes (`128-129`), two `add_bundle_validator` calls under both temporal modes (`136-138`), the F3 mapper (`145`, unchanged), and the `boot_orchestrator == self.name` slot-claims (`152-157`, unchanged — `boot_orchestrator` is the *separate* hub-slot gate, orthogonal to the orchestration token even though both read `"temporal"`).
- `temporal_bundle_validator.py` — `TemporalBundleValidator` (validate is inherently blocking; register once under `"temporal"`).

**API (`pipelex-api/api/`):**

- `api_config.py` — `ApiConfig.execution_mode: PipelexExecutionMode` + `allow_request_execution_mode_override: bool` (`51-52`); the `_reject_fire_and_forget_default` field-validator to **delete** (`54-74`); `resolve_execution_mode(requested, *, config)` (`103-121`) → rename `resolve_orchestration_mode`, logic unchanged (string compare; 403 on forbidden override); `api.toml` ships `execution_mode = "direct"`.
- `schemas/models.py` — `PipelineApiExtras.execution_mode: PipelexExecutionMode | None` (`153`); `PipelexApiExecuteRequest.execution_mode` (`204`); `_EXECUTION_MODE_DESCRIPTION` (`110-115`, lists the four enum values → open-token wording). Rename field → `orchestration_mode`.
- `routes/pipelex/pipeline.py` — `_async_start_mode` (`109-134`, **delete**); `ApiRunner.execute` resolves + rejects f&f (`208-214`, drop the rejection) and injects `_OrchestratorPipeRun` (`222`); `ApiRunner.start` calls `_async_start_mode(resolve_…(…))` (`283`, → resolve + capability-check); `_OrchestratorPipeRun` / `_pipe_output_from_run_output` (`73-153`, thread `delivery`); `validate_verdict` (`343-390`, rename the mode param). Routes `execute` (`530`) / `start` (`572`) thread the renamed extra.
- `error_types.py` — `EXECUTION_MODE_OVERRIDE_FORBIDDEN` (`23`, rename) and `FIRE_AND_FORGET_NOT_SUPPORTED` (`32`, **delete**); add the honest-`/start` rejection type (D-E).
- `routes/pipelex/validate.py` — threads `execution_mode` into `validate_verdict` (rename to `orchestration_mode`).
- The committed OpenAPI artifact (`docs/openapi/pipelex-api.openapi.yaml`) re-exports via `make openapi-export`; drift-gated by `make openapi-check`.

> **Mechanical-usage inventory** (every stray `PipelexExecutionMode` / `execution_mode` / mode-literal site across the three repos) is being swept and is appended in §9 — run that grep before declaring a phase done.

---

## 3. The design (locked)

### 3.1 Core types (`_plugins/pipelex/runtime_bridge/`)

- **`orchestration_mode.py`** (replaces `execution_mode.py`):
  - `OrchestrationMode: TypeAlias = str` — a *semantic* alias documenting intent at registry/protocol signatures (`dict[OrchestrationMode, …]`), assignment-compatible with plain `str` so plugins pass raw strings with no casts. (Chose the alias over `NewType` deliberately: a `NewType` forces `OrchestrationMode("temporal")` casts at every plugin boundary for zero validation benefit — the registry is the validator. See §8.)
  - `DIRECT_ORCHESTRATION_MODE: Final[str] = "direct"` — core's one built-in token, referenced by core code instead of the literal.
- **`delivery_mode.py`** — `class DeliveryMode(StrEnum): BLOCKING = "blocking"; FIRE_AND_FORGET = "fire_and_forget"`. Closed; exhaustive `match`/`case`; no default arm.
- **`OrchestratorProtocol`** (`plugins/orchestrator_registry.py`):
  - `async def run(self, *, pipe_job, delivery_assignment, delivery: DeliveryMode) -> PipelexPipeRunOutput`
  - `supports_fire_and_forget: bool` — capability the runner reads *before* dispatch (D-E). A `@property` or a plain class attribute; the protocol declares it as a read-only attribute.
- **`OrchestratorRegistry`** / **`BundleValidatorRegistry`** — re-key `dict[OrchestrationMode, …]` (i.e. `dict[str, …]`); `get_optional(*, mode: OrchestrationMode)` / `has`. `BundleValidatorProtocol.validate_bundles` stays blocking — pass no `delivery` (or `DeliveryMode.BLOCKING` if uniformity reads better; validate has no async variant).
- **`PluginRegistrar`** — `orchestrators` / `bundle_validators` + `_*_sources` become `dict[str, …]`; `add_orchestrator(*, mode: OrchestrationMode, …)` / `add_bundle_validator(*, mode: OrchestrationMode, …)`. The `_add` dup-guard is unchanged (works on any hashable key).
- **`Missing*Error`** — drop the `match`; generic message keyed on the string (D-F). Optionally keep a one-line special case for `"direct"` (*"DIRECT is core; this indicates a boot/discovery problem"*) via a plain `if mode == DIRECT_ORCHESTRATION_MODE:` — now a string compare, not an enum, so it does not offend the no-`==`-on-enums rule.
- **`payloads.py`** — `PipelexPipeRunInput`: `execution_mode` → `orchestration_mode: str = DIRECT_ORCHESTRATION_MODE` + `delivery: DeliveryMode = DeliveryMode.BLOCKING`.
- **`bridge.py`** — `is_direct = input_payload.orchestration_mode == DIRECT_ORCHESTRATION_MODE`; pass `delivery=input_payload.delivery` into `orchestrator.run(...)`; `_validate_input` checks `input_payload.delivery is DeliveryMode.FIRE_AND_FORGET` for the delivery-target requirement.
- **`DirectOrchestrator`** — `supports_fire_and_forget = False`; `run(*, pipe_job, delivery_assignment, delivery)` accepts `delivery` for protocol uniformity and ignores it (in-process always blocks; `/start` never reaches it because the capability check rejects first — the bridge/Tier-3 path that *could* pass f&f to direct simply blocks, see §8).

### 3.2 Temporal plugin (`pipelex-temporal`)

- **One `TemporalOrchestrator`** (`temporal_orchestrators.py`) — `supports_fire_and_forget = True`; `run` branches:
  - `DeliveryMode.BLOCKING` → `temporal_pipe_run.run(...)` then `make_workflow_id(...)` (today's `TemporalBlockingOrchestrator` body).
  - `DeliveryMode.FIRE_AND_FORGET` → `temporal_pipe_run.start(...)` → `is_completed=False` output (today's `TemporalFireAndForgetOrchestrator` body).
  - Keep the `_require_temporal_extra` guard and the `WorkflowExecutionError` wrapping in both arms.
- **`TEMPORAL_ORCHESTRATION_MODE: Final = "temporal"`** defined in `pipelex-temporal` (the token lives with the orchestrator). Distinct constant from the plugin's `name` / `boot_orchestrator` value even though both equal `"temporal"` — they are different concepts (registry key vs hub-slot gate).
- **Register once** — `add_orchestrator(mode=TEMPORAL_ORCHESTRATION_MODE, orchestrator=TemporalOrchestrator())`; `add_bundle_validator(mode=TEMPORAL_ORCHESTRATION_MODE, validator=TemporalBundleValidator())`. The two-mode duplication is gone. F3 mapper + boot-slot claims unchanged.

### 3.3 API (`pipelex-api`)

- **`ApiConfig`** — `orchestration_mode: str` + `allow_request_orchestration_mode_override: bool`; **delete** `_reject_fire_and_forget_default` (no f&f token can be configured anymore). `resolve_orchestration_mode(requested: str | None, *, config) -> str` — same default/override/403 logic. `api.toml`: `orchestration_mode = "direct"`, `allow_request_orchestration_mode_override = false`.
- **Wire** (`schemas/models.py`) — `PipelineApiExtras.orchestration_mode: str | None`; `PipelexApiExecuteRequest.orchestration_mode`; description reworded for the open token (*"e.g. `direct`, `temporal`; plugin-provided modes also accepted; an unregistered mode is refused at dispatch"*; drop the `temporal_fire_and_forget` value — no such token exists).
- **`/execute`** — resolve `orchestration_mode`; **no f&f rejection** (delete the `is_fire_and_forget` branch + `FIRE_AND_FORGET_NOT_SUPPORTED`); dispatch via `_OrchestratorPipeRun(orchestrator, delivery=DeliveryMode.BLOCKING)`.
- **`/start`** — resolve `orchestration_mode`; get orchestrator; **capability check (D-E):** `if not orchestrator.supports_fire_and_forget: raise_<4xx>(…, error_type=START_REQUIRES_ASYNC_ORCHESTRATION)`; dispatch with `delivery=DeliveryMode.FIRE_AND_FORGET`. **Delete `_async_start_mode`.**
- **`/validate`** — `validate_verdict(*, …, requested_orchestration_mode)`; resolve + dispatch through the validator registry (blocking). Verdict-as-value unchanged.
- **`error_types.py`** — rename `EXECUTION_MODE_OVERRIDE_FORBIDDEN` → `ORCHESTRATION_MODE_OVERRIDE_FORBIDDEN`; **delete** `FIRE_AND_FORGET_NOT_SUPPORTED`; **add** `START_REQUIRES_ASYNC_ORCHESTRATION` (D-E). Status code: §8.
- **OpenAPI** — `make openapi-export`; the request schemas now advertise `orchestration_mode`.

---

## 4. Phased implementation + checkpoints

Each phase: own repo, own branch, own commit, gates green, clean-context `/code-review` (hand the reviewer only the diff/SHA, never this plan). Ordering is load-bearing — core must land before its consumers.

### Phase 1 — core split (`pipelex`, `_plugins/`) ✅ DONE (`bd4e52827`)

Add `OrchestrationMode` (open `str` alias + `DIRECT_ORCHESTRATION_MODE`) and `DeliveryMode`; re-key both registries + the registrar stores on `str`; add `delivery` + `supports_fire_and_forget` to `OrchestratorProtocol`; update `DirectOrchestrator`; generic `Missing*Error`; split `PipelexPipeRunInput`; update `bridge.py` dispatch + `_validate_input`; delete `PipelexExecutionMode` and its three properties. Sweep §9. Gates: `make agent-check`, `make tb` (boot builds the re-keyed registries), `make agent-test`.

> **🛑 Checkpoint 1** — core green; registries keyed by string; boot still builds them; no orchestrator named in core error text. Capture an as-built (final type names/locations, the `OrchestrationMode` alias decision, the `supports_fire_and_forget` shape). Clean-context `/code-review` on the core diff.

### Phase 2 — Temporal collapse (`pipelex-temporal`) · **MAJOR** ✅ DONE (`01d7dc3`)

Collapse the two orchestrators into `TemporalOrchestrator` (delivery-branch + `supports_fire_and_forget=True`); define `TEMPORAL_ORCHESTRATION_MODE`; register orchestrator + validator **once** under `"temporal"`; F3 mapper + boot-slot claims unchanged. Update the in-memory integration tests to exercise both delivery values through the single orchestrator. Gates: `make agent-check`, `make agent-test` against the Phase 1 core.

> **🛑 Checkpoint 2** — the seam has a real consumer through the new shape: one `"temporal"` arm serving both deliveries; the base still has no idea Temporal exists. As-built + clean-context `/code-review` on both diffs.

### Phase 3 — API (`pipelex-api`) · **THE gate**

`ApiConfig` rename + drop the f&f validator + `resolve_orchestration_mode`; wire-model rename + open-token description; routes — delete `_async_start_mode`, `/execute` drop f&f-reject (delivery=BLOCKING), `/start` honest capability-reject (delivery=FIRE_AND_FORGET), `/validate` param rename; `error_types` changes; `make openapi-export`; docs + changelog. Sweep §9. Gates: `make agent-check`, `make agent-test`, `make openapi-check`.

> **🛑 Checkpoint 3** — `/execute`, `/start`, `/validate` all take `orchestration_mode`; delivery is endpoint-set; `/start` honestly rejects non-async modes; all the conflation workarounds are gone; OpenAPI in sync. **Verify the `/start`-honesty conformance question (§8) here.** As-built + clean-context `/code-review` on the API diff.

### Phase 4 — Mistral plugin migration (`pipelex-mistralai-workflows`) + bookkeeping ✅ DONE (`0eb3878`)

> **✅ DONE — see "### Phase 4 — as-built" in §10 for the full record** (incl. the load-bearing deviation: the plugin pins `../_workflows`, not the split worktree, so the split was first merged into `feature/mistralai-2x-bump`). The anchors below were re-derived against the live tip and were accurate; the migration matched this scope.

**⚠️ Scope correction (2026-06-22, from the Phases 1–2 `/code-review`): the Mistral work is NOT "token string + doc refs only" — that earlier estimate was wrong.** The external `pipelex-mistralai-workflows` plugin pins core editable, so it is already **red against Phase 1 core**: it carries live *code* that breaks under the `execution_mode → orchestration_mode` rename (`extra="forbid"`) + the `PipelexExecutionMode` deletion. This is the same Phase-2 shape the Temporal plugin took — a real code migration, not a string swap. **Anchors verified against tip `e508e73` (re-derive before starting — the tip will have moved):**

- `mistral_plugin.py:17` + `streaming.py:28` — `from pipelex.runtime_bridge.execution_mode import PipelexExecutionMode` → **`ImportError`** (module deleted). Re-point to `orchestration_mode` / `delivery_mode`.
- `mistral_plugin.py:31` — `add_orchestrator(mode=PipelexExecutionMode.MISTRAL_NATIVE, …)` registers under the **deleted enum member** → migrate to `add_orchestrator(mode="mistralai-workflows", …)` (the D-G token; the `"mistral_native"` literal lives at `mistral_plugin.py:21`).
- `mistral_orchestrator.py` — `MistralNativeOrchestrator.run` must adopt the new `OrchestratorProtocol.run(*, pipe_job, delivery_assignment, delivery: DeliveryMode)` signature and declare `supports_fire_and_forget = False` (Mistral-native is blocking-only).
- `streaming.py:77,117,121` — its own `execution_mode: str` wire field + `input_payload.execution_mode` read + `is PipelexExecutionMode.DIRECT` compare → rename to `orchestration_mode`, drop the enum compare (string-compare `== DIRECT_ORCHESTRATION_MODE` or branch on `delivery`).
- `primitives/__init__.py`, `primitives/pipe_run.py`, `primitives/scoped_library.py` — docstring/comment/`mistral_native_…` string refs (the genuinely doc-only part).

**Then the bookkeeping:** update `_plugins/TODOS.md` (Phase C as-built + follow-on section), mark the two superseded sibling plans, `pipelex-api` `CHANGELOG.md`, and `docs/specs/` / `conformance/` if any heading or test asserts mode values (run `make check-spec-links`).

---

## 5. Cross-repo state, pins, gates

- **Verify state FIRST (cold start).** Before cutting any branch, run the effort's orientation ritual — [`wip/plugins/resume-and-verify-state.md`](resume-and-verify-state.md) — and confirm each repo's working tree is clean and sits on the tip `_plugins/TODOS.md` "Current branches" names. This plan was written against those tips; if they have moved, re-derive the §2 / §9 anchors against the new tip rather than trusting the line numbers.
- **Branches:** cut fresh per phase off the verified current tips (`_plugins` core worktree; `pipelex-temporal`; `pipelex-api` off the `feature/Execute-per-request-mode` tip — this work stacks on the landed follow-ons it supersedes).
- **Pins:** `pipelex-api` and `pipelex-temporal` both pin `pipelex = { path = "../_plugins", editable = true }`, so a core edit is live in both immediately. `pipelex-api` does **not** depend on `pipelex-temporal` (Phase C dropped it) — test the API split with a stub orchestrator/validator registered for a non-`direct` test token; never import `temporalio` in `pipelex-api`. For a `pipelex-api` PR, the editable pin breaks CI — pin core to a git+https SHA + relock, flip back at release (the PR #27 `c60be1f` gotcha).
- **Gates:** core → `make agent-check` / `make tb` / `make agent-test`; temporal + api → `make agent-check` / `make agent-test`; api also `make openapi-check`; conformance → `make check-spec-links` if a spec heading is touched.

---

## 6. Spec / conformance

- **No verdict/result wire contract changes** — `/validate`'s 200-always + `is_valid` discriminant + `validation_errors[]`, and the `/execute` / `/start` result shapes, are all backend-independent and untouched. The only wire change is the **request** field rename `execution_mode → orchestration_mode` (a pipelex-api extension field, not protocol-core).
- **The one real conformance question is D-E's `/start` honesty** (§8): if the MTHDS-Protocol conformance suite asserts `/start` returns a `202` on the reference implementation's default (direct) config, honest-reject breaks it. Resolve before Phase 3 lands.
- If any `docs/specs/` heading documents the mode values or the per-endpoint delivery behavior, update it + its `> Verified by:` link and run `make check-spec-links`.

---

## 7. Testing

- **Core (Phase 1):** registry/registrar keyed by string (register + get + dup under arbitrary tokens incl. a synthetic `"acme"`); `MissingOrchestratorError` generic message for an unregistered token + the `"direct"` boot-bug special case; `DeliveryMode` exhaustive; `OrchestratorProtocol` round-trip with `delivery` + `supports_fire_and_forget`; `DirectOrchestrator.supports_fire_and_forget is False`; `PipelexPipeRunInput` defaults (`orchestration_mode="direct"`, `delivery=BLOCKING`); bridge `_validate_input` requires a delivery target iff `delivery is FIRE_AND_FORGET`. `make tb` green (boot builds the re-keyed registries).
- **Temporal (Phase 2):** in-memory-Temporal integration — the single `TemporalOrchestrator` returns a completed output for `delivery=BLOCKING` and an `is_completed=False` + `workflow_id` for `delivery=FIRE_AND_FORGET`; one registration serves both; validator registered once.
- **API (Phase 3, no temporal import):** `/execute` dispatches blocking with no f&f branch; `/start` honestly 4xxs when the resolved mode's stub orchestrator has `supports_fire_and_forget=False`, and acks when it's `True`; override policy on string tokens (default honored, different+allowed honored, different+forbidden → 403); an unregistered requested token → `MissingOrchestratorError` at dispatch; `/validate` unchanged verdict mapping; OpenAPI advertises `orchestration_mode`.

---

## 8. Open sub-decisions / risks for the implementing session

- **[RISK — ✅ RESOLVED 2026-06-22, before Phase 3] `/start` honesty vs MTHDS-Protocol conformance.** D-E makes the runner's `/start` 4xx on a direct-only deployment (the OSS default). **Investigated and cleared:** (1) the protocol spec `docs/specs/pipelex-mthds-protocol.md` (§"operations") explicitly **defers `execute`/`start` artifacts to pipelex-api's OpenAPI + run-lifecycle docs** — it details only the `validate` and `models` artifacts, so the protocol mandates **no** `/start` status code; (2) the `conformance/tests/pipelex_api/` suite exercises only `validate` / `models` / `version` — **no test POSTs to the runner `/v1/start`**; every conformance `202` (`conformance/tests/pipelex_platform/test_runs_results.py`, `docs/specs/pipelex-platform-api.md`) is the **platform's run-results polling** (a RUNNING run → 202), a different endpoint untouched by the runner's dispatch semantics; (3) the platform's `/v1/start` (wallet-gated front door) is distinct from the runner's `/start`, and the hosted platform runs the `temporal` orchestration mode (async-capable → no 4xx). **Net: honest 4xx on a direct-only base breaks no conformance test and no protocol clause.** Proceed with D-E; status code **400** (§ next bullet). The only consumer to update is pipelex-api's own `/start` tests + its OpenAPI/docs (owned by Phase 3).
- **Honest-`/start` status code.** `400` (symmetric with the deleted `FIRE_AND_FORGET_NOT_SUPPORTED`), `409 Conflict` (request conflicts with deployment capability), or `422`. Recommend `400` with a crisp message; flag `409` as the more semantically precise alternative.
- **`OrchestrationMode` shape.** Recommended: `TypeAlias = str` + a `Final` `"direct"` constant (open, cast-free for plugins). Alternative: a `NewType` (stronger signatures, but forces casts at every plugin boundary for no validation gain since the registry validates). Decide in Phase 1; record in the as-built.
- **`supports_fire_and_forget` location.** On the orchestrator (recommended — it owns its capability) vs registry-level metadata. Keep it on the protocol unless a second consumer needs it elsewhere.
- **Unknown requested token validation timing.** Let dispatch raise `MissingOrchestratorError` (recommended — single validation point, keeps `resolve_orchestration_mode` decoupled from the hub) vs an early `get_orchestrator_registry().has(...)` check in resolve (earlier 4xx but couples config to the hub). Default: dispatch-time.
- **[RESOLVED 2026-06-22 — bridge-level guard added] Tier-3 bridge + incompatible (mode, delivery).** A raw bridge caller could pass `delivery=FIRE_AND_FORGET` to `direct`. The original disposition was "let it block — `DirectOrchestrator` blocks regardless." **The combined Phases 1–2 `/code-review` found that understated the harm:** with a valid `delivery_assignment_dump` the request passes `_validate_input`'s delivery-target gate, then `DirectOrchestrator.run` ignores `delivery`, runs to completion, and returns `is_completed=True` — so the caller **blocks** for the full run AND its delivery target is **never invoked** while completion is **falsely acked**. **Decided: add the bridge-level capability guard** (the Tier-3 counterpart of `/start`'s D-E check), not "let it block." The bridge now resolves the orchestrator up front and `_validate_input` rejects `FIRE_AND_FORGET` on any orchestrator with `supports_fire_and_forget = False` (raises `PipelexBridgeDispatchError`), regardless of whether a delivery target was supplied. As-landed shape + tests: see the post-checkpoint hardening note in §10. (This is pure core/`_plugins` work; pipelex-api routes hit the registry directly, not the bridge, so Phase 3 is unaffected.)
- **`boot_orchestrator` vs the `"temporal"` token.** Both strings equal `"temporal"` but are distinct concepts (hub-slot boot gate vs orchestrator-registry key). Keep separate named constants; do not unify them just because the values coincide today.

---

## 9. Mechanical-usage inventory

> Swept across `_plugins/`, `pipelex-temporal/`, `pipelex-api/`. Actionable per-repo, keyed to the §3 decisions. **Re-run the grep at each phase boundary — do not trust a stale list:** `grep -rn 'PipelexExecutionMode\|execution_mode\|is_fire_and_forget\|requires_pipelex_temporal\|requires_mistral_workflows\|temporal_blocking\|temporal_fire_and_forget\|mistral_native' <repo>`.

### Core (`_plugins/pipelex/`)

**Source — split / rename / delete:**
- `runtime_bridge/execution_mode.py` → **replace** with `orchestration_mode.py` (`OrchestrationMode` alias + `DIRECT_ORCHESTRATION_MODE`) and `delivery_mode.py` (`DeliveryMode`). Delete the enum + its 3 properties.
- `runtime_bridge/payloads.py:16,30` — `PipelexPipeRunInput.execution_mode` → `orchestration_mode: str` + `delivery: DeliveryMode`.
- `runtime_bridge/bridge.py:36,85,92,94,164` — import; `is_direct` string-compare; registry dispatch; `MissingOrchestratorError`; `_validate_input` f&f-target check → `delivery is FIRE_AND_FORGET`; pass `delivery` to `run`.
- `runtime_bridge/exceptions.py:2,22-40,56-74` — `Missing{Orchestrator,BundleValidator}Error` — drop the per-mode `match`; generic message (D-F).
- `plugins/orchestrator_registry.py:3,21,31-42` — protocol `run(*, …, delivery)` + `supports_fire_and_forget`; registry `dict[str, …]`.
- `plugins/bundle_validator_registry.py:33,77-89` — registry `dict[str, …]` (validate stays blocking).
- `plugins/registrar.py:19,111-123,167-189` — `orchestrators`/`bundle_validators` + `_*_sources` → `dict[str, …]`; `add_orchestrator`/`add_bundle_validator` mode param typed `OrchestrationMode`.
- `runtime_bridge/direct_orchestrator.py:29,46-49` — `supports_fire_and_forget = False`; accept+ignore `delivery`.
- `plugins/direct/direct_plugin.py:5,20-22` — register under `DIRECT_ORCHESTRATION_MODE`.

**Tests — update/retarget:** `tests/unit/pipelex/runtime_bridge/test_execution_mode.py` (→ `test_orchestration_mode.py` + new `test_delivery_mode.py`; the property tests die with the properties), `test_input_models.py`, `test_dispatch.py`, `test_validation.py` (the f&f-target requirement now keys on `delivery`), `test_orchestrator_dispatch.py`, `test_direct_router_scoping.py`, `test_missing_bundle_validator_error.py` (parametrized over per-mode install hints → assert the generic message + the `"direct"` boot-bug case), `test_exceptions_disclosure.py`, `test_trace_context_contract.py`, `tests/unit/pipelex/plugins/test_bundle_validator_registry.py`, `test_plugin_discovery.py`, `tests/integration/pipelex/runtime_bridge/test_bridge_direct.py`.

**Docs:** the `execution_mode.py` module docstring (moves to the new modules); `TODOS.md` two-axis narrative (the `boot_orchestrator` vs `execution_mode` table, the f&f-derivation notes) — update at Phase 4 bookkeeping.

### Temporal (`pipelex-temporal/pipelex_temporal/`)

**Source:** `temporal_plugin.py:29,128-138` — collapse the two `add_orchestrator` + two `add_bundle_validator` calls to one each under `TEMPORAL_ORCHESTRATION_MODE`; `temporal_orchestrators.py` — merge `TemporalBlocking`/`TemporalFireAndForget` into one `TemporalOrchestrator` (`run` branches on `delivery`, `supports_fire_and_forget=True`); `temporal_bundle_validator.py` — unchanged impl, registered once; `temporal_cli.py` — docstring mention of `execution_mode`. `boot_orchestrator == self.name` gate (`:152`) is **unchanged** (separate concept — leave it).

**Tests:** `tests/unit/pipelex_temporal/test_bridge_temporal_dispatch.py` (both modes → one orchestrator + `delivery`), `test_trace_context_temporal.py`, `test_temporal_blocking_workflow_id.py`; the in-memory validator integration test (single registration).

### API (`pipelex-api/api/`)

**Source:** `api_config.py:27,51-74,103-122` — field rename + **delete** `_reject_fire_and_forget_default` + `.is_fire_and_forget` use; `resolve_execution_mode` → `resolve_orchestration_mode`. `schemas/models.py:19,110-115,153,204` — `PipelineApiExtras`/`PipelexApiExecuteRequest` field rename + open-token description. `routes/pipelex/pipeline.py:22,109-135,183,208,247,283,349,375` — **delete** `_async_start_mode`; `/execute` drop f&f-reject; `/start` capability-reject; `_OrchestratorPipeRun`/`_pipe_output_from_run_output` thread `delivery`; `validate_verdict` param rename. `routes/pipelex/validate.py:8,67-75,188` — `ValidateRequest.execution_mode` → `orchestration_mode` (confirm the model's home — route vs `models.py`). `error_types.py:13,23,32` — rename `EXECUTION_MODE_OVERRIDE_FORBIDDEN`; delete `FIRE_AND_FORGET_NOT_SUPPORTED`; add `START_REQUIRES_ASYNC_ORCHESTRATION`.

**Config:** `api/api.toml:15,19` — `execution_mode = "direct"` → `orchestration_mode = "direct"`; `allow_request_execution_mode_override` → `allow_request_orchestration_mode_override`; update the file comments.

**Tests:** `tests/unit/test_api_config.py` (drop the f&f-rejection test; rename), `test_execute_dispatch.py` (drop the 400-f&f case), `test_start_dispatch.py` (was "derives f&f variant" → now "honest-reject when not async-capable" + ack when capable), `test_validate_dispatch.py`, `test_protocol_conformance.py`.

**Docs + OpenAPI:** `api_config.py` / `api.toml` / `models.py` docstrings; `make openapi-export` regenerates `docs/openapi/pipelex-api.openapi.yaml`; `CHANGELOG.md`.

### Cross-cutting

- **`boot_orchestrator` is untouched** — it is the separate hub-slot boot gate (`registrar.config.plugins.boot_orchestrator`, the `temporal_plugin.py:152` claim guard), orthogonal to the orchestration token. Do not fold the two together even though both read `"temporal"`.
- **Fire-and-forget delivery-target validation** (`bridge.py:164-170`) moves from `execution_mode is TEMPORAL_FIRE_AND_FORGET` to `delivery is DeliveryMode.FIRE_AND_FORGET` — the only place that check should live post-split (the API config f&f-rejection and the `/execute` f&f-400 both delete).

---

## 10. Checkpoint log / as-built (append per phase — keep cold-start-ready)

### ⏸️ COLD-START RESUME — paused before Phase 3 (2026-06-22) — ✅ SUPERSEDED (Phase 3 DONE)

> **✅ This section is historical — Phase 3 is now landed (`pipelex-api` `dd84a9a`).** Read **"### Phase 3 — as-built (API split) — Checkpoint 3 cleared"** further down in §10 for the as-built + the one carry-forward item. The locked decisions below were applied as-is. The next cold start is **Phase 4** (Mistral plugin migration + bookkeeping); see the head **Status** line and the revised §4 Phase 4.

**Read this first on resume.** Phases 1 + 2 are landed, reviewed clean, and committed. Phase 3 (`pipelex-api`, THE gate) is **not started** — its branch is cut and the tree is clean. Everything Phase 3 needs is in **§3.3** (the locked design) and **§9 → API** (the mechanical inventory); the decisions below are already settled — do not re-litigate.

**Exact state — verify with `git -C <repo> log --oneline -1` before touching anything:**

- `_plugins` (core): branch `feature/orchestration-mode-delivery-split`. As of this tracker update the tip is **`5b70c2699`** (the cold-start checkpoint, successor of the Phase-1 tracker commit `919c09348`); **this commit lands the post-checkpoint bridge-guard change on top** (always verify the real tip with `git log` — it moves with each tracker update). (Phase 1 = `bd4e52827`; docs = `9c7a7785d`.) The bridge-guard change adds the Tier-3 fire-and-forget capability guard to `runtime_bridge/bridge.py` (+ tests + the `orchestrator-plugins.md` worked-example fix) — see the post-checkpoint hardening note just below.
- `pipelex-temporal`: branch `feature/orchestration-mode-delivery-split`, tip **`01d7dc3`**, clean. (Phase 2.)
- `pipelex-api`: branch `feature/orchestration-mode-delivery-split`, tip **`06cd5e6`** (= the cut point; **no Phase 3 code**), clean. Two partial edits (`error_types.py`, `api.toml`) were made then **reverted** — start Phase 3 from scratch against §3.3/§9.

**Cross-repo wiring (already true):** both `pipelex-temporal` and `pipelex-api` pin core **editable** (`../_plugins`), so the Phase 1 core is already live in both. `pipelex-api` does **not** depend on `pipelex-temporal` — test the API split with a **stub** orchestrator/validator registered under a non-`direct` test token (one with `supports_fire_and_forget=True` and one `False`); never `import temporalio` in `pipelex-api`.

**§8 RESOLVED — honest `/start` is safe.** The MTHDS-Protocol spec defers `execute`/`start` to pipelex-api's OpenAPI/docs; no conformance test POSTs the runner `/v1/start`; the hosted platform runs the async-capable `temporal` mode. So the runner's `/start` may honestly **400** on a direct-only base. (Details in §8.)

**Locked Phase 3 design decisions (apply as-is):**

- **Honest `/start` status code = `400`** via `raise_bad_request(message=…, error_type=ErrorType.START_REQUIRES_ASYNC_ORCHESTRATION)`. (409 was the flagged alternative; 400 chosen per §8.)
- **Restructure `ApiRunner.start`** so the capability check runs **before** `pipeline_run_setup` (no library load on a doomed request): resolve `orchestration_mode` → `get_orchestrator_registry().get_optional(mode=…)` (→ `MissingOrchestratorError` if `None`) → **`if not orchestrator.supports_fire_and_forget: raise_bad_request(START_REQUIRES_ASYNC_ORCHESTRATION)`** → `pipeline_run_setup(...)` → build `delivery_assignment` → `orchestrator.run(pipe_job=…, delivery_assignment=…, delivery=DeliveryMode.FIRE_AND_FORGET)`. **Delete `_async_start_mode`** entirely.
- **`_OrchestratorPipeRun`** gains a `delivery: DeliveryMode` ctor field; its `run` passes `delivery=self._delivery` to `orchestrator.run`. `/execute` constructs it with `delivery=DeliveryMode.BLOCKING`.
- **`/execute`**: delete the `execution_mode.is_fire_and_forget` → 400 block (and `FIRE_AND_FORGET_NOT_SUPPORTED`); dispatch `BLOCKING`.
- **`api_config.py`**: `ApiConfig.orchestration_mode: str` + `allow_request_orchestration_mode_override: bool`; **delete** the `_reject_fire_and_forget_default` field-validator (and the now-unused `field_validator` import + the `PipelexExecutionMode` import). `resolve_execution_mode` → `resolve_orchestration_mode(requested: str | None, *, config) -> str` (same default/override/403 logic; `error_type=ORCHESTRATION_MODE_OVERRIDE_FORBIDDEN`). `load_api_config`/`get_api_config` unchanged in shape.
- **`error_types.py`**: rename `EXECUTION_MODE_OVERRIDE_FORBIDDEN → ORCHESTRATION_MODE_OVERRIDE_FORBIDDEN`; delete `FIRE_AND_FORGET_NOT_SUPPORTED`; add `START_REQUIRES_ASYNC_ORCHESTRATION` (400). _(These exact edits were drafted then reverted — redo them.)_
- **`schemas/models.py`**: rename `_EXECUTION_MODE_DESCRIPTION → _ORCHESTRATION_MODE_DESCRIPTION`, reworded for the open token (drop the `direct`/`temporal_blocking`/`temporal_fire_and_forget`/`mistral_native` value list; say "e.g. `direct`, `temporal`; plugin-provided tokens accepted; an unregistered token is refused at dispatch; delivery is endpoint-set, not requestable"). `PipelineApiExtras.execution_mode → orchestration_mode: str | None`; `PipelexApiExecuteRequest.execution_mode → orchestration_mode`. Drop the `PipelexExecutionMode` import.
- **`routes/pipelex/pipeline.py`**: `_validate_extras` reads the `orchestration_mode` body key; rename every `requested_execution_mode → requested_orchestration_mode` (params on `execute`/`start`/`validate_verdict` + the two route handlers); drop the `PipelexExecutionMode` import, add `from pipelex.runtime_bridge.delivery_mode import DeliveryMode`; update docstrings (the `/start` one: no `_async_start_mode`; direct now honest-4xx, not blocking-202). **Pre-existing, do NOT widen scope:** `_validate_extras` labels all three extras with `error_type=INVALID_CALLBACK_URLS` on a malformed value — leave it (flagged in the execute follow-on's deferred list).
- **`routes/pipelex/validate.py`**: `ValidateRequest.execution_mode → orchestration_mode` (reworded description); route passes `requested_orchestration_mode=request_data.orchestration_mode`; docstring "a forbidden `execution_mode` override is a 403" → `orchestration_mode`.
- **`api.toml`**: `orchestration_mode = "direct"` + `allow_request_orchestration_mode_override = false`; reworded comments (open token; delivery is endpoint-set; flavor sets `orchestration_mode = "temporal"`). _(drafted then reverted — redo.)_

**Gates + after-work (Phase 3):** `make fui && make c` (this repo's lint/type — Python 3.11+), `make tp` (or `make t`) for tests, **`make openapi-export`** then **`make openapi-check`** (the request schemas now advertise `orchestration_mode`), update docs (`docs/configuration.md` exec-mode section → orchestration/delivery; `docs/pipe-run.md` `/start` "blocks in-process on direct" note → now honest-4xx), and `CHANGELOG.md`. **Tests to update (§9):** `test_api_config.py` (drop the configured-f&f-rejection test; rename to orchestration), `test_execute_dispatch.py` (drop the 400-f&f case), `test_start_dispatch.py` (was "derives f&f variant" → now "honest-400 when the resolved mode's stub orchestrator has `supports_fire_and_forget=False`" + "acks when True"), `test_validate_dispatch.py`, `test_protocol_conformance.py`. **CI gotcha:** the editable `../_plugins` pin breaks any `pipelex-api` PR CI — pin core to a git+https SHA + relock `uv.lock` (flip back at release). Not needed for local gates.

**Then Checkpoint 3** (§4): as-built here + clean-context `/code-review` on the `pipelex-api` diff. **Phase 4** (Mistral plugin migration + bookkeeping) is still pending — and its Mistral scope is **bigger than first estimated, not smaller**. Phase 1 moved the core-doc Mistral prose + `pipe_classification.py` + the Mistral `execution-modes.md` to `"mistralai-workflows"`, but the external `pipelex-mistralai-workflows` plugin still has live `execution_mode` / `PipelexExecutionMode` **code** — a deleted-enum import, a registration under the deleted enum member, its own `execution_mode` wire field + an `is PipelexExecutionMode.DIRECT` compare, and an orchestrator on the old `run` signature — all of which must migrate to `orchestration_mode` + `delivery` + the new protocol (same shape as the Temporal collapse). See the revised §4 Phase 4 for the verified anchors. The trackers/superseded-plan markers + `make check-spec-links` remain.

---

### Post-checkpoint hardening — Phases 1–2 combined `/code-review` (2026-06-22)

After Checkpoints 1 + 2 cleared, a clean-context `/code-review` over "what we've done so far" (Phases 1–2) surfaced a reachable behavioral gap in landed core code and a stale doc. Both fixed on the `_plugins` branch (uncommitted at time of writing — **commit before Phase 3**).

**Fix 1 — Tier-3 fire-and-forget capability guard (resolves the §8 Tier-3 open question).** The bridge forwarded `delivery=FIRE_AND_FORGET` to any resolved orchestrator without consulting `supports_fire_and_forget`, so `direct` + fire-and-forget + a valid `delivery_assignment_dump` passed `_validate_input`'s target gate, ran blocking to completion, returned `is_completed=True`, and **never invoked the delivery target** — block + false-ack + silent drop. As-landed shape:

- `run_pipe_via_bridge` now **resolves the orchestrator up front** (before opening the scoped library / building the pipe job), then calls `_validate_input`. A doomed request is rejected before any library/job work — symmetric with `/start`'s "capability check before `pipeline_run_setup`" (D-E). (Note: `MissingOrchestratorError` for an unregistered token now raises *before* the pipe-job build, not after — a behavior-equivalent reorder.)
- `_validate_input(input_payload, *, orchestrator, delivery_assignment)` gained the `orchestrator` param and a **capability gate checked first**: `if input_payload.delivery is FIRE_AND_FORGET and not orchestrator.supports_fire_and_forget → raise PipelexBridgeDispatchError` ("Orchestration mode '…' cannot honor fire-and-forget delivery…"), *before* the existing delivery-target gate. A valid target does not rescue a blocking-only orchestrator (the mode, not the target, is the problem). This is the first read site of `supports_fire_and_forget` in landed code (it was write-only after Phases 1–2).
- Tests: `test_validation.py` splits the FF tests into capability-gate vs target-gate (target-gate tests use an async-capable stand-in to reach that gate) + a real-path `run_pipe_via_bridge` rejection on `direct`+FF+target; `test_orchestrator_dispatch.py` adds a seam test proving a blocking-only orchestrator's `run` is **never awaited** for an FF request (`blocking_only.calls == []` — directly refutes the block/false-ack failure); `test_trace_context_contract.py`'s non-direct nulling test was updated to register a stand-in `temporal` orchestrator (it previously leaned on `MissingOrchestratorError` as a post-build stop-point, which the reorder removed).

**Fix 2 — stale worked-example doc.** `docs/under-the-hood/orchestrator-plugins.md` "Worked example" still described the deleted `TemporalBlockingOrchestrator` / `TemporalFireAndForgetOrchestrator` (the Phase 1 as-built had deferred this section to Phase 2, but Phase 2 — a different repo — missed the core-repo doc). Rewritten to the single collapsed `TemporalOrchestrator` (one `"temporal"` registration, delivery-branched `run`, `supports_fire_and_forget = True`).

**Deferred / folded elsewhere:** the review's other substantial cluster — `pipelex-api` + `pipelex-mistralai-workflows` unmigrated against Phase 1 core — is expected load-bearing-ordering breakage owned by Phases 3/4. The one correction it forced: **Phase 4's Mistral scope was underestimated** (real code, not a token rename) — folded into §4 Phase 4 + the cold-start resume note above. **Gates:** `make agent-check` green (pyright 0 / mypy 0 / ruff / plxt / keyword-only); targeted `runtime_bridge` + `plugins` suites green.

---

### Phase 1 — as-built (core split) — Checkpoint 1 cleared

**Branch / commit.** `feature/orchestration-mode-delivery-split` in the `_plugins` core worktree, cut from `feature/Orchestrator-dispatched-validate` tip `fa1b46637`. One commit: **`bd4e52827`**. Nothing pushed.

**Open sub-decisions (§8) — resolved with the recommended defaults:**

- **`OrchestrationMode` shape** → `TypeAlias = str` + `DIRECT_ORCHESTRATION_MODE: Final[str] = "direct"` in the new `pipelex/runtime_bridge/orchestration_mode.py`. No `NewType` (cast-free for plugins; the registry is the validator). A `test_orchestration_mode.py` pins `OrchestrationMode is str` + the `"direct"` literal.
- **`supports_fire_and_forget` location** → on the orchestrator. Declared as a protocol **data member** (`supports_fire_and_forget: bool` on `OrchestratorProtocol`, still `@runtime_checkable`); `DirectOrchestrator.supports_fire_and_forget = False` (plain class attr).
- **Generic `Missing*Error`** → both `MissingOrchestratorError` and `MissingBundleValidatorError` delegate to a shared module-level `_build_missing_message(*, noun, mode)`; the `"direct"` boot-bug case is a plain `if mode == DIRECT_ORCHESTRATION_MODE:` **string** compare (not an enum `match`, so it does not offend the no-`==`-on-enums rule). Message: *"No {orchestrator|bundle validator} is registered for orchestration mode '{mode}'; is its plugin installed?"* / for direct: *"...for the core 'direct' mode; this indicates a boot or plugin-discovery problem."* Names no plugin.
- **Delivery is a `run` parameter; validate stays blocking** → `OrchestratorProtocol.run(*, pipe_job, delivery_assignment, delivery: DeliveryMode)`. `BundleValidatorProtocol.validate_bundles` is unchanged (no `delivery` — validation is inherently blocking).
- **`_validate_input`** keys on `input_payload.delivery is DeliveryMode.FIRE_AND_FORGET`; message reworded "Fire-and-forget delivery requires …".

**Files.** New: `runtime_bridge/orchestration_mode.py`, `runtime_bridge/delivery_mode.py`. Deleted: `runtime_bridge/execution_mode.py`. Re-keyed `dict[str, …]`: `orchestrator_registry.py`, `bundle_validator_registry.py`, `registrar.py` (stores + `_*_sources` + `add_orchestrator`/`add_bundle_validator` mode params typed `OrchestrationMode`). Split `payloads.py` (`orchestration_mode: str` + `delivery: DeliveryMode`). `direct_plugin.py` registers under `DIRECT_ORCHESTRATION_MODE`. `direct_orchestrator.py` + `bridge.py` per §3.1.

**Deviations / extras beyond the literal §9 list (reconciled here):**

- **`plugins/exceptions.py`** — reworded `Duplicate{Orchestrator,BundleValidator}Error` prose "execution mode" → "orchestration mode" (their `mode` field was already `str`; only prose changed). Not in §9 but consistent with D-H.
- **`runtime_bridge/primitives/pipe_classification.py`** — two **docstring** prose mentions of the dead `MISTRAL_NATIVE` enum updated to `"mistralai-workflows"` (prose only; no token *registration* exists in core). The real D-G Mistral-token rename stays a Phase 4 item (deferred plugin).
- **`docs/under-the-hood/orchestrator-plugins.md`** — updated the **core-seam** sections (intro two-axis model, the seam-in-one-view pseudocode, the `OrchestratorProtocol` signature + `supports_fire_and_forget`, the `add_orchestrator(mode="temporal", …)` example, the generic-error section, the SPI table "Mode + delivery + errors" row pointing at the two new modules). **Deferred to Phase 2:** the "Worked example: the in-tree Temporal plugin" section still describes the two-orchestrator (`TemporalBlocking`/`TemporalFireAndForget`) shape — accurate until Phase 2 collapses them.

**Tests.** Deleted `test_execution_mode.py`; added `test_orchestration_mode.py` + `test_delivery_mode.py`. Updated `test_input_models.py`, `test_dispatch.py` (mistral→generic-hint), `test_validation.py` (delivery-centric f&f), `test_orchestrator_dispatch.py` (`_FakeOrchestrator` gains `supports_fire_and_forget` + `delivery` param; miss test asserts the generic message over tokens incl. a synthetic `"acme"`), `test_direct_router_scoping.py`, `test_missing_bundle_validator_error.py` (generic + `"direct"` boot-bug), `test_exceptions_disclosure.py` (generic hint survives STRICT), `test_trace_context_contract.py`, `test_bundle_validator_registry.py`, `test_plugin_discovery.py`, `test_bridge_direct.py`.

**Gates.** `make agent-check` (ruff/plxt/pyright 0/mypy 0/keyword-only ✓), `make tb` (boot builds the re-keyed registries), `make agent-test` — all green.

**⚠️ Cold-start note for Phase 2.** `pipelex-temporal` pins core **editable** (`../_plugins`), so this commit is live in it immediately. Its `TemporalBlockingOrchestrator` / `TemporalFireAndForgetOrchestrator` do **not** yet implement the new `OrchestratorProtocol.run(*, …, delivery)` signature and lack `supports_fire_and_forget`, so `pipelex-temporal`'s suite is expected to fail against this core until Phase 2 adopts the new shape (collapse to one `TemporalOrchestrator`). This is the intended load-bearing ordering (core lands first), not a regression.

### Phase 2 — as-built (Temporal collapse) — Checkpoint 2

**Branch / commit.** `feature/orchestration-mode-delivery-split` in `pipelex-temporal`, cut from `feature/Orchestrator-dispatched-validate` tip `459e04d`. One commit: **`01d7dc3`**. Nothing pushed. (`pipelex-temporal` pins core editable, so it built against the Phase 1 core `bd4e52827` live.)

**The collapse.** `temporal_orchestrators.py` — the two classes (`TemporalBlockingOrchestrator` / `TemporalFireAndForgetOrchestrator`) merged into one **`TemporalOrchestrator`** with `supports_fire_and_forget = True`. Its `run(*, pipe_job, delivery_assignment, delivery: DeliveryMode)` runs the shared prelude (`_require_temporal_extra()`, lazy `WorkflowExecutionError` / `make_temporal_pipe_run` imports, `dispatch_errors` tuple, `temporal_pipe_run = make_temporal_pipe_run()`) then `match delivery:` — `BLOCKING` = the old blocking body (`temporal_pipe_run.run(...)` → `make_workflow_id(...)` → `serialize_completed_output`), `FIRE_AND_FORGET` = the old f&f body (`.start(...)` → `is_completed=False` output). Exhaustive match, no default arm; `WorkflowExecutionError` + `PIPE_DISPATCH_ERRORS` wrapping kept in both arms; extra-guard runs before any temporalio import. Dispatch-error messages reworded to name the *delivery* ("temporal blocking delivery" / "temporal fire-and-forget delivery").

**The token.** `TEMPORAL_ORCHESTRATION_MODE: Final[str] = "temporal"` defined in `temporal_orchestrators.py` (lives with the orchestrator). **Distinct constant** from `temporal_activation.TEMPORAL_PLUGIN_NAME` / `boot_orchestrator` (the hub-slot gate) — both equal `"temporal"` but are different concepts; left separate per §8. `temporal_bundle_validator.py` imports the token from `temporal_orchestrators` (import-light; no cycle — orchestrators never imports the validator) and its missing-extra guard now raises `MissingBundleValidatorError(mode=TEMPORAL_ORCHESTRATION_MODE)`.

**Register once.** `temporal_plugin.py`: one `add_orchestrator(mode=TEMPORAL_ORCHESTRATION_MODE, …)` + one `add_bundle_validator(mode=TEMPORAL_ORCHESTRATION_MODE, …)` (the two-mode duplication is gone). F3 mapper + `boot_orchestrator == self.name` slot-claims **unchanged**. Smoke-confirmed: `registrar.orchestrators` / `bundle_validators` keys == `['temporal']`.

**Import-light preserved.** `register` imports no `temporalio` (the import-light subprocess test still passes: blocks `temporalio`, runs `register` under both `boot_orchestrator` arms). The subprocess assertion changed `len(orchestrators) == 2 → == 1`.

**Tests.** `test_bridge_temporal_dispatch.py` (now `orchestration_mode="temporal"` + `delivery=FIRE_AND_FORGET` for the f&f case — exercises both delivery branches through the **single** real `TemporalOrchestrator.run`, with `make_temporal_pipe_run` mocked), `test_temporal_blocking_workflow_id.py`, `test_trace_context_temporal.py`, `test_temporal_plugin_http_error_mapper.py` (count 2→1). The validator in-memory integration test (`test_temporal_bundle_validator_in_memory.py`) constructs the validator directly — no mode reference, unchanged. Minor docstring fix in `temporal_cli.py` (`execution_mode` → `orchestration_mode`).

**Gates.** `pipelex-temporal` `make agent-check` (ruff/pyright 0/mypy 0) + `make agent-test` (full suite incl. `temporal`-marked in-memory integration; the markers exclude only inference/llm/img_gen/extract/search/pipelex_api, NOT temporal) — all green against the Phase 1 core. **Bonus:** this repo runs Python 3.11, so the run also validated the new core modules' 3.10+ compatibility (`TypeAlias`/`Final`/`StrEnum`-from-`pipelex.types`).

**Cold-start note for Phase 3.** `pipelex-api` is the next consumer (THE gate). It pins core editable too, so Phase 1 core is live in it; `pipelex-api` does NOT depend on `pipelex-temporal` (test the API split with a stub orchestrator/validator under a non-`direct` token; never import `temporalio`). Resolve the §8 `/start`-honesty conformance question before Phase 3 lands. CI gotcha: the editable `../_plugins` pin breaks `pipelex-api` CI — pin core to a git+https SHA + relock for any PR.

**Code-review triage (Checkpoint 2).** One clean-context `pr-review-toolkit:code-reviewer` (pointed only at `01d7dc3`). **Verdict: no material issues, correct + standards-compliant.** It verified against parent `459e04d` that the collapse is byte-faithful (BLOCKING still recomputes/reports `make_workflow_id(...)` not the bare run id; FIRE_AND_FORGET still returns `is_completed=False` + `.start()`'s workflow id; `WorkflowExecutionError` + `PIPE_DISPATCH_ERRORS` wrapping kept in both arms; extra-guard runs before any temporalio import), the `match delivery:` is exhaustive with no default arm and no `==`/`is` on enum members, and **empirically confirmed import-light** (a `sys.meta_path` blocker proves importing the plugin + `temporal_bundle_validator` importing `TEMPORAL_ORCHESTRATION_MODE` from `temporal_orchestrators` pulls no `temporalio`, no cycle). Two non-defect observations (cosmetic error-string rewording; the test's `expected_claims = 4` structural count is the legitimate test-assertion exception to the no-hardcoded-counts rule). **No applied or deferred items.**

---

**Code-review triage (Checkpoint 1).** One clean-context `pr-review-toolkit:code-reviewer` (no plan/rationale handed in; pointed only at `bd4e52827`). **Verdict: correct + standards-compliant, no bugs, tests not weakened.** It independently confirmed: the `is_direct` move to a `==` string compare and `_validate_input`'s move to `delivery is FIRE_AND_FORGET` are faithful (the latter a deliberate, intended broadening — *any* f&f run now needs a delivery target, not just the old Temporal-f&f member); defaults preserved (`direct` + `BLOCKING`, never f&f); the `"direct"` boot-bug `==` compare and generic messages are standards-clean; no `==`-on-enum-member introduced. **One actionable finding — APPLIED:** three user-facing Mistral-workflows doc pages still described the deleted `execution_mode` / `PipelexExecutionMode` model — `docs/distributed-execution/mistral-workflows/execution-modes.md` (rewritten to the two-axis `orchestration_mode` + `delivery` model, retitled "Orchestration & Delivery", generic error wording, `mistralai-workflows` token), `index.md`, `your-first-pipelex-workflow.md`, and `docs/reliability/durable-execution.md` (field/token refs + the now-stale link labels). My §9 grep had been `.py`-only — these `.md` pages slipped it; folded into this commit. **No deferred items.**

---

### Phase 3 — as-built (API split) — Checkpoint 3 cleared (2026-06-22)

**Branch / commit.** `feature/orchestration-mode-delivery-split` in `pipelex-api`, cut from `06cd5e6` (the cut point named in the cold-start note). One commit: **`dd84a9a`**. Nothing pushed. THE gate — the third and last code repo of Direction A — is landed; all the conflation workarounds (`_async_start_mode`, the `/execute` f&f-400, the `ApiConfig` f&f field-validator) are gone.

**Applied exactly per §3.3 / §9 / §10 locked decisions:**

- **`error_types.py`** — `EXECUTION_MODE_OVERRIDE_FORBIDDEN → ORCHESTRATION_MODE_OVERRIDE_FORBIDDEN` (wire value `OrchestrationModeOverrideForbidden`); **deleted** `FIRE_AND_FORGET_NOT_SUPPORTED`; **added** `START_REQUIRES_ASYNC_ORCHESTRATION` (wire value `StartRequiresAsyncOrchestration`, status **400** per §8).
- **`api_config.py`** — `ApiConfig.orchestration_mode: str` + `allow_request_orchestration_mode_override: bool`; **deleted** `_reject_fire_and_forget_default` (+ the now-unused `field_validator` and `PipelexExecutionMode` imports). `resolve_execution_mode → resolve_orchestration_mode(requested: str | None, *, config) -> str` (string compare; 403 via `ORCHESTRATION_MODE_OVERRIDE_FORBIDDEN`; an *unregistered* token is NOT rejected here — it surfaces at dispatch). Module/class docstrings reworded to the open-token + endpoint-set-delivery model.
- **`api.toml`** — `orchestration_mode = "direct"` + `allow_request_orchestration_mode_override = false`; comments reworded (open token; delivery endpoint-set; flavor sets `orchestration_mode = "temporal"`).
- **`schemas/models.py`** — `_EXECUTION_MODE_DESCRIPTION → _ORCHESTRATION_MODE_DESCRIPTION` (open-token wording, delivery-not-requestable note); `PipelineApiExtras.orchestration_mode: str | None`; `PipelexApiExecuteRequest.orchestration_mode: str | None`; dropped the `PipelexExecutionMode` import.
- **`routes/pipelex/pipeline.py`** — **deleted `_async_start_mode`**. `_OrchestratorPipeRun` gained a `delivery: DeliveryMode` ctor field threaded into `orchestrator.run`. `/execute` drops the f&f-reject and dispatches `delivery=BLOCKING`. **`/start` restructured** so resolve → `get_optional` → `MissingOrchestratorError` if None → **capability check `if not orchestrator.supports_fire_and_forget: raise_bad_request(START_REQUIRES_ASYNC_ORCHESTRATION)`** all run **before `pipeline_run_setup`** (no library load on a doomed request); dispatch `delivery=FIRE_AND_FORGET`. `validate_verdict` param `requested_execution_mode → requested_orchestration_mode`; `_validate_extras` reads the `orchestration_mode` body key; both route handlers thread `requested_orchestration_mode=extras.orchestration_mode`; docstrings updated (the `/start` one: honest-400, not blocking-202). Import swap: drop `PipelexExecutionMode`, add `DeliveryMode`; `resolve_execution_mode → resolve_orchestration_mode`.
- **`routes/pipelex/validate.py`** — `ValidateRequest.execution_mode → orchestration_mode: str | None` (open-token description); route passes `requested_orchestration_mode=request_data.orchestration_mode`; dropped the `PipelexExecutionMode` import; "no verdict" docstring `execution_mode → orchestration_mode`.
- **OpenAPI** — `make openapi-export` regenerated `docs/openapi/pipelex-api.openapi.yaml` (now advertises `orchestration_mode`, no residual `execution_mode` / old enum values); `make openapi-check` clean.

**⚠️ Deviation reconciled — the pin was NOT editable as the cold-start note claimed.** The cold-start note (and §5) asserted "`pipelex-api` pins core editable." **In reality the branch sat on a git-SHA pin** (`[tool.uv.sources] pipelex = { git = …, rev = "51ff0941…" }`, from parent commit `c60be1f` "pin pipelex to a git SHA so CI can resolve it (was a local editable path)") — and that SHA predates Phase 1, so it has `execution_mode.py` and lacks `delivery_mode`/`orchestration_mode`. Phase 3 therefore did not compile against it. **Resolution:** flipped `[tool.uv.sources]` back to `pipelex = { path = "../_plugins", editable = true }` (matching `pipelex-temporal`'s convention) + `make li` to relock/reinstall, so the Phase 1 core split is live. **CARRY-FORWARD (the one open item):** this editable local-path pin is unresolvable in CI, so **before opening any `pipelex-api` PR, push the Phase 1 core and flip the pin to that pushed git+https SHA + relock** (the documented CI gotcha). `uv.lock` + `pyproject.toml` are part of `dd84a9a`.

**Tests (all updated to the two-axis model; §9 list).** `test_api_config.py` (dropped `TestApiConfigRejectsFireAndForgetDefault`; renamed to `TestResolveOrchestrationMode`; string tokens; 403 asserts `OrchestrationModeOverrideForbidden`), `test_execute_dispatch.py` (dropped the 400-f&f case; `_StubOrchestrator.run` gains `delivery` + `supports_fire_and_forget`; asserts `delivery is BLOCKING`), `test_start_dispatch.py` (deleted the `_async_start_mode` pure-mapping class; new `TestStartCapabilityGate` — ack-when-async-capable asserts `delivery is FIRE_AND_FORGET`, honest-400 asserts `StartRequiresAsyncOrchestration` + `stub.calls == []` proving `run` is never awaited), `test_validate_dispatch.py` (string `"temporal"` token; renamed forbidden-override test + asserts the error_type), `test_protocol_conformance.py` (`mode="direct"`; `fake_run` gains `delivery` + an `assert delivery is FIRE_AND_FORGET`; `fake_orchestrator.supports_fire_and_forget = True` so the new gate is exercised).

**Docs + CHANGELOG.** `docs/configuration.md` ("Execution mode" → "Orchestration mode", two-axis model, honest-400), `docs/pipe-run.md` (`/execute` + `/start` notes; the `workflow_id` field note — a 202 is now only ever an async backend's, direct returns 400), `docs/pipe-validate.md` (`orchestration_mode`-aware, no delivery axis), `CHANGELOG.md` `[Unreleased]` rewritten to the orchestration_mode/delivery model (the never-released flat `execution_mode` bullets reshaped — no backward-compat concern).

**Gates.** `pipelex-api` `make c` (ruff / pyright **0** / mypy **0**) + `make tp` (**319 passed**) + `make openapi-export` then `make openapi-check` (artifact up to date) — all green against the live Phase 1 core (editable `../_plugins`). Python 3.11.

**Code-review triage (Checkpoint 3).** One clean-context `pr-review-toolkit:code-reviewer` (no plan/rationale handed in; pointed only at `dd84a9a`, cross-checked against the live core contract in `../_plugins`). **Verdict: no must-fix issues — correct, complete, standards-compliant.** It independently confirmed: the capability-check ordering is right (resolve → lookup → missing-check → `supports_fire_and_forget` 400, all before `pipeline_run_setup`; the orchestrator is looked up **once** and reused at dispatch so the gate can't diverge); the **honest-400 actually fires on the real base** (core `DirectOrchestratorPlugin` in `plugins/builtins.py` registers `DirectOrchestrator` with `supports_fire_and_forget = False` under `"direct"`, so `/start` hits the capability 400, not the `MissingOrchestratorError` fallback — exactly as `test_start_honestly_400s…` asserts); no StrEnum/`==`-on-enum violation (`resolve_orchestration_mode`'s `==` is `str`/`str`; `DeliveryMode` only ever compared with `is`); Protocol signatures match; RFC 7807 helpers correct; the field-validator deletion + its test removal are justified (the invariant no longer exists), not a weakening; the test assertions strengthened (delivery-axis + never-awaited proofs). **One should-consider — the editable-pin CI break — is the carry-forward item already captured above** (flagged by the author in the commit body + inline comment; flip to a pushed git SHA before a PR). **No applied or deferred code items.**

**Cold-start note for Phase 4.** Phase 4 = the **bigger-than-first-estimated** Mistral plugin migration (real code, not a token swap — see the revised §4 Phase 4 for the verified `pipelex-mistralai-workflows` anchors: deleted-enum import, registration under the deleted member, its own `execution_mode` wire field + `is PipelexExecutionMode.DIRECT` compare, an orchestrator on the old `run` signature) **plus** bookkeeping: `_plugins/TODOS.md`, the two superseded sibling-plan markers, and `make check-spec-links` if any spec heading/test asserts mode values. Re-derive the §4 anchors against the live `pipelex-mistralai-workflows` tip before starting (they will have moved). The three landed repos (`_plugins` `e9e73e0eb`, `pipelex-temporal` `01d7dc3`, `pipelex-api` `dd84a9a`) all sit on `feature/orchestration-mode-delivery-split`, pinned core editable for local dev.

---

### Phase 4 — as-built (Mistral plugin migration + bookkeeping) — effort COMPLETE (2026-06-22)

**✅ This is the final phase. All four phases of the orchestration-mode/delivery split are landed.** The cold-start note just above is now historical — read this section for the as-built.

**⚠️ The deviation that shaped Phase 4 (the §4 anchors were re-derived and the pin assumption was wrong).** The plan (and §4/§5) assumed `pipelex-mistralai-workflows` pinned the Phase-1 core, so it would be "already red against Phase 1." **In reality the plugin pins core editable at `../_workflows`, NOT `../_plugins`** — and `_workflows` was on a *divergent* branch **`feature/mistralai-2x-bump`** that still carried the OLD `runtime_bridge` (had `execution_mode.py`, lacked `orchestration_mode`/`delivery_mode`). So the plugin was **green against its own core**, not red against the split — and could not be migrated in isolation. `feature/mistralai-2x-bump` and `feature/orchestration-mode-delivery-split` were divergent (neither an ancestor; common base `e460d072`); `mistralai-2x-bump` made **no** `runtime_bridge` changes relative to that base.

**Resolution (user-directed): merge the split into the plugin's core branch.** Merged `feature/orchestration-mode-delivery-split` → `feature/mistralai-2x-bump` in the `_workflows` worktree — **merge commit `a1448ad8f`, conflict-free** (the branches overlapped only in shared plugin-system work, which was identical). The merged core gives the plugin a runtime bridge carrying **both** the orchestration split and the mistralai-2x bump. Merged-core verification: `make tb` (boot/config) ✓, `make agent-check` (ruff/pyright 0/mypy 0/keyword-only) ✓, the merge-sensitive core suites (`runtime_bridge` + `plugins` + `test_plugin_config_loader` + `test_direct_bundle_validator`) all green.

**The Mistral migration — `pipelex-mistralai-workflows` commit `0eb3878` on `feature/Mistral-native`** (parent `e508e73`; nothing pushed). Same shape as the Phase 2 Temporal collapse — real code, not a token swap:

- **`mistral_orchestrator.py`** — defines `MISTRALAI_WORKFLOWS_ORCHESTRATION_MODE: Final[str] = "mistralai-workflows"` (the D-G token, **living with the orchestrator** exactly as `pipelex-temporal` puts `TEMPORAL_ORCHESTRATION_MODE` in `temporal_orchestrators.py`). `MistralNativeOrchestrator.run` adopts the new `OrchestratorProtocol.run(*, pipe_job, delivery_assignment, delivery: DeliveryMode)` signature and declares **`supports_fire_and_forget = False`** — Mistral-native always runs to completion in-place, so it is blocking-only like core `DirectOrchestrator`. It accepts `delivery` and ignores it; the bridge's pre-dispatch capability gate (the §10 "Fix 1" Tier-3 guard, present in the merged core) rejects a `FIRE_AND_FORGET` request against it before `run` is ever called, so `run` provably never receives f&f. (No `# noqa: ARG002` needed — unlike core, this repo's ruff `select` omits `ARG`, so the unused-arg rule does not fire; the linter auto-removed the noqa as unused.)
- **`mistral_plugin.py`** — registers `add_orchestrator(mode=MISTRALAI_WORKFLOWS_ORCHESTRATION_MODE, …)`; dropped the deleted-enum import. **The plugin name / entry-point `MISTRAL_NATIVE_PLUGIN_NAME = "mistral_native"` is KEPT, deliberately distinct from the orchestration token** — the plugin-discovery identity vs the registry key, the same two-concept separation `pipelex-temporal` keeps between `TEMPORAL_PLUGIN_NAME` and `TEMPORAL_ORCHESTRATION_MODE`. (D-G renamed the *token*, not the plugin name; renaming the entry-point would force a reinstall for zero gain.)
- **`streaming.py`** — wire field `execution_mode → orchestration_mode` (on `PipelexPipeRunStreamingState` + the read from the input); the `is PipelexExecutionMode.DIRECT` enum compare → `== DIRECT_ORCHESTRATION_MODE` (both plain `str` now — `==` is correct, matches how core's `bridge.py` compares it); docstrings reworded off `DIRECT`/`TEMPORAL_BLOCKING`/`TEMPORAL_FIRE_AND_FORGET` onto the `direct`/non-direct orchestration-mode framing. The per-step-streaming branch still correctly keys on "is this the in-process `direct` mode".
- **`primitives/`** — doc/comment refs to the deleted enum; the internal scoped-library id prefix `mistral_native_` → `mistralai_workflows_` (`scoped_library.py`; purely internal, ephemeral per-call id — no wire contract).
- **Tests** — `test_plugin_discovery.py` looks up + asserts the orchestrator under `MISTRALAI_WORKFLOWS_ORCHESTRATION_MODE` (real registration check via full boot; renamed `test_mistralai_workflows_orchestrator_is_registered`); the three `test_activities_*` construct `PipelexPipeRunInput(orchestration_mode=DIRECT_ORCHESTRATION_MODE)`; `test_activities_streaming.py` asserts the renamed `orchestration_mode` wire field; `test_scoped_library.py` asserts the renamed id prefix; doc-only refs in `test_router_dispatch.py` / `test_native_recursion.py` / `test_data/bridge_funcs.py`. No assertion weakened — field-for-field.
- **Docs** — `CLAUDE.md` public-surface contract (the deleted `execution_mode` module + the two other now-deleted symbols `PipelexBridgeRuntimeError` / `MissingPipelexTemporalExtraError` — stale since the Temporal externalization — replaced with the real surface: `payloads`, `orchestration_mode`, `delivery_mode`, `PipelexBridgeDispatchError`); `README.md` quick-start + a recast "Orchestration mode and delivery" section (also dropped the dead `[temporal]` extra — the README now says install `pipelex-temporal` for the `temporal` mode); the `wip/boundary-violation-mistral-native.md` allowed-surface list; `CHANGELOG.md` (new `### Changed` reconciliation bullet + reshaped the never-released bullets that named the old enum); `pyproject.toml` entry-point comment.

**Gates (Mistral plugin, against the merged editable `../_workflows` core).** `make agent-check` (ruff / pyright **0** / mypy **0**) + `make agent-test` + the full migrated integration suite (`test_plugin_discovery` + `test_activities_{direct,offloaded,streaming}` + `test_native_recursion` + `test_scoped_library`) — **all green** (12 passed in the targeted verbose run). Python 3.13.

**Code-review triage (Phase 4).** One clean-context `pr-review-toolkit:code-reviewer` (no plan handed in; pointed only at `c2013fb` — the pre-amend SHA — cross-checked against the live merged core in `../_workflows` + the Temporal reference). **Verdict: the code migration is correct and complete.** It independently confirmed: the `run` signature conforms to the live `OrchestratorProtocol`; `supports_fire_and_forget=False` + ignoring `delivery` is safe given the bridge's pre-dispatch FF rejection; registration under `"mistralai-workflows"` with the name/token split is sound (not a bug); the `streaming.py` `==` string compare is correct (vs `is`); tests not weakened; no StrEnum/`==`-on-enum-member violation, no `except Exception`, keyword-only respected, no `__init__.py` re-exports; no new pyright/mypy regression (the `reportUnknown*` on the untyped Mistral SDK return are pre-existing, identical count on the parent). **One must-fix — APPLIED:** `README.md` still imported the deleted `PipelexExecutionMode` and documented it in a "## Execution modes" section (my source/test grep had been `.py`+config-scoped — the README slipped, the same `.md`-slips-a-`.py`-grep the Phase 1 review caught). Rewritten to `orchestration_mode`/`delivery` + the dead `[temporal]`-extra fix. **One nit — APPLIED:** the `wip/boundary-violation-mistral-native.md` allowed-surface list named the deleted `execution_mode` module → updated. Both fixes folded into the commit by amend (`c2013fb` → **`0eb3878`**). **No deferred code items.**

**Bookkeeping done.** Superseded markers added to both sibling plans (`execute-per-request-execution-mode.md`, `orchestrator-dispatched-validate.md`) pointing here; `_plugins/TODOS.md` follow-on entry flipped to "DONE (Phases 1–4)" + the merge deviation recorded; the `pipelex-api` `CHANGELOG.md` was already on the orchestration_mode/delivery model from Phase 3 (`dd84a9a`) — no Phase 4 change needed there; `make check-spec-links` **not** triggered (verified: zero `docs/specs/` or `conformance/` references to mode values).

**Carry-forward / open items (none block the effort; flagged for whoever pushes/PRs):**

1. **`pipelex-api` editable-pin → pushed git-SHA flip (from Phase 3, still open).** `pipelex-api` pins core editable `../_plugins`; before any `pipelex-api` PR, push the core split and flip to that pushed git+https SHA + relock (the documented CI gotcha).
2. **`pipelex-mistralai-workflows` core pin.** It pins `../_workflows` editable; for a real release the same flip applies (push the merged core, pin a published/pinned ref). The plugin's own CHANGELOG already notes the `../_workflows` co-dev override is stripped at v0.1.0.
3. **`_workflows` ↔ `_plugins` doc divergence.** The `a1448ad8f` merge copied a *pre-Phase-4 snapshot* of the orchestration tracker docs (this plan, `TODOS.md`, the sibling plans) into `_workflows`. The Phase 4 bookkeeping edits above landed on **`_plugins`** (`feature/orchestration-mode-delivery-split`, the tracker SoT), so the `_workflows` copies are momentarily stale. They reconcile on the next merge of `feature/orchestration-mode-delivery-split` → `feature/mistralai-2x-bump` (or whenever the two core branches are integrated). Not a code concern — `_workflows`'s *code* is the superset (it contains the split); only the in-flight tracker prose differs.
4. **Two divergent core branches now exist** (`feature/orchestration-mode-delivery-split` in `_plugins`, `feature/mistralai-2x-bump` in `_workflows`, the latter containing the former via `a1448ad8f`). Integrating/landing them is a branch-strategy decision for the user — out of scope for this effort.
