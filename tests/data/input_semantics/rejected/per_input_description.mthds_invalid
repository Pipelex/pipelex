domain      = "input_semantics_rejected"
description = "REJECTED probe: a pipe input slot cannot carry its own description — the expanded slot table admits only concept and hints, so description is an unknown slot-table key"

[pipe.annotated_inputs]
type = "PipeLLM"
description = "Pipe trying to describe an input slot"
inputs = { doc = { concept = "Text", description = "the document to analyse" } }
output = "Text"
prompt = """
@doc
"""
