name = "feature_smart_inputs_claims_triage"
description = "A claims summary whose caller passes bare values — a string, a number, a dict, a list — shaped against the declared signature"

validity = "valid"
tier = "dry"
granularity = "focused"

covers = [
  "feature.smart_inputs",
]
