"""Framework-agnostic primitives shared across host-runtime integrations.

These helpers are used by both ``pipelex.temporal`` (raw Temporal cluster
mode) and ``pipelex_mistralai_workflows.primitives`` (Mistral-native mode).
They contain no host-runtime-specific imports — only Pipelex core types.
"""
